diff --git a/cmake/aurora_gx.cmake b/cmake/aurora_gx.cmake
index f8f9461..350b141 100644
--- a/cmake/aurora_gx.cmake
+++ b/cmake/aurora_gx.cmake
@@ -3,6 +3,8 @@ add_library(aurora_gx STATIC
         lib/gfx/depth_peek.cpp
         lib/gfx/encoding.cpp
         lib/gfx/frame.cpp
+        lib/gfx/flip_gles.cpp
+        lib/gfx/flip_sprites.cpp
         lib/gfx/pipeline_cache.cpp
         lib/gfx/recording.cpp
         lib/gfx/render_worker.cpp
@@ -19,6 +21,8 @@ add_library(aurora_gx STATIC
         lib/gx/regs.cpp
         lib/gx/dl.cpp
         lib/gx/fifo.cpp
+    lib/gx/flip_uber.cpp
+    lib/gx/flip_gles_uber.cpp
         lib/gx/gx.cpp
         lib/gx/texture.cpp
         lib/gx/pipeline.cpp
diff --git a/include/dolphin/gx/GXAurora.h b/include/dolphin/gx/GXAurora.h
index 1a312fb..ea9f8a2 100644
--- a/include/dolphin/gx/GXAurora.h
+++ b/include/dolphin/gx/GXAurora.h
@@ -96,6 +96,26 @@ extern "C" {
  */
 #define GX_AURORA_DRAW_INDEXED 0x0041
 
+/**
+ * Call a display list by reference (Flip). Followed by a u64 pointer and a u32
+ * byte length. The processor decodes it into resident geometry on first use.
+ */
+#define GX_AURORA_CALL_DL 0x0042
+
+/**
+ * Release resident geometry referencing [pointer, pointer + size).
+ * Followed by a u64 pointer and a u32 byte length.
+ */
+#define GX_AURORA_INVALIDATE_RESIDENT 0x0043
+
+/**
+ * Asynchronous frame markers (Flip). FRAME_BEGIN is followed by the u32 frame
+ * slot whose packet the processor should record into; FRAME_END has no payload
+ * and finishes the frame on the processor thread.
+ */
+#define GX_AURORA_FRAME_BEGIN 0x0044
+#define GX_AURORA_FRAME_END 0x0045
+
 #define GX2_SET_POLYGON_OFFSET 0x1000
 
 
diff --git a/lib/aurora.cpp b/lib/aurora.cpp
index 9430842..6ac83fd 100644
--- a/lib/aurora.cpp
+++ b/lib/aurora.cpp
@@ -1,13 +1,27 @@
 #include <aurora/aurora.h>
 #include <aurora/time.hpp>
+#include <chrono>
+#include <cstdio>
+#include <cstdlib>
 
 #ifdef AURORA_ENABLE_GX
+#ifdef MELEE_MIYOO_FLIP
+extern "C" void MeleeFlipPresent();
+extern "C" void MeleeFlipProfileSubmit(bool);
+extern "C" void MeleeFlipProfileSwap(bool);
+#endif
 #include "gfx/resources.hpp"
 #include "gfx/frame.hpp"
+#ifdef MELEE_MIYOO_FLIP
+#include "gfx/flip_gles.hpp"
+#endif
 #include "gfx/recording.hpp"
 #include "gfx/render_worker.hpp"
+#include "gfx/pipeline_cache.hpp"
 #include "gx/command_processor.hpp"
 #include "gx/fifo.hpp"
+#include "dolphin/gx/GXAurora.h"
+#include "gx/fifo.hpp"
 #include "gx/gx.hpp"
 #include "gx/texture.hpp"
 #include "imgui.hpp"
@@ -253,10 +267,22 @@ bool begin_frame() noexcept {
   }
 
   imgui::new_frame(window::get_window_size());
+#ifdef MELEE_MIYOO_FLIP
+  if (gx::fifo::async_frames()) gx::fifo::recycle();
+#endif
   if (!gfx::begin_frame()) {
     return false;
   }
   gx::fifo::begin_frame();
+#ifdef MELEE_MIYOO_FLIP
+  if (gx::fifo::async_frames()) {
+    // The worker begins recording into this slot when it reaches the marker.
+    gx::fifo::write_u8(GX_AURORA);
+    gx::fifo::write_u16(GX_AURORA_FRAME_BEGIN);
+    gx::fifo::write_u32(static_cast<uint32_t>(gfx::detail::producer_frame_slot()));
+    gx::fifo::publish();
+  }
+#endif
 #endif
   return true;
 }
@@ -264,10 +290,17 @@ bool begin_frame() noexcept {
 void end_frame() noexcept {
   ZoneScoped;
 #ifdef AURORA_ENABLE_GX
-  gx::fifo::drain();
-  gx::fifo::end_frame();
-  gx::texture::end_frame();
-  gfx::finish();
+#ifdef MELEE_MIYOO_FLIP
+  const bool asyncFrame = gx::fifo::async_frames();
+#else
+  constexpr bool asyncFrame = false;
+#endif
+  if (!asyncFrame) {
+    gx::fifo::drain();
+    gx::fifo::end_frame();
+    gx::texture::end_frame();
+    gfx::finish();
+  }
   auto imguiDrawData = imgui::freeze();
 
   const auto& presentSource = webgpu::present_source();
@@ -285,12 +318,32 @@ void end_frame() noexcept {
   }
 #endif
 
-  gfx::end_frame([rmlBindGroup = std::move(rmlBindGroup), rmlOverlay, viewport,
+  gfx::EndFrameCallback endFrameCallback = [rmlBindGroup = std::move(rmlBindGroup), rmlOverlay, viewport,
                   imguiDrawData = std::move(imguiDrawData)](
                      wgpu::CommandEncoder& encoder, std::vector<gfx::AfterSubmitCallback> afterSubmitCallbacks) {
     wgpu::Texture currentTexture;
     wgpu::TextureView currentView;
     auto surfaceStatus = wgpu::SurfaceGetCurrentTextureStatus::Error;
+#ifdef MELEE_MIYOO_FLIP
+    static const bool phaseProfile = std::getenv("MELEE_FLIP_PROFILE") != nullptr;
+    static double acquireMs = 0, encodeMs = 0, submitMs = 0, presentMs = 0; static unsigned phaseFrames = 0;
+    auto phaseNow = std::chrono::steady_clock::now();
+    const auto phase = [&](double& acc) { const auto n = std::chrono::steady_clock::now(); acc += std::chrono::duration<double, std::milli>(n - phaseNow).count(); phaseNow = n; };
+#endif
+#ifdef MELEE_MIYOO_FLIP
+    // Scene-on-surface: the frame's EFB passes already rendered into a
+    // presented texture acquired at encode time.
+    bool sceneOnSurface = false;
+    if (auto [surfaceTexture, surfaceView] = gfx::detail::take_flip_frame_surface(); surfaceTexture) {
+      currentTexture = std::move(surfaceTexture);
+      currentView = std::move(surfaceView);
+      surfaceStatus = wgpu::SurfaceGetCurrentTextureStatus::SuccessOptimal;
+      sceneOnSurface = true;
+    }
+    if (!sceneOnSurface)
+#else
+    constexpr bool sceneOnSurface = false;
+#endif
     {
       window::SurfaceLock surfaceLock;
       if (window::is_presentable() && g_surface) {
@@ -305,14 +358,51 @@ void end_frame() noexcept {
       }
     }
 
+#ifdef MELEE_MIYOO_FLIP
+    if (phaseProfile) phase(acquireMs);
+#endif
     const bool canPresent = currentTexture && currentView;
-    if (canPresent) {
+    // Scene-on-surface: the scene is already in the presented texture when no
+    // overlay is composited and the viewport covers the whole surface.
+    const bool fullSurfaceViewport =
+        viewport.left == 0.f && viewport.top == 0.f &&
+        static_cast<uint32_t>(viewport.width + 0.5f) == webgpu::g_graphicsConfig.surfaceConfiguration.width &&
+        static_cast<uint32_t>(viewport.height + 0.5f) == webgpu::g_graphicsConfig.surfaceConfiguration.height;
+    const bool skipPresentCopy = sceneOnSurface && !(rmlBindGroup && rmlOverlay) && fullSurfaceViewport;
+    if (canPresent && skipPresentCopy) {
+      // Test captures read the EFB texture: mirror the surface into it.
+      static const bool captureCopy = std::getenv("MELEE_MATRIX_TEST") != nullptr || std::getenv("MELEE_RENDER_CHECK") != nullptr;
+      if (captureCopy) {
+        const auto& efb = webgpu::present_source();
+        const wgpu::TexelCopyTextureInfo src{.texture = currentTexture};
+        const wgpu::TexelCopyTextureInfo dst{.texture = efb.texture};
+        const wgpu::Extent3D size{efb.size.width, efb.size.height, 1};
+        encoder.CopyTextureToTexture(&src, &dst, &size);
+      }
+    }
+    if (canPresent && !skipPresentCopy) {
       wgpu::BindGroup presentBindGroup;
       if (rmlBindGroup && !rmlOverlay) {
         presentBindGroup = rmlBindGroup;
       } else {
-        const auto& resampledSource = webgpu::resample_present_source(encoder, viewport);
-        presentBindGroup = webgpu::create_copy_bind_group(resampledSource);
+#ifdef MELEE_MIYOO_FLIP
+        // The Flip panel matches the EFB size: skip the intermediate resample pass
+        // (a full-screen render pass per frame) and copy the EFB directly.
+        static const bool directPresent = [] { const char* v = std::getenv("MELEE_FLIP_DIRECT_PRESENT"); return !v || std::strcmp(v, "0"); }();
+        const auto vw = static_cast<uint32_t>(viewport.width + 0.5f), vh = static_cast<uint32_t>(viewport.height + 0.5f);
+        const auto& efbSource = webgpu::present_source();
+        if (directPresent && efbSource.size.width == vw && efbSource.size.height == vh) {
+          presentBindGroup = webgpu::create_copy_bind_group(efbSource);
+          // Whole-surface copy without overlays: let the direct path blit it.
+          if (gfx::detail::flip_present_blit_enabled() && !(rmlBindGroup && rmlOverlay) && viewport.left == 0.f && viewport.top == 0.f &&
+              vw == webgpu::g_graphicsConfig.surfaceConfiguration.width && vh == webgpu::g_graphicsConfig.surfaceConfiguration.height)
+            gfx::detail::flip_request_present_blit(efbSource.view, vw, vh);
+        } else
+#endif
+        {
+          const auto& resampledSource = webgpu::resample_present_source(encoder, viewport);
+          presentBindGroup = webgpu::create_copy_bind_group(resampledSource);
+        }
       }
       {
         const std::array attachments{
@@ -343,6 +433,13 @@ void end_frame() noexcept {
         }
         pass.End();
       }
+    }
+    if (canPresent) {
+#ifdef MELEE_MIYOO_FLIP
+      // An empty overlay still costs the driver a render pass; skip it in-game.
+      static const bool skipEmptyImgui = [] { const char* v = std::getenv("MELEE_FLIP_SKIP_EMPTY_IMGUI"); return !v || std::strcmp(v, "0"); }();
+      if (!skipEmptyImgui || imgui::has_draws(imguiDrawData))
+#endif
       {
         const std::array attachments{
             wgpu::RenderPassColorAttachment{
@@ -369,9 +466,21 @@ void end_frame() noexcept {
     webgpu::gpu_prof::frame_end(encoder);
     const wgpu::CommandBufferDescriptor cmdBufDescriptor{.label = "Redraw command buffer"};
     const auto buffer = encoder.Finish(&cmdBufDescriptor);
+#ifdef MELEE_MIYOO_FLIP
+    if (phaseProfile) phase(encodeMs);
+#endif
     {
       ZoneScopedN("Queue Submit");
+#ifdef MELEE_MIYOO_FLIP
+      MeleeFlipProfileSubmit(true);
+      gfx::detail::install_flip_gles_frame();
+#endif
       g_queue.Submit(1, &buffer);
+#ifdef MELEE_MIYOO_FLIP
+      gfx::detail::uninstall_flip_gles_frame();
+      MeleeFlipProfileSubmit(false);
+      if (phaseProfile) phase(submitMs);
+#endif
     }
     webgpu::gpu_prof::after_submit();
     if (canPresent && g_surface) {
@@ -380,9 +489,26 @@ void end_frame() noexcept {
       {
         window::SurfaceLock surfaceLock;
         if (window::is_presentable()) {
+#ifdef MELEE_MIYOO_FLIP
+          MeleeFlipProfileSwap(true);
+#endif
           status = g_surface.Present();
+#ifdef MELEE_MIYOO_FLIP
+          MeleeFlipProfileSwap(false);
+          MeleeFlipPresent();
+#endif
         }
       }
+#ifdef MELEE_MIYOO_FLIP
+      if (phaseProfile) {
+        phase(presentMs);
+        if (++phaseFrames % 120 == 0) {
+          std::fprintf(stderr, "[flip-render-callback] acquire_ms=%.3f encode_ms=%.3f submit_ms=%.3f present_ms=%.3f\n",
+                       acquireMs / 120, encodeMs / 120, submitMs / 120, presentMs / 120);
+          acquireMs = encodeMs = submitMs = presentMs = 0;
+        }
+      }
+#endif
       if (status) {
         gfx::after_present();
       } else {
@@ -439,7 +565,19 @@ void end_frame() noexcept {
     TracyPlot("aurora: lastIndexSize", static_cast<int64_t>(stats.lastIndexSize));
     TracyPlot("aurora: lastStorageSize", static_cast<int64_t>(stats.lastStorageSize));
     TracyPlot("aurora: lastTextureUploadSize", static_cast<int64_t>(stats.lastTextureUploadSize));
-  });
+  };
+#ifdef MELEE_MIYOO_FLIP
+  if (asyncFrame) {
+    // Hand the presentation closure to the worker and close the frame in-stream.
+    gfx::defer_end_frame(std::move(endFrameCallback));
+    gx::fifo::write_u8(GX_AURORA);
+    gx::fifo::write_u16(GX_AURORA_FRAME_END);
+    gx::fifo::publish();
+    gx::fifo::end_frame_producer();
+    return;
+  }
+#endif
+  gfx::end_frame(std::move(endFrameCallback));
 
 #endif
 }
@@ -451,6 +589,13 @@ AuroraInfo aurora_initialize(int argc, char* argv[], const AuroraConfig* config)
   return aurora::initialize(argc, argv, *config);
 }
 void aurora_shutdown() { aurora::shutdown(); }
+#ifdef AURORA_ENABLE_GX
+extern "C" uint64_t aurora_fifo_drain_wait_ns() { return aurora::gx::fifo::drain_wait_ns(); }
+extern "C" uint64_t aurora_fifo_process_ns() { return aurora::gx::fifo::process_ns(); }
+extern "C" uint64_t aurora_render_worker_busy_ns() { return aurora::gfx::render_worker::busy_ns(); }
+extern "C" uint64_t aurora_pipeline_wait_ns() { return aurora::gfx::pipeline_wait_ns(); }
+extern "C" uint64_t aurora_pipeline_wait_count() { return aurora::gfx::pipeline_wait_count(); }
+#endif
 const AuroraEvent* aurora_update() { return aurora::update(); }
 bool aurora_begin_frame() { return aurora::begin_frame(); }
 void aurora_end_frame() { aurora::end_frame(); }
diff --git a/lib/dawn/BackendBinding.cpp b/lib/dawn/BackendBinding.cpp
index 447ead8..a960827 100644
--- a/lib/dawn/BackendBinding.cpp
+++ b/lib/dawn/BackendBinding.cpp
@@ -1,4 +1,7 @@
 #include "BackendBinding.hpp"
+#ifdef MELEE_MIYOO_FLIP
+extern "C" void* MeleeFlipNativeWindow();
+#endif
 
 #include <aurora/webgpu.hpp>
 
@@ -17,6 +20,11 @@ std::shared_ptr<wgpu::ChainedStruct> SetupWindowAndGetSurfaceDescriptor(SDL_Wind
 #if defined(SDL_PLATFORM_MACOS) || defined(SDL_PLATFORM_IOS) || defined(SDL_PLATFORM_TVOS)
   return SetupWindowAndGetSurfaceDescriptorCocoa(window);
 #else
+#ifdef MELEE_MIYOO_FLIP
+  auto desc = std::make_shared<wgpu::DawnSurfaceSourceEGLNativeWindow>();
+  desc->window = MeleeFlipNativeWindow();
+  return desc;
+#endif
   const auto props = SDL_GetWindowProperties(window);
 #if defined(SDL_PLATFORM_ANDROID)
   std::shared_ptr<wgpu::SurfaceSourceAndroidNativeWindow> desc =
diff --git a/lib/dolphin/gx/GXDispList.cpp b/lib/dolphin/gx/GXDispList.cpp
index 5bf9460..91f7e82 100644
--- a/lib/dolphin/gx/GXDispList.cpp
+++ b/lib/dolphin/gx/GXDispList.cpp
@@ -1,5 +1,7 @@
 #include "gx.hpp"
 #include "__gx.h"
+#include "dolphin/gx/GXAurora.h"
+#include <cstdlib>
 
 #include "../../gx/fifo.hpp"
 
@@ -57,6 +59,19 @@ void GXCallDisplayList(const void* data, u32 nbytes) {
     __GXSendFlushPrim();
   }
 
+  // Flip: reference the list instead of copying it. The FIFO processor keeps
+  // decoded geometry resident and validates the list bytes on every call.
+  static const bool byReference = [] {
+    const char* v = std::getenv("MELEE_FLIP_RESIDENT_DL");
+    return !v || std::strcmp(v, "0");
+  }();
+  if (byReference && !aurora::gx::fifo::in_display_list() && nbytes) {
+    GX_WRITE_AURORA(GX_AURORA_CALL_DL);
+    GX_WRITE_U64(reinterpret_cast<u64>(data));
+    GX_WRITE_U32(nbytes);
+    aurora::gx::fifo::publish();
+    return;
+  }
   // Write display list contents to the FIFO
   aurora::gx::fifo::write_data(data, nbytes);
   aurora::gx::fifo::publish();
diff --git a/lib/dolphin/gx/GXManage.cpp b/lib/dolphin/gx/GXManage.cpp
index 7e62821..7ac4db6 100644
--- a/lib/dolphin/gx/GXManage.cpp
+++ b/lib/dolphin/gx/GXManage.cpp
@@ -270,6 +270,7 @@ void GXDrawDone() {
 void GXSetDrawDone() {
   GXFlush();
   GX_WRITE_RAS_REG(kDrawDoneCommand);
+  aurora::gx::fifo::mark_draw_done();
   aurora::gx::fifo::publish();
 }
 
diff --git a/lib/dolphin/gx/GXTexture.cpp b/lib/dolphin/gx/GXTexture.cpp
index 04f97f5..bde853b 100644
--- a/lib/dolphin/gx/GXTexture.cpp
+++ b/lib/dolphin/gx/GXTexture.cpp
@@ -5,6 +5,9 @@
 #include "dolphin/gx/GXAurora.h"
 
 #include <algorithm>
+#include <cstdlib>
+#include <cstring>
+#include <xxhash.h>
 
 #include "tracy/Tracy.hpp"
 
@@ -77,6 +80,31 @@ void init_texobj_common(GXTexObj_& obj, const void* data, u16 width, u16 height,
   obj.data = data;
 }
 
+// HSD builds a fresh GXTexObj on the stack for every material every frame, so a
+// counter identity never repeats and every load re-hashes the whole image. Derive
+// the identity from the image description instead; the texture cache still
+// verifies content on reuse, so a stale or colliding identity only costs a miss.
+bool stable_identities() {
+  static const bool enabled = [] { const char* v = std::getenv("MELEE_FLIP_STABLE_TEXID"); return !v || std::strcmp(v, "0"); }();
+  return enabled;
+}
+u32 stable_tex_obj_id(const GXTexObj_& obj) {
+  // mode0 carries wrap/filter/bias: the bound-texture cache keeps the object
+  // descriptor, so sampler-relevant state must be part of the identity too.
+  struct Key { u64 data; u32 width, height, format, lod, flags, mode0, tlut; } key{
+      reinterpret_cast<u64>(obj.data), obj.mWidth, obj.mHeight, obj.mFormat, obj.mode1 & 0xFFFFu, obj.flags & 3u,
+      obj.mode0 & 0x00FFFFFFu, static_cast<u32>(obj.tlut)};
+  const u64 hash = XXH3_64bits(&key, sizeof(key));
+  const u32 id = static_cast<u32>(hash ^ (hash >> 32));
+  return id ? id : 1u;
+}
+u32 stable_tlut_obj_id(const GXTlutObj_& obj) {
+  struct Key { u64 data; u32 format, entries; } key{reinterpret_cast<u64>(obj.data), static_cast<u32>(obj.format), obj.numEntries};
+  const u64 hash = XXH3_64bits(&key, sizeof(key));
+  const u32 id = static_cast<u32>(hash ^ (hash >> 32));
+  return id ? id : 1u;
+}
+
 void emit_loaded_texobj_metadata(const GXTexObj_& obj, GXTexMapID id) {
   GX_WRITE_AURORA(GX_AURORA_LOAD_TEXOBJ);
   GX_WRITE_U8(static_cast<u8>(id));
@@ -234,6 +262,7 @@ void GXLoadTexObj(GXTexObj* obj_, GXTexMapID id) {
   __gx->dirtyState |= 1;
   __gx->bpSent = 1;
 
+  if (stable_identities()) obj->texObjId = stable_tex_obj_id(*obj);
   emit_loaded_texobj_metadata(*obj, id);
 }
 
@@ -325,7 +354,8 @@ void GXInitTlutObjData(GXTlutObj* obj_, const void* data) {
 }
 
 void GXLoadTlut(const GXTlutObj* obj_, u32 idx) {
-  auto* obj = reinterpret_cast<const GXTlutObj_*>(obj_);
+  auto* obj = reinterpret_cast<GXTlutObj_*>(const_cast<GXTlutObj*>(obj_));
+  if (stable_identities()) obj->tlutObjId = stable_tlut_obj_id(*obj);
   __GXFlushTextureState();
   GX_WRITE_RAS_REG(obj->loadTlut0);
 
diff --git a/lib/gfx/clear.cpp b/lib/gfx/clear.cpp
index cc73248..b6a7318 100644
--- a/lib/gfx/clear.cpp
+++ b/lib/gfx/clear.cpp
@@ -1,12 +1,34 @@
 #include "clear.hpp"
 
 #include "encoding.hpp"
+#include "hash.hpp"
+#include "pipeline_cache.hpp"
 #include "../webgpu/gpu.hpp"
 #include "tracy/Tracy.hpp"
 
+#ifdef MELEE_MIYOO_FLIP
+#include <absl/container/flat_hash_map.h>
+#include <mutex>
+#endif
+
 namespace aurora::gfx::clear {
 using webgpu::g_device;
 
+#ifdef MELEE_MIYOO_FLIP
+namespace {
+std::mutex sFlipPipelineConfigMutex;
+absl::flat_hash_map<PipelineRef, PipelineConfig> sFlipPipelineConfigs;
+}
+
+bool find_flip_pipeline_config(PipelineRef ref, PipelineConfig& config) {
+  std::lock_guard lock{sFlipPipelineConfigMutex};
+  const auto it=sFlipPipelineConfigs.find(ref);
+  if(it==sFlipPipelineConfigs.end()) return false;
+  config=it->second;
+  return true;
+}
+#endif
+
 namespace {
 wgpu::ColorWriteMask clear_write_mask(bool clearColor, bool clearAlpha) {
   auto writeMask = wgpu::ColorWriteMask::None;
@@ -77,6 +99,12 @@ PipelineConfig make_pipeline_config(const RenderTargetLayout& layout, bool clear
 
 wgpu::RenderPipeline create_pipeline(const PipelineConfig& config) {
   ZoneScoped;
+#ifdef MELEE_MIYOO_FLIP
+  {
+    std::lock_guard lock{sFlipPipelineConfigMutex};
+    sFlipPipelineConfigs[xxh3_hash(config,static_cast<HashType>(ShaderType::Clear))]=config;
+  }
+#endif
   const bool writesSceneColor = config.clearColor || config.clearAlpha;
   const auto source = shader_source(writesSceneColor);
   wgpu::ShaderSourceWGSL sourceDescriptor{};
diff --git a/lib/gfx/clear.hpp b/lib/gfx/clear.hpp
index 0f7a946..007ba10 100644
--- a/lib/gfx/clear.hpp
+++ b/lib/gfx/clear.hpp
@@ -34,4 +34,7 @@ PipelineConfig make_pipeline_config(const RenderTargetLayout& layout, bool clear
                                     bool clearDepth) noexcept;
 wgpu::RenderPipeline create_pipeline(const PipelineConfig& config);
 void render(const DrawData& data, const wgpu::RenderPassEncoder& pass, const wgpu::Extent3D& targetSize);
+#ifdef MELEE_MIYOO_FLIP
+bool find_flip_pipeline_config(PipelineRef ref, PipelineConfig& config);
+#endif
 } // namespace aurora::gfx::clear
diff --git a/lib/gfx/encoding.cpp b/lib/gfx/encoding.cpp
index b3054be..55bc730 100644
--- a/lib/gfx/encoding.cpp
+++ b/lib/gfx/encoding.cpp
@@ -1,4 +1,14 @@
+#ifdef MELEE_MIYOO_FLIP
+#include <chrono>
+#include "../gx/flip_vertex.hpp"
+#include "flip_gles.hpp"
+#endif
 #include "encoding.hpp"
+#ifdef MELEE_MIYOO_FLIP
+#include "flip_upload.hpp"
+#include "flip_profile.hpp"
+#include "flip_sprites.hpp"
+#endif
 
 #include "frame.hpp"
 
@@ -13,6 +23,7 @@
 #include "../rmlui/pipeline.hpp"
 #endif
 #include "../webgpu/gpu.hpp"
+#include "../window.hpp"
 #include "../webgpu/gpu_prof.hpp"
 
 #include <algorithm>
@@ -200,11 +211,44 @@ void render(wgpu::CommandEncoder& cmd, FramePacket& frame, RenderPass& passInfo,
     return;
   }
 
+#ifdef MELEE_MIYOO_FLIP
+  // Scene-on-surface: EFB passes render into the presented texture. The
+  // texture is acquired when the frame's first EFB pass is encoded (the render
+  // worker has presented the previous frame by then) and handed to end_frame.
+  bool sceneOnSurface = false;
+  {
+    const auto& scene = passInfo.colorAttachments[SceneColorAttachmentIndex];
+    const auto& surfaceConfig = webgpu::g_graphicsConfig.surfaceConfiguration;
+    if (flip_scene_on_surface_enabled() && passInfo.msaaSamples == 1 && passInfo.colorAttachmentCount == 1 &&
+        scene.view && scene.view.Get() == webgpu::g_frameBuffer.view.Get() && scene.size.width == surfaceConfig.width &&
+        scene.size.height == surfaceConfig.height) {
+      if (!frame.flipSurfaceTried) {
+        frame.flipSurfaceTried = true;
+        window::SurfaceLock surfaceLock;
+        if (window::is_presentable() && webgpu::g_surface) {
+          wgpu::SurfaceTexture surfaceTexture;
+          webgpu::g_surface.GetCurrentTexture(&surfaceTexture);
+          if (surfaceTexture.status == wgpu::SurfaceGetCurrentTextureStatus::SuccessOptimal && surfaceTexture.texture) {
+            frame.flipSurfaceTexture = std::move(surfaceTexture.texture);
+            frame.flipSurfaceView = frame.flipSurfaceTexture.CreateView();
+          }
+        }
+      }
+      sceneOnSurface = static_cast<bool>(frame.flipSurfaceView);
+    }
+  }
+  const wgpu::Texture& copySourceTexture = sceneOnSurface ? frame.flipSurfaceTexture : passInfo.copySourceTexture;
+  const wgpu::TextureView& copySourceView = sceneOnSurface ? frame.flipSurfaceView : passInfo.copySourceView;
+#else
+  constexpr bool sceneOnSurface = false;
+  const wgpu::Texture& copySourceTexture = passInfo.copySourceTexture;
+  const wgpu::TextureView& copySourceView = passInfo.copySourceView;
+#endif
   std::array<wgpu::RenderPassColorAttachment, MaxColorAttachments> attachments{};
   for (uint32_t i = 0; i < passInfo.colorAttachmentCount; ++i) {
     const auto& source = passInfo.colorAttachments[i];
     attachments[i] = {
-        .view = source.view,
+        .view = (sceneOnSurface && i == SceneColorAttachmentIndex) ? frame.flipSurfaceView : source.view,
         .resolveTarget = source.resolveView,
         .loadOp = source.loadOp != wgpu::LoadOp::Undefined ? source.loadOp
                                                            : (source.clear ? wgpu::LoadOp::Clear : wgpu::LoadOp::Load),
@@ -246,6 +290,9 @@ void render(wgpu::CommandEncoder& cmd, FramePacket& frame, RenderPass& passInfo,
   };
 
   auto pass = cmd.BeginRenderPass(&renderPassDescriptor);
+#ifdef MELEE_MIYOO_FLIP
+  if(!encode_flip_gles_resources(pass,passInfo,label))
+#endif
   render_pass(pass, frame, passInfo);
   pass.End();
 
@@ -254,20 +301,21 @@ void render(wgpu::CommandEncoder& cmd, FramePacket& frame, RenderPass& passInfo,
                                       passInfo.colorAttachments[SceneColorAttachmentIndex].size, passInfo.msaaSamples);
   }
 
-  if (passInfo.resolveTarget) {
-    const auto& dstSize = passInfo.resolveTarget->size;
-    const bool needsConversion = tex_copy_conv::needs_conversion(passInfo.resolveFormat);
-    const bool needsScaling = dstSize.width != static_cast<uint32_t>(passInfo.resolveRect.width) ||
-                              dstSize.height != static_cast<uint32_t>(passInfo.resolveRect.height);
-    const bool isDepth = gx::is_depth_format(passInfo.resolveFormat);
+  const auto resolve = [&](const TextureHandle& resolveTarget, GXTexFmt resolveFormat, const ClipRect& resolveRect,
+                           const Range& resolveUniformRange) {
+    const auto& dstSize = resolveTarget->size;
+    const bool needsConversion = tex_copy_conv::needs_conversion(resolveFormat);
+    const bool needsScaling = dstSize.width != static_cast<uint32_t>(resolveRect.width) ||
+                              dstSize.height != static_cast<uint32_t>(resolveRect.height);
+    const bool isDepth = gx::is_depth_format(resolveFormat);
     if (isDepth && passInfo.msaaSamples > 1) {
       Log.fatal("Depth tex copies from multisampled EFB targets are not supported");
     }
     const tex_copy_conv::ConvRequest convReq{
-        .fmt = passInfo.resolveFormat,
-        .srcView = isDepth ? passInfo.copySourceDepthView : passInfo.copySourceView,
-        .uniformRange = passInfo.resolveUniformRange,
-        .dst = passInfo.resolveTarget,
+        .fmt = resolveFormat,
+        .srcView = isDepth ? passInfo.copySourceDepthView : copySourceView,
+        .uniformRange = resolveUniformRange,
+        .dst = resolveTarget,
         .sampleFilter = needsScaling ? tex_copy_conv::SampleFilter::Linear : tex_copy_conv::SampleFilter::Nearest,
     };
     if (needsConversion) {
@@ -277,29 +325,70 @@ void render(wgpu::CommandEncoder& cmd, FramePacket& frame, RenderPass& passInfo,
     } else {
       const webgpu::gpu_prof::Zone zone{cmd, "EFB copy"};
       const wgpu::TexelCopyTextureInfo src{
-          .texture = passInfo.copySourceTexture,
+          .texture = copySourceTexture,
           .origin =
               wgpu::Origin3D{
-                  .x = static_cast<uint32_t>(passInfo.resolveRect.x),
-                  .y = static_cast<uint32_t>(passInfo.resolveRect.y),
+                  .x = static_cast<uint32_t>(resolveRect.x),
+                  .y = static_cast<uint32_t>(resolveRect.y),
               },
       };
       const wgpu::TexelCopyTextureInfo dst{
-          .texture = passInfo.resolveTarget->texture,
+          .texture = resolveTarget->texture,
       };
       const wgpu::Extent3D size{
-          .width = static_cast<uint32_t>(passInfo.resolveRect.width),
-          .height = static_cast<uint32_t>(passInfo.resolveRect.height),
+          .width = static_cast<uint32_t>(resolveRect.width),
+          .height = static_cast<uint32_t>(resolveRect.height),
           .depthOrArrayLayers = 1,
       };
       cmd.CopyTextureToTexture(&src, &dst, &size);
     }
+  };
+#ifdef MELEE_MIYOO_FLIP
+  // Fused shadow pass: convert both copies in one two-target pass when they
+  // share the format and neither needs scaling (MELEE_FLIP_DUAL_CONV=0 splits).
+  static const bool dualConv = [] {
+    const char* v = std::getenv("MELEE_FLIP_DUAL_CONV");
+    return v == nullptr || std::strcmp(v, "0") != 0;
+  }();
+  bool dualDone = false;
+  if (dualConv && passInfo.resolveTarget && passInfo.flipExtraResolves.size() == 1 &&
+      passInfo.flipDualResolveUniform.size == 32) {
+    const auto& extra = passInfo.flipExtraResolves[0];
+    const auto unscaled = [](const TextureHandle& target, const ClipRect& rect) {
+      return target->size.width == static_cast<uint32_t>(rect.width) &&
+             target->size.height == static_cast<uint32_t>(rect.height);
+    };
+    if (extra.target && extra.format == passInfo.resolveFormat && !gx::is_depth_format(passInfo.resolveFormat) &&
+        tex_copy_conv::dual_supported(passInfo.resolveFormat) && unscaled(passInfo.resolveTarget, passInfo.resolveRect) &&
+        unscaled(extra.target, extra.rect) && extra.target->size.width == passInfo.resolveTarget->size.width &&
+        extra.target->size.height == passInfo.resolveTarget->size.height) {
+      const tex_copy_conv::ConvRequest convReq{
+          .fmt = passInfo.resolveFormat,
+          .srcView = copySourceView,
+          .uniformRange = passInfo.resolveUniformRange,
+          .dst = passInfo.resolveTarget,
+          .sampleFilter = tex_copy_conv::SampleFilter::Nearest,
+      };
+      tex_copy_conv::run_dual(cmd, convReq, extra.target, passInfo.flipDualResolveUniform);
+      dualDone = true;
+    }
   }
+  if (!dualDone) {
+#endif
+  if (passInfo.resolveTarget) {
+    resolve(passInfo.resolveTarget, passInfo.resolveFormat, passInfo.resolveRect, passInfo.resolveUniformRange);
+  }
+#ifdef MELEE_MIYOO_FLIP
+  for (const auto& extra : passInfo.flipExtraResolves) {
+    if (extra.target) resolve(extra.target, extra.format, extra.rect, extra.uniformRange);
+  }
+  }
+#endif
 
   if (passInfo.snapshotColorDst) {
     const webgpu::gpu_prof::Zone zone{cmd, "Pass snapshot"};
     const wgpu::TexelCopyTextureInfo src{
-        .texture = passInfo.copySourceTexture,
+        .texture = copySourceTexture,
     };
     const wgpu::TexelCopyTextureInfo dst{
         .texture = passInfo.snapshotColorDst,
@@ -353,8 +442,35 @@ void copy_staging_to_high_water(wgpu::CommandEncoder& cmd, FramePacket& frame, c
     return;
   }
   const webgpu::gpu_prof::Zone zone{cmd, "Staging copies"};
-  const auto& highWater = op.highWater;
+  auto highWater = op.highWater;
+#ifdef MELEE_MIYOO_FLIP
+  // Geometry is immutable within a packet. Upload it once before executing
+  // any render passes, avoiding repeated modification of live Mali buffers.
+  if (gx::flip_vertex_enabled()) {
+    highWater.verts = frame.copied.verts;
+    highWater.uniforms = frame.copied.uniforms;
+    highWater.indices = frame.copied.indices;
+    highWater.storage = frame.copied.storage;
+  }
+#endif
   auto& res = resources();
+#ifdef MELEE_MIYOO_FLIP
+  // Copy touched rows before drawing; later high-water marks refresh partial rows.
+  const auto copyTexture = [&](uint32_t copied, uint32_t high, uint64_t offset, const wgpu::Texture& tex) {
+    if (high <= copied) return;
+    const uint32_t first = copied / 4096;
+    const uint32_t last = (high + 4095) / 4096;
+    const wgpu::TexelCopyBufferInfo src{
+      .layout = {.offset = offset + first * 4096, .bytesPerRow = 4096},
+      .buffer = staging_buffer(frame.stagingBuffer),
+    };
+    const wgpu::TexelCopyTextureInfo dst{.texture = tex, .origin = {0, first, 0}};
+    const wgpu::Extent3D extent{1024, last - first, 1};
+    cmd.CopyBufferToTexture(&src, &dst, &extent);
+  };
+  if(!gx::flip_vertex_enabled()) copyTexture(frame.copied.verts, highWater.verts, VertexStagingOffset, res.vertexTexture);
+  copyTexture(frame.copied.storage, highWater.storage, StorageStagingOffset, res.storageTexture);
+#endif
   copy_staging_buffer_range(cmd, frame, frame.copied.verts, highWater.verts, VertexStagingOffset, res.vertexBuffer);
   copy_staging_buffer_range(cmd, frame, frame.copied.uniforms, highWater.uniforms, UniformStagingOffset,
                             res.uniformBuffer);
@@ -363,12 +479,16 @@ void copy_staging_to_high_water(wgpu::CommandEncoder& cmd, FramePacket& frame, c
                             res.storageBuffer);
 
   if constexpr (UseTextureBuffer) {
+    uint64_t textureBase = TextureUploadStagingOffset;
+#ifdef MELEE_MIYOO_FLIP
+    if (gx::flip_cpu_upload_enabled()) textureBase = 0;
+#endif
     for (size_t i = frame.copied.textureUploadCount; i < op.textureUploads.size(); ++i) {
       const auto& item = *op.textureUploads[i];
       const wgpu::TexelCopyBufferInfo buf{
           .layout =
               wgpu::TexelCopyBufferLayout{
-                  .offset = item.buffer ? item.layout.offset : item.layout.offset + TextureUploadStagingOffset,
+                  .offset = item.buffer ? item.layout.offset : item.layout.offset + textureBase,
                   .bytesPerRow = AURORA_ALIGN(item.layout.bytesPerRow, 256),
                   .rowsPerImage = item.layout.rowsPerImage,
               },
@@ -383,6 +503,149 @@ void copy_staging_to_high_water(wgpu::CommandEncoder& cmd, FramePacket& frame, c
 } // namespace
 
 namespace detail {
+#ifdef MELEE_MIYOO_FLIP
+void upload_flip_geometry(FramePacket& frame) {
+  flip_profile::begin(frame.frameId,"render");
+  flip_profile::Scope uploadProfile("geometry_upload",0,frame.verts.size()+frame.uniforms.size()+frame.indices.size());
+  if (!gx::flip_vertex_enabled()) return;
+  auto& res = resources();
+  if (gx::flip_cpu_upload_enabled()) {
+    const auto uploadStart=std::chrono::steady_clock::now();
+    const bool nativeStream=upload_flip_gles_stream(frame);
+    static const bool dirtyUploads=[] {const char* v=std::getenv("MELEE_FLIP_DIRTY_UPLOAD");return v&&std::strcmp(v,"0");}();
+    // Extension callbacks receive writable shared buffers. Invalidate the
+    // shadow for such frames, including the following frame, rather than
+    // assuming that only this uploader can have changed their contents.
+    bool cacheable=dirtyUploads&&frame.encoderTasks.empty()&&!nativeStream;
+    for(const auto& pass:frame.renderPasses) for(const auto& command:pass.commands)
+      if(command.type==CommandType::CustomDraw) cacheable=false;
+    if(!cacheable) {
+      res.flipUploadedVertices.clear();res.flipUploadedIndices.clear();res.flipUploadedUniforms.clear();
+    }
+    size_t compared=0,written=0,writes=0;
+    const auto changed=[&](const wgpu::Buffer& dst,const uint8_t* data,size_t size,
+                            std::vector<uint8_t>& previous,size_t chunk) {
+      compared+=size;
+      flip_upload_changed(previous,{data,size},chunk,[&](size_t offset,size_t bytes) {
+        webgpu::g_queue.WriteBuffer(dst,offset,data+offset,bytes);written+=bytes;++writes;
+      });
+    };
+    const auto upload = [](const wgpu::Buffer& dst, const ByteBuffer& src) {
+      if (src.size()) webgpu::g_queue.WriteBuffer(dst, 0, src.data(), AURORA_ALIGN(src.size(), 4));
+    };
+    const auto* mappedSlot=frame.flipMapped?flip_mapped_slot(frame):nullptr;
+    const bool mappedVerts=mappedSlot&&mappedSlot->vertexData;
+    static const bool sparseUniforms=[] {const char* v=std::getenv("MELEE_FLIP_SPARSE_UNIFORMS");return !v||std::strcmp(v,"0");}();
+    if(gx::flip_uniform_windows_enabled()&&sparseUniforms) {
+      for(const auto& range:frame.flipOrdinaryUniforms)
+        webgpu::g_queue.WriteBuffer(res.uniformBuffer,range.offset,frame.uniforms.data()+range.offset,AURORA_ALIGN(range.size,4));
+    } else upload(res.uniformBuffer, frame.uniforms);
+    // Frames recorded into persistently mapped storage need Dawn copies of the
+    // uniform windows and indices only when a pass falls back to the reference
+    // renderer; qualified direct passes bind the mapped GL buffers themselves.
+    // Encoder tasks may read the streaming buffers through Dawn, except the
+    // sprite pass tasks, which only touch their own targets.
+    bool streamTasks=false;
+    for(const auto& task:frame.encoderTasks) if(!flip_sprites::is_sprite_task(task.type)) {streamTasks=true;break;}
+    bool mappedDirectOnly=frame.flipMapped&&!streamTasks;
+    if(mappedDirectOnly) for(const auto& pass:frame.renderPasses) {
+      if(!pass.sealed||pass.discardable) continue;
+      bool gxDraw=false;
+      for(const auto& command:pass.commands) {
+        if(command.type==CommandType::CustomDraw) {mappedDirectOnly=false;break;}
+        if(command.type!=CommandType::Draw) continue;
+        clear::DrawData clearDraw; gx::DrawData draw;
+        if(decode_flip_clear_draw(command.data.draw,clearDraw)) continue;
+        if(decode_flip_gx_draw(command.data.draw,draw)) gxDraw=true;
+      }
+      if(gxDraw&&!pass.flipDirectResourcesOnly) {mappedDirectOnly=false;break;}
+    }
+    if(frame.flipMapped) {
+      // Uploads from mapped memory bypass the shadow compare (it would read
+      // write-combined storage); reference frames copy the used ranges whole.
+      res.flipUploadedUniforms.clear();res.flipUploadedIndices.clear();
+    }
+    // Diagnostic: also push the mapped data through Dawn's buffers so the direct
+    // path can be pointed at either copy (MELEE_FLIP_MAPPED_MIRROR=1 uploads,
+    // MELEE_FLIP_MAPPED_BIND_DAWN=1 makes the direct path bind Dawn's copies).
+    static const bool mappedMirror=[] {const char* v=std::getenv("MELEE_FLIP_MAPPED_MIRROR");return v&&v[0]=='1';}();
+    if(!nativeStream) {
+      // Vertices recorded into mapped storage reach the GPU through the slot's
+      // own GL buffer; Dawn's copy is refreshed whole only for reference passes
+      // (the shadow compare must not read write-combined memory).
+      if(mappedVerts) { res.flipUploadedVertices.clear(); if(!mappedDirectOnly||mappedMirror) upload(res.vertexBuffer, frame.verts); }
+      else if(cacheable) changed(res.vertexBuffer,frame.verts.data(),AURORA_ALIGN(frame.verts.size(),4),res.flipUploadedVertices,4096);
+      else upload(res.vertexBuffer, frame.verts);
+    }
+    if(mappedDirectOnly&&!mappedMirror) {
+      for (uint32_t offset=0; offset<frame.uniforms.size(); offset+=gx::FlipUniformWindow) flip_uniform_window(offset/gx::FlipUniformWindow);
+    } else if (gx::flip_uniform_windows_enabled()&&!nativeStream) {
+      if(cacheable) res.flipUploadedUniforms.resize(std::max<size_t>(res.flipUploadedUniforms.size(),
+          (frame.uniforms.size()+gx::FlipUniformWindow-1)/gx::FlipUniformWindow));
+      for (uint32_t offset=0; offset<frame.uniforms.size(); offset+=gx::FlipUniformWindow) {
+        const uint32_t index=offset/gx::FlipUniformWindow;
+        flip_uniform_window(index);
+        const size_t bytes=std::min<size_t>(gx::FlipUniformWindow,frame.uniforms.size()-offset);
+        if(cacheable&&!frame.flipMapped) changed(res.flipUniformBuffers[index],frame.uniforms.data()+offset,AURORA_ALIGN(bytes,4),
+                             res.flipUploadedUniforms[index],256);
+        else webgpu::g_queue.WriteBuffer(res.flipUniformBuffers[index],0,frame.uniforms.data()+offset,AURORA_ALIGN(bytes,4));
+      }
+    }
+    static const bool mappedIndices=[] {const char* v=std::getenv("MELEE_FLIP_MAPPED_USE");return !v||!std::strcmp(v,"indices")||!std::strcmp(v,"all");}();
+    if(!nativeStream&&(!mappedDirectOnly||mappedMirror||!mappedIndices)) {
+      if(cacheable&&!frame.flipMapped) changed(res.indexBuffer,frame.indices.data(),AURORA_ALIGN(frame.indices.size(),4),res.flipUploadedIndices,4096);
+      else upload(res.indexBuffer, frame.indices);
+    }
+    if(cacheable&&frame.frameId%120==0&&std::getenv("MELEE_FLIP_PROFILE"))
+      std::fprintf(stderr,"[flip-upload] compared=%zu written=%zu ranges=%zu\n",compared,written,writes);
+    static const bool uploadPhase=std::getenv("MELEE_FLIP_PROFILE")!=nullptr;
+    const auto tGeom=std::chrono::steady_clock::now();
+    upload(res.storageBuffer, frame.storage);
+    const auto tStorage=std::chrono::steady_clock::now();
+    upload(staging_buffer(frame.stagingBuffer), frame.textureUpload);
+    const auto tTex=std::chrono::steady_clock::now();
+    if (frame.stats.lastStorageSize) {
+      const wgpu::TexelCopyTextureInfo dst{.texture=res.storageTexture};
+      const wgpu::TexelCopyBufferLayout layout{.bytesPerRow=4096};
+      const wgpu::Extent3D size{1024,(frame.stats.lastStorageSize+4095)/4096,1};
+      webgpu::g_queue.WriteTexture(&dst,frame.storage.data(),size.height*4096,&layout,&size);
+    }
+    if(uploadPhase) {
+      static double geomMs=0,storageMs=0,texMs=0,texTexMs=0; static size_t texBytes=0,texCount=0,storageBytes=0; static unsigned frames=0;
+      const auto tEnd=std::chrono::steady_clock::now();
+      const auto ms=[](auto a,auto b){return std::chrono::duration<double,std::milli>(b-a).count();};
+      geomMs+=ms(uploadStart,tGeom); storageMs+=ms(tGeom,tStorage); texMs+=ms(tStorage,tTex); texTexMs+=ms(tTex,tEnd);
+      texBytes+=frame.textureUpload.size(); texCount+=frame.textureUploads.size(); storageBytes+=frame.storage.size();
+      if(++frames%120==0) {
+        std::fprintf(stderr,"[flip-upload-phase] geom_ms=%.3f storage_ms=%.3f texstage_ms=%.3f storagetex_ms=%.3f tex_bytes=%zu tex_count=%.1f storage_bytes=%zu\n",
+          geomMs/120,storageMs/120,texMs/120,texTexMs/120,texBytes/120,texCount/120.0,storageBytes/120);
+        geomMs=storageMs=texMs=texTexMs=0; texBytes=texCount=storageBytes=0;
+      }
+    }
+    return;
+  }
+  auto cmd = webgpu::g_device.CreateCommandEncoder();
+  copy_staging_buffer_range(cmd, frame, frame.copied.verts, frame.stats.lastVertSize,
+                           VertexStagingOffset, res.vertexBuffer);
+  copy_staging_buffer_range(cmd, frame, frame.copied.uniforms, frame.stats.lastUniformSize,
+                           UniformStagingOffset, res.uniformBuffer);
+  copy_staging_buffer_range(cmd, frame, frame.copied.indices, frame.stats.lastIndexSize,
+                           IndexStagingOffset, res.indexBuffer);
+  // Storage can include fog tables read through the integer texture fallback.
+  if (frame.stats.lastStorageSize) {
+    const wgpu::TexelCopyBufferInfo src{.layout={.offset=StorageStagingOffset,.bytesPerRow=4096},
+                                      .buffer=staging_buffer(frame.stagingBuffer)};
+    const wgpu::TexelCopyTextureInfo dst{.texture=res.storageTexture};
+    const wgpu::Extent3D size{1024,(frame.stats.lastStorageSize+4095)/4096,1};
+    cmd.CopyBufferToTexture(&src,&dst,&size);
+  }
+  copy_staging_buffer_range(cmd, frame, frame.copied.storage, frame.stats.lastStorageSize,
+                           StorageStagingOffset, res.storageBuffer);
+  auto buffer=cmd.Finish();
+  webgpu::g_queue.Submit(1,&buffer);
+}
+#endif
+
 void encode_op(wgpu::CommandEncoder& cmd, FramePacket& frame, const FrameOp& op) {
   copy_staging_to_high_water(cmd, frame, op);
   switch (op.type) {
@@ -399,11 +662,34 @@ void encode_op(wgpu::CommandEncoder& cmd, FramePacket& frame, const FrameOp& op)
     break;
   case FrameOpType::EncoderTask:
     if (op.encoderTask != nullptr) {
+#ifdef MELEE_MIYOO_FLIP
+      flip_sprites::set_scene_view(frame.flipSurfaceView ? frame.flipSurfaceView : webgpu::g_frameBuffer.view);
+#endif
       execute_encoder_task(cmd, frame, *op.encoderTask);
     }
     break;
   }
 }
+#ifdef MELEE_MIYOO_FLIP
+bool flip_scene_on_surface_enabled() {
+  static const bool enabled = [] {
+    const char* v = std::getenv("MELEE_FLIP_SCENE_ON_SURFACE");
+    return v == nullptr || std::strcmp(v, "0") != 0;
+  }();
+  return enabled;
+}
+namespace {
+wgpu::Texture g_flipFrameSurfaceTexture;
+wgpu::TextureView g_flipFrameSurfaceView;
+} // namespace
+void set_flip_frame_surface(wgpu::Texture texture, wgpu::TextureView view) {
+  g_flipFrameSurfaceTexture = std::move(texture);
+  g_flipFrameSurfaceView = std::move(view);
+}
+std::pair<wgpu::Texture, wgpu::TextureView> take_flip_frame_surface() {
+  return {std::exchange(g_flipFrameSurfaceTexture, {}), std::exchange(g_flipFrameSurfaceView, {})};
+}
+#endif
 } // namespace detail
 
 bool bind_pipeline(PipelineRef ref, const wgpu::RenderPassEncoder& pass) {
diff --git a/lib/gfx/encoding.hpp b/lib/gfx/encoding.hpp
index 1b25b3d..01695a5 100644
--- a/lib/gfx/encoding.hpp
+++ b/lib/gfx/encoding.hpp
@@ -7,6 +7,9 @@ namespace aurora::gfx {
 bool bind_pipeline(PipelineRef ref, const wgpu::RenderPassEncoder& pass);
 
 namespace detail {
+#ifdef MELEE_MIYOO_FLIP
+void upload_flip_geometry(FramePacket& frame);
+#endif
 void encode_op(wgpu::CommandEncoder& encoder, FramePacket& frame, const FrameOp& op);
 }
 
diff --git a/lib/gfx/flip_gles.cpp b/lib/gfx/flip_gles.cpp
new file mode 100644
index 0000000..b002481
--- /dev/null
+++ b/lib/gfx/flip_gles.cpp
@@ -0,0 +1,1155 @@
+#include "flip_gles.hpp"
+
+#ifdef MELEE_MIYOO_FLIP
+#include "clear.hpp"
+#include "frame_packet.hpp"
+#include "pipeline_cache.hpp"
+#include "resource_cache.hpp"
+#include "resources.hpp"
+#include "flip_stream.hpp"
+#include "flip_profile.hpp"
+#include "../gx/flip_vertex.hpp"
+#include "../gx/flip_resident.hpp"
+#include "../gx/flip_gles_uber.hpp"
+#include "../gx/flip_uber.hpp"
+#include "../gx/pipeline.hpp"
+#include "../webgpu/gpu.hpp"
+
+#include <dawn/native/OpenGLBackend.h>
+#include <GLES3/gl31.h>
+#include <EGL/egl.h>
+
+#include <algorithm>
+#include <array>
+#include <bit>
+#include <cmath>
+#include <chrono>
+#include <cstdio>
+#include <cstdlib>
+#include <cstring>
+#include <limits>
+#include <vector>
+#include <unordered_map>
+#include <unordered_set>
+
+namespace aurora::gfx::detail {
+namespace {
+constexpr Module Log{"aurora::gfx::flip_gles"};
+// The Flip SDK supplies core GLES headers only. Load this optional extension
+// dynamically after checking its advertised name.
+constexpr GLenum GL_TIME_ELAPSED_EXT=0x88BF,GL_GPU_DISJOINT_EXT=0x8FBB;
+using GetQuery64=void (GL_APIENTRY *)(GLuint,GLenum,GLuint64*);
+struct PlanCommand {
+  CommandType type=CommandType::DebugMarker;
+  Viewport viewport{};
+  ClipRect scissor{};
+  gx::DrawData draw{};
+  clear::DrawData clear{};
+  bool isClear=false;
+  bool sortable=false; // opaque, depth-tested and depth-written with LESS/LEQUAL: order-independent barring equal depths
+};
+struct PassPlan {
+  wgpu::TextureView target;
+  std::string label;
+  std::vector<PlanCommand> commands;
+  uint32_t width=0,height=0;
+  uint32_t segmentStart=UINT32_MAX; // fused shadow pass: first command of the shifted segment
+  bool eligible=false;
+};
+std::vector<PassPlan> sPlans;
+size_t sNextPlan=0;
+const FlipMappedSlot* sMapped=nullptr; // this frame's persistently mapped uniform/index storage
+const FlipMappedSlot* sMappedUniforms=nullptr;
+const FlipMappedSlot* sMappedIndices=nullptr;
+const FlipMappedSlot* sMappedVertices=nullptr;
+GLuint sVao=0;
+GLuint sClearProgram=0;
+uint32_t sEnabledAttributes=0;
+uint64_t sFrameNumber=0;
+// Per-frame GL call counts for the direct path (MELEE_FLIP_PROFILE, [flip-gl-calls]).
+struct GlCallStats { unsigned programs=0,pipelineStates=0,textures=0,texParams=0,samplers=0,ubos=0,vbos=0,layouts=0,immediates=0,draws=0,constantDraws=0; };
+GlCallStats sGlCalls;
+struct PresentBlit { wgpu::TextureView source; uint32_t width=0,height=0; bool pending=false; };
+PresentBlit sPresentBlit;
+GLuint sPresentReadFbo=0,sPresentReadAttachment=0;
+// Dawn has bound and cleared the swapchain framebuffer when the pass callback
+// runs; copying the EFB with glBlitFramebuffer replaces a full-screen textured
+// draw and its pass setup (~0.8 ms of driver time per frame).
+bool present_blit() {
+  const auto src=dawn::native::opengl::GetDirectGLTextureView(sPresentBlit.source.Get());
+  const GLuint name=src.texture?src.texture:src.renderbuffer;
+  if(!name) return false;
+  if(!sPresentReadFbo) glGenFramebuffers(1,&sPresentReadFbo);
+  glBindFramebuffer(GL_READ_FRAMEBUFFER,sPresentReadFbo);
+  if(sPresentReadAttachment!=name) {
+    if(src.texture) glFramebufferTexture2D(GL_READ_FRAMEBUFFER,GL_COLOR_ATTACHMENT0,GL_TEXTURE_2D,src.texture,0);
+    else glFramebufferRenderbuffer(GL_READ_FRAMEBUFFER,GL_COLOR_ATTACHMENT0,GL_RENDERBUFFER,src.renderbuffer);
+    if(glCheckFramebufferStatus(GL_READ_FRAMEBUFFER)!=GL_FRAMEBUFFER_COMPLETE) { glBindFramebuffer(GL_READ_FRAMEBUFFER,0); sPresentReadAttachment=0; return false; }
+    sPresentReadAttachment=name;
+  }
+  const GLboolean scissor=glIsEnabled(GL_SCISSOR_TEST);
+  if(scissor) glDisable(GL_SCISSOR_TEST);
+  glBlitFramebuffer(0,0,GLint(sPresentBlit.width),GLint(sPresentBlit.height),0,0,GLint(sPresentBlit.width),GLint(sPresentBlit.height),GL_COLOR_BUFFER_BIT,GL_NEAREST);
+  if(scissor) glEnable(GL_SCISSOR_TEST);
+  glBindFramebuffer(GL_READ_FRAMEBUFFER,0);
+  const GLenum err=glGetError();
+  if(err!=GL_NO_ERROR) { static bool once=false; if(!once) { once=true; std::fprintf(stderr,"[flip-present-blit] GL error 0x%x\n",err); } return true; }
+  return true;
+}
+FlipStream sStream;
+
+struct TextureBinding {
+  uint32_t binding=0;
+  std::vector<uint32_t> textures,samplers;
+};
+struct PreparedPipeline {
+  gx::PipelineConfig config{};
+  wgpu::RenderPipeline owner;
+  dawn::native::opengl::DirectGLPipelineInfo gl{};
+  gx::FlipVertexLayout layout{};
+  GLuint uniformBinding=0;
+  GLint immediates=-1;
+  // Batched conventional-vertex pipelines read the matrix index and uniform
+  // record from vertex data; no field of tint_immediates is referenced.
+  bool immediatesUnused=false;   // only _pad is read: update on change
+  uint32_t lastImmediatePad=UINT32_MAX;
+  bool nativeUber=false;
+  std::vector<uint32_t> storageUnits;
+  std::vector<TextureBinding> textures;
+};
+std::unordered_map<PipelineRef,PreparedPipeline> sPrepared;
+constexpr GLuint Unknown=UINT32_MAX;
+GLuint sLastProgram=Unknown,sLastUniform=Unknown,sLastUniformBinding=Unknown;
+uint32_t sLastUniformOffset=Unknown;
+uint32_t sLastLayout=Unknown;
+PipelineRef sLastPipeline=UINTPTR_MAX;
+GLuint sLastVertexBuffer=Unknown; uint32_t sLastVertexOffset=UINT32_MAX,sLastVertexStride=UINT32_MAX;
+GLuint sLastRecordBuffer=Unknown; uint32_t sLastRecordOffset=UINT32_MAX;
+GLuint sBindingDivisor=Unknown; // binding 0 step: 1 for instanced point sprites
+// One vertex array object per attribute layout (MELEE_FLIP_LAYOUT_VAO=1,
+// experiment, off by default): a layout switch becomes glBindVertexArray instead
+// of ~10 attribute format calls and vertex-buffer binds drop 82 -> 21 per frame,
+// yet the render worker does not move (the driver's per-draw cost is not in
+// these calls). Buffer bindings are VAO state, so each VAO remembers its own.
+struct LayoutVao { GLuint vao=0,ebo=0; GLuint vertexBuffer=Unknown; uint32_t vertexOffset=UINT32_MAX,stride=UINT32_MAX; GLuint recordBuffer=Unknown; uint32_t recordOffset=UINT32_MAX; };
+std::unordered_map<uint32_t,LayoutVao> sLayoutVaos;
+LayoutVao* sCurrentVao=nullptr;
+GLuint sPassEbo=0;
+bool layout_vao_enabled() {
+  static const bool enabled=[] {const char* v=std::getenv("MELEE_FLIP_LAYOUT_VAO");return v&&std::strcmp(v,"0");}();
+  return enabled;
+}
+GLuint zero_records_gl_buffer() {
+  static GLuint name=0;
+  if(!name) name=dawn::native::opengl::GetDirectGLBuffer(resources().flipZeroRecords.Get());
+  return name;
+}
+struct ResidentArenaGL { uint32_t generation=UINT32_MAX; GLuint name=0; };
+std::vector<ResidentArenaGL> sResidentGL;
+GLuint resident_gl_buffer(uint32_t resident) {
+  if(sResidentGL.size()<resident) sResidentGL.resize(resident);
+  auto& entry=sResidentGL[resident-1];
+  uint32_t generation=0;
+  const auto& buffer=gx::resident::buffer(resident,&generation);
+  if(entry.generation!=generation||!entry.name) {
+    entry.name=buffer?dawn::native::opengl::GetDirectGLBuffer(buffer.Get()):0;
+    entry.generation=generation;
+  }
+  return entry.name;
+}
+struct TextureUnitState {
+  bool valid=false;
+  dawn::native::opengl::DirectGLTextureInfo texture{};
+  GLuint sampler=Unknown;
+};
+std::array<TextureUnitState,32> sTextureState;
+
+bool cache_enabled() {
+  static const bool enabled=[] {const char* v=std::getenv("MELEE_FLIP_DIRECT_CACHE");return !v||std::strcmp(v,"0");}();
+  return enabled;
+}
+
+bool profile_enabled() {
+  static const bool enabled=[] {const char* v=std::getenv("MELEE_FLIP_PROFILE");return v&&std::strcmp(v,"0");}();
+  return enabled;
+}
+
+struct PassTimer {
+  using Clock=std::chrono::steady_clock;
+  Clock::time_point start;
+  GLuint query=0;
+  uint64_t frame=sFrameNumber;
+  struct Pending {GLuint query;uint64_t frame;};
+  static inline std::vector<Pending> pending;
+  static inline GetQuery64 get64=nullptr;
+  static inline bool initialized=false;
+  PassTimer() {
+    if(!profile_enabled()) return;
+    if(!initialized) {
+      const char* extensions=reinterpret_cast<const char*>(glGetString(GL_EXTENSIONS));
+      if(extensions&&std::strstr(extensions,"GL_EXT_disjoint_timer_query"))
+        get64=reinterpret_cast<GetQuery64>(eglGetProcAddress("glGetQueryObjectui64vEXT"));
+      initialized=true;
+    }
+    if(get64) {
+      GLint disjoint=0;glGetIntegerv(GL_GPU_DISJOINT_EXT,&disjoint);
+      if(disjoint) {
+        for(auto& result:pending) glDeleteQueries(1,&result.query);
+        pending.clear();
+      }
+      for(auto it=pending.begin();it!=pending.end();) {
+        GLuint ready=0;glGetQueryObjectuiv(it->query,GL_QUERY_RESULT_AVAILABLE,&ready);
+        if(!ready) {++it;continue;}
+        GLuint64 ns=0;get64(it->query,GL_QUERY_RESULT,&ns);
+        if(!disjoint) std::fprintf(stderr,"[flip-direct-gpu] frame=%llu gpu_ms=%.3f\n",
+            static_cast<unsigned long long>(it->frame),double(ns)/1e6);
+        glDeleteQueries(1,&it->query);it=pending.erase(it);
+      }
+      // Sample asynchronously. Never wait for a GPU result in the submit path.
+      if(frame%60==0&&pending.size()<8) {
+        glGenQueries(1,&query);glBeginQuery(GL_TIME_ELAPSED_EXT,query);
+      }
+    }
+    start=Clock::now();
+  }
+  ~PassTimer() {
+    if(!profile_enabled()) return;
+    if(query) {glEndQuery(GL_TIME_ELAPSED_EXT);pending.push_back({query,frame});}
+    if(frame%60==0) std::fprintf(stderr,"[flip-direct-cpu] frame=%llu wall_ms=%.3f\n",
+        static_cast<unsigned long long>(frame),std::chrono::duration<double,std::milli>(Clock::now()-start).count());
+  }
+};
+
+int direct_mode() {
+  static const int value=[] {
+    const char* s=std::getenv("MELEE_FLIP_DIRECT_GLES");
+    if(!s||!std::strcmp(s,"0")) return 0;
+    const int parsed=std::atoi(s);
+    return parsed>0?std::min(parsed,6):6;
+  }();
+  return value;
+}
+
+uint32_t draw_limit() {
+  static const uint32_t value=[] {
+    const char* s=std::getenv("MELEE_FLIP_DIRECT_DRAW_LIMIT");
+    if(!s||!*s) return std::numeric_limits<uint32_t>::max();
+    return static_cast<uint32_t>(std::max(0,std::atoi(s)));
+  }();
+  return value;
+}
+
+bool gl_ok(const char* section,uint32_t passIndex,uint32_t drawIndex) {
+  static const bool everyDraw=[] {const char* v=std::getenv("MELEE_FLIP_DIRECT_CHECKS");return !v||std::strcmp(v,"0");}();
+  if(!everyDraw&&(!std::strcmp(section,"pipeline-state")||!std::strcmp(section,"resources")||!std::strcmp(section,"draw")))
+    return true;
+  const GLenum error=glGetError();
+  if(error==GL_NO_ERROR) return true;
+  std::fprintf(stderr,"[flip-direct-error] frame=%llu pass=%u draw=%u section=%s error=0x%x\n",
+               static_cast<unsigned long long>(sFrameNumber),passIndex,drawIndex,section,error);
+  while(glGetError()!=GL_NO_ERROR) {}
+  return false;
+}
+
+GLuint compile_shader(GLenum type,const char* source,const char* label) {
+  const GLuint shader=glCreateShader(type);
+  glShaderSource(shader,1,&source,nullptr);
+  glCompileShader(shader);
+  GLint ok=GL_FALSE;
+  glGetShaderiv(shader,GL_COMPILE_STATUS,&ok);
+  if(ok==GL_TRUE) return shader;
+  char log[2048]{};GLsizei length=0;
+  glGetShaderInfoLog(shader,sizeof(log),&length,log);
+  std::fprintf(stderr,"[flip-direct-error] %s shader compile: %.*s\n",label,int(length),log);
+  glDeleteShader(shader);
+  return 0;
+}
+
+GLuint clear_program() {
+  if(sClearProgram) return sClearProgram;
+  constexpr const char* vertex=R"GLSL(#version 310 es
+precision highp float;
+const vec2 positions[3]=vec2[3](vec2(-1.0,1.0),vec2(-1.0,-3.0),vec2(3.0,1.0));
+void main(){gl_Position=vec4(positions[gl_VertexID],0.0,1.0);}
+)GLSL";
+  constexpr const char* fragment=R"GLSL(#version 310 es
+precision highp float;
+layout(location=0) out vec4 color;
+void main(){color=vec4(1.0);}
+)GLSL";
+  const GLuint vs=compile_shader(GL_VERTEX_SHADER,vertex,"clear vertex");
+  const GLuint fs=compile_shader(GL_FRAGMENT_SHADER,fragment,"clear fragment");
+  if(!vs||!fs) {if(vs)glDeleteShader(vs);if(fs)glDeleteShader(fs);return 0;}
+  const GLuint program=glCreateProgram();
+  glAttachShader(program,vs);glAttachShader(program,fs);glLinkProgram(program);
+  glDeleteShader(vs);glDeleteShader(fs);
+  GLint ok=GL_FALSE;glGetProgramiv(program,GL_LINK_STATUS,&ok);
+  if(ok!=GL_TRUE) {
+    char log[2048]{};GLsizei length=0;glGetProgramInfoLog(program,sizeof(log),&length,log);
+    std::fprintf(stderr,"[flip-direct-error] clear program link: %.*s\n",int(length),log);
+    glDeleteProgram(program);return 0;
+  }
+  sClearProgram=program;
+  return program;
+}
+
+GLenum compare_func(GXCompare f) {
+  switch(f) {
+  case GX_NEVER:return GL_NEVER;
+  case GX_LESS:return gx::UseReversedZ?GL_GREATER:GL_LESS;
+  case GX_EQUAL:return GL_EQUAL;
+  case GX_LEQUAL:return gx::UseReversedZ?GL_GEQUAL:GL_LEQUAL;
+  case GX_GREATER:return gx::UseReversedZ?GL_LESS:GL_GREATER;
+  case GX_NEQUAL:return GL_NOTEQUAL;
+  case GX_GEQUAL:return gx::UseReversedZ?GL_LEQUAL:GL_GEQUAL;
+  case GX_ALWAYS:return GL_ALWAYS;
+  default:return GL_ALWAYS;
+  }
+}
+
+GLenum blend_factor(GXBlendFactor f,bool destination) {
+  switch(f) {
+  case GX_BL_ZERO:return GL_ZERO;
+  case GX_BL_ONE:return GL_ONE;
+  case GX_BL_SRCCLR:return destination?GL_SRC_COLOR:GL_DST_COLOR;
+  case GX_BL_INVSRCCLR:return destination?GL_ONE_MINUS_SRC_COLOR:GL_ONE_MINUS_DST_COLOR;
+  case GX_BL_SRCALPHA:return GL_SRC_ALPHA;
+  case GX_BL_INVSRCALPHA:return GL_ONE_MINUS_SRC_ALPHA;
+  case GX_BL_DSTALPHA:return GL_DST_ALPHA;
+  case GX_BL_INVDSTALPHA:return GL_ONE_MINUS_DST_ALPHA;
+  default:return GL_ONE;
+  }
+}
+
+// Redundant-state filter. Consecutive GX pipelines usually differ only in
+// their TEV program, so most fixed-function state calls can be skipped.
+// Mali's driver validates every state call; filtering them is a real saving.
+struct GLStateCache {
+  bool valid=false;
+  bool cull=false; GLenum cullFace=0;
+  bool depth=false; GLenum depthFunc=0; GLboolean depthMask=GL_FALSE;
+  GLboolean colorMask[4]={GL_TRUE,GL_TRUE,GL_TRUE,GL_TRUE};
+  bool blend=false; GLenum equation=0,src=0,dst=0,dstA=0,srcA=0; bool separate=false; bool accum=false; float constantAlpha=-1.f;
+  bool polygonOffset=false; float slope=0,bias=0;
+};
+GLStateCache sGLState;
+
+void apply_pipeline_state(const gx::PipelineConfig& c) {
+  static const bool filter=[] {const char* v=std::getenv("MELEE_FLIP_STATE_CACHE");return !v||std::strcmp(v,"0");}();
+  auto& st=sGLState;
+  const bool fresh=!filter||!st.valid;
+  // GX pipelines use WebGPU CW winding. Both Tint and our native vertex
+  // shader invert clip-space Y for GLES, which reverses the winding.
+  if(fresh) glFrontFace(GL_CCW);
+  const bool cull=c.cullMode!=GX_CULL_NONE;
+  const GLenum cullFace=c.cullMode==GX_CULL_FRONT?GL_FRONT:GL_BACK;
+  if(fresh||st.cull!=cull) { if(cull) glEnable(GL_CULL_FACE); else glDisable(GL_CULL_FACE); }
+  if(cull&&(fresh||!st.cull||st.cullFace!=cullFace)) glCullFace(cullFace);
+  const bool depth=c.depthCompare!=0;
+  const GLenum depthFunc=compare_func(c.depthFunc);
+  if(fresh||st.depth!=depth) { if(depth) glEnable(GL_DEPTH_TEST); else glDisable(GL_DEPTH_TEST); }
+  if(depth&&(fresh||!st.depth||st.depthFunc!=depthFunc)) glDepthFunc(depthFunc);
+  const GLboolean depthMask=c.depthCompare&&c.depthUpdate?GL_TRUE:GL_FALSE;
+  if(fresh||st.depthMask!=depthMask) glDepthMask(depthMask);
+  const bool accum=(c.shaderConfig.flipFlags&gx::FlipSpriteAccum)!=0; // half-res sprite accumulation
+  const GLboolean colorMask[4]={GLboolean(c.colorUpdate),GLboolean(c.colorUpdate),GLboolean(c.colorUpdate),GLboolean(accum||c.alphaUpdate)};
+  if(fresh||std::memcmp(st.colorMask,colorMask,4)) glColorMask(colorMask[0],colorMask[1],colorMask[2],colorMask[3]);
+  GLenum src=GL_ONE,dst=GL_ZERO,equation=GL_FUNC_ADD;
+  if(c.blendMode==GX_BM_BLEND) {src=blend_factor(c.blendFacSrc,false);dst=blend_factor(c.blendFacDst,true);}
+  else if(c.blendMode==GX_BM_SUBTRACT) {src=dst=GL_ONE;equation=GL_FUNC_REVERSE_SUBTRACT;}
+  else if(c.blendMode==GX_BM_LOGIC) {
+    if(c.blendOp==GX_LO_CLEAR) src=dst=GL_ZERO;
+    else if(c.blendOp==GX_LO_NOOP) {src=GL_ZERO;dst=GL_ONE;}
+  }
+  const bool blend=c.blendMode!=GX_BM_NONE||c.dstAlpha!=UINT32_MAX||accum;
+  if(accum&&c.blendMode==GX_BM_NONE) {src=GL_SRC_ALPHA;dst=GL_ZERO;} // premultiplied store
+  if(fresh||st.blend!=blend) { if(blend) glEnable(GL_BLEND); else glDisable(GL_BLEND); }
+  if(blend) {
+    if(fresh||!st.blend||st.equation!=equation) glBlendEquationSeparate(equation,equation);
+    if(c.dstAlpha!=UINT32_MAX) {
+      const float constantAlpha=c.dstAlpha/255.f;
+      if(fresh||st.constantAlpha!=constantAlpha) glBlendColor(0,0,0,constantAlpha);
+      if(fresh||!st.blend||!st.separate||st.accum||st.src!=src||st.dst!=dst) glBlendFuncSeparate(src,dst,GL_CONSTANT_ALPHA,GL_ZERO);
+      st.separate=true; st.accum=false; st.constantAlpha=constantAlpha;
+    } else if(accum) {
+      const bool opaque=c.blendMode==GX_BM_NONE, attenuates=dst==GL_ONE_MINUS_SRC_ALPHA;
+      if(fresh||!st.blend||!st.separate||!st.accum||st.src!=src||st.dst!=dst)
+        glBlendFuncSeparate(src,dst,opaque||attenuates?GL_ONE:GL_ZERO,opaque?GL_ZERO:attenuates?GL_ONE_MINUS_SRC_ALPHA:GL_ONE);
+      st.separate=true; st.accum=true;
+    } else {
+      if(fresh||!st.blend||st.separate||st.src!=src||st.dst!=dst) glBlendFunc(src,dst);
+      st.separate=false; st.accum=false;
+    }
+    st.equation=equation; st.src=src; st.dst=dst;
+  }
+  const float bias=std::round((gx::UseReversedZ?-1.f:1.f)*std::bit_cast<float>(c.polygonOffsetBits));
+  const float slope=(gx::UseReversedZ?-1.f:1.f)*std::bit_cast<float>(c.polygonOffsetScaleBits);
+  const bool polygonOffset=bias!=0.f||slope!=0.f;
+  if(fresh||st.polygonOffset!=polygonOffset) { if(polygonOffset) glEnable(GL_POLYGON_OFFSET_FILL); else glDisable(GL_POLYGON_OFFSET_FILL); }
+  if(polygonOffset&&(fresh||!st.polygonOffset||st.slope!=slope||st.bias!=bias)) glPolygonOffset(slope,bias);
+  st.valid=true; st.cull=cull; st.cullFace=cullFace; st.depth=depth; st.depthFunc=depthFunc; st.depthMask=depthMask;
+  std::memcpy(st.colorMask,colorMask,4); st.blend=blend; st.polygonOffset=polygonOffset; st.slope=slope; st.bias=bias;
+}
+
+// Texture parameters (mip range, swizzle) are object state. Within one direct
+// pass no other code touches these objects, so set them once per object per pass.
+// Sampler state is folded into the texture object as well (GX textures almost
+// always use one sampler), so draws do not rebind sampler objects on Mali.
+struct SamplerParams { GLint wrapS,wrapT,minFilter,magFilter; GLfloat minLod,maxLod; GLint compareMode,compareFunc; GLfloat anisotropy; };
+bool texture_sampler_state_enabled() {
+  static const bool enabled=[] {const char* v=std::getenv("MELEE_FLIP_TEXTURE_SAMPLER_STATE");return !v||std::strcmp(v,"0");}();
+  return enabled&&cache_enabled();
+}
+const SamplerParams& sampler_params(GLuint sampler) {
+  static std::unordered_map<GLuint,SamplerParams> cache;
+  auto it=cache.find(sampler);
+  if(it!=cache.end()) return it->second;
+  SamplerParams p{};
+  glGetSamplerParameteriv(sampler,GL_TEXTURE_WRAP_S,&p.wrapS);
+  glGetSamplerParameteriv(sampler,GL_TEXTURE_WRAP_T,&p.wrapT);
+  glGetSamplerParameteriv(sampler,GL_TEXTURE_MIN_FILTER,&p.minFilter);
+  glGetSamplerParameteriv(sampler,GL_TEXTURE_MAG_FILTER,&p.magFilter);
+  glGetSamplerParameterfv(sampler,GL_TEXTURE_MIN_LOD,&p.minLod);
+  glGetSamplerParameterfv(sampler,GL_TEXTURE_MAX_LOD,&p.maxLod);
+  glGetSamplerParameteriv(sampler,GL_TEXTURE_COMPARE_MODE,&p.compareMode);
+  glGetSamplerParameteriv(sampler,GL_TEXTURE_COMPARE_FUNC,&p.compareFunc);
+  p.anisotropy=1.f;
+  glGetSamplerParameterfv(sampler,0x84FE /*GL_TEXTURE_MAX_ANISOTROPY_EXT*/,&p.anisotropy);
+  while(glGetError()!=GL_NO_ERROR) {}
+  return cache.emplace(sampler,p).first->second;
+}
+struct TextureSamplerState { GLuint sampler=Unknown; };
+std::unordered_map<GLuint,TextureSamplerState> sTextureSamplers; // texture object -> sampler whose state it carries
+void apply_sampler_to_texture(GLenum target,GLuint texture,GLuint sampler) {
+  auto& st=sTextureSamplers[texture];
+  if(st.sampler==sampler) return;
+  const auto& p=sampler_params(sampler);
+  ++sGlCalls.texParams; glTexParameteri(target,GL_TEXTURE_WRAP_S,p.wrapS);
+  glTexParameteri(target,GL_TEXTURE_WRAP_T,p.wrapT);
+  glTexParameteri(target,GL_TEXTURE_MIN_FILTER,p.minFilter);
+  glTexParameteri(target,GL_TEXTURE_MAG_FILTER,p.magFilter);
+  glTexParameterf(target,GL_TEXTURE_MIN_LOD,p.minLod);
+  glTexParameterf(target,GL_TEXTURE_MAX_LOD,p.maxLod);
+  glTexParameteri(target,GL_TEXTURE_COMPARE_MODE,p.compareMode);
+  if(p.compareMode!=GL_NONE) glTexParameteri(target,GL_TEXTURE_COMPARE_FUNC,p.compareFunc);
+  st.sampler=sampler;
+}
+struct TextureParams { GLint baseMipLevel,maxMipLevel; unsigned swizzle[4]; };
+std::unordered_map<GLuint,TextureParams> sTextureParams;
+GLuint sActiveUnit=Unknown;
+
+void bind_texture_unit(const dawn::native::opengl::DirectGLTextureInfo& texture,GLuint unit) {
+  if(cache_enabled()&&unit<sTextureState.size()) {
+    auto& state=sTextureState[unit];
+    const auto& last=state.texture;
+    if(state.valid&&last.texture==texture.texture&&last.target==texture.target&&
+       last.baseMipLevel==texture.baseMipLevel&&last.maxMipLevel==texture.maxMipLevel&&
+       std::equal(std::begin(last.swizzle),std::end(last.swizzle),std::begin(texture.swizzle))) return;
+    // Mip limits and swizzles belong to the texture object, not the unit.
+    // A different view of an aliased object invalidates the other unit caches.
+    for(auto& other:sTextureState)
+      if(&other!=&state&&other.valid&&other.texture.texture==texture.texture) other.valid=false;
+    state.valid=true;state.texture=texture;
+  }
+  if(!cache_enabled()||sActiveUnit!=unit) {glActiveTexture(GL_TEXTURE0+unit);sActiveUnit=unit;}
+  glBindTexture(texture.target,texture.texture); ++sGlCalls.textures;
+  const TextureParams params{GLint(texture.baseMipLevel),GLint(texture.maxMipLevel),
+    {texture.swizzle[0],texture.swizzle[1],texture.swizzle[2],texture.swizzle[3]}};
+  if(cache_enabled()) {
+    auto [it,inserted]=sTextureParams.try_emplace(texture.texture,params);
+    if(!inserted) {
+      if(!std::memcmp(&it->second,&params,sizeof(params))) return;
+      it->second=params;
+    }
+  }
+  ++sGlCalls.texParams; glTexParameteri(texture.target,GL_TEXTURE_BASE_LEVEL,texture.baseMipLevel);
+  glTexParameteri(texture.target,GL_TEXTURE_MAX_LEVEL,texture.maxMipLevel);
+  if(texture.swizzle[0]!=GL_NONE) {
+    glTexParameteri(texture.target,GL_TEXTURE_SWIZZLE_R,texture.swizzle[0]);
+    glTexParameteri(texture.target,GL_TEXTURE_SWIZZLE_G,texture.swizzle[1]);
+    glTexParameteri(texture.target,GL_TEXTURE_SWIZZLE_B,texture.swizzle[2]);
+    glTexParameteri(texture.target,GL_TEXTURE_SWIZZLE_A,texture.swizzle[3]);
+  }
+}
+
+// Resolved GL names for one (bind group, pipeline texture bindings) pair. Bind
+// groups are immutable; entries are dropped when the bind group cache evicts.
+struct ResolvedTextures {
+  uint64_t generation=0;
+  struct Entry { dawn::native::opengl::DirectGLTextureInfo texture; GLuint sampler; };
+  std::vector<Entry> entries; // one per PreparedPipeline::textures element
+};
+std::unordered_map<uint64_t,ResolvedTextures> sResolvedTextures;
+const ResolvedTextures& resolve_textures(BindGroupRef group,PipelineRef pipeline,const PreparedPipeline& p) {
+  using namespace dawn::native::opengl;
+  const uint64_t generation=bind_group_cache_generation();
+  const uint64_t key=group*0x9E3779B97F4A7C15ull^uint64_t(pipeline);
+  auto it=sResolvedTextures.find(key);
+  if(it!=sResolvedTextures.end()&&it->second.generation==generation) return it->second;
+  if(it==sResolvedTextures.end()) it=sResolvedTextures.emplace(key,ResolvedTextures{}).first;
+  auto& r=it->second; r.generation=generation; r.entries.clear();
+  const auto bindGroup=find_bind_group(group);
+  for(const auto& t:p.textures)
+    r.entries.push_back({GetDirectGLBindGroupTexture(bindGroup.Get(),t.binding),GetDirectGLBindGroupSampler(bindGroup.Get(),t.binding+1)});
+  return r;
+}
+
+PreparedPipeline* prepare_pipeline(PipelineRef ref) {
+  using namespace dawn::native::opengl;
+  if(auto it=sPrepared.find(ref);it!=sPrepared.end()) return &it->second;
+  PreparedPipeline p;
+  if(!gx::find_flip_pipeline_config(ref,p.config)) return nullptr;
+  p.nativeUber=gx::flip_native_uber_enabled()&&(p.config.shaderConfig.flipFlags&gx::FlipUberShader);
+  if(p.nativeUber) {
+    p.gl.program=gx::flip_gles_uber_program(p.config.shaderConfig);
+    p.gl.topology=GL_TRIANGLES;
+    if(!p.gl.program) return nullptr;
+    // Native program creation changes current GL program.
+    sLastProgram=Unknown;
+  } else {
+    if(!get_pipeline(ref,p.owner)) return nullptr;
+    p.gl=GetDirectGLRenderPipeline(p.owner.Get());
+    p.uniformBinding=GetDirectGLBufferBinding(p.owner.Get(),1,0);
+    p.immediates=glGetUniformLocation(p.gl.program,"tint_immediates");
+    static const bool skipImmediates=[] {const char* v=std::getenv("MELEE_FLIP_SKIP_IMMEDIATES");return !v||std::strcmp(v,"0");}();
+    const auto& sc=p.config.shaderConfig;
+    p.immediatesUnused=skipImmediates&&(sc.flipFlags&gx::FlipVertexInput)&&(sc.flipFlags&gx::FlipBatchedDraws)&&
+      !sc.fogRangeEnabled&&std::bit_cast<float>(p.config.polygonOffsetClampBits)==0.f;
+  }
+  p.layout=gx::flip_vertex_attributes(p.config.shaderConfig);
+  const auto units=[&](uint32_t group,uint32_t binding,bool sampler) {
+    uint32_t values[32];
+    const auto count=GetDirectGLTextureUnits(p.owner.Get(),group,binding,sampler,values,32);
+    return std::vector<uint32_t>(values,values+std::min(count,32u));
+  };
+  if(!p.nativeUber) p.storageUnits=units(0,1,false);
+  for(uint32_t binding=0;binding<32;binding+=2) {
+    TextureBinding t{.binding=binding};
+    if(p.nativeUber) {
+      const auto name="tex"+std::to_string(binding/2);
+      if(glGetUniformLocation(p.gl.program,name.c_str())<0) continue;
+      t.textures={binding/2};t.samplers={binding/2};
+    }
+    else {t.textures=units(2,binding,false);t.samplers=units(2,binding+1,true);}
+    if(!t.textures.empty()) p.textures.push_back(std::move(t));
+  }
+  return &sPrepared.emplace(ref,std::move(p)).first->second;
+}
+
+void bind_draw_resources(const gx::DrawData& d,PreparedPipeline& p,bool constantRecord) {
+  flip_profile::Scope resourcesProfile("gl_resources");
+  using namespace dawn::native::opengl;
+  auto& res=resources();
+  const uint32_t window=d.uniformRange.offset/gx::FlipUniformWindow;
+  {
+  flip_profile::Scope profile("gl_uniform_bind");
+  if(window<res.flipUniformBuffers.size()) {
+    const GLuint buffer=sMappedUniforms?sMappedUniforms->uniforms:sStream.current?sStream.current->uniforms.name:GetDirectGLBuffer(res.flipUniformBuffers[window].Get());
+    const uint32_t offset=(sMappedUniforms||sStream.current)?window*gx::FlipUniformWindow:0;
+    const GLuint binding=p.uniformBinding;
+    if(!cache_enabled()||sLastUniform!=buffer||sLastUniformBinding!=binding||sLastUniformOffset!=offset) {
+      glBindBufferRange(GL_UNIFORM_BUFFER,binding,buffer,offset,gx::FlipUniformWindow); ++sGlCalls.ubos;
+      sLastUniform=buffer;sLastUniformBinding=binding;sLastUniformOffset=offset;
+    }
+    static const bool mappedCheck=[] {const char* v=std::getenv("MELEE_FLIP_MAPPED_CHECK");return v&&v[0]=='1';}();
+    if(mappedCheck&&sMapped) {
+      // Diagnostic: is the record this draw will read actually present in mapped memory?
+      const uint32_t record=(d.uniformRange.offset%gx::FlipUniformWindow)/gx::FlipUniformStride;
+      const uint8_t* rec=sMapped->uniformData+d.uniformRange.offset;
+      uint32_t nonzero=0; for(unsigned i=0;i<64;i+=4){uint32_t w;std::memcpy(&w,rec+i,4);nonzero|=w;}
+      static uint32_t zeroRecords=0,records=0,maxOffset=0; ++records; if(!nonzero) ++zeroRecords; maxOffset=std::max<uint32_t>(maxOffset,d.uniformRange.offset);
+      if(sFrameNumber%120==0&&records) {std::fprintf(stderr,"[flip-mapped-check] records=%u zero=%u max_offset=%u window=%u record=%u resident=%u\n",records,zeroRecords,maxOffset,window,record,d.resident);}
+      if(sFrameNumber%120==1) {records=zeroRecords=maxOffset=0;}
+    }
+  }
+  }
+  {
+  flip_profile::Scope profile("gl_texture_bind");
+  const auto staticGroup=res.staticBindGroup.Get();
+  if(!p.storageUnits.empty()) {
+    const auto texture=GetDirectGLBindGroupTexture(staticGroup,1);
+    for(auto unit:p.storageUnits) bind_texture_unit(texture,unit);
+  }
+  if(d.bindGroups.textureBindGroup) {
+    const auto& resolved=resolve_textures(d.bindGroups.textureBindGroup,d.pipeline,p);
+    for(size_t i=0;i<p.textures.size();++i) {
+      const auto& t=p.textures[i];
+      const auto& texture=resolved.entries[i].texture;
+      const GLuint sampler=resolved.entries[i].sampler;
+      for(auto unit:t.textures) bind_texture_unit(texture,unit);
+      if(texture_sampler_state_enabled()&&texture.texture) {
+        // Texture unit for this binding is active after bind_texture_unit; the
+        // texture object now carries the sampler state and unit samplers are 0.
+        for(auto unit:t.textures) {
+          if(sActiveUnit!=unit) {glActiveTexture(GL_TEXTURE0+unit);sActiveUnit=unit;}
+          apply_sampler_to_texture(texture.target,texture.texture,sampler);
+        }
+        for(auto unit:t.samplers) {
+          if(!cache_enabled()||unit>=sTextureState.size()||sTextureState[unit].sampler!=0) {
+            glBindSampler(unit,0); ++sGlCalls.samplers;
+            if(unit<sTextureState.size()) sTextureState[unit].sampler=0;
+          }
+        }
+      } else for(auto unit:t.samplers) {
+        if(!cache_enabled()||unit>=sTextureState.size()||sTextureState[unit].sampler!=sampler) {
+          glBindSampler(unit,sampler); ++sGlCalls.samplers;
+          if(unit<sTextureState.size()) sTextureState[unit].sampler=sampler;
+        }
+      }
+    }
+  }
+  }
+  {
+  flip_profile::Scope profile("gl_vertex_bind");
+  const auto& layout=p.layout;
+  GLuint vertex; uint32_t vertexOffset;
+  const GLuint frameVertex=sMappedVertices?sMappedVertices->vertices:sStream.current?sStream.current->vertices.name:GetDirectGLBuffer(res.vertexBuffer.Get());
+  if(gx::resident_arena(d)) { vertex=resident_gl_buffer(gx::resident_arena(d)); vertexOffset=0; }
+  else { vertex=frameVertex; vertexOffset=d.vertRange.offset; }
+  uint32_t used=0;
+  for(unsigned a=0;a<layout.count;++a) used|=1u<<layout.attributes[a].shaderLocation;
+  const bool recordSlot=(p.config.shaderConfig.flipFlags&gx::FlipBatchedDraws)&&p.config.shaderConfig.lineMode==0;
+  if(recordSlot) used|=(1u<<15)|(1u<<16); // distinct from line layouts that use location 15 in slot 0
+  GLuint recordBuffer=0; uint32_t recordOffset=0;
+  if(recordSlot) {
+    // Slot 1: per-vertex uniform records for merged resident draws, else zeros.
+    if(gx::resident_per_vertex(d)) { recordBuffer=frameVertex; recordOffset=d.immediateData.vtxStart; }
+    else { recordBuffer=zero_records_gl_buffer(); recordOffset=0; }
+  }
+  const auto set_layout=[&]() {
+    for(unsigned a=0;a<layout.count;++a) {
+      const auto& attr=layout.attributes[a];
+      const GLuint loc=attr.shaderLocation;
+      if(loc==0) glVertexAttribIFormat(loc,3,GL_UNSIGNED_INT,attr.offset);
+      else {
+        const GLint count=loc==3||loc==4||loc==15?4:loc>=5&&loc<=12?2:3;
+        glVertexAttribFormat(loc,count,GL_FLOAT,GL_FALSE,attr.offset);
+      }
+      glVertexAttribBinding(loc,0);
+      glEnableVertexAttribArray(loc);
+    }
+    if(recordSlot) {
+      glVertexAttribIFormat(15,1,GL_UNSIGNED_INT,0);
+      glVertexAttribBinding(15,1);
+      glEnableVertexAttribArray(15);
+    }
+  };
+  if(layout_vao_enabled()&&cache_enabled()) {
+    if(sLastLayout!=used||!sCurrentVao) {
+      auto& v=sLayoutVaos[used];
+      if(!v.vao) {
+        glGenVertexArrays(1,&v.vao);
+        glBindVertexArray(v.vao);
+        set_layout();
+      } else glBindVertexArray(v.vao);
+      sBindingDivisor=Unknown; // per-VAO state
+      if(v.ebo!=sPassEbo) { glBindBuffer(GL_ELEMENT_ARRAY_BUFFER,sPassEbo); v.ebo=sPassEbo; }
+      sCurrentVao=&v; sLastLayout=used; ++sGlCalls.layouts;
+    }
+    auto& v=*sCurrentVao;
+    if(v.vertexBuffer!=vertex||v.vertexOffset!=vertexOffset||v.stride!=layout.stride) {
+      glBindVertexBuffer(0,vertex,vertexOffset,layout.stride); ++sGlCalls.vbos;
+      v.vertexBuffer=vertex;v.vertexOffset=vertexOffset;v.stride=layout.stride;
+    }
+    if(recordSlot&&(v.recordBuffer!=recordBuffer||v.recordOffset!=recordOffset)) {
+      glBindVertexBuffer(1,recordBuffer,recordOffset,4); ++sGlCalls.vbos;
+      v.recordBuffer=recordBuffer;v.recordOffset=recordOffset;
+    }
+  } else {
+  if(!cache_enabled()||sLastVertexBuffer!=vertex||sLastVertexOffset!=vertexOffset||sLastVertexStride!=layout.stride) {
+    glBindVertexBuffer(0,vertex,vertexOffset,layout.stride); ++sGlCalls.vbos;
+    sLastVertexBuffer=vertex;sLastVertexOffset=vertexOffset;sLastVertexStride=layout.stride;
+  }
+  if(recordSlot&&(!cache_enabled()||sLastRecordBuffer!=recordBuffer||sLastRecordOffset!=recordOffset)) {
+    glBindVertexBuffer(1,recordBuffer,recordOffset,4); ++sGlCalls.vbos;
+    sLastRecordBuffer=recordBuffer;sLastRecordOffset=recordOffset;
+  }
+  if(!cache_enabled()||sLastLayout!=used) {
+  set_layout();
+  for(unsigned loc=0;loc<16;++loc) if((sEnabledAttributes&(1u<<loc))&&!(used&(1u<<loc)))
+    glDisableVertexAttribArray(loc);
+  sEnabledAttributes=used;
+  sLastLayout=used; ++sGlCalls.layouts;
+  }
+  }
+  // Instanced point sprites step binding 0 per instance (one record per point).
+  const GLuint divisor=(p.config.shaderConfig.flipFlags&gx::FlipInstancedPoints)?1u:0u;
+  if(!cache_enabled()||sBindingDivisor!=divisor) { glVertexBindingDivisor(0,divisor); sBindingDivisor=divisor; }
+  }
+
+  flip_profile::Scope immediateProfile("gl_immediates");
+  if(p.immediates>=0) {
+    // Batched shaders only read _pad (record base): streamed draws pass 0, resident
+    // draws pass their record. glUniform1uiv costs ~0.4 us here, a UBO rebind ~5 us.
+    const uint32_t record=(d.uniformRange.offset%gx::FlipUniformWindow)/gx::FlipUniformStride;
+    const bool batched=(p.config.shaderConfig.flipFlags&gx::FlipBatchedDraws)!=0;
+    // Constant-record programs take the record from the immediates in both stages.
+    const uint32_t pad=(!batched||constantRecord||(gx::resident_arena(d)&&!gx::resident_per_vertex(d)))?record:0;
+    if(p.immediatesUnused) {
+      if(p.lastImmediatePad!=pad) {
+        auto immediate=d.immediateData; immediate._pad=pad;
+        glUniform1uiv(p.immediates,sizeof(immediate)/sizeof(uint32_t),reinterpret_cast<const GLuint*>(&immediate)); ++sGlCalls.immediates;
+        p.lastImmediatePad=pad;
+      }
+    } else {
+      auto immediate=d.immediateData; immediate._pad=pad;
+      glUniform1uiv(p.immediates,sizeof(immediate)/sizeof(uint32_t),reinterpret_cast<const GLuint*>(&immediate)); ++sGlCalls.immediates;
+      p.lastImmediatePad=pad;
+    }
+  }
+}
+
+bool render_draw(const gx::DrawData& d,int mode,uint32_t passIndex,uint32_t drawIndex) {
+  static const bool trace=[] {const char* v=std::getenv("MELEE_FLIP_DRAW_TRACE");return v&&v[0]=='1';}();
+  if(trace&&sFrameNumber==600) {
+    uint64_t uniformHash=0; const uint8_t* rec=nullptr;
+    if(sMapped) rec=sMapped->uniformData+d.uniformRange.offset;
+    if(rec) { for(unsigned i=0;i<256;i+=8){uint64_t w;std::memcpy(&w,rec+i,8);uniformHash=uniformHash*1099511628211ull^w;} }
+    std::fprintf(stderr,"[flip-draw-trace] pass=%u draw=%u pipeline=%llx res=%u vtx=%u idx=%u idxoff=%u vert=%u uni=%u tex=%llx ufp=%llx dst=%x\n",
+      passIndex,drawIndex,(unsigned long long)d.pipeline,d.resident,d.vtxCount,d.indexCount,d.idxRange.offset,d.vertRange.offset,
+      d.uniformRange.offset,(unsigned long long)d.bindGroups.textureBindGroup,(unsigned long long)uniformHash,d.dstAlpha);
+  }
+  if(flip_profile::enabled()) flip_profile::state.tag=(uint64_t(passIndex)<<32)|drawIndex;
+  flip_profile::Scope drawProfile("gl_draw_total");
+  auto* prepared=prepare_pipeline(d.pipeline);
+  if(!prepared) {
+    std::fprintf(stderr,"[flip-direct-error] frame=%llu pass=%u draw=%u section=pipeline-lookup\n",
+                 static_cast<unsigned long long>(sFrameNumber),passIndex,drawIndex);
+    return false;
+  }
+  // Unmerged draws use the material's constant-record variant once it has
+  // compiled (queued without blocking on the FIFO worker); until then, or for
+  // merged draws, the dynamic-record program stays in use.
+  PreparedPipeline* program=prepared; bool constantRecord=false;
+  if(gx::resident_constant_record(d)) {
+    if(const auto cref=gx::flip_constant_variant(d.pipeline)) {
+      if(auto* cp=prepare_pipeline(cref)) { program=cp; constantRecord=true; ++sGlCalls.constantDraws; }
+    }
+  }
+  const auto& native=program->gl;
+  if(flip_profile::active()) {
+    const auto& c=prepared->config.shaderConfig;
+    char text[512];
+    std::snprintf(text,sizeof(text),"tag=%llu pipeline=%llu program=%u vertices=%u indices=%u vertex_stride=%u tev=%u texture_group=%llu window=%u program_change=%u pipeline_change=%u",
+      (unsigned long long)flip_profile::state.tag,(unsigned long long)d.pipeline,native.program,d.vtxCount,d.indexCount,
+      prepared->layout.stride,c.tevStageCount,(unsigned long long)d.bindGroups.textureBindGroup,
+      d.uniformRange.offset/gx::FlipUniformWindow,sLastProgram!=native.program,sLastPipeline!=d.pipeline);
+    flip_profile::state.patterns.emplace_back(text);
+  }
+  {
+  flip_profile::Scope profile("gl_pipeline_state");
+  if(!cache_enabled()||sLastProgram!=native.program) {glUseProgram(native.program);sLastProgram=native.program;++sGlCalls.programs;}
+  if(!cache_enabled()||sLastPipeline!=d.pipeline) {apply_pipeline_state(prepared->config);sLastPipeline=d.pipeline;++sGlCalls.pipelineStates;}
+  }
+  if(!gl_ok("pipeline-state",passIndex,drawIndex)||mode<4) return true;
+  bind_draw_resources(d,*program,constantRecord);
+  if(!gl_ok("resources",passIndex,drawIndex)||mode<5||drawIndex>=draw_limit()) return true;
+  {
+  flip_profile::Scope profile("gl_draw_call"); ++sGlCalls.draws;
+  const GLenum indexType=gx::resident_arena(d)?GL_UNSIGNED_INT:GL_UNSIGNED_SHORT;
+  static const bool rangeElements=[] {const char* v=std::getenv("MELEE_FLIP_RANGE_ELEMENTS");return !v||std::strcmp(v,"0");}();
+  if(d.instanceCount==1) {
+    if(d.indexCount) {
+      if(rangeElements) {
+        // Explicit vertex range: the driver need not scan the index data.
+        GLuint start=0,end=d.vtxCount?d.vtxCount-1:0;
+        if(gx::resident_arena(d)) {const uint32_t stride=prepared->layout.stride; start=d.vertRange.offset/stride; end=(d.vertRange.offset+d.vertRange.size)/stride; end=end?end-1:0;}
+        glDrawRangeElements(native.topology,start,end,d.indexCount,indexType,reinterpret_cast<const void*>(uintptr_t(d.idxRange.offset)));
+      } else glDrawElements(native.topology,d.indexCount,indexType,reinterpret_cast<const void*>(uintptr_t(d.idxRange.offset)));
+    }
+    else glDrawArrays(native.topology,0,d.vtxCount);
+  } else if(d.indexCount) glDrawElementsInstanced(native.topology,d.indexCount,indexType,
+      reinterpret_cast<const void*>(uintptr_t(d.idxRange.offset)),d.instanceCount);
+  else glDrawArraysInstanced(native.topology,0,d.vtxCount,d.instanceCount);
+  }
+  if(mode>=6) glMemoryBarrier(GL_TEXTURE_FETCH_BARRIER_BIT);
+  return gl_ok("draw",passIndex,drawIndex);
+}
+
+bool render_clear(const clear::DrawData& d,uint32_t width,uint32_t height,
+                  uint32_t passIndex,uint32_t drawIndex) {
+  clear::PipelineConfig config;
+  if(!clear::find_flip_pipeline_config(d.pipeline,config)) {
+    std::fprintf(stderr,"[flip-direct-error] frame=%llu pass=%u draw=%u section=clear-pipeline-lookup\n",
+                 static_cast<unsigned long long>(sFrameNumber),passIndex,drawIndex);
+    return false;
+  }
+  const GLuint program=clear_program();
+  if(!program) return false;
+  glUseProgram(program);
+  sLastProgram=Unknown;sLastPipeline=UINTPTR_MAX;sGLState=GLStateCache{};
+  glDisable(GL_CULL_FACE);
+  glEnable(GL_BLEND);
+  glBlendEquationSeparate(GL_FUNC_ADD,GL_FUNC_ADD);
+  glBlendFuncSeparate(GL_CONSTANT_COLOR,GL_ZERO,GL_CONSTANT_ALPHA,GL_ZERO);
+  glBlendColor(d.color.r,d.color.g,d.color.b,d.color.a);
+  glDisable(GL_STENCIL_TEST);
+  glDisable(GL_POLYGON_OFFSET_FILL);
+  glEnable(GL_DEPTH_TEST);
+  glDepthFunc(GL_ALWAYS);
+  glColorMask(config.clearColor,config.clearColor,config.clearColor,config.clearAlpha);
+  glDepthMask(config.clearDepth?GL_TRUE:GL_FALSE);
+  glViewport(0,0,width,height);
+  glDepthRangef(d.depth,d.depth);
+  glScissor(0,0,width,height);
+  glDrawArrays(GL_TRIANGLES,0,3);
+  return gl_ok("clear",passIndex,drawIndex);
+}
+
+bool render_pass(void*,uint32_t passIndex,const char* label) {
+  flip_profile::Scope profile("gl_pass",passIndex);
+  if(sPresentBlit.pending&&label&&!std::strcmp(label,"EFB copy render pass")) {
+    sPresentBlit.pending=false;
+    const bool done=direct_mode()>=5&&present_blit();
+    sPresentBlit.source=nullptr;
+    return done;
+  }
+  if(sNextPlan>=sPlans.size()) return false;
+  auto& plan=sPlans[sNextPlan];
+  if(!label||plan.label!=label) return false;
+  const auto target=dawn::native::opengl::GetDirectGLTextureView(plan.target.Get());
+  ++sNextPlan;
+  if(!plan.eligible) return false;
+  PassTimer timer;
+  const int mode=direct_mode();
+  while(glGetError()!=GL_NO_ERROR) {}
+  if(sFrameNumber<8)
+    std::fprintf(stderr,"[flip-direct] frame=%llu pass=%u mode=%d commands=%zu target=%u\n",
+                 static_cast<unsigned long long>(sFrameNumber),passIndex,mode,
+                 plan.commands.size(),target.texture?target.texture:target.renderbuffer);
+  const auto reset_memos=[] {
+    sLastProgram=sLastUniform=sLastUniformBinding=sLastLayout=Unknown;
+    sLastUniformOffset=Unknown;
+    sLastPipeline=UINTPTR_MAX;sTextureState={};
+    sGLState=GLStateCache{};sActiveUnit=Unknown;
+    sLastVertexBuffer=Unknown;sLastVertexOffset=sLastVertexStride=UINT32_MAX;
+    sLastRecordBuffer=Unknown;sLastRecordOffset=UINT32_MAX;sBindingDivisor=Unknown;
+    for(auto& [ref,prepared]:sPrepared) prepared.lastImmediatePad=UINT32_MAX;
+  };
+  if(mode>=2) {
+    if(!sVao) glGenVertexArrays(1,&sVao);
+    glBindVertexArray(sVao);
+    sPassEbo=sMappedIndices?sMappedIndices->indices:sStream.current?sStream.current->indices.name:
+        dawn::native::opengl::GetDirectGLBuffer(resources().indexBuffer.Get());
+    glBindBuffer(GL_ELEMENT_ARRAY_BUFFER,sPassEbo);
+    sCurrentVao=nullptr; // Dawn may have changed VAO/buffer state between passes
+    reset_memos();
+    glEnable(GL_SCISSOR_TEST);
+    glDisable(GL_STENCIL_TEST);
+    // Diagnostic: MELEE_FLIP_DUMP_EFB_PASS=<frame> writes the EFB as this pass
+    // starts (before its draws) to data/diagnostics/efb-dump-f<frame>-p<pass>.ppm.
+    static const long dumpFrame=[] {const char* v=std::getenv("MELEE_FLIP_DUMP_EFB_PASS");return v?std::atol(v):-1;}();
+    if(dumpFrame>=0&&static_cast<long>(sFrameNumber)==dumpFrame&&(target.texture||target.renderbuffer)) {
+      GLint drawFbo=0; glGetIntegerv(GL_DRAW_FRAMEBUFFER_BINDING,&drawFbo);
+      GLuint fbo=0; glGenFramebuffers(1,&fbo); glBindFramebuffer(GL_READ_FRAMEBUFFER,fbo);
+      if(target.texture) glFramebufferTexture2D(GL_READ_FRAMEBUFFER,GL_COLOR_ATTACHMENT0,GL_TEXTURE_2D,target.texture,0);
+      else glFramebufferRenderbuffer(GL_READ_FRAMEBUFFER,GL_COLOR_ATTACHMENT0,GL_RENDERBUFFER,target.renderbuffer);
+      std::vector<uint8_t> pixels(size_t(plan.width)*plan.height*4);
+      glReadPixels(0,0,plan.width,plan.height,GL_RGBA,GL_UNSIGNED_BYTE,pixels.data());
+      const GLenum err=glGetError();
+      glBindFramebuffer(GL_READ_FRAMEBUFFER,GLuint(drawFbo)); glDeleteFramebuffers(1,&fbo);
+      char path[128]; std::snprintf(path,sizeof(path),"data/diagnostics/efb-dump-f%ld-p%u.ppm",dumpFrame,passIndex);
+      if(FILE* f=std::fopen(path,"wb")) {
+        std::fprintf(f,"P6\n%u %u\n255\n",plan.width,plan.height);
+        for(uint32_t y=0;y<plan.height;++y) for(uint32_t x=0;x<plan.width;++x) std::fwrite(&pixels[(size_t(y)*plan.width+x)*4],1,3,f);
+        std::fclose(f);
+      }
+      std::fprintf(stderr,"[flip-efb-dump] frame=%ld pass=%u file=%s gl-error=%u\n",dumpFrame,passIndex,path,err);
+    }
+    gl_ok("pass-state",passIndex,0);
+  }
+  uint32_t drawIndex=0;
+  // Diagnostic/safety: MELEE_FLIP_FUSE_RESET=1 treats the shifted segment of a
+  // fused shadow pass like a pass start for the state memos.
+  static const bool fuseReset=[] {const char* v=std::getenv("MELEE_FLIP_FUSE_RESET");return v&&std::strcmp(v,"0");}();
+  for(size_t commandIndex=0;commandIndex<plan.commands.size();++commandIndex) {
+    const auto& command=plan.commands[commandIndex];
+    if(fuseReset&&mode>=2&&commandIndex==plan.segmentStart) reset_memos();
+    if(command.type==CommandType::SetViewport) {
+      if(mode<2) continue;
+      const auto& v=command.viewport;
+      glViewport(GLint(v.left),GLint(v.top),GLsizei(v.width),GLsizei(v.height));
+      const float nearDepth=gx::UseReversedZ?1.f-v.zfar:v.znear;
+      const float farDepth=gx::UseReversedZ?1.f-v.znear:v.zfar;
+      glDepthRangef(nearDepth,farDepth);
+    } else if(command.type==CommandType::SetScissor) {
+      if(mode<2) continue;
+      const auto& s=command.scissor;
+      const auto x=std::min(static_cast<uint32_t>(s.x),plan.width);
+      const auto y=std::min(static_cast<uint32_t>(s.y),plan.height);
+      glScissor(x,y,std::min(static_cast<uint32_t>(s.width),plan.width-x),
+                    std::min(static_cast<uint32_t>(s.height),plan.height-y));
+    } else if(command.type==CommandType::Draw) {
+      if(mode>=3) {
+        if(command.isClear) render_clear(command.clear,plan.width,plan.height,passIndex,drawIndex);
+        else render_draw(command.draw,mode,passIndex,drawIndex);
+      }
+      ++drawIndex;
+    }
+  }
+  gl_ok("pass-end",passIndex,drawIndex);
+  // Once an eligible pass is intercepted it must not also be replayed by Dawn,
+  // even if a diagnostic GL error occurred after some direct commands ran.
+  return true;
+}
+}
+
+bool flip_present_blit_enabled() {
+  // Off by default: the blit costs the same as the copy pass on this driver and
+  // moves 20 scanout pixels by one unit (EFB unchanged).
+  static const bool enabled=[] {const char* v=std::getenv("MELEE_FLIP_PRESENT_BLIT");return v&&std::strcmp(v,"0")&&direct_mode()>=5;}();
+  return enabled;
+}
+void flip_request_present_blit(const wgpu::TextureView& source,uint32_t width,uint32_t height) {
+  sPresentBlit={source,width,height,true};
+}
+bool encode_flip_gles_resources(const wgpu::RenderPassEncoder& encoder,RenderPass& pass,std::string_view label) {
+  pass.flipDirectLabel=label;
+  static const bool enabled=[] {const char* v=std::getenv("MELEE_FLIP_DIRECT_PACKET");return v&&std::strcmp(v,"0");}();
+  if(!enabled||direct_mode()<5||pass.msaaSamples!=1||pass.colorAttachmentCount!=1||
+     !gx::flip_vertex_enabled()||!gx::flip_uniform_windows_enabled()) return false;
+  // Diagnostic: MELEE_FLIP_FUSE_DAWN=1 leaves fused shadow passes (two resolves)
+  // to Dawn's reference renderer while every other pass stays direct.
+  static const bool fusedViaDawn=[] {const char* v=std::getenv("MELEE_FLIP_FUSE_DAWN");return v&&std::strcmp(v,"0");}();
+  if(fusedViaDawn&&!pass.flipExtraResolves.empty()) return false;
+  std::unordered_set<PipelineRef> seenPipelines;
+  std::vector<wgpu::RenderPipeline> pipelines;
+  std::unordered_set<uint64_t> textures;
+  std::unordered_set<uint32_t> windows,arenas;
+  bool hasDraw=false;
+  // Decide for the whole immutable pass before recording anything. Failure
+  // preserves all reference commands, never a partially intercepted pass.
+  for(const auto& command:pass.commands) {
+    if(command.type==CommandType::CustomDraw) return false;
+    if(command.type!=CommandType::Draw) continue;
+    gx::DrawData d;clear::DrawData clear;
+    if(decode_flip_clear_draw(command.data.draw,clear)) continue;
+    if(!decode_flip_gx_draw(command.data.draw,d)) return false;
+    hasDraw=true;
+    if(seenPipelines.insert(d.pipeline).second) {
+      gx::PipelineConfig c;wgpu::RenderPipeline p;
+      if(!get_pipeline(d.pipeline,p)||!gx::find_flip_pipeline_config(d.pipeline,c)||
+         c.shaderConfig.fogRangeEnabled||!(c.shaderConfig.flipFlags&gx::FlipBatchedDraws)||
+         std::bit_cast<float>(c.polygonOffsetClampBits)!=0.f) return false;
+      pipelines.push_back(std::move(p));
+    }
+    if(d.bindGroups.textureBindGroup) textures.insert(d.bindGroups.textureBindGroup);
+    windows.insert(d.uniformRange.offset/gx::FlipUniformWindow);
+    if(gx::resident_arena(d)) arenas.insert(gx::resident_arena(d));
+  }
+  if(!hasDraw) return false;
+  const auto& res=resources();
+  // The direct path issues the GL work itself. Dawn only needs enough recorded
+  // state to keep pass setup/teardown correct; pipelines, uniform windows and
+  // texture groups stay alive through the GX caches, so recording every one of
+  // them (hundreds per pass) is pure command-buffer and lazy-clear overhead.
+  static const bool minimalRecord=[] {const char* v=std::getenv("MELEE_FLIP_DIRECT_RECORD_MIN");return !v||std::strcmp(v,"0");}();
+  for(auto arena:arenas) encoder.SetVertexBuffer(0,gx::resident::buffer(arena));
+  encoder.SetVertexBuffer(0,res.vertexBuffer);
+  encoder.SetIndexBuffer(res.indexBuffer,wgpu::IndexFormat::Uint16);
+  encoder.SetBindGroup(0,res.staticBindGroup);
+  if(minimalRecord) {
+    if(!pipelines.empty()) encoder.SetPipeline(pipelines.front());
+    for(auto window:windows) flip_uniform_window(window); // ensure the window buffers exist
+  } else {
+    for(const auto& p:pipelines) encoder.SetPipeline(p);
+    const uint32_t zero=0;
+    for(auto window:windows) encoder.SetBindGroup(1,flip_uniform_window(window),1,&zero);
+    for(auto texture:textures) encoder.SetBindGroup(2,find_bind_group(texture));
+  }
+  pass.flipDirectResourcesOnly=true;
+  return true;
+}
+
+void prepare_flip_gles_frame(FramePacket& frame) {
+  sPlans.clear();sNextPlan=0;++sFrameNumber;
+  static const bool glCallsProfile=std::getenv("MELEE_FLIP_PROFILE")!=nullptr;
+  if(glCallsProfile) {
+    static GlCallStats sum; static unsigned frames=0;
+    sum.programs+=sGlCalls.programs; sum.pipelineStates+=sGlCalls.pipelineStates; sum.textures+=sGlCalls.textures; sum.texParams+=sGlCalls.texParams;
+    sum.samplers+=sGlCalls.samplers; sum.ubos+=sGlCalls.ubos; sum.vbos+=sGlCalls.vbos; sum.layouts+=sGlCalls.layouts; sum.immediates+=sGlCalls.immediates; sum.draws+=sGlCalls.draws; sum.constantDraws+=sGlCalls.constantDraws;
+    if(++frames%120==0) {
+      std::fprintf(stderr,"[flip-gl-calls] per frame: draws=%.1f constant=%.1f programs=%.1f pipeline-states=%.1f textures=%.1f tex-params=%.1f samplers=%.1f ubos=%.1f vbos=%.1f layouts=%.1f immediates=%.1f\n",
+        sum.draws/120.0,sum.constantDraws/120.0,sum.programs/120.0,sum.pipelineStates/120.0,sum.textures/120.0,sum.texParams/120.0,sum.samplers/120.0,sum.ubos/120.0,sum.vbos/120.0,sum.layouts/120.0,sum.immediates/120.0);
+      sum={};
+    }
+  }
+  sGlCalls={};
+  // Texture-object parameter memos persist across passes and frames; forget
+  // entries whose GL names died so a recycled name starts clean.
+  GLuint dead[256];
+  while(uint32_t n=dawn::native::opengl::GetDirectGLDestroyedTextures(webgpu::g_device.Get(),dead,256)) {
+    for(uint32_t i=0;i<n;++i) {
+      sTextureParams.erase(dead[i]);
+      sTextureSamplers.erase(dead[i]);
+      for(auto& unit:sTextureState) if(unit.valid&&unit.texture.texture==dead[i]) unit.valid=false;
+    }
+    if(n<256) break;
+  }
+  static const bool bindDawn=[] {const char* v=std::getenv("MELEE_FLIP_MAPPED_BIND_DAWN");return v&&v[0]=='1';}();
+  sMapped=bindDawn?nullptr:flip_mapped_slot(frame);
+  // Bisect helpers: MELEE_FLIP_MAPPED_USE=uniforms|indices binds only that stream from mapped storage.
+  // Default: uniforms only. Index data read by the driver from persistently
+  // mapped storage produced stale draws on this Mali driver (see v89 trials).
+  // Default: both streams. Index data consumed from persistently mapped storage
+  // requires glDrawRangeElements (explicit vertex ranges) on this Mali driver;
+  // plain glDrawElements produced stale draws (v89 trials).
+  static const int mappedUse=[] {const char* v=std::getenv("MELEE_FLIP_MAPPED_USE");if(!v)return 7;if(!std::strcmp(v,"uniforms"))return 1;if(!std::strcmp(v,"indices"))return 2;if(!std::strcmp(v,"vertices"))return 4;if(!std::strcmp(v,"all"))return 7;return 7;}();
+  sMappedUniforms=sMapped&&(mappedUse&1)?sMapped:nullptr;
+  sMappedIndices=sMapped&&(mappedUse&2)?sMapped:nullptr;
+  sMappedVertices=sMapped&&(mappedUse&4)&&sMapped->vertexData?sMapped:nullptr;
+  sPresentBlit=PresentBlit{}; // a request is valid for one frame only
+  if(!direct_mode()) return;
+  for(auto& pass:frame.renderPasses) {
+    if(!pass.sealed||pass.discardable||!pass.colorAttachmentCount) continue;
+    PassPlan plan{.target=pass.colorAttachments[0].view,
+                  .label=pass.flipDirectLabel,
+                  .width=pass.colorAttachments[0].size.width,
+                  .height=pass.colorAttachments[0].size.height,
+                  .segmentStart=pass.flipSegmentStart,
+                  .eligible=pass.msaaSamples==1&&pass.colorAttachmentCount==1&&
+                            gx::flip_vertex_enabled()&&gx::flip_uniform_windows_enabled()};
+    bool hasGxDraw=false;
+    uint32_t gxDraws=0,clearDraws=0,otherDraws=0,customDraws=0;
+    for(const auto& command:pass.commands) {
+      PlanCommand out{.type=command.type};
+      if(command.type==CommandType::SetViewport) out.viewport=command.data.setViewport;
+      else if(command.type==CommandType::SetScissor) out.scissor=command.data.setScissor;
+      else if(command.type==CommandType::Draw) {
+        if(decode_flip_gx_draw(command.data.draw,out.draw)) {
+          hasGxDraw=true;++gxDraws;
+          gx::PipelineConfig config;
+          // These paths still need Dawn's packed immediate/depth-clamp ABI.
+          // Retain the complete reference pass when it is required.
+          if(!gx::find_flip_pipeline_config(out.draw.pipeline,config)||
+             config.shaderConfig.fogRangeEnabled||!(config.shaderConfig.flipFlags&gx::FlipBatchedDraws)||
+             std::bit_cast<float>(config.polygonOffsetClampBits)!=0.f) plan.eligible=false;
+          else out.sortable=config.depthCompare&&config.depthUpdate&&(config.depthFunc==GX_LESS||config.depthFunc==GX_LEQUAL)&&
+                            config.blendMode==GX_BM_NONE&&config.colorUpdate&&config.polygonOffsetBits==0;
+        }
+        else if(decode_flip_clear_draw(command.data.draw,out.clear)) {out.isClear=true;++clearDraws;}
+        else {plan.eligible=false;++otherDraws;}
+      } else if(command.type==CommandType::CustomDraw) {plan.eligible=false;++customDraws;}
+      plan.commands.push_back(out);
+    }
+    plan.eligible&=hasGxDraw;
+    // Experiment (MELEE_FLIP_SORT_OPAQUE=1): issue runs of consecutive opaque,
+    // depth-ordered draws grouped by program, textures and uniform window so
+    // the driver sees fewer state changes. Not exact when opaque surfaces share
+    // depth values, hence opt-in; the frozen EFB capture is the check.
+    static const bool sortOpaque=[] {const char* v=std::getenv("MELEE_FLIP_SORT_OPAQUE");return v&&std::strcmp(v,"0");}();
+    if(sortOpaque&&plan.eligible) {
+      static unsigned runs=0,sorted=0,frames=0;
+      auto& cmds=plan.commands;
+      size_t i=0;
+      while(i<cmds.size()) {
+        if(cmds[i].type!=CommandType::Draw||cmds[i].isClear||!cmds[i].sortable) {++i;continue;}
+        size_t j=i;
+        while(j<cmds.size()&&cmds[j].type==CommandType::Draw&&!cmds[j].isClear&&cmds[j].sortable) ++j;
+        if(j-i>1) {
+          std::stable_sort(cmds.begin()+i,cmds.begin()+j,[](const PlanCommand& a,const PlanCommand& b) {
+            if(a.draw.pipeline!=b.draw.pipeline) return a.draw.pipeline<b.draw.pipeline;
+            if(a.draw.bindGroups.textureBindGroup!=b.draw.bindGroups.textureBindGroup) return a.draw.bindGroups.textureBindGroup<b.draw.bindGroups.textureBindGroup;
+            return a.draw.uniformRange.offset/gx::FlipUniformWindow<b.draw.uniformRange.offset/gx::FlipUniformWindow;
+          });
+          ++runs; sorted+=static_cast<unsigned>(j-i);
+        }
+        i=j;
+      }
+      if(std::getenv("MELEE_FLIP_PROFILE")&&++frames%360==0) {
+        std::fprintf(stderr,"[flip-sort] per pass: runs=%.1f sorted-draws=%.1f\n",runs/360.0,sorted/360.0); runs=sorted=0;
+      }
+    }
+    AURORA_ASSERT(!pass.flipDirectResourcesOnly||plan.eligible,"Direct resource-only pass lost eligibility");
+    if((sFrameNumber%120)==0)
+      std::fprintf(stderr,"[flip-direct-plan] frame=%llu pass=%zu eligible=%u gx=%u clear=%u other=%u custom=%u commands=%zu\n",
+                   static_cast<unsigned long long>(sFrameNumber),sPlans.size(),plan.eligible,
+                   gxDraws,clearDraws,otherDraws,customDraws,plan.commands.size());
+    sPlans.push_back(std::move(plan));
+  }
+}
+
+// Resident arena partial uploads: stage the bytes in a fresh GL buffer (never in
+// use, so no implicit sync) and copy GPU-side. Runs on the render worker before
+// the frame's submit, i.e. ordered after the previous frame's draws.
+struct ResidentUploadRequest { GLuint target; const uint8_t* data; const std::pair<size_t,size_t>* ranges; size_t count; };
+void resident_direct_upload(const wgpu::Buffer& buffer,const uint8_t* data,const std::pair<size_t,size_t>* ranges,size_t count) {
+  if(!count) return;
+  ResidentUploadRequest request{dawn::native::opengl::GetDirectGLBuffer(buffer.Get()),data,ranges,count};
+  const bool ran=request.target&&dawn::native::opengl::RunDirectGL(webgpu::g_device.Get(),[](void* p) {
+    const auto& r=*static_cast<ResidentUploadRequest*>(p);
+    size_t total=0; for(size_t i=0;i<r.count;++i) total+=r.ranges[i].second;
+    GLuint staging=0; glGenBuffers(1,&staging);
+    glBindBuffer(GL_COPY_READ_BUFFER,staging);
+    glBufferData(GL_COPY_READ_BUFFER,GLsizeiptr(total),nullptr,GL_STREAM_DRAW);
+    size_t cursor=0;
+    for(size_t i=0;i<r.count;++i) { glBufferSubData(GL_COPY_READ_BUFFER,GLintptr(cursor),GLsizeiptr(r.ranges[i].second),r.data+r.ranges[i].first); cursor+=r.ranges[i].second; }
+    glBindBuffer(GL_COPY_WRITE_BUFFER,r.target);
+    cursor=0;
+    for(size_t i=0;i<r.count;++i) { glCopyBufferSubData(GL_COPY_READ_BUFFER,GL_COPY_WRITE_BUFFER,GLintptr(cursor),GLintptr(r.ranges[i].first),GLsizeiptr(r.ranges[i].second)); cursor+=r.ranges[i].second; }
+    glBindBuffer(GL_COPY_READ_BUFFER,0); glBindBuffer(GL_COPY_WRITE_BUFFER,0);
+    glDeleteBuffers(1,&staging);
+    const GLenum err=glGetError();
+    if(err!=GL_NO_ERROR) { static bool once=false; if(!once) { once=true; std::fprintf(stderr,"[flip-resident-copy] GL error 0x%x\n",err); } }
+  },&request);
+  if(!ran) for(size_t i=0;i<count;++i) webgpu::g_queue.WriteBuffer(buffer,ranges[i].first,data+ranges[i].first,ranges[i].second);
+}
+void install_flip_gles_frame() {
+  if(direct_mode()) dawn::native::opengl::SetDirectGLRenderPassCallback(render_pass,nullptr);
+  static const bool residentCopy=[] {const char* v=std::getenv("MELEE_FLIP_RESIDENT_COPY");return !v||std::strcmp(v,"0");}();
+  if(direct_mode()>=5&&residentCopy) gx::resident::state().directUpload=resident_direct_upload;
+}
+void uninstall_flip_gles_frame() {
+  dawn::native::opengl::SetDirectGLRenderPassCallback(nullptr,nullptr);
+  if(sStream.pending) dawn::native::opengl::RunDirectGL(webgpu::g_device.Get(),[](void*) {sStream.finish();},nullptr);
+  flip_profile::end();
+}
+bool upload_flip_gles_stream(FramePacket& frame) {
+  sStream.current=nullptr;
+  static const bool enabled=[] {const char* v=std::getenv("MELEE_FLIP_STREAM_UPLOAD");return v&&!std::strcmp(v,"1");}();
+  if(!enabled||direct_mode()<5||!frame.encoderTasks.empty()) return false;
+  static const unsigned every=[] {const char* v=std::getenv("MELEE_FLIP_STREAM_EVERY");return v?std::max(1,std::atoi(v)):1;}();
+  if(frame.frameId%every) return false;
+  bool hasDraw=false;
+  // Only skip reference uploads if every consumer of shared geometry is a
+  // qualified resource-only GX pass. Never feed stale buffers to a fallback.
+  for(const auto& pass:frame.renderPasses) {
+    if(!pass.sealed||pass.discardable) continue;
+    for(const auto& command:pass.commands) {
+      if(command.type==CommandType::CustomDraw) return false;
+      if(command.type!=CommandType::Draw) continue;
+      clear::DrawData clear;
+      if(decode_flip_clear_draw(command.data.draw,clear)) continue;
+      gx::DrawData draw;
+      if(!decode_flip_gx_draw(command.data.draw,draw)||!pass.flipDirectResourcesOnly) return false;
+      hasDraw=true;
+    }
+  }
+  if(!hasDraw) return false;
+  struct Upload {FramePacket* frame;bool success=false;} upload{&frame};
+  const bool ran=dawn::native::opengl::RunDirectGL(webgpu::g_device.Get(),[](void* data) {
+    auto& upload=*static_cast<Upload*>(data);const auto& f=*upload.frame;
+    upload.success=sStream.upload(f.verts.data(),AURORA_ALIGN(f.verts.size(),4),
+                                  f.indices.data(),AURORA_ALIGN(f.indices.size(),4),
+                                  f.uniforms.data(),AURORA_ALIGN(f.uniforms.size(),4),profile_enabled());
+  },&upload);
+  return ran&&upload.success;
+}
+void shutdown_flip_gles() {
+  uninstall_flip_gles_frame();
+  if(!direct_mode()) return;
+  dawn::native::opengl::RunDirectGL(webgpu::g_device.Get(),[](void*) {
+    sStream.shutdown();
+    for(auto& pending:PassTimer::pending) glDeleteQueries(1,&pending.query);
+    PassTimer::pending.clear();PassTimer::get64=nullptr;PassTimer::initialized=false;
+    gx::shutdown_flip_gles_uber();
+    if(sVao) glDeleteVertexArrays(1,&sVao);
+    for(auto& [key,v]:sLayoutVaos) if(v.vao) glDeleteVertexArrays(1,&v.vao);
+    sLayoutVaos.clear(); sCurrentVao=nullptr;
+    if(sClearProgram) glDeleteProgram(sClearProgram);
+    sVao=sClearProgram=sEnabledAttributes=0;
+  },nullptr);
+  sPrepared.clear();sPlans.clear();sNextPlan=0;sFrameNumber=0;sResolvedTextures.clear();sTextureParams.clear();sResidentGL.clear();sTextureSamplers.clear();
+}
+}
+#endif
diff --git a/lib/gfx/flip_gles.hpp b/lib/gfx/flip_gles.hpp
new file mode 100644
index 0000000..713c602
--- /dev/null
+++ b/lib/gfx/flip_gles.hpp
@@ -0,0 +1,26 @@
+#pragma once
+
+#ifdef MELEE_MIYOO_FLIP
+#include <webgpu/webgpu_cpp.h>
+#include <string_view>
+namespace aurora::gfx::detail {
+struct FramePacket;
+struct RenderPass;
+bool encode_flip_gles_resources(const wgpu::RenderPassEncoder& encoder, RenderPass& pass, std::string_view label);
+void prepare_flip_gles_frame(FramePacket& frame);
+bool upload_flip_gles_stream(FramePacket& frame);
+void install_flip_gles_frame();
+// Present: ask the direct path to replace the next "EFB copy render pass" with a
+// framebuffer blit of `source` (already the swapchain-sized EFB) into the pass's
+// attached target. Falls back to the recorded pass when the blit is unavailable.
+void flip_request_present_blit(const wgpu::TextureView& source, uint32_t width, uint32_t height);
+bool flip_present_blit_enabled();
+// Scene-on-surface (MELEE_FLIP_SCENE_ON_SURFACE, default on): EFB passes
+// render into the frame's presented texture; the present copy pass is skipped.
+bool flip_scene_on_surface_enabled();
+void set_flip_frame_surface(wgpu::Texture texture, wgpu::TextureView view);
+std::pair<wgpu::Texture, wgpu::TextureView> take_flip_frame_surface();
+void uninstall_flip_gles_frame();
+void shutdown_flip_gles();
+}
+#endif
diff --git a/lib/gfx/flip_profile.hpp b/lib/gfx/flip_profile.hpp
new file mode 100644
index 0000000..e75fb20
--- /dev/null
+++ b/lib/gfx/flip_profile.hpp
@@ -0,0 +1,77 @@
+#pragma once
+// Sampled, nested CPU timings. No GL query, fence, or GPU wait is introduced.
+#include <array>
+#include <atomic>
+#include <chrono>
+#include <cstdio>
+#include <cstdlib>
+#include <cstring>
+#include <unordered_map>
+#include <string>
+#include <vector>
+#include <ctime>
+
+namespace aurora::gfx::detail::flip_profile {
+inline bool enabled() {
+#if MELEE_FLIP_DEEP_TIMERS
+  static const bool value=[] {const char* s=std::getenv("MELEE_FLIP_DEEP_PROFILE");return s&&!std::strcmp(s,"1");}();
+  return value;
+#else
+  return false;
+#endif
+}
+inline unsigned interval() {
+  static const unsigned value=[] {const char* s=std::getenv("MELEE_FLIP_DEEP_INTERVAL");return s?std::max(1,std::atoi(s)):120;}();
+  return value;
+}
+inline uint64_t cpu_now() {timespec t{};clock_gettime(CLOCK_THREAD_CPUTIME_ID,&t);return uint64_t(t.tv_sec)*1000000000+t.tv_nsec;}
+inline uint64_t wall_now() {return std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now().time_since_epoch()).count();}
+inline bool cpu_enabled() {static const bool value=std::getenv("MELEE_FLIP_DEEP_CPU")!=nullptr;return value;}
+struct Totals {uint64_t calls=0,wall=0,cpu=0,selfWall=0,selfCpu=0,units=0;};
+struct Key {const char* name;uint64_t tag;bool operator==(const Key&) const=default;};
+struct KeyHash {size_t operator()(const Key& key) const {return (reinterpret_cast<uintptr_t>(key.name)>>3)^(key.tag*0x9e3779b97f4a7c15ULL);}};
+struct Scope;
+struct State {
+  bool active=false;uint64_t frame=0,tag=0;const char* lane="unknown";Scope* parent=nullptr;
+  std::unordered_map<Key,Totals,KeyHash> totals;
+  std::vector<std::string> patterns;
+};
+inline thread_local State state;
+inline std::atomic<uint64_t> fifoFrame{0};
+inline bool active() {return enabled()&&state.active;}
+inline void begin(uint64_t frame,const char* lane) {
+  if(!enabled()) return;
+  state.active=enabled()&&frame%interval()==0;
+  state.frame=frame;state.lane=lane;state.tag=0;state.parent=nullptr;
+  state.totals.clear();state.patterns.clear();
+  if(state.active) {state.totals.reserve(4096);state.patterns.reserve(512);}
+}
+struct Scope {
+  bool on;const char* name;uint64_t tag,units,wall,cpu,childWall,childCpu;Scope* parent;
+  __attribute__((always_inline)) Scope(const char* label,uint64_t key=UINT64_MAX,uint64_t count=0):on(active()) {
+    if(!on) return;
+    name=label;tag=key==UINT64_MAX?state.tag:key;units=count;childWall=childCpu=0;cpu=0;
+    parent=state.parent;state.parent=this;if(cpu_enabled()) cpu=cpu_now();wall=wall_now();
+  }
+  __attribute__((always_inline)) ~Scope() {if(on) finish();}
+  __attribute__((noinline)) void finish() {
+    const uint64_t elapsedWall=wall_now()-wall,elapsedCpu=cpu_enabled()?cpu_now()-cpu:0;
+    state.parent=parent;
+    if(parent) {parent->childWall+=elapsedWall;parent->childCpu+=elapsedCpu;}
+    auto& sum=state.totals[{name,tag}];++sum.calls;sum.wall+=elapsedWall;sum.cpu+=elapsedCpu;
+    sum.selfWall+=elapsedWall>childWall?elapsedWall-childWall:0;
+    sum.selfCpu+=elapsedCpu>childCpu?elapsedCpu-childCpu:0;sum.units+=units;
+  }
+};
+inline void end() {
+  if(!enabled()) return;
+  if(!state.active) return;
+  state.active=false;
+  for(const auto& [key,s]:state.totals)
+    std::fprintf(stderr,"[flip-deep] frame=%llu lane=%s zone=%s tag=%llu calls=%llu wall_us=%.3f cpu_us=%.3f self_wall_us=%.3f self_cpu_us=%.3f units=%llu cpu_measured=%u\n",
+      (unsigned long long)state.frame,state.lane,key.name,(unsigned long long)key.tag,
+      (unsigned long long)s.calls,s.wall/1000.,s.cpu/1000.,s.selfWall/1000.,s.selfCpu/1000.,(unsigned long long)s.units,cpu_enabled());
+  for(const auto& pattern:state.patterns)
+    std::fprintf(stderr,"[flip-pattern] frame=%llu lane=%s %s\n",(unsigned long long)state.frame,state.lane,pattern.c_str());
+}
+}
diff --git a/lib/gfx/flip_sprites.cpp b/lib/gfx/flip_sprites.cpp
new file mode 100644
index 0000000..31b541b
--- /dev/null
+++ b/lib/gfx/flip_sprites.cpp
@@ -0,0 +1,252 @@
+#include "flip_sprites.hpp"
+
+#include "frame.hpp"
+#include "../webgpu/gpu.hpp"
+#include "../webgpu/gpu_prof.hpp"
+
+#include <cstdlib>
+#include <cstring>
+#include <mutex>
+
+namespace aurora::gfx::flip_sprites {
+using webgpu::g_device;
+
+bool enabled() noexcept { return threshold() != 0; }
+uint32_t threshold() noexcept {
+  static const uint32_t value = [] {
+    const char* v = std::getenv("MELEE_FLIP_HALFRES_SPRITES");
+    // Opt-in: on the Flip the pass saves ~8 ms of GPU on Fountain of Dreams but
+    // its three extra passes cost the render worker more than that scene, which
+    // is CPU-bound, gets back (v133). Set e.g. 4000 to enable for sprite-heavy frames.
+    return v ? static_cast<uint32_t>(std::strtoul(v, nullptr, 10)) : 0u;
+  }();
+  return value;
+}
+
+namespace {
+std::mutex g_mutex;
+webgpu::TextureWithSampler g_color;
+webgpu::TextureWithSampler g_depth;
+wgpu::TextureView g_sceneView;
+wgpu::RenderPipeline g_prepPipeline, g_compositePipeline;
+wgpu::BindGroupLayout g_prepLayout, g_compositeLayout;
+wgpu::Sampler g_linearSampler;
+EncoderTaskId g_prepTask = InvalidEncoderTask, g_compositeTask = InvalidEncoderTask;
+
+constexpr const char* PrepShader = R"(
+@group(0) @binding(0) var scene_depth: texture_depth_2d;
+var<private> positions: array<vec2f, 3> = array(vec2f(-1.0, 1.0), vec2f(-1.0, -3.0), vec2f(3.0, 1.0));
+@vertex fn vs_main(@builtin(vertex_index) vi: u32) -> @builtin(position) vec4f {
+    return vec4f(positions[vi], 0.0, 1.0);
+}
+struct Out { @builtin(frag_depth) depth: f32, @location(0) color: vec4f };
+@fragment fn fs_main(@builtin(position) pos: vec4f) -> Out {
+    let size = vec2i(textureDimensions(scene_depth));
+    let coord = clamp(vec2i(pos.xy) * 2, vec2i(0), size - vec2i(1));
+    var out: Out;
+    out.depth = textureLoad(scene_depth, coord, 0);
+    out.color = vec4f(0.0);
+    return out;
+}
+)";
+constexpr const char* CompositeShader = R"(
+@group(0) @binding(0) var samp: sampler;
+@group(0) @binding(1) var sprites: texture_2d<f32>;
+struct VertexOutput { @builtin(position) pos: vec4f, @location(0) uv: vec2f };
+var<private> positions: array<vec2f, 3> = array(vec2f(-1.0, 1.0), vec2f(-1.0, -3.0), vec2f(3.0, 1.0));
+var<private> uvs: array<vec2f, 3> = array(vec2f(0.0, 0.0), vec2f(0.0, 2.0), vec2f(2.0, 0.0));
+@vertex fn vs_main(@builtin(vertex_index) vi: u32) -> VertexOutput {
+    var out: VertexOutput;
+    out.pos = vec4f(positions[vi], 0.0, 1.0);
+    out.uv = uvs[vi];
+    return out;
+}
+@fragment fn fs_main(in: VertexOutput) -> @location(0) vec4f {
+    return textureSample(sprites, samp, in.uv);
+}
+)";
+
+wgpu::ShaderModule make_module(const char* source, const char* label) {
+  const wgpu::ShaderSourceWGSL wgsl{wgpu::ShaderSourceWGSL::Init{.code = source}};
+  const wgpu::ShaderModuleDescriptor desc{.nextInChain = &wgsl, .label = label};
+  return g_device.CreateShaderModule(&desc);
+}
+
+void ensure_pipelines() {
+  if (g_prepPipeline) return;
+  const auto colorFormat = webgpu::g_graphicsConfig.surfaceConfiguration.format;
+  {
+    const std::array entries{wgpu::BindGroupLayoutEntry{
+        .binding = 0,
+        .visibility = wgpu::ShaderStage::Fragment,
+        .texture = {.sampleType = wgpu::TextureSampleType::Depth, .viewDimension = wgpu::TextureViewDimension::e2D},
+    }};
+    const wgpu::BindGroupLayoutDescriptor layoutDesc{.label = "Sprite prep layout", .entryCount = entries.size(), .entries = entries.data()};
+    g_prepLayout = g_device.CreateBindGroupLayout(&layoutDesc);
+    const auto module = make_module(PrepShader, "Sprite prep");
+    const std::array targets{wgpu::ColorTargetState{.format = colorFormat, .writeMask = wgpu::ColorWriteMask::None}};
+    const wgpu::FragmentState fragment{.module = module, .entryPoint = "fs_main", .targetCount = targets.size(), .targets = targets.data()};
+    const wgpu::DepthStencilState depth{
+        .format = webgpu::g_graphicsConfig.depthFormat,
+        .depthWriteEnabled = true,
+        .depthCompare = wgpu::CompareFunction::Always,
+    };
+    const wgpu::PipelineLayoutDescriptor plDesc{.bindGroupLayoutCount = 1, .bindGroupLayouts = &g_prepLayout};
+    const auto pipelineLayout = g_device.CreatePipelineLayout(&plDesc);
+    const wgpu::RenderPipelineDescriptor desc{
+        .label = "Sprite prep",
+        .layout = pipelineLayout,
+        .vertex = {.module = module, .entryPoint = "vs_main"},
+        .primitive = {.topology = wgpu::PrimitiveTopology::TriangleList},
+        .depthStencil = &depth,
+        .fragment = &fragment,
+    };
+    g_prepPipeline = g_device.CreateRenderPipeline(&desc);
+  }
+  {
+    const std::array entries{
+        wgpu::BindGroupLayoutEntry{.binding = 0, .visibility = wgpu::ShaderStage::Fragment, .sampler = {.type = wgpu::SamplerBindingType::Filtering}},
+        wgpu::BindGroupLayoutEntry{.binding = 1, .visibility = wgpu::ShaderStage::Fragment,
+                                   .texture = {.sampleType = wgpu::TextureSampleType::Float, .viewDimension = wgpu::TextureViewDimension::e2D}},
+    };
+    const wgpu::BindGroupLayoutDescriptor layoutDesc{.label = "Sprite composite layout", .entryCount = entries.size(), .entries = entries.data()};
+    g_compositeLayout = g_device.CreateBindGroupLayout(&layoutDesc);
+    const auto module = make_module(CompositeShader, "Sprite composite");
+    // EFB' = sprites.rgb + EFB * (1 - coverage); EFB alpha untouched.
+    static const wgpu::BlendState blend{
+        .color = {.operation = wgpu::BlendOperation::Add, .srcFactor = wgpu::BlendFactor::One, .dstFactor = wgpu::BlendFactor::OneMinusSrcAlpha},
+        .alpha = {.operation = wgpu::BlendOperation::Add, .srcFactor = wgpu::BlendFactor::Zero, .dstFactor = wgpu::BlendFactor::One},
+    };
+    const std::array targets{wgpu::ColorTargetState{
+        .format = colorFormat,
+        .blend = &blend,
+        .writeMask = wgpu::ColorWriteMask::Red | wgpu::ColorWriteMask::Green | wgpu::ColorWriteMask::Blue,
+    }};
+    const wgpu::FragmentState fragment{.module = module, .entryPoint = "fs_main", .targetCount = targets.size(), .targets = targets.data()};
+    const wgpu::PipelineLayoutDescriptor plDesc{.bindGroupLayoutCount = 1, .bindGroupLayouts = &g_compositeLayout};
+    const auto pipelineLayout = g_device.CreatePipelineLayout(&plDesc);
+    const wgpu::RenderPipelineDescriptor desc{
+        .label = "Sprite composite",
+        .layout = pipelineLayout,
+        .vertex = {.module = module, .entryPoint = "vs_main"},
+        .primitive = {.topology = wgpu::PrimitiveTopology::TriangleList},
+        .fragment = &fragment,
+    };
+    g_compositePipeline = g_device.CreateRenderPipeline(&desc);
+    const wgpu::SamplerDescriptor samplerDesc{
+        .label = "Sprite composite sampler",
+        .addressModeU = wgpu::AddressMode::ClampToEdge,
+        .addressModeV = wgpu::AddressMode::ClampToEdge,
+        .magFilter = wgpu::FilterMode::Linear,
+        .minFilter = wgpu::FilterMode::Linear,
+    };
+    g_linearSampler = g_device.CreateSampler(&samplerDesc);
+  }
+}
+
+void prep(const EncoderTaskContext&, const wgpu::CommandEncoder& cmd, const void*, size_t, void*) {
+  ensure_pipelines();
+  webgpu::TextureWithSampler color, depth;
+  {
+    std::lock_guard lock{g_mutex};
+    color = g_color;
+    depth = g_depth;
+  }
+  if (!color.view || !depth.view || !webgpu::g_depthBuffer.view) return;
+  const std::array entries{wgpu::BindGroupEntry{.binding = 0, .textureView = webgpu::g_depthBuffer.view}};
+  const wgpu::BindGroupDescriptor bgDesc{.layout = g_prepLayout, .entryCount = entries.size(), .entries = entries.data()};
+  const auto bindGroup = g_device.CreateBindGroup(&bgDesc);
+  const std::array attachments{wgpu::RenderPassColorAttachment{
+      .view = color.view, .loadOp = wgpu::LoadOp::Clear, .storeOp = wgpu::StoreOp::Store, .clearValue = {0.0, 0.0, 0.0, 0.0}}};
+  const wgpu::RenderPassDepthStencilAttachment depthAttachment{
+      .view = depth.view,
+      .depthLoadOp = wgpu::LoadOp::Clear,
+      .depthStoreOp = wgpu::StoreOp::Store,
+      .depthClearValue = 1.f,
+  };
+  const wgpu::RenderPassDescriptor passDesc{
+      .label = "Sprite prep pass",
+      .colorAttachmentCount = attachments.size(),
+      .colorAttachments = attachments.data(),
+      .depthStencilAttachment = &depthAttachment,
+      .timestampWrites = webgpu::gpu_prof::pass_writes("Sprite prep"),
+  };
+  const auto pass = cmd.BeginRenderPass(&passDesc);
+  pass.SetPipeline(g_prepPipeline);
+  pass.SetBindGroup(0, bindGroup);
+  pass.Draw(3);
+  pass.End();
+}
+
+void composite(const EncoderTaskContext&, const wgpu::CommandEncoder& cmd, const void*, size_t, void*) {
+  ensure_pipelines();
+  webgpu::TextureWithSampler color;
+  {
+    std::lock_guard lock{g_mutex};
+    color = g_color;
+  }
+  const wgpu::TextureView scene = g_sceneView ? g_sceneView : webgpu::g_frameBuffer.view;
+  if (!color.view || !scene) return;
+  const std::array entries{
+      wgpu::BindGroupEntry{.binding = 0, .sampler = g_linearSampler},
+      wgpu::BindGroupEntry{.binding = 1, .textureView = color.view},
+  };
+  const wgpu::BindGroupDescriptor bgDesc{.layout = g_compositeLayout, .entryCount = entries.size(), .entries = entries.data()};
+  const auto bindGroup = g_device.CreateBindGroup(&bgDesc);
+  const std::array attachments{wgpu::RenderPassColorAttachment{.view = scene, .loadOp = wgpu::LoadOp::Load, .storeOp = wgpu::StoreOp::Store}};
+  const wgpu::RenderPassDescriptor passDesc{
+      .label = "Sprite composite pass",
+      .colorAttachmentCount = attachments.size(),
+      .colorAttachments = attachments.data(),
+      .timestampWrites = webgpu::gpu_prof::pass_writes("Sprite composite"),
+  };
+  const auto pass = cmd.BeginRenderPass(&passDesc);
+  pass.SetPipeline(g_compositePipeline);
+  pass.SetBindGroup(0, bindGroup);
+  pass.Draw(3);
+  pass.End();
+}
+
+void ensure_targets() {
+  const auto& efb = webgpu::g_frameBuffer;
+  const uint32_t width = std::max(efb.size.width / 2, 1u), height = std::max(efb.size.height / 2, 1u);
+  std::lock_guard lock{g_mutex};
+  if (g_color.view && g_color.size.width == width && g_color.size.height == height) return;
+  g_color = webgpu::create_render_texture(width, height, false);
+  const wgpu::TextureDescriptor depthDesc{
+      .label = "Sprite depth",
+      .usage = wgpu::TextureUsage::RenderAttachment,
+      .dimension = wgpu::TextureDimension::e2D,
+      .size = {width, height, 1},
+      .format = webgpu::g_graphicsConfig.depthFormat,
+      .mipLevelCount = 1,
+      .sampleCount = 1,
+  };
+  auto texture = g_device.CreateTexture(&depthDesc);
+  auto view = texture.CreateView();
+  g_depth = {.texture = std::move(texture), .view = std::move(view), .size = {width, height, 1}, .format = webgpu::g_graphicsConfig.depthFormat};
+}
+} // namespace
+
+const webgpu::TextureWithSampler& color_target() {
+  ensure_targets();
+  return g_color;
+}
+const webgpu::TextureWithSampler& depth_target() {
+  ensure_targets();
+  return g_depth;
+}
+EncoderTaskId prep_task() {
+  if (g_prepTask == InvalidEncoderTask) g_prepTask = register_encoder_task_type({.label = "Sprite prep", .callback = prep});
+  return g_prepTask;
+}
+EncoderTaskId composite_task() {
+  if (g_compositeTask == InvalidEncoderTask)
+    g_compositeTask = register_encoder_task_type({.label = "Sprite composite", .callback = composite});
+  return g_compositeTask;
+}
+void set_scene_view(wgpu::TextureView view) { g_sceneView = std::move(view); }
+bool is_sprite_task(EncoderTaskId type) noexcept {
+  return type != InvalidEncoderTask && (type == g_prepTask || type == g_compositeTask);
+}
+} // namespace aurora::gfx::flip_sprites
diff --git a/lib/gfx/flip_sprites.hpp b/lib/gfx/flip_sprites.hpp
new file mode 100644
index 0000000..2de27d8
--- /dev/null
+++ b/lib/gfx/flip_sprites.hpp
@@ -0,0 +1,23 @@
+#pragma once
+// Half-resolution sprite pass (Miyoo Flip). Fountain of Dreams draws ~25k
+// blended point sprites that cost ~8 ms of GPU fill at 640x480. When a frame
+// draws more than MELEE_FLIP_HALFRES_SPRITES points (opt-in; 0 = off, default),
+// runs of eligible point draws render into a 320x240 target with a downsampled
+// scene depth and are composited back with premultiplied alpha.
+#include "../webgpu/gpu.hpp"
+#include "aurora/gfx.hpp"
+
+namespace aurora::gfx::flip_sprites {
+bool enabled() noexcept;
+uint32_t threshold() noexcept;
+// Half-res color/depth targets sized to the current EFB (created on demand).
+const webgpu::TextureWithSampler& color_target();
+const webgpu::TextureWithSampler& depth_target();
+// Encoder tasks: prepare (clear color, downsample the scene depth) and composite.
+EncoderTaskId prep_task();
+EncoderTaskId composite_task();
+// True for the two sprite tasks (they never read the streaming buffers).
+bool is_sprite_task(EncoderTaskId type) noexcept;
+// The scene color view the composite draws onto (set per frame op by the encoder).
+void set_scene_view(wgpu::TextureView view);
+} // namespace aurora::gfx::flip_sprites
diff --git a/lib/gfx/flip_stream.hpp b/lib/gfx/flip_stream.hpp
new file mode 100644
index 0000000..fbf1a7c
--- /dev/null
+++ b/lib/gfx/flip_stream.hpp
@@ -0,0 +1,121 @@
+#pragma once
+// Native GLES upload ownership. Each slot is immutable to the CPU from submit
+// until its completion fence signals; persistent mapping alone is not a lock.
+#include <GLES3/gl31.h>
+#include <EGL/egl.h>
+#include <array>
+#include <bit>
+#include <chrono>
+#include <cstdio>
+#include <cstring>
+#include <cstdlib>
+#include "flip_upload.hpp"
+
+namespace aurora::gfx::detail {
+struct FlipStream {
+  using StorageProc=void(GL_APIENTRY*)(GLenum,GLsizeiptr,const void*,GLbitfield);
+  struct Buffer { GLuint name=0; void* mapped=nullptr; size_t capacity=0; std::vector<uint8_t> shadow; };
+  struct Slot { Buffer vertices,indices,uniforms; GLsync done=nullptr; };
+  std::array<Slot,3> slots{};
+  unsigned next=0;
+  Slot* current=nullptr;
+  StorageProc storage=nullptr;
+  bool initialized=false,disabled=false,pending=false;
+  uint64_t uploads=0;
+
+  static bool wait(Slot& slot) {
+    if(!slot.done) return true;
+    GLenum status;
+    do { status=glClientWaitSync(slot.done,GL_SYNC_FLUSH_COMMANDS_BIT,1000000000); }
+    while(status==GL_TIMEOUT_EXPIRED);
+    if(status==GL_WAIT_FAILED) return false;
+    glDeleteSync(slot.done);slot.done=nullptr;return true;
+  }
+  static void destroy(Buffer& buffer) {
+    if(buffer.name) {
+      glBindBuffer(GL_COPY_WRITE_BUFFER,buffer.name);
+      if(buffer.mapped) glUnmapBuffer(GL_COPY_WRITE_BUFFER);
+      glDeleteBuffers(1,&buffer.name);
+    }
+    buffer={};
+  }
+  bool reserve(Buffer& buffer,size_t bytes) {
+    if(buffer.capacity>=bytes) return true;
+    destroy(buffer);
+    buffer.capacity=std::bit_ceil(std::max<size_t>(65536,bytes));
+    glGenBuffers(1,&buffer.name);
+    glBindBuffer(GL_COPY_WRITE_BUFFER,buffer.name);
+    constexpr GLbitfield flags=GL_MAP_WRITE_BIT|0x0040|0x0080; // persistent + coherent
+    storage(GL_COPY_WRITE_BUFFER,buffer.capacity,nullptr,flags);
+    if(glGetError()!=GL_NO_ERROR) {destroy(buffer);return false;}
+    buffer.mapped=glMapBufferRange(GL_COPY_WRITE_BUFFER,0,buffer.capacity,flags);
+    if(!buffer.mapped||glGetError()!=GL_NO_ERROR) {destroy(buffer);return false;}
+    // A partially used final uniform window must still have defined contents.
+    std::memset(buffer.mapped,0,buffer.capacity);
+    return true;
+  }
+  bool upload(const void* vertices,size_t vertexBytes,const void* indices,size_t indexBytes,
+              const void* uniforms,size_t uniformBytes,bool profile) {
+    current=nullptr;
+    if(disabled) return false;
+    if(!initialized) {
+      initialized=true;
+      const char* extensions=reinterpret_cast<const char*>(glGetString(GL_EXTENSIONS));
+      if(extensions&&std::strstr(extensions,"GL_EXT_buffer_storage"))
+        storage=reinterpret_cast<StorageProc>(eglGetProcAddress("glBufferStorageEXT"));
+      if(!storage) {disabled=true;return false;}
+    }
+    auto& slot=slots[next];
+    const auto start=std::chrono::steady_clock::now();
+    if(!wait(slot)) {
+      // No CPU overwrite is safe after a failed fence wait.
+      std::fprintf(stderr,"[flip-stream-error] completion fence failed\n");
+      std::abort();
+    }
+    const auto waited=std::chrono::steady_clock::now();
+    if(!reserve(slot.vertices,vertexBytes)||!reserve(slot.indices,indexBytes)||
+       !reserve(slot.uniforms,(uniformBytes+65535)&~size_t(65535))) {
+      disabled=true;
+      while(glGetError()!=GL_NO_ERROR) {}
+      std::fprintf(stderr,"[flip-stream] persistent allocation unavailable; retaining reference uploads\n");
+      return false;
+    }
+    size_t written=0;
+    static const bool dirty=[] {const char* v=std::getenv("MELEE_FLIP_STREAM_DIRTY");return !v||std::strcmp(v,"0");}();
+    const auto copy=[&](Buffer& buffer,const void* data,size_t bytes,size_t chunk) {
+      if(!bytes) return;
+      if(!dirty) {std::memcpy(buffer.mapped,data,bytes);written+=bytes;return;}
+      flip_upload_changed(buffer.shadow,{static_cast<const uint8_t*>(data),bytes},chunk,
+        [&](size_t offset,size_t length) {
+          std::memcpy(static_cast<uint8_t*>(buffer.mapped)+offset,static_cast<const uint8_t*>(data)+offset,length);
+          written+=length;
+        });
+    };
+    copy(slot.vertices,vertices,vertexBytes,4096);
+    copy(slot.indices,indices,indexBytes,4096);
+    copy(slot.uniforms,uniforms,uniformBytes,256);
+    current=&slot;pending=true;next=(next+1)%slots.size();
+    if(profile&&++uploads%120==0) {
+      const auto end=std::chrono::steady_clock::now();
+      std::fprintf(stderr,"[flip-stream] bytes=%zu written=%zu wait_ms=%.3f copy_ms=%.3f slots=3\n",
+        vertexBytes+indexBytes+uniformBytes,written,
+        std::chrono::duration<double,std::milli>(waited-start).count(),
+        std::chrono::duration<double,std::milli>(end-waited).count());
+    }
+    return true;
+  }
+  void finish() {
+    if(!pending) return;
+    current->done=glFenceSync(GL_SYNC_GPU_COMMANDS_COMPLETE,0);
+    if(!current->done) {std::fprintf(stderr,"[flip-stream-error] cannot fence submitted storage\n");std::abort();}
+    pending=false;
+  }
+  void shutdown() {
+    for(auto& slot:slots) {
+      if(!wait(slot)) std::abort();
+      destroy(slot.vertices);destroy(slot.indices);destroy(slot.uniforms);
+    }
+    current=nullptr;pending=false;next=0;initialized=disabled=false;storage=nullptr;uploads=0;
+  }
+};
+}
diff --git a/lib/gfx/flip_upload.hpp b/lib/gfx/flip_upload.hpp
new file mode 100644
index 0000000..967c8a7
--- /dev/null
+++ b/lib/gfx/flip_upload.hpp
@@ -0,0 +1,35 @@
+#pragma once
+#include <algorithm>
+#include <cassert>
+#include <cstring>
+#include <span>
+#include <vector>
+#include <cstdint>
+
+namespace aurora::gfx::detail {
+// Shadow exactly the bytes already submitted to a persistent buffer. Keep its
+// tail when a later frame is shorter: partial GL updates preserve that tail.
+// Compare all input bytes, including mutable/aliased geometry and uniforms.
+template <typename Write>
+void flip_upload_changed(std::vector<uint8_t>& previous, std::span<const uint8_t> data,
+                         size_t chunk, Write&& write) {
+  assert(chunk && chunk % 4 == 0 && data.size() % 4 == 0);
+  if(data.empty()) return;
+  const size_t known=previous.size();
+  previous.resize(std::max(known,data.size()));
+  size_t begin=data.size();
+  const auto flush=[&](size_t end) {
+    if(begin==data.size()) return;
+    write(begin,end-begin);
+    std::memcpy(previous.data()+begin,data.data()+begin,end-begin);
+    begin=data.size();
+  };
+  for(size_t offset=0;offset<data.size();offset+=chunk) {
+    const size_t end=std::min(offset+chunk,data.size());
+    const bool changed=end>known||std::memcmp(previous.data()+offset,data.data()+offset,end-offset)!=0;
+    if(changed) { if(begin==data.size()) begin=offset; }
+    else flush(offset);
+  }
+  flush(data.size());
+}
+} // namespace aurora::gfx::detail
diff --git a/lib/gfx/frame.cpp b/lib/gfx/frame.cpp
index 9fd83ef..14a52cb 100644
--- a/lib/gfx/frame.cpp
+++ b/lib/gfx/frame.cpp
@@ -1,4 +1,16 @@
+#ifdef MELEE_MIYOO_FLIP
+#include "../gx/flip_vertex.hpp"
+#endif
 #include "frame.hpp"
+#ifdef MELEE_MIYOO_FLIP
+#include "flip_gles.hpp"
+#include "../gx/flip_resident.hpp"
+#include <dawn/native/OpenGLBackend.h>
+#include <GLES3/gl31.h>
+#include <EGL/egl.h>
+#include <cstring>
+#endif
+#include "encoding.hpp"
 
 #include "depth_peek.hpp"
 #include "pipeline_cache.hpp"
@@ -48,8 +60,141 @@ enum class BufferMapState {
 };
 
 std::array<wgpu::Buffer, StagingBufferCount> g_stagingBuffers;
+#ifdef MELEE_MIYOO_FLIP
+std::array<std::unique_ptr<u8, decltype(&std::free)>, StagingBufferCount> g_flipCpuStaging{
+  std::unique_ptr<u8, decltype(&std::free)>(nullptr, &std::free),
+  std::unique_ptr<u8, decltype(&std::free)>(nullptr, &std::free),
+  std::unique_ptr<u8, decltype(&std::free)>(nullptr, &std::free)};
+#endif
 std::array<std::atomic<BufferMapState>, StagingBufferCount> g_mappingStates;
 uint32_t g_frameIndex = UINT32_MAX;
+#ifdef MELEE_MIYOO_FLIP
+size_t g_producerFrameSlot = 0;
+std::mutex g_deferredEndFrameMutex;
+std::array<EndFrameCallback, FrameSlotCount> g_deferredEndFrame;
+std::array<detail::FlipMappedSlot, StagingBufferCount> g_flipMapped;
+std::atomic<bool> g_flipMappedReady{false};
+size_t g_flipMappedUniformBytes = 0;
+std::deque<size_t> g_flipMappedPendingRelease; // staging slots whose frame is still in flight on the GPU
+bool flip_mapped_enabled() {
+  static const bool enabled = [] {
+    const char* v = std::getenv("MELEE_FLIP_MAPPED_STREAM");
+    const char* legacy = std::getenv("MELEE_FLIP_STREAM_UPLOAD");
+    return (!v || std::strcmp(v, "0")) && !(legacy && !std::strcmp(legacy, "1")) && gx::flip_cpu_upload_enabled() &&
+           gx::flip_uniform_windows_enabled();
+  }();
+  return enabled;
+}
+// Render worker, GL context current: create and map the slots once.
+void flip_create_mapped_slots(void*) {
+  using StorageProc = void (GL_APIENTRY*)(GLenum, GLsizeiptr, const void*, GLbitfield);
+  const char* extensions = reinterpret_cast<const char*>(glGetString(GL_EXTENSIONS));
+  StorageProc storage = nullptr;
+  if (extensions && std::strstr(extensions, "GL_EXT_buffer_storage"))
+    storage = reinterpret_cast<StorageProc>(eglGetProcAddress("glBufferStorageEXT"));
+  if (!storage) { std::fprintf(stderr, "[flip-mapped] GL_EXT_buffer_storage unavailable; using staged uploads\n"); return; }
+  // Persistent, explicitly flushed mapping: the FIFO worker writes from another
+  // core, so the written ranges are flushed on the render thread before submit
+  // rather than relying on coherent-mapping semantics for cross-thread writes.
+  constexpr GLbitfield storageFlags = GL_MAP_WRITE_BIT | 0x0040; // persistent
+  constexpr GLbitfield mapFlags = GL_MAP_WRITE_BIT | 0x0040 | GL_MAP_FLUSH_EXPLICIT_BIT;
+  const auto make = [&](GLuint& name, uint8_t*& data, size_t bytes) {
+    glGenBuffers(1, &name);
+    glBindBuffer(GL_COPY_WRITE_BUFFER, name);
+    storage(GL_COPY_WRITE_BUFFER, bytes, nullptr, storageFlags);
+    data = static_cast<uint8_t*>(glMapBufferRange(GL_COPY_WRITE_BUFFER, 0, bytes, mapFlags));
+    if (glGetError() != GL_NO_ERROR || !data) { data = nullptr; return false; }
+    std::memset(data, 0, bytes);
+    return true;
+  };
+  static const size_t uniformBytes = [] {
+    const char* v = std::getenv("MELEE_FLIP_MAPPED_UNIFORM_MB");
+    const size_t mb = v ? std::max(1, std::atoi(v)) : UniformBufferSize / (1024 * 1024);
+    return std::min<size_t>(mb * 1024 * 1024, UniformBufferSize);
+  }();
+  g_flipMappedUniformBytes = uniformBytes;
+  // Streamed vertices too: partial glBufferSubData into the shared vertex buffer
+  // made the Mali driver wait for the previous frame's GPU work (~10 ms per
+  // frame in moving scenes); the fenced slots already guarantee the storage is
+  // free when the FIFO worker writes it.
+  static const bool mappedVertices = [] { const char* v = std::getenv("MELEE_FLIP_MAPPED_VERTICES"); return !v || std::strcmp(v, "0"); }();
+  for (auto& slot : g_flipMapped) {
+    // A whole extra window lets resident draws bind a full window at any record.
+    if (!make(slot.uniforms, slot.uniformData, uniformBytes + gx::FlipUniformWindow) ||
+        !make(slot.indices, slot.indexData, IndexBufferSize) ||
+        (mappedVertices && !make(slot.vertices, slot.vertexData, VertexBufferSize))) {
+      std::fprintf(stderr, "[flip-mapped] persistent mapping failed; using staged uploads\n");
+      while (glGetError() != GL_NO_ERROR) {}
+      for (auto& reset : g_flipMapped) { reset.uniformData = reset.indexData = reset.vertexData = nullptr; }
+      return;
+    }
+  }
+  glBindBuffer(GL_COPY_WRITE_BUFFER, 0);
+  std::fprintf(stderr, "[flip-mapped] uniform%s and index streams recorded into persistently mapped storage\n", mappedVertices ? ", vertex" : "");
+  g_flipMappedReady.store(true, std::memory_order_release);
+}
+struct FlipFlushRequest { const detail::FlipMappedSlot* slot; size_t uniformBytes, indexBytes, vertexBytes; };
+void flip_flush_slot(void* data) {
+  const auto& r = *static_cast<FlipFlushRequest*>(data);
+  const auto flush = [](GLuint name, size_t bytes) {
+    if (!bytes) return;
+    glBindBuffer(GL_COPY_WRITE_BUFFER, name);
+    glFlushMappedBufferRange(GL_COPY_WRITE_BUFFER, 0, static_cast<GLsizeiptr>((bytes + 3) & ~size_t(3)));
+  };
+  flush(r.slot->uniforms, r.uniformBytes);
+  flush(r.slot->indices, r.indexBytes);
+  if (r.slot->vertexData) flush(r.slot->vertices, r.vertexBytes);
+  glBindBuffer(GL_COPY_WRITE_BUFFER, 0);
+}
+// Diagnostic (MELEE_FLIP_MAPPED_VERIFY_GPU=1): copy the mapped buffers through the
+// GPU into a readable buffer and compare with what the CPU wrote.
+void flip_verify_gpu_view(void* data) {
+  const auto& r = *static_cast<FlipFlushRequest*>(data);
+  static GLuint scratch = 0; static size_t scratchSize = 0; static unsigned frames = 0;
+  const auto verify = [&](GLuint name, const uint8_t* cpu, size_t bytes, const char* what) {
+    bytes = (bytes + 3) & ~size_t(3);
+    if (!bytes) return;
+    if (scratchSize < bytes) {
+      if (scratch) glDeleteBuffers(1, &scratch);
+      glGenBuffers(1, &scratch);
+      glBindBuffer(GL_COPY_WRITE_BUFFER, scratch);
+      glBufferData(GL_COPY_WRITE_BUFFER, bytes, nullptr, GL_DYNAMIC_READ);
+      scratchSize = bytes;
+    }
+    glBindBuffer(GL_COPY_READ_BUFFER, name);
+    glBindBuffer(GL_COPY_WRITE_BUFFER, scratch);
+    glCopyBufferSubData(GL_COPY_READ_BUFFER, GL_COPY_WRITE_BUFFER, 0, 0, bytes);
+    const auto* gpu = static_cast<const uint8_t*>(glMapBufferRange(GL_COPY_WRITE_BUFFER, 0, bytes, GL_MAP_READ_BIT));
+    if (!gpu) { std::fprintf(stderr, "[flip-mapped-verify] %s: map failed 0x%x\n", what, glGetError()); return; }
+    size_t mismatches = 0, first = SIZE_MAX;
+    for (size_t i = 0; i < bytes; i += 4) if (std::memcmp(gpu + i, cpu + i, 4)) { ++mismatches; if (first == SIZE_MAX) first = i; }
+    glUnmapBuffer(GL_COPY_WRITE_BUFFER);
+    std::fprintf(stderr, "[flip-mapped-verify] %s bytes=%zu mismatching_words=%zu first=%zu\n", what, bytes, mismatches, first);
+  };
+  if (++frames % 120 != 0) return;
+  verify(r.slot->uniforms, r.slot->uniformData, r.uniformBytes, "uniforms");
+  verify(r.slot->indices, r.slot->indexData, r.indexBytes, "indices");
+  if (r.slot->vertexData) verify(r.slot->vertices, r.slot->vertexData, r.vertexBytes, "vertices");
+  while (glGetError() != GL_NO_ERROR) {}
+}
+void flip_fence_slot(void* data) {
+  auto& slot = *static_cast<detail::FlipMappedSlot*>(data);
+  if (slot.fence) glDeleteSync(static_cast<GLsync>(slot.fence));
+  slot.fence = glFenceSync(GL_SYNC_GPU_COMMANDS_COMPLETE, 0);
+  glFlush();
+}
+struct FlipFenceQuery { detail::FlipMappedSlot* slot; bool block; bool done; };
+void flip_check_fence(void* data) {
+  auto& q = *static_cast<FlipFenceQuery*>(data);
+  if (!q.slot->fence) { q.done = true; return; }
+  GLenum status;
+  do {
+    status = glClientWaitSync(static_cast<GLsync>(q.slot->fence), GL_SYNC_FLUSH_COMMANDS_BIT, q.block ? 1000000000 : 0);
+  } while (q.block && status == GL_TIMEOUT_EXPIRED);
+  q.done = status == GL_ALREADY_SIGNALED || status == GL_CONDITION_SATISFIED || status == GL_WAIT_FAILED;
+  if (q.done) { glDeleteSync(static_cast<GLsync>(q.slot->fence)); q.slot->fence = nullptr; }
+}
+#endif
 
 std::array<FramePacket, FrameSlotCount> g_framePackets;
 uint64_t g_nextFrameId = 1;
@@ -214,6 +359,13 @@ void pace_frame_start() {
 }
 
 void map_staging_buffer(size_t slot, bool releaseSlotOnCompletion = false) {
+#ifdef MELEE_MIYOO_FLIP
+  if (gx::flip_cpu_upload_enabled()) {
+    g_mappingStates[slot].store(BufferMapState::Mapped, std::memory_order_release);
+    if (releaseSlotOnCompletion) g_stagingSlots.release(slot);
+    return;
+  }
+#endif
   auto expected = BufferMapState::Unmapped;
   if (!g_mappingStates[slot].compare_exchange_strong(expected, BufferMapState::Mapping, std::memory_order_acq_rel,
                                                      std::memory_order_acquire)) {
@@ -244,6 +396,24 @@ void map_staging_buffer(size_t slot, bool releaseSlotOnCompletion = false) {
 namespace detail {
 
 Resources& resources() noexcept { return g_resources; }
+#ifdef MELEE_MIYOO_FLIP
+const FlipMappedSlot* flip_mapped_slot(const FramePacket& frame) {
+  return frame.flipMapped ? &g_flipMapped[frame.stagingBuffer] : nullptr;
+}
+wgpu::BindGroup flip_uniform_window(uint32_t index) {
+  AURORA_ASSERT(index < UniformBufferSize / gx::FlipUniformWindow, "Uniform window overflow");
+  while (g_resources.flipUniformGroups.size() <= index) {
+    // Twice the window: resident draws bind a full window starting at their own record.
+    const wgpu::BufferDescriptor desc{.label="Flip uniform window",.usage=wgpu::BufferUsage::Uniform|wgpu::BufferUsage::CopyDst,.size=2*gx::FlipUniformWindow};
+    auto buffer=g_device.CreateBuffer(&desc);
+    const std::array entries{wgpu::BindGroupEntry{.binding=0,.buffer=buffer,.offset=0,.size=gx::FlipUniformWindow}};
+    const wgpu::BindGroupDescriptor group{.label="Flip uniform window",.layout=g_resources.uniformBindGroupLayout,.entryCount=entries.size(),.entries=entries.data()};
+    g_resources.flipUniformBuffers.push_back(buffer);
+    g_resources.flipUniformGroups.push_back(g_device.CreateBindGroup(&group));
+  }
+  return g_resources.flipUniformGroups[index];
+}
+#endif
 
 const wgpu::Buffer& staging_buffer(size_t slot) { return g_stagingBuffers[slot]; }
 
@@ -420,6 +590,15 @@ void initialize() {
                "Shared Storage Buffer");
   for (size_t i = 0; i < g_stagingBuffers.size(); ++i) {
     const auto label = fmt::format("Staging Buffer {}", i);
+#ifdef MELEE_MIYOO_FLIP
+    if (gx::flip_cpu_upload_enabled()) {
+      g_flipCpuStaging[i].reset(static_cast<u8*>(std::calloc(1, StagingBufferSize)));
+      AURORA_ASSERT(g_flipCpuStaging[i], "Cannot allocate CPU staging");
+      createBuffer(g_stagingBuffers[i], wgpu::BufferUsage::CopyDst | wgpu::BufferUsage::CopySrc,
+                   TextureUploadSize, label.c_str());
+      continue;
+    }
+#endif
     createBuffer(g_stagingBuffers[i], wgpu::BufferUsage::MapWrite | wgpu::BufferUsage::CopySrc, StagingBufferSize,
                  label.c_str());
   }
@@ -431,6 +610,42 @@ void initialize() {
   }
 
   {
+#ifdef MELEE_MIYOO_FLIP
+    const auto makeTexture = [](uint32_t bytes, const char* label) {
+      const wgpu::TextureDescriptor desc{
+        .label = label,
+        .usage = wgpu::TextureUsage::TextureBinding | wgpu::TextureUsage::CopyDst,
+        .size = {1024, bytes / 4096, 1},
+        .format = wgpu::TextureFormat::R32Uint,
+      };
+      return g_device.CreateTexture(&desc);
+    };
+    g_resources.vertexTexture = makeTexture(gx::flip_vertex_enabled() ? 4096 : VertexBufferSize, "Vertex fetch texture");
+    {
+      const wgpu::BufferDescriptor zeroDesc{.label = "Flip zero records", .usage = wgpu::BufferUsage::Vertex | wgpu::BufferUsage::CopyDst,
+                                            .size = 65536 * 4};
+      g_resources.flipZeroRecords = g_device.CreateBuffer(&zeroDesc);
+      static const std::vector<uint32_t> zeros(65536, 0u);
+      g_queue.WriteBuffer(g_resources.flipZeroRecords, 0, zeros.data(), zeros.size() * 4);
+    }
+    g_resources.storageTexture = makeTexture(StorageBufferSize, "Array fetch texture");
+    const std::array layoutEntries{
+      wgpu::BindGroupLayoutEntry{
+        .binding = 0, .visibility = wgpu::ShaderStage::Vertex,
+        .texture = {.sampleType = wgpu::TextureSampleType::Uint, .viewDimension = wgpu::TextureViewDimension::e2D}},
+      wgpu::BindGroupLayoutEntry{
+        .binding = 1, .visibility = wgpu::ShaderStage::Vertex | wgpu::ShaderStage::Fragment,
+        .texture = {.sampleType = wgpu::TextureSampleType::Uint, .viewDimension = wgpu::TextureViewDimension::e2D}},
+    };
+    const wgpu::BindGroupLayoutDescriptor layoutDesc{.entryCount = layoutEntries.size(), .entries = layoutEntries.data()};
+    g_resources.staticBindGroupLayout = g_device.CreateBindGroupLayout(&layoutDesc);
+    const std::array entries{
+      wgpu::BindGroupEntry{.binding = 0, .textureView = g_resources.vertexTexture.CreateView()},
+      wgpu::BindGroupEntry{.binding = 1, .textureView = g_resources.storageTexture.CreateView()},
+    };
+    const wgpu::BindGroupDescriptor desc{.layout = g_resources.staticBindGroupLayout, .entryCount = entries.size(), .entries = entries.data()};
+    g_resources.staticBindGroup = g_device.CreateBindGroup(&desc);
+#else
     constexpr std::array layoutEntries{
         // Vertex data buffer
         wgpu::BindGroupLayoutEntry{
@@ -474,6 +689,7 @@ void initialize() {
         .entries = entries.data(),
     };
     g_resources.staticBindGroup = g_device.CreateBindGroup(&bindGroupDescriptor);
+#endif
   }
 
   {
@@ -509,6 +725,14 @@ void initialize() {
         .entries = entries.data(),
     };
     g_resources.uniformBindGroup = g_device.CreateBindGroup(&bindGroupDescriptor);
+#ifdef MELEE_MIYOO_FLIP
+    auto tableEntries=entries;
+    tableEntries[0].size=gx::FlipUniformWindow;
+    auto tableDesc=bindGroupDescriptor;
+    tableDesc.label="Flip uniform table";
+    tableDesc.entries=tableEntries.data();
+    g_resources.flipUniformBindGroup=g_device.CreateBindGroup(&tableDesc);
+#endif
   }
 
   gx::initialize();
@@ -521,6 +745,10 @@ void initialize() {
 void shutdown() {
   render_worker::synchronize();
   render_worker::shutdown();
+#ifdef MELEE_MIYOO_FLIP
+  shutdown_flip_gles();
+  gx::resident::shutdown();
+#endif
   g_processEventsQueued.store(false, std::memory_order_release);
   g_lastPresentNs.store(0, std::memory_order_release);
   g_presentPeriodNs.store(0, std::memory_order_release);
@@ -548,11 +776,25 @@ void shutdown() {
     g_runtimeEncoderTaskTypes.clear();
     g_freeEncoderTaskTypeSlots.clear();
   }
+#ifdef MELEE_MIYOO_FLIP
+  g_resources.flipUniformBindGroup = {};
+  g_resources.flipUniformGroups.clear();
+  g_resources.flipUniformBuffers.clear();
+  g_resources.flipUploadedUniforms.clear();
+  g_resources.flipUploadedVertices.clear();
+  g_resources.flipUploadedIndices.clear();
+  g_resources.vertexTexture = {};
+  g_resources.storageTexture = {};
+  g_resources.flipZeroRecords = {};
+#endif
   g_resources.vertexBuffer = {};
   g_resources.uniformBuffer = {};
   g_resources.indexBuffer = {};
   g_resources.storageBuffer = {};
   g_stagingBuffers.fill({});
+#ifdef MELEE_MIYOO_FLIP
+  for (auto& ptr : g_flipCpuStaging) ptr.reset();
+#endif
   for (auto& packet : g_framePackets) {
     packet = {};
   }
@@ -633,17 +875,49 @@ bool begin_frame() {
     if (size <= 0) {
       return;
     }
-    buf = ByteBuffer{static_cast<u8*>(stagingBuf.GetMappedRange(bufferOffset, size)), static_cast<size_t>(size)};
+#ifdef MELEE_MIYOO_FLIP
+    if (gx::flip_cpu_upload_enabled())
+      buf = ByteBuffer{g_flipCpuStaging[*stagingSlot].get() + bufferOffset, static_cast<size_t>(size)};
+    else
+#endif
+      buf = ByteBuffer{static_cast<u8*>(stagingBuf.GetMappedRange(bufferOffset, size)), static_cast<size_t>(size)};
     bufferOffset += size;
   };
+#ifdef MELEE_MIYOO_FLIP
+  if (flip_mapped_enabled() && g_flipMappedReady.load(std::memory_order_acquire)) {
+    auto& slot = g_flipMapped[*stagingSlot];
+    if (slot.vertexData) {
+      frame.verts = ByteBuffer{slot.vertexData, static_cast<size_t>(VertexBufferSize)};
+      bufferOffset += VertexBufferSize;
+    } else {
+      mapBuffer(frame.verts, VertexBufferSize);
+    }
+    frame.uniforms = ByteBuffer{slot.uniformData, g_flipMappedUniformBytes};
+    frame.indices = ByteBuffer{slot.indexData, static_cast<size_t>(IndexBufferSize)};
+    frame.flipMapped = true;
+    bufferOffset += UniformBufferSize + IndexBufferSize;
+  } else {
+    mapBuffer(frame.verts, VertexBufferSize);
+    mapBuffer(frame.uniforms, UniformBufferSize);
+    mapBuffer(frame.indices, IndexBufferSize);
+  }
+#else
   mapBuffer(frame.verts, VertexBufferSize);
   mapBuffer(frame.uniforms, UniformBufferSize);
   mapBuffer(frame.indices, IndexBufferSize);
+#endif
   mapBuffer(frame.storage, StorageBufferSize);
   if constexpr (UseTextureBuffer) {
     mapBuffer(frame.textureUpload, TextureUploadSize);
   }
 
+#ifdef MELEE_MIYOO_FLIP
+  if (detail::flip_async_frames()) {
+    g_producerFrameSlot = frameSlot;
+    g_cpuFrameStart = PresentClock::now();
+    return true;
+  }
+#endif
   begin_recording(frame, frameSlot);
   begin_pipeline_frame();
   render_worker::enqueue_begin_frame(frame.frameId, [frameSlot] {
@@ -655,6 +929,59 @@ bool begin_frame() {
   return true;
 }
 
+#ifdef MELEE_MIYOO_FLIP
+namespace detail {
+bool flip_async_frames() {
+  static const bool enabled = [] { const char* v = std::getenv("MELEE_FLIP_ASYNC_FIFO"); return v && !std::strcmp(v, "1"); }();
+  return enabled;
+}
+size_t producer_frame_slot() { return g_producerFrameSlot; }
+void begin_frame_worker(uint32_t frameSlot) {
+  CHECK(frameSlot < FrameSlotCount, "Invalid frame slot in stream");
+  auto& frame = g_framePackets[frameSlot];
+  begin_recording(frame, frameSlot);
+  begin_pipeline_frame();
+  render_worker::enqueue_begin_frame(frame.frameId, [frameSlot] {
+    constexpr wgpu::CommandEncoderDescriptor EncoderDescriptor{.label = "Redraw encoder"};
+    g_framePackets[frameSlot].encoder = g_device.CreateCommandEncoder(&EncoderDescriptor);
+    webgpu::gpu_prof::frame_begin(g_framePackets[frameSlot].encoder);
+  });
+}
+void end_frame_worker() {
+  const size_t slot = current_frame_slot();
+  EndFrameCallback callback;
+  {
+    std::lock_guard lock{g_deferredEndFrameMutex};
+    callback = std::move(g_deferredEndFrame[slot]);
+    g_deferredEndFrame[slot] = nullptr;
+  }
+  end_frame(std::move(callback));
+}
+} // namespace detail
+void defer_end_frame(EndFrameCallback callback) {
+  std::lock_guard lock{g_deferredEndFrameMutex};
+  g_deferredEndFrame[g_producerFrameSlot] = std::move(callback);
+}
+#endif
+
+#ifdef MELEE_MIYOO_FLIP
+// Render worker. Staged frames release immediately; mapped frames fence their
+// slot and release the oldest fenced slot, blocking only if two are pending.
+void flip_release_staging(size_t stagingSlot, bool wasMapped) {
+  if (!wasMapped) { map_staging_buffer(stagingSlot, true); return; }
+  dawn::native::opengl::RunDirectGL(g_device.Get(), flip_fence_slot, &g_flipMapped[stagingSlot]);
+  g_flipMappedPendingRelease.push_back(stagingSlot);
+  while (!g_flipMappedPendingRelease.empty()) {
+    const size_t oldest = g_flipMappedPendingRelease.front();
+    FlipFenceQuery query{&g_flipMapped[oldest], g_flipMappedPendingRelease.size() >= 2, false};
+    dawn::native::opengl::RunDirectGL(g_device.Get(), flip_check_fence, &query);
+    if (!query.done) break;
+    g_flipMappedPendingRelease.pop_front();
+    map_staging_buffer(oldest, true);
+  }
+}
+#endif
+
 void end_frame(EndFrameCallback callback) {
   ZoneScoped;
   if (g_cpuFrameStart.time_since_epoch().count() != 0) {
@@ -672,9 +999,63 @@ void end_frame(EndFrameCallback callback) {
 
   const size_t stagingSlot = frame.stagingBuffer;
   render_worker::enqueue_end_frame(frameId, [frameSlot, stagingSlot, callback = std::move(callback)]() mutable {
+#ifdef MELEE_MIYOO_FLIP
+    static bool flipMappedInit = false;
+#endif
     auto& packet = g_framePackets[frameSlot];
-    g_stagingBuffers[stagingSlot].Unmap();
+#ifdef MELEE_MIYOO_FLIP
+    const bool wasMapped = packet.flipMapped;
+    if (!flipMappedInit && flip_mapped_enabled()) {
+      flipMappedInit = true;
+      dawn::native::opengl::RunDirectGL(g_device.Get(), flip_create_mapped_slots, nullptr);
+    }
+    if (!gx::flip_cpu_upload_enabled())
+#endif
+      g_stagingBuffers[stagingSlot].Unmap();
     g_mappingStates[stagingSlot].store(BufferMapState::Unmapped, std::memory_order_release);
+#ifdef MELEE_MIYOO_FLIP
+    static const bool phaseProfile = std::getenv("MELEE_FLIP_PROFILE") != nullptr;
+    const auto phaseStart = std::chrono::steady_clock::now();
+    if (const auto* mapped = detail::flip_mapped_slot(packet)) {
+      FlipFlushRequest flush{mapped, packet.uniforms.size(), packet.indices.size(), mapped->vertexData ? packet.verts.size() : 0};
+      dawn::native::opengl::RunDirectGL(g_device.Get(), flip_flush_slot, &flush);
+      static const bool verifyGpu = [] { const char* v = std::getenv("MELEE_FLIP_MAPPED_VERIFY_GPU"); return v && v[0] == '1'; }();
+      if (verifyGpu) dawn::native::opengl::RunDirectGL(g_device.Get(), flip_verify_gpu_view, &flush);
+    }
+    upload_flip_geometry(packet);
+    const auto afterUpload = std::chrono::steady_clock::now();
+    prepare_flip_gles_frame(packet);
+    set_flip_frame_surface(std::move(packet.flipSurfaceTexture), std::move(packet.flipSurfaceView));
+    const auto afterPrepare = std::chrono::steady_clock::now();
+    if (phaseProfile) {
+      static double uploadMs = 0, prepareMs = 0, callbackMs = 0; static unsigned frames = 0;
+      uploadMs += std::chrono::duration<double, std::milli>(afterUpload - phaseStart).count();
+      prepareMs += std::chrono::duration<double, std::milli>(afterPrepare - afterUpload).count();
+      struct Report { double& cb; std::chrono::steady_clock::time_point start; ~Report() { cb += std::chrono::duration<double, std::milli>(std::chrono::steady_clock::now() - start).count();
+        if (++frames % 120 == 0) { std::fprintf(stderr, "[flip-render-phase] upload_ms=%.3f prepare_ms=%.3f callback_ms=%.3f\n", uploadMs / 120, prepareMs / 120, cb / 120); uploadMs = prepareMs = cb = 0; } } };
+      static thread_local Report* dummy = nullptr; (void)dummy;
+      Report report{callbackMs, afterPrepare};
+      auto encoder = std::move(packet.encoder);
+      const auto stats = packet.stats;
+      auto afterSubmitCallbacks = std::move(packet.afterSubmitCallbacks);
+      packet = {};
+      g_resources.stats.drawCallCount = stats.drawCallCount;
+      g_resources.stats.mergedDrawCallCount = stats.mergedDrawCallCount;
+      g_resources.stats.lastVertSize = stats.lastVertSize;
+      g_resources.stats.lastUniformSize = stats.lastUniformSize;
+      g_resources.stats.lastIndexSize = stats.lastIndexSize;
+      g_resources.stats.lastStorageSize = stats.lastStorageSize;
+      g_resources.stats.lastTextureUploadSize = stats.lastTextureUploadSize;
+      if (callback) {
+        callback(encoder, std::move(afterSubmitCallbacks));
+      }
+      g_frameSlots.release(frameSlot);
+      expire_cached_bind_groups();
+      flip_release_staging(stagingSlot, wasMapped);
+      process_events();
+      return;
+    }
+#endif
     auto encoder = std::move(packet.encoder);
     const auto stats = packet.stats;
     auto afterSubmitCallbacks = std::move(packet.afterSubmitCallbacks);
@@ -691,7 +1072,7 @@ void end_frame(EndFrameCallback callback) {
     }
     g_frameSlots.release(frameSlot);
     expire_cached_bind_groups();
-    map_staging_buffer(stagingSlot, true);
+    flip_release_staging(stagingSlot, wasMapped);
     process_events();
   });
 }
diff --git a/lib/gfx/frame.hpp b/lib/gfx/frame.hpp
index 0b86ffe..19314c6 100644
--- a/lib/gfx/frame.hpp
+++ b/lib/gfx/frame.hpp
@@ -8,12 +8,40 @@
 namespace aurora::gfx::detail {
 
 inline constexpr size_t FrameSlotCount = 2;
+#ifdef MELEE_MIYOO_FLIP
+inline constexpr size_t StagingBufferCount = FrameSlotCount + 1;
+#else
 inline constexpr size_t StagingBufferCount = FrameSlotCount + 3;
+#endif
 inline constexpr uint64_t StagingBufferSize = UniformBufferSize + VertexBufferSize + IndexBufferSize +
                                               StorageBufferSize + (UseTextureBuffer ? TextureUploadSize : 0);
 
 const wgpu::Buffer& staging_buffer(size_t slot);
 
+#ifdef MELEE_MIYOO_FLIP
+// Persistently mapped GL storage for one staging slot. The FIFO worker records
+// uniform records and indices into it directly; the direct GLES path binds the
+// GL names, so nothing is compared or copied per frame. A slot is recycled only
+// after the fence created behind its frame's submission has signalled.
+struct FlipMappedSlot {
+  unsigned uniforms = 0, indices = 0, vertices = 0; // GL buffer names
+  uint8_t* uniformData = nullptr;
+  uint8_t* indexData = nullptr;
+  uint8_t* vertexData = nullptr;         // null when the vertex stream stays staged
+  void* fence = nullptr;                 // GLsync of the last frame that used the slot
+  bool inUse = false;
+};
+// Render-worker/frame-packet accessor: null when the packet did not use mapped storage.
+const FlipMappedSlot* flip_mapped_slot(const FramePacket& frame);
+
+// Asynchronous frames: the producer only reserves the frame; recording starts
+// and ends on the FIFO worker when it reaches the frame markers.
+bool flip_async_frames();
+size_t producer_frame_slot();
+void begin_frame_worker(uint32_t frameSlot);
+void end_frame_worker();
+#endif
+
 struct RegisteredDrawType {
   DrawCallback draw = nullptr;
   void* userdata = nullptr;
@@ -35,6 +63,10 @@ void initialize();
 void shutdown();
 bool begin_frame();
 void end_frame(EndFrameCallback callback);
+#ifdef MELEE_MIYOO_FLIP
+// Asynchronous frames: store the presentation closure for the worker's end_frame.
+void defer_end_frame(EndFrameCallback callback);
+#endif
 uint32_t current_frame() noexcept;
 void after_submit() noexcept;
 void gpu_synchronize();
diff --git a/lib/gfx/frame_packet.hpp b/lib/gfx/frame_packet.hpp
index d50ce6a..9f31962 100644
--- a/lib/gfx/frame_packet.hpp
+++ b/lib/gfx/frame_packet.hpp
@@ -12,6 +12,9 @@
 #include <string>
 #include <vector>
 
+namespace aurora::gx { struct DrawData; }
+namespace aurora::gfx::clear { struct DrawData; }
+
 namespace aurora::gfx::detail {
 
 struct StagingHighWater {
@@ -104,6 +107,22 @@ struct RenderPass {
   bool discardable = false;
   bool captureDepthSnapshot = false;
   bool sealed = false;
+#ifdef MELEE_MIYOO_FLIP
+  std::string flipDirectLabel;
+  bool flipDirectResourcesOnly = false;
+  // Additional EFB copies resolved from this pass (shadow-pass fusion records a
+  // second shadow map into the same pass, shifted right of the first copy).
+  struct FlipResolve {
+    TextureHandle target;
+    GXTexFmt format = GX_TF_RGBA8;
+    ClipRect rect{};
+    Range uniformRange{};
+  };
+  std::vector<FlipResolve> flipExtraResolves;
+  uint8_t flipWriteMask = 0; // channels written by recorded GX draws: 1 color, 2 alpha, 4 depth
+  uint32_t flipSegmentStart = UINT32_MAX; // first command of a fused (shifted) shadow segment
+  Range flipDualResolveUniform{}; // both UV transforms for a two-target conversion pass
+#endif
   std::vector<tex_palette_conv::ConvRequest> paletteConvs;
 
   RenderTargetLayout target_layout() const noexcept;
@@ -192,6 +211,28 @@ struct FramePacket {
 #endif
   ByteBuffer verts;
   ByteBuffer uniforms;
+#ifdef MELEE_MIYOO_FLIP
+  // GX records are uploaded to separate physical UBO windows. Only these
+  // ordinary ranges are needed by resolve/conversion/custom draw buffers.
+  std::vector<Range> flipOrdinaryUniforms;
+  // Uniforms/indices were recorded straight into persistently mapped GL storage.
+  bool flipMapped = false;
+  // Scene-on-surface: the presented texture acquired when this frame's first
+  // EFB pass was encoded; EFB passes render into it and the present copy is
+  // skipped (MELEE_FLIP_SCENE_ON_SURFACE).
+  wgpu::Texture flipSurfaceTexture;
+  wgpu::TextureView flipSurfaceView;
+  bool flipSurfaceTried = false;
+  // Per-frame uniform-record stream for resident geometry: one u32 per arena
+  // vertex, located in `verts` at flipRecordOffset; arena i starts at vertex
+  // flipRecordArenaBase[i] and covers flipRecordArenaCount[i] vertices.
+  uint32_t flipRecordOffset = UINT32_MAX;
+  std::array<uint32_t, 8> flipRecordArenaBase{};
+  std::array<uint32_t, 8> flipRecordArenaCount{};
+  // CPU copy of the record region: `verts` may be write-combined mapped memory,
+  // which must never be read back on the FIFO worker.
+  std::vector<uint32_t> flipRecordShadow;
+#endif
   ByteBuffer indices;
   ByteBuffer storage;
   ByteBuffer textureUpload;
@@ -204,4 +245,9 @@ struct FramePacket {
   AuroraStats stats{};
 };
 
+#ifdef MELEE_MIYOO_FLIP
+bool decode_flip_gx_draw(const DrawCommand& command, gx::DrawData& data) noexcept;
+bool decode_flip_clear_draw(const DrawCommand& command, clear::DrawData& data) noexcept;
+#endif
+
 } // namespace aurora::gfx::detail
diff --git a/lib/gfx/pipeline_cache.cpp b/lib/gfx/pipeline_cache.cpp
index bff9b47..dcd4388 100644
--- a/lib/gfx/pipeline_cache.cpp
+++ b/lib/gfx/pipeline_cache.cpp
@@ -1,4 +1,5 @@
 #include "pipeline_cache.hpp"
+#include <chrono>
 
 #include "clear.hpp"
 #include "resources.hpp"
@@ -429,6 +430,8 @@ static void notify_pipeline_ready(bool queued) {
 
 static PipelineRef g_lastPipelineRef = std::numeric_limits<PipelineRef>::max();
 
+extern std::atomic<uint64_t> g_flipPipelineWaitNs;
+extern std::atomic<uint64_t> g_flipPipelineWaits;
 template <typename PipelineConfig>
 static PipelineRef find_pipeline_impl(ShaderType type, const PipelineConfig& config, NewPipelineCallback&& cb,
                                       PipelinePriority priority = PipelinePriority::Normal,
@@ -540,7 +543,13 @@ static PipelineRef find_pipeline_impl(ShaderType type, const PipelineConfig& con
 
   if (blocking && !pipelineReady) {
     std::unique_lock lock{g_pipelineMutex};
-    g_pipelineReadyCv.wait(lock, [=] { return g_pipelines.contains(hash) || g_pipelineThreadEnd; });
+    {
+      // Flip accounting: time the FIFO worker spends blocked on shader/pipeline builds.
+      const auto waitStart = std::chrono::steady_clock::now();
+      g_pipelineReadyCv.wait(lock, [=] { return g_pipelines.contains(hash) || g_pipelineThreadEnd; });
+      g_flipPipelineWaitNs.fetch_add(std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now() - waitStart).count(), std::memory_order_relaxed);
+      g_flipPipelineWaits.fetch_add(1, std::memory_order_relaxed);
+    }
     auto pipelineIt = g_pipelines.find(hash);
     if (pipelineIt != g_pipelines.end() && persist && firstFrameUsed < pipelineIt->second.firstFrameUsed) {
       pipelineIt->second.firstFrameUsed = firstFrameUsed;
@@ -1106,9 +1115,21 @@ PipelineRef find_pipeline(ShaderType type, const clear::PipelineConfig& config,
 
 template <>
 PipelineRef find_pipeline(ShaderType type, const gx::PipelineConfig& config, NewPipelineCallback&& cb) {
+#ifdef MELEE_MIYOO_FLIP
+  // A first-use compile may stall, but must not silently omit the primitive.
+  return find_pipeline_impl(type, config, std::move(cb), PipelinePriority::Blocking);
+#else
   return find_pipeline_impl(type, config, std::move(cb));
+#endif
 }
 
+#ifdef MELEE_MIYOO_FLIP
+template <>
+PipelineRef find_pipeline_async(ShaderType type, const gx::PipelineConfig& config, NewPipelineCallback&& cb) {
+  return find_pipeline_impl(type, config, std::move(cb), PipelinePriority::Normal);
+}
+#endif
+
 #ifdef AURORA_ENABLE_RMLUI
 template <>
 PipelineRef find_pipeline(ShaderType type, const rmlui::PipelineConfig& config, NewPipelineCallback&& cb) {
@@ -1129,7 +1150,15 @@ void initialize_pipeline_cache() {
     g_pipelineThread = std::thread(pipeline_worker);
   }
 
+#ifdef MELEE_MIYOO_FLIP
+  // The shared 1 GiB device cannot afford to compile every cached stage and
+  // diagnostic backend in the background. Dawn's disk shader cache remains
+  // available to first-use compiles; only speculative pipeline creation stops.
+  const char* prewarm=std::getenv("MELEE_FLIP_PREWARM_PIPELINES");
+  const size_t loadedCount = prewarm && !std::strcmp(prewarm,"1") ? load_pipeline_cache() : 0;
+#else
   const size_t loadedCount = load_pipeline_cache();
+#endif
   if (!g_pipelineCacheBroken && loadedCount > 0) {
     g_gpuCachePrunePending = true;
   }
@@ -1162,7 +1191,19 @@ void shutdown_pipeline_cache() {
   createdPipelines = 0;
 }
 
+std::atomic<uint64_t> g_flipPipelineWaitNs{0};
+std::atomic<uint64_t> g_flipPipelineWaits{0};
+uint64_t pipeline_wait_ns() noexcept { return g_flipPipelineWaitNs.load(std::memory_order_relaxed); }
+uint64_t pipeline_wait_count() noexcept { return g_flipPipelineWaits.load(std::memory_order_relaxed); }
+
 void begin_pipeline_frame() {
+#ifdef MELEE_MIYOO_FLIP
+  if(std::getenv("MELEE_FLIP_PROFILE") && current_frame()%300==0) {
+    std::lock_guard guard{g_pipelineMutex};
+    std::fprintf(stderr,"[flip-pipelines] frame=%u live=%zu queued=%zu background=%zu\n",current_frame(),
+      g_pipelines.size(),g_pipelineQueue.size(),g_backgroundPipelineQueue.size());
+  }
+#endif
   if (!g_hasPipelineThread) {
     g_pipelinesPerFrame = 0;
   }
diff --git a/lib/gfx/pipeline_cache.hpp b/lib/gfx/pipeline_cache.hpp
index 7eb4723..52f638e 100644
--- a/lib/gfx/pipeline_cache.hpp
+++ b/lib/gfx/pipeline_cache.hpp
@@ -29,11 +29,18 @@ using NewPipelineCallback = std::function<wgpu::RenderPipeline()>;
 void initialize_pipeline_cache();
 void shutdown_pipeline_cache();
 void begin_pipeline_frame();
+uint64_t pipeline_wait_ns() noexcept;
+uint64_t pipeline_wait_count() noexcept;
 void end_pipeline_frame();
 
 template <typename Config>
 PipelineRef find_pipeline(ShaderType type, const Config& config, NewPipelineCallback&& cb);
 
 bool get_pipeline(PipelineRef ref, wgpu::RenderPipeline& pipeline);
+#ifdef MELEE_MIYOO_FLIP
+// Queue a pipeline without waiting for its compile (draws fall back until it exists).
+template <typename Config>
+PipelineRef find_pipeline_async(ShaderType type, const Config& config, NewPipelineCallback&& cb);
+#endif
 
 } // namespace aurora::gfx
diff --git a/lib/gfx/recording.cpp b/lib/gfx/recording.cpp
index d04db0f..8a775a8 100644
--- a/lib/gfx/recording.cpp
+++ b/lib/gfx/recording.cpp
@@ -13,12 +13,21 @@
 #include "../gx/fifo.hpp"
 #include "../gx/gx.hpp"
 #include "../gx/pipeline.hpp"
+#ifdef MELEE_MIYOO_FLIP
+#include "../gx/flip_uber.hpp"
+#include "../gx/flip_resident.hpp"
+#include "flip_profile.hpp"
+#include "flip_sprites.hpp"
+#endif
 #ifdef AURORA_ENABLE_RMLUI
 #include "../rmlui/pipeline.hpp"
 #endif
 #include "../window.hpp"
 
 #include <array>
+#include <cmath>
+#include <cstdio>
+#include <cstdlib>
 #include <cstring>
 #include <new>
 #include <optional>
@@ -35,6 +44,9 @@
 namespace aurora::gfx {
 using namespace detail;
 
+template <>
+PipelineRef pipeline_ref(const clear::PipelineConfig& config);
+
 namespace {
 constexpr Module Log{"aurora::gfx"};
 
@@ -69,6 +81,76 @@ struct FrameRecorder {
 
 FrameRecorder g_recorder;
 
+#ifdef MELEE_MIYOO_FLIP
+// Shadow-pass fusion (MELEE_FLIP_FUSE_PASSES, default on). Melee renders its
+// two 256x256 shadow maps as separate EFB passes, each ending in an EFB copy
+// with a full clear; on the Mali blob every render pass costs ~0.5 ms of driver
+// time. The second shadow is therefore recorded into the same pass with its
+// viewport and scissor shifted right of the first copy rectangle, and the pass
+// carries two resolves. The fusion is speculative: anything that could change
+// the image (a viewport that does not fit, a clear or custom draw, a draw that
+// samples the first copy, a palette conversion, any other pass break) splits
+// the shifted segment back out into its own pass with its coordinates restored.
+struct FlipPassFusion {
+  bool active = false;
+  uint32_t pass = UINT32_MAX;
+  size_t segmentStart = 0;
+  int32_t offsetX = 0;
+  TextureHandle firstTarget;
+  Vec4<float> clearColorValue{};
+  float clearDepthValue = 1.f;
+  // Clear flags of the first copy (they shape the pass a split has to recreate)
+  // and the channels (1 color, 2 alpha, 4 depth) written by each segment.
+  bool clearColor1 = false, clearAlpha1 = false, clearDepth1 = false;
+  uint8_t firstWrites = 0;
+  uint8_t segmentWrites = 0;
+  struct Stats {
+    unsigned frames = 0, fused = 0, refused = 0, split = 0;
+    unsigned refuseClear = 0, refuseState = 0, refuseRegion = 0, refuseRoom = 0, refuseDraws = 0;
+    unsigned splitViewport = 0, splitScissor = 0, splitClear = 0, splitCustom = 0, splitTexture = 0, splitBreak = 0,
+             splitSecond = 0, splitPalette = 0;
+  } stats;
+};
+FlipPassFusion g_fusion;
+
+bool flip_fuse_enabled() {
+  static const bool enabled = [] {
+    const char* v = std::getenv("MELEE_FLIP_FUSE_PASSES");
+    return v == nullptr || std::strcmp(v, "0") != 0;
+  }();
+  return enabled;
+}
+bool flip_fuse_trace() {
+  static const bool trace = [] {
+    const char* v = std::getenv("MELEE_FLIP_FUSE_TRACE");
+    return v != nullptr && std::strcmp(v, "0") != 0;
+  }();
+  return trace;
+}
+// Channels the current GX state lets a draw write (matches the pipeline's
+// write mask and depthWriteEnabled = depthCompare && depthUpdate).
+uint8_t flip_current_write_mask() {
+  return (gx::g_gxState.colorUpdate ? 1u : 0u) | (gx::g_gxState.alphaUpdate ? 2u : 0u) |
+         (gx::g_gxState.depthCompare && gx::g_gxState.depthUpdate ? 4u : 0u);
+}
+void flip_fusion_split(const char* reason, unsigned& counter);
+void flip_fusion_settle(const char* reason, unsigned& counter);
+// Half-res sprite segment state (see flip_sprites.hpp).
+struct FlipSpriteSegment {
+  bool active = false;
+  uint32_t framePoints = 0, prevFramePoints = 0;
+  Viewport savedViewport{};
+  ClipRect savedScissor{};
+  struct Stats { unsigned frames = 0, segments = 0, points = 0, refused = 0; } stats;
+};
+FlipSpriteSegment g_sprites;
+void flip_sprite_end_segment();
+inline void flip_sprite_settle_impl() { if (g_sprites.active) flip_sprite_end_segment(); }
+#define FLIP_FUSION_SETTLE(reason, counter) (flip_sprite_settle_impl(), flip_fusion_settle(reason, g_fusion.stats.counter))
+#else
+#define FLIP_FUSION_SETTLE(reason, counter) ((void)0)
+#endif
+
 std::string pass_label(std::string_view kind) {
 #ifdef AURORA_GFX_DEBUG_GROUPS
   if (!g_recorder.debugGroupStack.empty()) {
@@ -288,18 +370,79 @@ void push_command(CommandType type, const Command::Data& data) {
       Log.warn("Dropping command {}", magic_enum::enum_name(type));
       return;
     }
+#ifdef MELEE_MIYOO_FLIP
+  Command::Data shifted{.setViewport = {}};
+  const Command::Data* payload = &data;
+  if (g_fusion.active) {
+    // Commands of the shifted segment of a fused pass: move viewport and scissor
+    // right of the first copy, or give up the fusion if they cannot fit.
+    const auto efbWidth =
+        static_cast<int32_t>(current_render_passes()[g_fusion.pass].colorAttachments[SceneColorAttachmentIndex].size.width);
+    if (type == CommandType::SetViewport) {
+      const auto& vp = data.setViewport;
+      if (vp.left < 0.f || static_cast<int32_t>(std::ceil(vp.left + vp.width)) + g_fusion.offsetX > efbWidth) {
+        flip_fusion_split("viewport", g_fusion.stats.splitViewport);
+      } else {
+        shifted = data;
+        shifted.setViewport.left += static_cast<float>(g_fusion.offsetX);
+        payload = &shifted;
+      }
+    } else if (type == CommandType::SetScissor) {
+      if (data.setScissor.x < 0) {
+        flip_fusion_split("scissor", g_fusion.stats.splitScissor);
+      } else {
+        shifted = data;
+        shifted.setScissor.x += g_fusion.offsetX;
+        payload = &shifted;
+      }
+    } else if (type == CommandType::CustomDraw) {
+      flip_fusion_split("custom", g_fusion.stats.splitCustom);
+    } else if (type == CommandType::Draw) {
+      clear::DrawData clearDraw;
+      if (!decode_flip_clear_draw(data.draw, clearDraw)) {
+        g_fusion.segmentWrites |= flip_current_write_mask();
+      }
+    }
+  }
+  if (g_sprites.active) {
+    // Half-res sprite pass: viewport and scissor at half scale.
+    if (type == CommandType::SetViewport) {
+      shifted = *payload;
+      auto& vp = shifted.setViewport;
+      vp.left *= 0.5f; vp.top *= 0.5f; vp.width *= 0.5f; vp.height *= 0.5f;
+      payload = &shifted;
+    } else if (type == CommandType::SetScissor) {
+      shifted = *payload;
+      auto& sc = shifted.setScissor;
+      const int32_t x0 = sc.x >> 1, y0 = sc.y >> 1;
+      const int32_t x1 = (sc.x + sc.width + 1) >> 1, y1 = (sc.y + sc.height + 1) >> 1;
+      sc = {x0, y0, std::max(x1 - x0, 0), std::max(y1 - y0, 0)};
+      payload = &shifted;
+    }
+  }
+#else
+  const Command::Data* payload = &data;
+#endif
   auto& renderPass = current_render_passes()[g_recorder.currentRenderPass];
   AURORA_ASSERT(!renderPass.sealed, "Attempted to append command {} to sealed render pass {}",
                 magic_enum::enum_name(type), g_recorder.currentRenderPass);
   if (type == CommandType::Draw || type == CommandType::CustomDraw) {
     renderPass.hasDraws = true;
+#ifdef MELEE_MIYOO_FLIP
+    // GX draws only: a clear draw's channels come from its pipeline config
+    // (flip_leading_clear), not from the GX state current when it was pushed.
+    clear::DrawData clearDraw;
+    if (type == CommandType::Draw && !decode_flip_clear_draw(data.draw, clearDraw)) {
+      renderPass.flipWriteMask |= flip_current_write_mask();
+    }
+#endif
   }
   renderPass.commands.push_back({
       .type = type,
 #ifdef AURORA_GFX_DEBUG_GROUPS
       .debugGroupStack = g_recorder.debugGroupStack,
 #endif
-      .data = data,
+      .data = *payload,
   });
 }
 
@@ -423,6 +566,7 @@ void suspend_efb() {
                 "suspend_efb called outside of an active recording frame");
   AURORA_ASSERT(!g_recorder.inOffscreen, "suspend_efb called while offscreen rendering is active");
   AURORA_ASSERT(!g_recorder.suspendedEfbPass, "suspend_efb called with an EFB pass already suspended");
+  FLIP_FUSION_SETTLE("break", splitBreak);
 
   auto& currentPass = current_render_passes()[g_recorder.currentRenderPass];
   if (!currentPass.has_consumer()) {
@@ -520,8 +664,423 @@ void enqueue_op(FramePacket& frame, uint32_t opIndex) {
   });
 }
 
+// The EFB pass that follows an EFB copy: same targets, contents loaded unless
+// cleared.
+RenderPass make_efb_continuation(const RenderPass& prevPass, bool clearDepth, float clearDepthValue,
+                                 bool fullColorClear, Vec4<float> clearColorValue) {
+  RenderPass newPass{
+      .label = pass_label(g_recorder.inOffscreen ? "Offscreen" : "EFB"),
+      .colorAttachments = prevPass.colorAttachments,
+      .colorAttachmentCount = prevPass.colorAttachmentCount,
+      .depthStencilView = prevPass.depthStencilView,
+      .depthStencilFormat = prevPass.depthStencilFormat,
+      .copySourceTexture = prevPass.copySourceTexture,
+      .copySourceView = prevPass.copySourceView,
+      .copySourceDepthView = prevPass.copySourceDepthView,
+      .msaaSamples = prevPass.msaaSamples,
+      .clearDepthValue = clearDepthValue,
+      .clearDepth = clearDepth,
+      .hasDepth = prevPass.hasDepth,
+      .hasStencil = prevPass.hasStencil,
+  };
+  for (uint32_t i = 0; i < newPass.colorAttachmentCount; ++i) {
+    auto& color = newPass.colorAttachments[i];
+    color.loadOp = wgpu::LoadOp::Undefined;
+    color.clear = false;
+  }
+  if (fullColorClear) {
+    auto& sceneColor = newPass.colorAttachments[SceneColorAttachmentIndex];
+    sceneColor.clear = true;
+    sceneColor.clearValue = clearColorValue;
+  }
+  newPass.commands.reserve(2048);
+  return newPass;
+}
+
+#ifdef MELEE_MIYOO_FLIP
+bool flip_vec4_equal(const Vec4<float>& a, const Vec4<float>& b) {
+  return a.x() == b.x() && a.y() == b.y() && a.z() == b.z() && a.w() == b.w();
+}
+
+// Scans commands [begin, end): 0 if every draw stays within [minLeft, maxRight)
+// horizontally, 1 if a clear or custom draw is present, 2 if a draw can reach
+// outside the region. Passes always start with viewport/scissor commands, so a
+// draw before either is treated as unbounded.
+int flip_segment_check(const RenderPass& pass, size_t begin, size_t end, int32_t minLeft, int32_t maxRight) {
+  const auto& size = pass.colorAttachments[SceneColorAttachmentIndex].size;
+  Viewport vp{0.f, 0.f, static_cast<float>(size.width), static_cast<float>(size.height), 0.f, 1.f};
+  ClipRect sc{0, 0, static_cast<int32_t>(size.width), static_cast<int32_t>(size.height)};
+  for (size_t i = begin; i < end; ++i) {
+    const auto& cmd = pass.commands[i];
+    switch (cmd.type) {
+    case CommandType::SetViewport:
+      vp = cmd.data.setViewport;
+      break;
+    case CommandType::SetScissor:
+      sc = cmd.data.setScissor;
+      break;
+    case CommandType::Draw: {
+      clear::DrawData clearDraw;
+      if (decode_flip_clear_draw(cmd.data.draw, clearDraw)) {
+        return 1;
+      }
+      const auto left = std::max(static_cast<int32_t>(std::floor(vp.left)), sc.x);
+      const auto right = std::min(static_cast<int32_t>(std::ceil(vp.left + vp.width)), sc.x + sc.width);
+      if (left < right && (left < minLeft || right > maxRight)) {
+        return 2;
+      }
+    } break;
+    case CommandType::CustomDraw:
+      return 1;
+    default:
+      break;
+    }
+  }
+  return 0;
+}
+
+std::array<float, 4> flip_uv_transform(const RenderPass& pass, const ClipRect& rect) {
+  const auto srcW = static_cast<float>(pass.colorAttachments[SceneColorAttachmentIndex].size.width);
+  const auto srcH = static_cast<float>(pass.colorAttachments[SceneColorAttachmentIndex].size.height);
+  return {
+      static_cast<float>(rect.x) / srcW,
+      static_cast<float>(rect.y) / srcH,
+      static_cast<float>(rect.width) / srcW,
+      static_cast<float>(rect.height) / srcH,
+  };
+}
+Range flip_resolve_uniform(const RenderPass& pass, const ClipRect& rect) {
+  return push_uniform(flip_uv_transform(pass, rect));
+}
+
+// A pass that follows a color- or alpha-only EFB copy clear opens with a
+// full-EFB clear draw (resolve_pass_into). It clears the whole attachment like
+// a load-op clear would; report which channels it clears and with what color.
+struct FlipLeadingClear {
+  bool present = false;
+  bool color = false, alpha = false, depth = false;
+  Vec4<float> value{};
+};
+FlipLeadingClear flip_leading_clear(const RenderPass& pass) {
+  FlipLeadingClear out;
+  if (pass.commands.empty() || pass.commands[0].type != CommandType::Draw) {
+    return out;
+  }
+  clear::DrawData draw;
+  clear::PipelineConfig config;
+  if (!decode_flip_clear_draw(pass.commands[0].data.draw, draw) || !clear::find_flip_pipeline_config(draw.pipeline, config)) {
+    return out;
+  }
+  out.present = true;
+  out.color = config.clearColor;
+  out.alpha = config.clearAlpha;
+  out.depth = config.clearDepth;
+  out.value = {static_cast<float>(draw.color.r), static_cast<float>(draw.color.g), static_cast<float>(draw.color.b),
+               static_cast<float>(draw.color.a)};
+  return out;
+}
+
+// Called after the first resolve of `pass` has been recorded. Starts recording
+// the following EFB pass into the same RenderPass, shifted right of `rect`.
+bool flip_fusion_begin(RenderPass& pass, const ClipRect& rect, bool clearColor, bool clearAlpha, bool clearDepth,
+                       Vec4<float> clearColorValue, float clearDepthValue) {
+  auto& stats = g_fusion.stats;
+  if (!flip_fuse_enabled() || g_recorder.inOffscreen || g_recorder.suppressRenderWorker) {
+    return false;
+  }
+  const FlipLeadingClear leading = flip_leading_clear(pass);
+  const uint8_t leadingMask = (leading.color ? 1u : 0u) | (leading.alpha ? 2u : 0u) | (leading.depth ? 4u : 0u);
+  const uint8_t firstWrites = pass.flipWriteMask | leadingMask;
+  // The second pass would start from the copy's clear where it clears, and
+  // from the first pass's untouched pixels elsewhere. Region 2 of the fused
+  // pass holds the pass-start state instead, so every channel the copy clears
+  // must have been cleared identically at pass start, and every channel it
+  // does not clear must be unwritten by the first segment. (Melee's shadow
+  // copies clear color only: alpha and Z updates are off in that pass.)
+  const uint8_t clearMask = (clearColor ? 1u : 0u) | (clearAlpha ? 2u : 0u) | (clearDepth ? 4u : 0u);
+  if ((firstWrites & ~clearMask) != 0) {
+    ++stats.refused, ++stats.refuseClear;
+    return false;
+  }
+  const auto& color = pass.colorAttachments[SceneColorAttachmentIndex];
+  if (pass.colorAttachmentCount != 1 || pass.msaaSamples != 1 || !pass.flipExtraResolves.empty()) {
+    ++stats.refused, ++stats.refuseState;
+    return false;
+  }
+  // Each channel the copy clears must already hold the same clear value from
+  // the pass start: a load-op clear, or the leading full-EFB clear draw.
+  const bool loadCleared = color.clear && color.loadOp != wgpu::LoadOp::Load;
+  const auto& lv = leading.value;
+  const auto& cv = color.clearValue;
+  const bool rgbCleared = (loadCleared && cv.x() == clearColorValue.x() && cv.y() == clearColorValue.y() &&
+                           cv.z() == clearColorValue.z()) ||
+                          (leading.color && lv.x() == clearColorValue.x() && lv.y() == clearColorValue.y() &&
+                           lv.z() == clearColorValue.z());
+  const bool alphaCleared = (loadCleared && cv.w() == clearColorValue.w()) || (leading.alpha && lv.w() == clearColorValue.w());
+  if ((clearColor && !rgbCleared) || (clearAlpha && !alphaCleared) ||
+      (clearDepth && !(pass.clearDepth && pass.clearDepthValue == clearDepthValue))) {
+    ++stats.refused, ++stats.refuseState;
+    return false;
+  }
+  const auto efbWidth = static_cast<int32_t>(color.size.width);
+  const auto efbHeight = static_cast<int32_t>(color.size.height);
+  if (rect.x < 0 || rect.y < 0 || rect.width <= 0 || rect.height <= 0 || rect.x + rect.width > efbWidth ||
+      rect.y + rect.height > efbHeight) {
+    ++stats.refused, ++stats.refuseRegion;
+    return false;
+  }
+  const int32_t offsetX = rect.x + rect.width;
+  if (offsetX + rect.width > efbWidth) {
+    ++stats.refused, ++stats.refuseRoom;
+    return false;
+  }
+  // Only a first segment that wrote nothing but color can complete a fusion:
+  // the second copy has to clear every channel either segment wrote, and
+  // shadow copies clear color only. Fountain's reflection pass writes depth;
+  // attempting it would defer that large pass and split every frame.
+  if ((firstWrites & ~1u) != 0) {
+    ++stats.refused, ++stats.refuseClear;
+    return false;
+  }
+  if (const int check = flip_segment_check(pass, leading.present ? 1 : 0, pass.commands.size(), 0, offsetX); check != 0) {
+    ++stats.refused;
+    if (check == 1) ++stats.refuseDraws; else ++stats.refuseRegion;
+    return false;
+  }
+  g_fusion.active = true;
+  g_fusion.pass = g_recorder.currentRenderPass;
+  g_fusion.segmentStart = pass.commands.size();
+  g_fusion.offsetX = offsetX;
+  g_fusion.firstTarget = pass.resolveTarget;
+  g_fusion.clearColorValue = clearColorValue;
+  g_fusion.clearDepthValue = clearDepthValue;
+  g_fusion.clearColor1 = clearColor;
+  g_fusion.clearAlpha1 = clearAlpha;
+  g_fusion.clearDepth1 = clearDepth;
+  g_fusion.firstWrites = firstWrites;
+  g_fusion.segmentWrites = 0;
+  // push_command shifts these while the fusion is active.
+  push_command(CommandType::SetViewport, Command::Data{.setViewport = g_recorder.cachedViewport});
+  push_command(CommandType::SetScissor, Command::Data{.setScissor = g_recorder.cachedScissor});
+  return true;
+}
+
+// Second EFB copy while a fusion is active: attach it to the fused pass.
+bool flip_fusion_complete(const TextureHandle& texture, const ClipRect& rect, bool clearColor, bool clearAlpha,
+                          bool clearDepth, GXTexFmt format) {
+  auto& pass = current_render_passes()[g_fusion.pass];
+  // After the second copy the fused pass leaves the second shadow in a
+  // different place than the original would. Aurora clears the whole EFB for
+  // the channels a copy clears, so those match; anything either segment wrote
+  // to an uncleared channel would remain visible in the wrong place.
+  const uint8_t clearMask = (clearColor ? 1u : 0u) | (clearAlpha ? 2u : 0u) | (clearDepth ? 4u : 0u);
+  const bool fullClear = ((g_fusion.firstWrites | g_fusion.segmentWrites) & ~clearMask) == 0;
+  const auto& color = pass.colorAttachments[SceneColorAttachmentIndex];
+  const auto efbWidth = static_cast<int32_t>(color.size.width);
+  const auto efbHeight = static_cast<int32_t>(color.size.height);
+  ClipRect shifted = rect;
+  shifted.x += g_fusion.offsetX;
+  if (!fullClear || rect.x < 0 || rect.y < 0 || rect.width <= 0 || rect.height <= 0 || shifted.x + shifted.width > efbWidth ||
+      rect.y + rect.height > efbHeight) {
+    return false;
+  }
+  if (flip_segment_check(pass, g_fusion.segmentStart, pass.commands.size(), g_fusion.offsetX, efbWidth) != 0) {
+    return false;
+  }
+  pass.flipExtraResolves.push_back({
+      .target = texture,
+      .format = format,
+      .rect = shifted,
+      .uniformRange = flip_resolve_uniform(pass, shifted),
+  });
+  pass.flipSegmentStart = static_cast<uint32_t>(g_fusion.segmentStart);
+  // Both transforms together for the two-target conversion pass.
+  const auto uv1 = flip_uv_transform(pass, pass.resolveRect);
+  const auto uv2 = flip_uv_transform(pass, shifted);
+  const std::array<float, 8> both{uv1[0], uv1[1], uv1[2], uv1[3], uv2[0], uv2[1], uv2[2], uv2[3]};
+  pass.flipDualResolveUniform = push_uniform(both);
+  g_fusion.active = false;
+  g_fusion.firstTarget = {};
+  ++g_fusion.stats.fused;
+  return true;
+}
+
+// Gives up an active fusion: the shifted segment becomes its own pass again.
+void flip_fusion_split(const char* reason, unsigned& counter) {
+  if (!g_fusion.active) {
+    return;
+  }
+  g_fusion.active = false;
+  g_fusion.firstTarget = {};
+  ++counter;
+  ++g_fusion.stats.split;
+  auto& passes = current_render_passes();
+  auto& fused = passes[g_fusion.pass];
+  const bool fullColorClear = g_fusion.clearColor1 && g_fusion.clearAlpha1;
+  RenderPass newPass =
+      make_efb_continuation(fused, g_fusion.clearDepth1, g_fusion.clearDepthValue, fullColorClear, g_fusion.clearColorValue);
+  newPass.flipWriteMask = g_fusion.segmentWrites;
+  if (!fullColorClear && (g_fusion.clearColor1 || g_fusion.clearAlpha1)) {
+    // The copy would have opened the pass with a color- or alpha-only clear draw.
+    const auto& value = g_fusion.clearColorValue;
+    newPass.commands.push_back(Command{
+        .type = CommandType::Draw,
+        .data = {.draw = make_draw_command<clear::render>(clear::DrawData{
+                     .pipeline = pipeline_ref(clear::make_pipeline_config(newPass.target_layout(), g_fusion.clearColor1,
+                                                                          g_fusion.clearAlpha1, false)),
+                     .color = wgpu::Color{.r = value.x(), .g = value.y(), .b = value.z(), .a = value.w()},
+                 })},
+    });
+    newPass.hasDraws = true;
+  }
+  const auto segment = fused.commands.begin() + static_cast<std::ptrdiff_t>(g_fusion.segmentStart);
+  newPass.commands.insert(newPass.commands.end(), std::make_move_iterator(segment),
+                          std::make_move_iterator(fused.commands.end()));
+  fused.commands.erase(segment, fused.commands.end());
+  fused.hasDraws = false;
+  for (const auto& cmd : fused.commands) {
+    if (cmd.type == CommandType::Draw || cmd.type == CommandType::CustomDraw) {
+      fused.hasDraws = true;
+      break;
+    }
+  }
+  for (auto& cmd : newPass.commands) {
+    if (cmd.type == CommandType::SetViewport) {
+      cmd.data.setViewport.left -= static_cast<float>(g_fusion.offsetX);
+    } else if (cmd.type == CommandType::SetScissor) {
+      cmd.data.setScissor.x -= g_fusion.offsetX;
+    } else if (cmd.type == CommandType::Draw || cmd.type == CommandType::CustomDraw) {
+      newPass.hasDraws = true;
+    }
+  }
+  if (flip_fuse_trace()) {
+    std::fprintf(stderr, "[flip-pass-fuse] split reason=%s pass=%u moved=%zu\n", reason, g_fusion.pass,
+                 newPass.commands.size());
+  }
+  enqueue_pass(current_frame_packet(), g_fusion.pass);
+  passes.emplace_back(std::move(newPass));
+  g_recorder.currentRenderPass = static_cast<uint32_t>(passes.size() - 1);
+}
+
+void flip_fusion_settle(const char* reason, unsigned& counter) { flip_fusion_split(reason, counter); }
+
+// ---- Half-res sprite segments -------------------------------------------
+void flip_append_encoder_task(FramePacket& frame, EncoderTaskId type) {
+  const auto taskIndex = static_cast<uint32_t>(frame.encoderTasks.size());
+  auto& task = frame.encoderTasks.emplace_back(EncoderTask{.type = type});
+  task.payloadSize = 0;
+  const auto opIndex = static_cast<uint32_t>(frame.ops.size());
+  frame.ops.emplace_back(capture_frame_op(frame, FrameOpType::EncoderTask, taskIndex));
+  enqueue_op(frame, opIndex);
+}
+
+unsigned g_spriteRefuse[6]{}; // lineMode, blendMode, factors, depthWrite, dstAlpha, colorUpdate
+bool flip_sprite_eligible(const gx::PipelineConfig& c) {
+  bool ok = true;
+  if (c.shaderConfig.lineMode != 3) { ++g_spriteRefuse[0]; ok = false; }
+  // Opaque (alpha-tested) sprites or SRCALPHA blends onto INVSRCALPHA/ONE.
+  if (c.blendMode != GX_BM_BLEND && c.blendMode != GX_BM_NONE) { ++g_spriteRefuse[1]; ok = false; }
+  if (c.blendMode == GX_BM_BLEND &&
+      !(c.blendFacSrc == GX_BL_SRCALPHA && (c.blendFacDst == GX_BL_INVSRCALPHA || c.blendFacDst == GX_BL_ONE))) { ++g_spriteRefuse[2]; ok = false; }
+  // Depth-writing sprites only update the half-res depth; geometry drawn after
+  // them in the frame is not occluded by them (an approximation of the pass).
+  if (c.depthCompare && c.depthUpdate) { ++g_spriteRefuse[3]; }
+  if (c.dstAlpha != UINT32_MAX) { ++g_spriteRefuse[4]; ok = false; }
+  if (!c.colorUpdate) { ++g_spriteRefuse[5]; ok = false; }
+  return ok;
+}
+
+void flip_sprite_begin_segment() {
+  auto& frame = current_frame_packet();
+  flip_fusion_settle("sprites", g_fusion.stats.splitBreak);
+  g_sprites.savedViewport = g_recorder.cachedViewport;
+  g_sprites.savedScissor = g_recorder.cachedScissor;
+  // Seal the EFB pass so far, prepare the half-res targets (clear + depth
+  // downsample), then record the sprites into the half-res pass.
+  enqueue_pass(frame, g_recorder.currentRenderPass);
+  g_recorder.currentRenderPass = UINT32_MAX;
+  flip_append_encoder_task(frame, flip_sprites::prep_task());
+  const auto& color = flip_sprites::color_target();
+  const auto& depth = flip_sprites::depth_target();
+  begin_color_pass(ColorPassDescriptor{
+      .label = "Sprites",
+      .colorView = color.view,
+      .colorFormat = color.format,
+      .depthStencilView = depth.view,
+      .depthStencilFormat = depth.format,
+      .targetSize = color.size,
+      .colorLoadOp = wgpu::LoadOp::Load,
+      .colorStoreOp = wgpu::StoreOp::Store,
+      .hasDepth = true,
+      .depthLoadOp = wgpu::LoadOp::Load,
+      .depthStoreOp = wgpu::StoreOp::Store,
+  });
+  g_recorder.cachedViewport = g_sprites.savedViewport;
+  g_recorder.cachedScissor = g_sprites.savedScissor;
+  g_sprites.active = true;
+  push_command(CommandType::SetViewport, Command::Data{.setViewport = g_recorder.cachedViewport});
+  push_command(CommandType::SetScissor, Command::Data{.setScissor = g_recorder.cachedScissor});
+  ++g_sprites.stats.segments;
+}
+
+void flip_sprite_end_segment() {
+  g_sprites.active = false;
+  auto& frame = current_frame_packet();
+  end_color_pass();
+  flip_append_encoder_task(frame, flip_sprites::composite_task());
+  // Resume the EFB pass, loading its contents.
+  auto& pass = current_render_passes().emplace_back();
+  pass.label = pass_label("EFB");
+  set_efb_targets(pass);
+  for (uint32_t i = 0; i < pass.colorAttachmentCount; ++i) {
+    pass.colorAttachments[i].clear = false;
+    pass.colorAttachments[i].loadOp = wgpu::LoadOp::Undefined;
+  }
+  pass.clearDepth = false;
+  pass.commands.reserve(2048);
+  g_recorder.currentRenderPass = static_cast<uint32_t>(current_render_passes().size() - 1);
+  push_command(CommandType::SetViewport, Command::Data{.setViewport = g_recorder.cachedViewport});
+  push_command(CommandType::SetScissor, Command::Data{.setScissor = g_recorder.cachedScissor});
+}
+
+void flip_sprite_frame_end() {
+  auto& s = g_sprites;
+  s.prevFramePoints = s.framePoints;
+  s.framePoints = 0;
+  if (++s.stats.frames % 600 == 0) {
+    std::fprintf(stderr,
+                 "[flip-sprites] frames=%u segments=%u points=%u refused=%u (line=%u blend=%u factors=%u zwrite=%u dstalpha=%u "
+                 "color=%u) last_frame_points=%u\n",
+                 s.stats.frames, s.stats.segments, s.stats.points, s.stats.refused, g_spriteRefuse[0], g_spriteRefuse[1],
+                 g_spriteRefuse[2], g_spriteRefuse[3], g_spriteRefuse[4], g_spriteRefuse[5], s.prevFramePoints);
+    s.stats = {};
+    for (auto& n : g_spriteRefuse) n = 0;
+  }
+}
+
+void flip_fusion_frame_end() {
+  auto& s = g_fusion.stats;
+  if (++s.frames % 600 != 0) {
+    return;
+  }
+  std::fprintf(stderr,
+               "[flip-pass-fuse] frames=%u fused=%u refused=%u (clear=%u state=%u region=%u room=%u draws=%u) "
+               "split=%u (viewport=%u scissor=%u clear=%u custom=%u texture=%u break=%u second=%u palette=%u)\n",
+               s.frames, s.fused, s.refused, s.refuseClear, s.refuseState, s.refuseRegion, s.refuseRoom, s.refuseDraws,
+               s.split, s.splitViewport, s.splitScissor, s.splitClear, s.splitCustom, s.splitTexture, s.splitBreak,
+               s.splitSecond, s.splitPalette);
+  s = {};
+}
+#endif
+
 void enqueue_pass(FramePacket& frame, uint32_t passIndex) {
   seal_pass(frame, passIndex);
+#ifdef MELEE_MIYOO_FLIP
+  // Resident geometry decoded while recording this pass must reach the GPU
+  // buffers before the pass is encoded and submitted.
+  if (gx::resident::has_pending() && !g_recorder.suppressRenderWorker)
+    render_worker::enqueue_work([] { gx::resident::render_flush(); });
+#endif
   const auto opIndex = static_cast<uint32_t>(frame.ops.size());
   frame.ops.emplace_back(capture_frame_op(frame, FrameOpType::RenderPass, passIndex));
   enqueue_op(frame, opIndex);
@@ -531,6 +1090,9 @@ void enqueue_pass(FramePacket& frame, uint32_t passIndex) {
 namespace detail {
 
 void begin_recording(FramePacket& packet, size_t frameSlot) {
+#ifdef MELEE_MIYOO_FLIP
+  if(flip_profile::enabled()) flip_profile::fifoFrame.store(packet.frameId,std::memory_order_release);
+#endif
   CHECK(!g_recorder.active(), "A recording session is already active");
   g_recorder.packet = &packet;
   g_recorder.frameSlot = frameSlot;
@@ -552,6 +1114,9 @@ void begin_recording(FramePacket& packet, size_t frameSlot) {
   push_command(CommandType::SetScissor, Command::Data{.setScissor = g_recorder.cachedScissor});
 }
 
+size_t current_frame_slot() { return g_recorder.frameSlot; }
+FramePacket& recording_frame_packet() { return g_recorder.frame(); }
+
 RecordedFrame end_recording() {
   CHECK(g_recorder.active(), "No active recording session");
   AURORA_ASSERT(!g_recorder.inOffscreen, "end_frame called while offscreen rendering is active");
@@ -673,6 +1238,7 @@ void queue_texture_copy(wgpu::TexelCopyTextureInfo src, wgpu::TexelCopyTextureIn
   ZoneScoped;
   auto& frame = current_frame_packet();
   if (g_recorder.currentRenderPass != UINT32_MAX) {
+    FLIP_FUSION_SETTLE("break", splitBreak);
     enqueue_pass(frame, g_recorder.currentRenderPass);
     g_recorder.currentRenderPass = UINT32_MAX;
   }
@@ -692,6 +1258,7 @@ void begin_color_pass(const ColorPassDescriptor& desc) {
   ZoneScoped;
   auto& frame = current_frame_packet();
   if (g_recorder.currentRenderPass != UINT32_MAX) {
+    FLIP_FUSION_SETTLE("break", splitBreak);
     enqueue_pass(frame, g_recorder.currentRenderPass);
   }
 
@@ -780,6 +1347,7 @@ void set_scissor(const ClipRect& cmd) noexcept {
 
 template <>
 void push_draw_command(clear::DrawData data) {
+  FLIP_FUSION_SETTLE("clear", splitClear);
   push_draw_command(make_draw_command<clear::render>(data));
 }
 
@@ -790,53 +1358,43 @@ PipelineRef pipeline_ref(const clear::PipelineConfig& config) {
 
 void resolve_pass_into(TextureHandle texture, ClipRect rect, bool clearColor, bool clearAlpha, bool clearDepth,
                        Vec4<float> clearColorValue, float clearDepthValue, GXTexFmt resolveFormat) {
+  bool fusedSecond = false;
+#ifdef MELEE_MIYOO_FLIP
+  if (g_fusion.active) {
+    fusedSecond = flip_fusion_complete(texture, rect, clearColor, clearAlpha, clearDepth, resolveFormat);
+    if (!fusedSecond) {
+      flip_fusion_split("second", g_fusion.stats.splitSecond);
+    }
+  }
+#endif
   // Resolve current render pass
   auto& prevPass = current_render_passes()[g_recorder.currentRenderPass];
-  prevPass.resolveTarget = std::move(texture);
-  prevPass.resolveRect = rect;
-  prevPass.resolveFormat = resolveFormat;
-  // Push UV transform uniform for tex_copy_conv (crop region in UV space)
-  const auto srcW = static_cast<float>(prevPass.colorAttachments[SceneColorAttachmentIndex].size.width);
-  const auto srcH = static_cast<float>(prevPass.colorAttachments[SceneColorAttachmentIndex].size.height);
-  const std::array uvTransform{
-      static_cast<float>(rect.x) / srcW,
-      static_cast<float>(rect.y) / srcH,
-      static_cast<float>(rect.width) / srcW,
-      static_cast<float>(rect.height) / srcH,
-  };
-  prevPass.resolveUniformRange = push_uniform(uvTransform);
+  if (!fusedSecond) {
+    prevPass.resolveTarget = std::move(texture);
+    prevPass.resolveRect = rect;
+    prevPass.resolveFormat = resolveFormat;
+    // Push UV transform uniform for tex_copy_conv (crop region in UV space)
+    const auto srcW = static_cast<float>(prevPass.colorAttachments[SceneColorAttachmentIndex].size.width);
+    const auto srcH = static_cast<float>(prevPass.colorAttachments[SceneColorAttachmentIndex].size.height);
+    const std::array uvTransform{
+        static_cast<float>(rect.x) / srcW,
+        static_cast<float>(rect.y) / srcH,
+        static_cast<float>(rect.width) / srcW,
+        static_cast<float>(rect.height) / srcH,
+    };
+    prevPass.resolveUniformRange = push_uniform(uvTransform);
+#ifdef MELEE_MIYOO_FLIP
+    if (flip_fusion_begin(prevPass, rect, clearColor, clearAlpha, clearDepth, clearColorValue, clearDepthValue)) {
+      return;
+    }
+#endif
+  }
   enqueue_pass(current_frame_packet(), g_recorder.currentRenderPass);
 
   // Populate new render pass from previous
-  const auto msaaSamples = prevPass.msaaSamples;
-  RenderPass newPass{
-      .label = pass_label(g_recorder.inOffscreen ? "Offscreen" : "EFB"),
-      .colorAttachments = prevPass.colorAttachments,
-      .colorAttachmentCount = prevPass.colorAttachmentCount,
-      .depthStencilView = prevPass.depthStencilView,
-      .depthStencilFormat = prevPass.depthStencilFormat,
-      .copySourceTexture = prevPass.copySourceTexture,
-      .copySourceView = prevPass.copySourceView,
-      .copySourceDepthView = prevPass.copySourceDepthView,
-      .msaaSamples = msaaSamples,
-      .clearDepthValue = clearDepthValue,
-      .clearDepth = clearDepth,
-      .hasDepth = prevPass.hasDepth,
-      .hasStencil = prevPass.hasStencil,
-  };
   const bool fullColorClear = clearColor && clearAlpha;
-  for (uint32_t i = 0; i < newPass.colorAttachmentCount; ++i) {
-    auto& color = newPass.colorAttachments[i];
-    color.loadOp = wgpu::LoadOp::Undefined;
-    color.clear = false;
-  }
-  if (fullColorClear) {
-    auto& sceneColor = newPass.colorAttachments[SceneColorAttachmentIndex];
-    sceneColor.clear = true;
-    sceneColor.clearValue = clearColorValue;
-  }
-  newPass.commands.reserve(2048);
-  current_render_passes().emplace_back(std::move(newPass));
+  current_render_passes().emplace_back(
+      make_efb_continuation(prevPass, clearDepth, clearDepthValue, fullColorClear, clearColorValue));
   ++g_recorder.currentRenderPass;
 
   if (!fullColorClear && (clearColor || clearAlpha)) {
@@ -858,6 +1416,7 @@ void resolve_pass_into(TextureHandle texture, ClipRect rect, bool clearColor, bo
 }
 
 void queue_palette_conv(tex_palette_conv::ConvRequest req) {
+  FLIP_FUSION_SETTLE("palette", splitPalette);
   auto& renderPass = current_render_passes()[g_recorder.currentRenderPass];
   AURORA_ASSERT(!renderPass.sealed, "Attempted to append palette conversion to sealed render pass {}",
                 g_recorder.currentRenderPass);
@@ -973,6 +1532,7 @@ bool resolve_pass(const ResolveDesc& desc, ResolvedTargets& out) {
     wantDepth = false;
   }
 
+  FLIP_FUSION_SETTLE("break", splitBreak);
   auto& prevPass = current_render_passes()[g_recorder.currentRenderPass];
   const uint32_t width = prevPass.colorAttachments[SceneColorAttachmentIndex].size.width;
   const uint32_t height = prevPass.colorAttachments[SceneColorAttachmentIndex].size.height;
@@ -1040,6 +1600,7 @@ bool push_encoder_task(EncoderTaskId type, const void* payload, size_t payloadSi
   // pass that loads the existing contents. EFB writes persist into the continuation, so
   // content keeps the sealed pass alive even without a consumer.
   auto& frame = current_frame_packet();
+  FLIP_FUSION_SETTLE("break", splitBreak);
   auto& prevPass = current_render_passes()[g_recorder.currentRenderPass];
   prevPass.discardable = !prevPass.has_consumer() && !prevPass.has_content();
   enqueue_pass(frame, g_recorder.currentRenderPass);
@@ -1063,6 +1624,23 @@ void push_draw_command(gx::DrawData data) {
   push_draw_command(make_draw_command<gx::render>(data));
 }
 
+#ifdef MELEE_MIYOO_FLIP
+namespace detail {
+bool decode_flip_gx_draw(const DrawCommand& command,gx::DrawData& data) noexcept {
+  const auto marker=make_draw_command<gx::render>(data).encoder;
+  if(command.encoder!=marker) return false;
+  std::memcpy(&data,command.payload.data(),sizeof(data));
+  return true;
+}
+bool decode_flip_clear_draw(const DrawCommand& command,clear::DrawData& data) noexcept {
+  const auto marker=make_draw_command<clear::render>(data).encoder;
+  if(command.encoder!=marker) return false;
+  std::memcpy(&data,command.payload.data(),sizeof(data));
+  return true;
+}
+}
+#endif
+
 #ifdef AURORA_ENABLE_RMLUI
 template <>
 void push_draw_command(rmlui::DrawData data) {
@@ -1070,8 +1648,61 @@ void push_draw_command(rmlui::DrawData data) {
 }
 #endif
 
+#ifdef MELEE_MIYOO_FLIP
+static gx::PipelineConfig flip_canonical_config(const gx::PipelineConfig& config) {
+  auto canonical=config;
+  if (canonical.shaderConfig.flipFlags & gx::FlipUberShader)
+    gx::flip_uber_canonicalize(canonical.shaderConfig);
+  canonical.shaderConfig.vtxStride=0;
+  for (auto& attr : canonical.shaderConfig.attrs) {
+    if (attr.attrType == GX_NONE) continue;
+    attr.attrType=GX_DIRECT;
+    attr.compType=GX_F32;
+    attr.offset=attr.stride=attr.frac=0;
+    attr.le=attr.nbt3=false;
+  }
+  return canonical;
+}
+bool flip_sprite_point_draw(const gx::PipelineConfig& config, uint32_t points) {
+  auto& s = g_sprites;
+  s.framePoints += points;
+  if (!flip_sprites::enabled() || g_recorder.inOffscreen || g_recorder.currentRenderPass == UINT32_MAX) {
+    if (s.active) flip_sprite_end_segment();
+    return false;
+  }
+  if (!flip_sprite_eligible(config)) {
+    ++s.stats.refused;
+    if (s.active) flip_sprite_end_segment();
+    return false;
+  }
+  if (!s.active) {
+    if (s.prevFramePoints < flip_sprites::threshold()) return false;
+    flip_sprite_begin_segment();
+  }
+  s.stats.points += points;
+  return s.active;
+}
+void flip_sprite_settle() {
+  if (g_sprites.active) flip_sprite_end_segment();
+}
+void flip_fusion_texture_bound(const TextureHandle& handle) noexcept {
+  if (g_fusion.active && handle && handle.get() == g_fusion.firstTarget.get()) {
+    flip_fusion_split("texture", g_fusion.stats.splitTexture);
+  }
+}
+PipelineRef pipeline_ref_async(const gx::PipelineConfig& config) {
+  const auto canonical = (config.shaderConfig.flipFlags & gx::FlipVertexInput) ? flip_canonical_config(config) : config;
+  return find_pipeline_async(ShaderType::GX, canonical, [=] { return create_pipeline(canonical); });
+}
+#endif
 template <>
 PipelineRef pipeline_ref(const gx::PipelineConfig& config) {
+#ifdef MELEE_MIYOO_FLIP
+  if (config.shaderConfig.flipFlags & gx::FlipVertexInput) {
+    const auto canonical = flip_canonical_config(config);
+    return find_pipeline(ShaderType::GX, canonical, [=] { return create_pipeline(canonical); });
+  }
+#endif
   return find_pipeline(ShaderType::GX, config, [=] { return create_pipeline(config); });
 }
 
@@ -1089,6 +1720,11 @@ void finish() {
   }
   AURORA_ASSERT(!g_recorder.inOffscreen, "finish called while offscreen rendering is active");
   if (g_recorder.currentRenderPass != UINT32_MAX) {
+    FLIP_FUSION_SETTLE("break", splitBreak);
+#ifdef MELEE_MIYOO_FLIP
+    flip_fusion_frame_end();
+    flip_sprite_frame_end();
+#endif
     auto& frame = current_frame_packet();
     frame.uniforms.append_zeroes(gx::MaxUniformSize);
     auto& pass = frame.renderPasses[g_recorder.currentRenderPass];
@@ -1114,12 +1750,41 @@ Range push_indices(const uint8_t* data, size_t length, size_t alignment) {
   return push(current_frame_packet().indices, data, length, alignment);
 }
 
+#ifdef MELEE_MIYOO_FLIP
+bool flip_indices_follow(Range previous) {
+  return current_frame_packet().indices.size() == previous.offset + previous.size;
+}
+Range map_flip_verts(size_t length,size_t alignment,uint8_t*& data) {
+  data=nullptr;
+  if(!check_recording("map_flip_verts")) return {};
+  auto& vertices=current_frame_packet().verts;
+  const auto range=map(vertices,length,alignment);
+  data=vertices.data()+range.offset;
+  return range;
+}
+Range push_flip_uniform(const uint8_t* data, size_t length) {
+  if(!check_recording("push_flip_uniform")) return {};
+  // Each complete GX record resides in one immutable 4 KiB slot. Sixteen
+  // records share a 64 KiB UBO binding; EFB pass ordering remains unchanged.
+  return push(current_frame_packet().uniforms, data, length, 4096);
+}
+#endif
 Range push_uniform(const uint8_t* data, size_t length) {
   ZoneScoped;
   if (!check_recording("push_uniform")) {
     return {};
   }
-  return push(current_frame_packet().uniforms, data, length, resources().limits.minUniformBufferOffsetAlignment);
+  auto& frame=current_frame_packet();
+  const auto range=push(frame.uniforms, data, length, resources().limits.minUniformBufferOffsetAlignment);
+#ifdef MELEE_MIYOO_FLIP
+  if(range.size) {
+    auto& ordinary=frame.flipOrdinaryUniforms;
+    if(!ordinary.empty()&&range.offset<=ordinary.back().offset+ordinary.back().size+256)
+      ordinary.back().size=range.offset+range.size-ordinary.back().offset;
+    else ordinary.push_back(range);
+  }
+#endif
+  return range;
 }
 
 Range push_storage(const uint8_t* data, size_t length) {
diff --git a/lib/gfx/recording.hpp b/lib/gfx/recording.hpp
index cd2b794..2fb92f1 100644
--- a/lib/gfx/recording.hpp
+++ b/lib/gfx/recording.hpp
@@ -16,6 +16,8 @@ struct RecordedFrame {
 
 void begin_recording(FramePacket& packet, size_t frameSlot);
 RecordedFrame end_recording();
+size_t current_frame_slot();
+FramePacket& recording_frame_packet(); // the packet currently being recorded (FIFO worker)
 void shutdown_recording();
 void increment_merged_draw_count() noexcept;
 
@@ -27,6 +29,7 @@ void seed_offscreen_cache(uint32_t width, uint32_t height, wgpu::TextureFormat c
 
 } // namespace aurora::gfx::detail
 
+namespace aurora::gx { struct PipelineConfig; }
 namespace aurora::gfx {
 struct ColorPassDescriptor {
   const char* label = nullptr;
@@ -76,6 +79,11 @@ Range push_indices(ArrayRef<T> data, size_t alignment) {
   return push_indices(reinterpret_cast<const uint8_t*>(data.data()), data.size() * sizeof(T), alignment);
 }
 Range push_uniform(const uint8_t* data, size_t length);
+#ifdef MELEE_MIYOO_FLIP
+Range push_flip_uniform(const uint8_t* data, size_t length);
+Range map_flip_verts(size_t length, size_t alignment, uint8_t*& data);
+bool flip_indices_follow(Range previous);
+#endif
 template <typename T>
 Range push_uniform(const T& data) {
   return push_uniform(reinterpret_cast<const uint8_t*>(&data), sizeof(T));
@@ -97,6 +105,17 @@ template <typename DrawData>
 DrawData* get_last_draw_command();
 template <typename PipelineConfig>
 PipelineRef pipeline_ref(const PipelineConfig& config);
+#ifdef MELEE_MIYOO_FLIP
+PipelineRef pipeline_ref_async(const gx::PipelineConfig& config);
+// Shadow-pass fusion: a draw that samples the first copy of a fused pass must
+// split the pass so the copy is resolved before the draw.
+void flip_fusion_texture_bound(const TextureHandle& handle) noexcept;
+// Half-res sprite pass: called for every GX point draw; true when the draw must
+// use its sprite-accumulation pipeline variant (it is being recorded into the
+// half-res sprite pass). Non-point draws call flip_sprite_settle().
+bool flip_sprite_point_draw(const gx::PipelineConfig& config, uint32_t points);
+void flip_sprite_settle();
+#endif
 
 void resolve_pass_into(TextureHandle texture, ClipRect rect, bool clearColor, bool clearAlpha, bool clearDepth,
                        Vec4<float> clearColorValue, float clearDepthValue, GXTexFmt resolveFormat = GX_TF_RGBA8);
diff --git a/lib/gfx/render_worker.cpp b/lib/gfx/render_worker.cpp
index 85783b6..0359368 100644
--- a/lib/gfx/render_worker.cpp
+++ b/lib/gfx/render_worker.cpp
@@ -4,12 +4,16 @@
 
 #include <algorithm>
 #include <atomic>
+#include <cstdlib>
 #include <chrono>
 
 #include <tracy/Tracy.hpp>
 
 namespace aurora::gfx::render_worker {
 namespace {
+std::atomic<uint64_t> g_busyNs{0};
+const bool g_profileBusy = [] { const char* v = std::getenv("MELEE_FLIP_PROFILE"); return v && v[0] != '0'; }();
+
 constexpr size_t QueueCapacity = 256;
 constexpr auto IdlePumpInterval = std::chrono::milliseconds{1};
 
@@ -46,7 +50,14 @@ void worker_main(std::stop_token token) {
 
     if (item->work) {
       ZoneScopedN("QueueItem work");
-      item->work();
+      if (g_profileBusy) {
+        const auto start = std::chrono::steady_clock::now();
+        item->work();
+        g_busyNs.fetch_add(std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now() - start).count(),
+                           std::memory_order_relaxed);
+      } else {
+        item->work();
+      }
     }
     complete_sync(item->sync);
     g_pendingItems.fetch_sub(1, std::memory_order_acq_rel);
@@ -58,6 +69,10 @@ void worker_main(std::stop_token token) {
   g_workerThreadId = {};
 }
 
+}  // namespace
+uint64_t busy_ns() noexcept { return g_busyNs.load(std::memory_order_relaxed); }
+namespace {
+
 void enqueue(QueueItem item) {
   ZoneScoped;
   if (is_worker_thread()) {
diff --git a/lib/gfx/render_worker.hpp b/lib/gfx/render_worker.hpp
index 7d652ca..33b6b9d 100644
--- a/lib/gfx/render_worker.hpp
+++ b/lib/gfx/render_worker.hpp
@@ -84,5 +84,7 @@ void synchronize();
 
 bool is_worker_thread() noexcept;
 bool is_idle() noexcept;
+// Accumulated nanoseconds the worker spent executing items (MELEE_FLIP_PROFILE only).
+uint64_t busy_ns() noexcept;
 
 } // namespace aurora::gfx::render_worker
diff --git a/lib/gfx/resource_cache.cpp b/lib/gfx/resource_cache.cpp
index 8c131ab..14cec17 100644
--- a/lib/gfx/resource_cache.cpp
+++ b/lib/gfx/resource_cache.cpp
@@ -1,4 +1,5 @@
 #include "resource_cache.hpp"
+#include <atomic>
 
 #include "frame.hpp"
 #include "hash.hpp"
@@ -7,6 +8,7 @@
 #include <cstddef>
 #include <cstdint>
 #include <mutex>
+#include <vector>
 
 #include <absl/container/flat_hash_map.h>
 #include <tracy/Tracy.hpp>
@@ -36,6 +38,11 @@ constexpr Module Log{"aurora::gfx"};
 
 struct CachedBindGroup {
   wgpu::BindGroup bindGroup;
+#ifdef MELEE_MIYOO_FLIP
+  std::vector<WGPUBindGroupEntry> entries;
+  WGPUBindGroupLayout layout = nullptr;
+  std::vector<BindGroupRef> textureBanks;
+#endif
   uint32_t lastUsedFrame = 0;
 };
 
@@ -56,6 +63,9 @@ void clear_bind_group_cache() {
   g_cachedBindGroups.clear();
 }
 
+std::atomic<uint64_t> g_bindGroupCacheGeneration{0};
+uint64_t bind_group_cache_generation() { return g_bindGroupCacheGeneration.load(std::memory_order_acquire); }
+
 void expire_cached_bind_groups() {
   std::lock_guard lock{g_bindGroupCacheMutex};
   const auto frameIndex = current_frame();
@@ -67,6 +77,7 @@ void expire_cached_bind_groups() {
   for (auto it = g_cachedBindGroups.begin(); it != g_cachedBindGroups.end();) {
     if (frameIndex - it->second.lastUsedFrame > BindGroupCacheRetainFrames) {
       g_cachedBindGroups.erase(it++);
+      g_bindGroupCacheGeneration.fetch_add(1, std::memory_order_release);
     } else {
       ++it;
     }
@@ -89,6 +100,10 @@ BindGroupRef bind_group_ref(const WGPUBindGroupDescriptor& descriptor) {
     auto bindGroup = wgpu::BindGroup::Acquire(wgpuDeviceCreateBindGroup(webgpu::g_device.Get(), &descriptor));
     g_cachedBindGroups.emplace(id, CachedBindGroup{
                                        .bindGroup = std::move(bindGroup),
+#ifdef MELEE_MIYOO_FLIP
+                                       .entries = {descriptor.entries, descriptor.entries + descriptor.entryCount},
+                                       .layout = descriptor.layout,
+#endif
                                        .lastUsedFrame = current_frame(),
                                    });
   } else {
@@ -97,6 +112,80 @@ BindGroupRef bind_group_ref(const WGPUBindGroupDescriptor& descriptor) {
   return id;
 }
 
+#ifdef MELEE_MIYOO_FLIP
+BindGroupRef append_texture_bind_group_ref(BindGroupRef group, BindGroupRef candidate,
+                                           uint32_t bankEntries, int& bank) {
+  std::lock_guard lock{g_bindGroupCacheMutex};
+  const auto a = g_cachedBindGroups.find(group);
+  const auto b = g_cachedBindGroups.find(candidate);
+  CHECK(a != g_cachedBindGroups.end() && b != g_cachedBindGroups.end(), "Cannot pair missing bind groups");
+  CHECK(bankEntries && a->second.layout == b->second.layout && a->second.entries.size() == 32 &&
+            b->second.entries.size() == 32,
+        "Cannot pair incompatible bind groups");
+  if (a->second.textureBanks.empty()) a->second.textureBanks.push_back(group);
+  if (b->second.textureBanks.empty()) b->second.textureBanks.push_back(candidate);
+  const BindGroupRef candidateBank = b->second.textureBanks.front();
+  for (uint32_t i = 0; i < a->second.textureBanks.size(); ++i) {
+    if (a->second.textureBanks[i] == candidateBank) {
+      bank = static_cast<int>(i);
+      return group;
+    }
+  }
+  // Keep the sixteenth sampled-image unit free for Aurora's emulated storage
+  // texture.  Mali advertises 16 fragment image units total, so filling all
+  // 16 sampler slots makes Dawn reject an otherwise valid pipeline.
+  constexpr uint32_t MaxTextureBankEntries = 15 * 2;
+  if ((a->second.textureBanks.size() + 1) * bankEntries > MaxTextureBankEntries) {
+    bank = -1;
+    return {};
+  }
+  bank = static_cast<int>(a->second.textureBanks.size());
+  auto entries = a->second.entries;
+  const uint32_t destination = static_cast<uint32_t>(bank) * bankEntries;
+  for (uint32_t i = 0; i < bankEntries; ++i) {
+    entries[destination + i] = b->second.entries[i];
+    entries[destination + i].binding = destination + i;
+  }
+  const WGPUBindGroupDescriptor descriptor{
+      .label = {"GX Multi-bank Texture Bind Group", WGPU_STRLEN},
+      .layout = a->second.layout,
+      .entryCount = entries.size(),
+      .entries = entries.data(),
+  };
+  const auto id = xxh3_hash(descriptor);
+  auto found = g_cachedBindGroups.find(id);
+  if (found == g_cachedBindGroups.end()) {
+    a->second.lastUsedFrame = current_frame();
+    b->second.lastUsedFrame = current_frame();
+    auto bindGroup = wgpu::BindGroup::Acquire(wgpuDeviceCreateBindGroup(webgpu::g_device.Get(), &descriptor));
+    auto banks = a->second.textureBanks;
+    banks.push_back(candidateBank);
+    found = g_cachedBindGroups.emplace(id, CachedBindGroup{
+        .bindGroup = std::move(bindGroup), .entries = std::move(entries), .layout = descriptor.layout,
+        .textureBanks = std::move(banks),
+        .lastUsedFrame = current_frame()}).first;
+  } else {
+    found->second.lastUsedFrame = current_frame();
+    a->second.lastUsedFrame = current_frame();
+    b->second.lastUsedFrame = current_frame();
+  }
+  return id;
+}
+
+int texture_bind_group_bank(BindGroupRef group, BindGroupRef candidate) {
+  std::lock_guard lock{g_bindGroupCacheMutex};
+  const auto g = g_cachedBindGroups.find(group);
+  const auto c = g_cachedBindGroups.find(candidate);
+  CHECK(g != g_cachedBindGroups.end() && c != g_cachedBindGroups.end(), "Missing texture bind group");
+  if (g->second.textureBanks.empty()) g->second.textureBanks.push_back(group);
+  if (c->second.textureBanks.empty()) c->second.textureBanks.push_back(candidate);
+  const auto wanted = c->second.textureBanks.front();
+  for (uint32_t i = 0; i < g->second.textureBanks.size(); ++i)
+    if (g->second.textureBanks[i] == wanted) return static_cast<int>(i);
+  return -1;
+}
+#endif
+
 wgpu::BindGroup find_bind_group(BindGroupRef id) {
   std::lock_guard lock{g_bindGroupCacheMutex};
   const auto it = g_cachedBindGroups.find(id);
diff --git a/lib/gfx/resource_cache.hpp b/lib/gfx/resource_cache.hpp
index c133abd..ddcbfd8 100644
--- a/lib/gfx/resource_cache.hpp
+++ b/lib/gfx/resource_cache.hpp
@@ -5,12 +5,20 @@
 namespace aurora::gfx {
 
 BindGroupRef bind_group_ref(const WGPUBindGroupDescriptor& descriptor);
+#ifdef MELEE_MIYOO_FLIP
+BindGroupRef append_texture_bind_group_ref(BindGroupRef group, BindGroupRef candidate,
+                                           uint32_t bankEntries, int& bank);
+int texture_bind_group_bank(BindGroupRef group, BindGroupRef candidate);
+#endif
 wgpu::BindGroup find_bind_group(BindGroupRef id);
 wgpu::Sampler sampler_ref(const wgpu::SamplerDescriptor& descriptor);
 
 namespace detail {
 void clear_bind_group_cache();
 void expire_cached_bind_groups();
+// Increments whenever a cached bind group is destroyed; derived caches keyed by
+// BindGroupRef must be dropped when it changes.
+uint64_t bind_group_cache_generation();
 void shutdown_resource_cache();
 } // namespace detail
 
diff --git a/lib/gfx/resources.hpp b/lib/gfx/resources.hpp
index 3fdc0ee..ae33eff 100644
--- a/lib/gfx/resources.hpp
+++ b/lib/gfx/resources.hpp
@@ -4,14 +4,36 @@
 
 namespace aurora::gfx {
 inline constexpr bool UseTextureBuffer = true;
+#ifdef MELEE_MIYOO_FLIP
+// Bounded pools for the Flip's 1 GiB shared CPU/GPU memory.
+inline constexpr uint64_t UniformBufferSize = 8 * 1024 * 1024;
+inline constexpr uint64_t VertexBufferSize = 12 * 1024 * 1024;
+inline constexpr uint64_t IndexBufferSize = 2 * 1024 * 1024;
+inline constexpr uint64_t StorageBufferSize = 4 * 1024 * 1024;
+inline constexpr uint64_t TextureUploadSize = 12 * 1024 * 1024;
+#else
 inline constexpr uint64_t UniformBufferSize = 25165824; // 24 MiB
 inline constexpr uint64_t VertexBufferSize = 5242880;   // 5 MiB
 inline constexpr uint64_t IndexBufferSize = 2097152;    // 2 MiB
 inline constexpr uint64_t StorageBufferSize = 8388608;  // 8 MiB
 inline constexpr uint64_t TextureUploadSize = 25165824; // 24 MiB
+#endif
 
 namespace detail {
+#ifdef MELEE_MIYOO_FLIP
+wgpu::BindGroup flip_uniform_window(uint32_t index);
+#endif
 struct Resources {
+#ifdef MELEE_MIYOO_FLIP
+  wgpu::BindGroup flipUniformBindGroup;
+  std::vector<wgpu::Buffer> flipUniformBuffers;
+  std::vector<wgpu::BindGroup> flipUniformGroups;
+  std::vector<std::vector<uint8_t>> flipUploadedUniforms;
+  std::vector<uint8_t> flipUploadedVertices, flipUploadedIndices;
+  wgpu::Texture vertexTexture;
+  wgpu::Texture storageTexture;
+  wgpu::Buffer flipZeroRecords; // 64K zero u32 records for draws without per-vertex records
+#endif
   wgpu::Buffer vertexBuffer;
   wgpu::Buffer uniformBuffer;
   wgpu::Buffer indexBuffer;
diff --git a/lib/gfx/tex_copy_conv.cpp b/lib/gfx/tex_copy_conv.cpp
index 83c3786..387920c 100644
--- a/lib/gfx/tex_copy_conv.cpp
+++ b/lib/gfx/tex_copy_conv.cpp
@@ -327,7 +327,87 @@ static wgpu::BindGroupLayout g_depthBindGroupLayout;
 static wgpu::Sampler g_nearestSampler;
 static wgpu::Sampler g_linearSampler;
 static absl::flat_hash_map<GXTexFmt, wgpu::RenderPipeline> g_pipelines;
+static absl::flat_hash_map<GXTexFmt, wgpu::RenderPipeline> g_dualPipelines;
 static wgpu::RenderPipeline g_blitPipeline;
+
+// Dual-target variant: the same conversion applied to two source rectangles,
+// written to two attachments (fused shadow passes resolve both shadow maps in
+// one pass; each pass costs ~0.5 ms of driver time on the Flip).
+static constexpr std::string_view DualShaderPreamble = R"(
+@group(0) @binding(0) var src_samp: sampler;
+@group(0) @binding(1) var src: texture_2d<f32>;
+
+struct UVTransform {
+    offset: vec2f,
+    scale: vec2f,
+    offset2: vec2f,
+    scale2: vec2f,
+};
+@group(0) @binding(2) var<uniform> uv_xf: UVTransform;
+
+struct VertexOutput {
+    @builtin(position) pos: vec4f,
+    @location(0) uv: vec2f,
+    @location(1) uv2: vec2f,
+};
+
+var<private> positions: array<vec2f, 3> = array(
+    vec2f(-1.0, 1.0),
+    vec2f(-1.0, -3.0),
+    vec2f(3.0, 1.0),
+);
+var<private> uvs: array<vec2f, 3> = array(
+    vec2f(0.0, 0.0),
+    vec2f(0.0, 2.0),
+    vec2f(2.0, 0.0),
+);
+
+@vertex fn vs_main(@builtin(vertex_index) vi: u32) -> VertexOutput {
+    var out: VertexOutput;
+    out.pos = vec4f(positions[vi], 0.0, 1.0);
+    out.uv = uvs[vi] * uv_xf.scale + uv_xf.offset;
+    out.uv2 = uvs[vi] * uv_xf.scale2 + uv_xf.offset2;
+    return out;
+}
+
+fn intensity(rgb: vec3f) -> f32 {
+    // ITU-R BT.601 luma coefficients
+    return dot(rgb, vec3f(0.257, 0.504, 0.098)) + 16.0 / 255.0;
+}
+
+fn quantize4(v: f32) -> f32 {
+    return floor(v * 16.0) / 15.0;
+}
+)"sv;
+
+static constexpr std::string_view DualFragmentEpilogue = R"(
+struct DualOutput {
+    @location(0) a: vec4f,
+    @location(1) b: vec4f,
+};
+@fragment fn fs_main(in: VertexOutput) -> DualOutput {
+    var out: DualOutput;
+    out.a = conv(in.uv);
+    out.b = conv(in.uv2);
+    return out;
+}
+)"sv;
+
+// Rewrites a single-target fragment shader into `fn conv(uv: vec2f) -> vec4f`.
+static std::string dual_fragment_source(std::string_view fragShader) {
+  static constexpr std::string_view signature = "@fragment fn fs_main(in: VertexOutput) -> @location(0) vec4f {";
+  std::string source{fragShader};
+  const auto pos = source.find(signature);
+  if (pos == std::string::npos) {
+    return {};
+  }
+  source.replace(pos, signature.size(), "fn conv(uv: vec2f) -> vec4f {");
+  for (size_t at = source.find("in.uv"); at != std::string::npos; at = source.find("in.uv", at + 2)) {
+    source.replace(at, 5, "uv");
+  }
+  source += DualFragmentEpilogue;
+  return source;
+}
 static wgpu::BindGroupLayout g_depthSnapshotBindGroupLayout;
 static wgpu::BindGroupLayout g_depthSnapshotBindGroupLayoutMS;
 static wgpu::RenderPipeline g_depthSnapshotPipeline;
@@ -447,7 +527,57 @@ static wgpu::RenderPipeline create_pipeline(const ConvPipeline& conv, const std:
   return g_device.CreateRenderPipeline(&pipelineDescriptor);
 }
 
+static wgpu::RenderPipeline create_dual_pipeline(const ConvPipeline& conv, const wgpu::BindGroupLayout& bindGroupLayout) {
+  const std::string frag = dual_fragment_source(conv.fragShader);
+  if (frag.empty()) {
+    return {};
+  }
+  std::string shaderSource;
+  shaderSource += DualShaderPreamble;
+  shaderSource += frag;
+  const std::string label = std::string{conv.label} + " Dual";
+  const wgpu::ShaderSourceWGSL wgslSource{wgpu::ShaderSourceWGSL::Init{
+      .code = shaderSource.c_str(),
+  }};
+  const wgpu::ShaderModuleDescriptor moduleDescriptor{
+      .nextInChain = &wgslSource,
+      .label = label.c_str(),
+  };
+  const auto module = g_device.CreateShaderModule(&moduleDescriptor);
+  const std::array colorTargets{
+      wgpu::ColorTargetState{.format = conv.outputFormat},
+      wgpu::ColorTargetState{.format = conv.outputFormat},
+  };
+  const wgpu::FragmentState fragmentState{
+      .module = module,
+      .entryPoint = "fs_main",
+      .targetCount = colorTargets.size(),
+      .targets = colorTargets.data(),
+  };
+  const wgpu::PipelineLayoutDescriptor layoutDescriptor{
+      .bindGroupLayoutCount = 1,
+      .bindGroupLayouts = &bindGroupLayout,
+  };
+  const auto pipelineLayout = g_device.CreatePipelineLayout(&layoutDescriptor);
+  const wgpu::RenderPipelineDescriptor pipelineDescriptor{
+      .label = label.c_str(),
+      .layout = pipelineLayout,
+      .vertex =
+          wgpu::VertexState{
+              .module = module,
+              .entryPoint = "vs_main",
+          },
+      .primitive =
+          wgpu::PrimitiveState{
+              .topology = wgpu::PrimitiveTopology::TriangleList,
+          },
+      .fragment = &fragmentState,
+  };
+  return g_device.CreateRenderPipeline(&pipelineDescriptor);
+}
+
 bool needs_conversion(const GXTexFmt fmt) { return g_pipelines.contains(fmt); }
+bool dual_supported(const GXTexFmt fmt) { return g_dualPipelines.contains(fmt); }
 
 void initialize() {
   static constexpr std::array bindGroupLayoutEntries{
@@ -518,6 +648,11 @@ void initialize() {
     if (conv.outputFormat != to_wgpu(conv.fmt)) {
       Log.fatal("Output format mismatch for {}", conv.fmt);
     }
+#ifdef MELEE_MIYOO_FLIP
+    if (auto dual = create_dual_pipeline(conv, g_bindGroupLayout)) {
+      g_dualPipelines[conv.fmt] = std::move(dual);
+    }
+#endif
   }
   // Skip depth copies in compatibility mode
   if (webgpu::g_hasCoreFeatures) {
@@ -552,6 +687,7 @@ void initialize() {
 
 void shutdown() {
   g_pipelines.clear();
+  g_dualPipelines.clear();
   g_blitPipeline = {};
   g_bindGroupLayout = {};
   g_depthBindGroupLayout = {};
@@ -638,6 +774,62 @@ static void execute(const wgpu::CommandEncoder& cmd, const ConvRequest& req, con
   pass.End();
 }
 
+void run_dual(const wgpu::CommandEncoder& cmd, const ConvRequest& req, const TextureHandle& dst2,
+              const Range dualUniformRange) {
+  const auto it = g_dualPipelines.find(req.fmt);
+  if (it == g_dualPipelines.end()) {
+    Log.fatal("No dual copy conversion pipeline for format {}", static_cast<int>(req.fmt));
+  }
+  const auto& sampler = req.sampleFilter == SampleFilter::Linear ? g_linearSampler : g_nearestSampler;
+  const std::array bindGroupEntries{
+      wgpu::BindGroupEntry{
+          .binding = 0,
+          .sampler = sampler,
+      },
+      wgpu::BindGroupEntry{
+          .binding = 1,
+          .textureView = req.srcView,
+      },
+      wgpu::BindGroupEntry{
+          .binding = 2,
+          .buffer = detail::resources().uniformBuffer,
+          .offset = dualUniformRange.offset,
+          .size = dualUniformRange.size,
+      },
+  };
+  const wgpu::BindGroupDescriptor bindGroupDescriptor{
+      .layout = g_bindGroupLayout,
+      .entryCount = bindGroupEntries.size(),
+      .entries = bindGroupEntries.data(),
+  };
+  const auto bindGroup = g_device.CreateBindGroup(&bindGroupDescriptor);
+  const std::array colorAttachments{
+      wgpu::RenderPassColorAttachment{
+          .view = req.dst->attachmentTextureView,
+          .loadOp = wgpu::LoadOp::Clear,
+          .storeOp = wgpu::StoreOp::Store,
+          .clearValue = {0.0, 0.0, 0.0, 0.0},
+      },
+      wgpu::RenderPassColorAttachment{
+          .view = dst2->attachmentTextureView,
+          .loadOp = wgpu::LoadOp::Clear,
+          .storeOp = wgpu::StoreOp::Store,
+          .clearValue = {0.0, 0.0, 0.0, 0.0},
+      },
+  };
+  const wgpu::RenderPassDescriptor renderPassDescriptor{
+      .label = "TexCopyConv Dual Pass",
+      .colorAttachmentCount = colorAttachments.size(),
+      .colorAttachments = colorAttachments.data(),
+      .timestampWrites = webgpu::gpu_prof::pass_writes("EFB copy convert"),
+  };
+  const auto pass = cmd.BeginRenderPass(&renderPassDescriptor);
+  pass.SetPipeline(it->second);
+  pass.SetBindGroup(0, bindGroup);
+  pass.Draw(3);
+  pass.End();
+}
+
 void run(const wgpu::CommandEncoder& cmd, const ConvRequest& req) {
   const auto it = g_pipelines.find(req.fmt);
   if (it == g_pipelines.end()) {
diff --git a/lib/gfx/tex_copy_conv.hpp b/lib/gfx/tex_copy_conv.hpp
index 34fc5f4..2d67e61 100644
--- a/lib/gfx/tex_copy_conv.hpp
+++ b/lib/gfx/tex_copy_conv.hpp
@@ -25,6 +25,10 @@ void initialize();
 void shutdown();
 void run(const wgpu::CommandEncoder& cmd, const ConvRequest& req);
 void blit(const wgpu::CommandEncoder& cmd, const ConvRequest& req);
+// Two same-format, unscaled conversions from one source in a single pass with
+// two color targets. `dualUniformRange` holds both UV transforms (32 bytes).
+bool dual_supported(GXTexFmt fmt);
+void run_dual(const wgpu::CommandEncoder& cmd, const ConvRequest& req, const TextureHandle& dst2, Range dualUniformRange);
 
 bool snapshot_depth_supported() noexcept;
 void snapshot_depth(const wgpu::CommandEncoder& cmd, const wgpu::TextureView& srcDepth, uint32_t msaaSamples,
diff --git a/lib/gfx/tex_palette_conv.cpp b/lib/gfx/tex_palette_conv.cpp
index 392dcc8..a0914c7 100644
--- a/lib/gfx/tex_palette_conv.cpp
+++ b/lib/gfx/tex_palette_conv.cpp
@@ -100,6 +100,17 @@ static PipelineInfo create_pipeline(std::string_view fragBindingsAndShader, wgpu
   shaderSource.reserve(ShaderPreambleVtx.size() + fragBindingsAndShader.size());
   shaderSource += ShaderPreambleVtx;
   shaderSource += fragBindingsAndShader;
+  // Texture arrays: the source (an EFB copy texture) is a 2D array; read layer 0.
+  const bool srcArray = texture_arrays_enabled();
+  if (srcArray) {
+    const auto replace = [&](std::string_view from, std::string_view to) {
+      for (size_t pos = shaderSource.find(from); pos != std::string::npos; pos = shaderSource.find(from, pos + to.size()))
+        shaderSource.replace(pos, from.size(), to);
+    };
+    replace("var src: texture_2d<i32>;", "var src: texture_2d_array<i32>;");
+    replace("var src: texture_2d<f32>;", "var src: texture_2d_array<f32>;");
+    replace("textureLoad(src, coord, 0)", "textureLoad(src, coord, 0, 0)");
+  }
 
   wgpu::ShaderSourceWGSL wgslSource{};
   wgslSource.code = shaderSource.c_str();
@@ -124,7 +135,7 @@ static PipelineInfo create_pipeline(std::string_view fragBindingsAndShader, wgpu
           .texture =
               wgpu::TextureBindingLayout{
                   .sampleType = srcSampleType,
-                  .viewDimension = wgpu::TextureViewDimension::e2D,
+                  .viewDimension = srcArray ? wgpu::TextureViewDimension::e2DArray : wgpu::TextureViewDimension::e2D,
               },
       },
       wgpu::BindGroupLayoutEntry{
diff --git a/lib/gfx/texture.cpp b/lib/gfx/texture.cpp
index 029a3d0..cbd2c5e 100644
--- a/lib/gfx/texture.cpp
+++ b/lib/gfx/texture.cpp
@@ -10,8 +10,13 @@
 
 #include <algorithm>
 #include <cstdint>
+#include <cstdlib>
+#include <cstring>
 #include <memory>
+#include <mutex>
+#include <unordered_map>
 #include <utility>
+#include <vector>
 
 #include <fmt/format.h>
 #include <tracy/Tracy.hpp>
@@ -21,9 +26,124 @@ namespace aurora::gfx {
 using webgpu::g_device;
 using webgpu::g_queue;
 
+bool texture_arrays_enabled() noexcept {
+#ifdef MELEE_MIYOO_FLIP
+  static const bool enabled = [] {
+    const char* v = std::getenv("MELEE_FLIP_TEXTURE_ARRAYS");
+    return v == nullptr || std::strcmp(v, "0") != 0;
+  }();
+  return enabled;
+#else
+  return false;
+#endif
+}
+
 namespace {
 constexpr Module Log{"aurora::gfx"};
 
+// Layer pool for texture arrays. Slabs are 2D array textures; a GX texture
+// takes one layer and returns it when its TextureRef dies.
+struct ArraySlab {
+  wgpu::Texture texture;
+  wgpu::TextureView view;
+  uint32_t layers = 0;
+  std::vector<uint32_t> freeLayers;
+};
+struct ArrayKey {
+  uint32_t width, height, mips;
+  wgpu::TextureFormat format;
+  u32 gxFormat;
+  uint64_t groupKey;
+  bool operator==(const ArrayKey& o) const {
+    return width == o.width && height == o.height && mips == o.mips && format == o.format && gxFormat == o.gxFormat &&
+           groupKey == o.groupKey;
+  }
+};
+struct ArrayKeyHash {
+  size_t operator()(const ArrayKey& k) const {
+    uint64_t h = k.groupKey * 0x9E3779B97F4A7C15ull;
+    h ^= (uint64_t(k.width) << 40) ^ (uint64_t(k.height) << 20) ^ (uint64_t(k.mips) << 12) ^ uint64_t(k.format) ^
+         (uint64_t(k.gxFormat) << 48);
+    return static_cast<size_t>(h ^ (h >> 29));
+  }
+};
+std::mutex g_arrayMutex;
+std::unordered_map<ArrayKey, std::vector<std::shared_ptr<ArraySlab>>, ArrayKeyHash> g_arraySlabs;
+struct ArrayStats { uint32_t slabs = 0, layersUsed = 0; } g_arrayStats;
+
+bool setup_swizzle(wgpu::TextureComponentSwizzleDescriptor& swizzle, u32 format) noexcept;
+const wgpu::TextureBindingViewDimension* array_binding_chain();
+
+std::pair<std::shared_ptr<ArraySlab>, uint32_t> allocate_array_layer(const ArrayKey& key, const char* label) {
+  std::lock_guard lock{g_arrayMutex};
+  auto& slabs = g_arraySlabs[key];
+  for (auto& slab : slabs) {
+    if (!slab->freeLayers.empty()) {
+      const uint32_t layer = slab->freeLayers.back();
+      slab->freeLayers.pop_back();
+      ++g_arrayStats.layersUsed;
+      return {slab, layer};
+    }
+  }
+  // Grow geometrically per class (2, 4, 8, ... layers, at most ~2 MiB per slab
+  // and 32 layers) so memory tracks the textures actually in use.
+  const uint64_t layerBytes = std::max<uint64_t>(calc_texture_size(key.format, key.width, key.height, key.mips), 1);
+  const uint32_t byBudget = static_cast<uint32_t>(std::clamp<uint64_t>((2u << 20) / layerBytes, 1, 32));
+  // First slab: small textures start with room for neighbours (merges need
+  // textures of one class in one slab), large ones start alone.
+  const uint64_t area = uint64_t(key.width) * key.height;
+  uint32_t layers = area <= 64 * 64 ? 4 : area <= 128 * 128 ? 2 : 1;
+  if (!slabs.empty()) layers = std::min<uint32_t>(slabs.back()->layers * 2, 32);
+  layers = std::max<uint32_t>(std::min(layers, byBudget), 1);
+  auto slab = std::make_shared<ArraySlab>();
+  slab->layers = layers;
+  const std::string slabLabel = fmt::format("{} array x{}", label, layers);
+  const wgpu::TextureDescriptor textureDescriptor{
+      .nextInChain = array_binding_chain(),
+      .label = slabLabel.c_str(),
+      .usage = wgpu::TextureUsage::TextureBinding | wgpu::TextureUsage::CopyDst,
+      .dimension = wgpu::TextureDimension::e2D,
+      .size = {key.width, key.height, layers},
+      .format = key.format,
+      .mipLevelCount = key.mips,
+      .sampleCount = 1,
+  };
+  slab->texture = g_device.CreateTexture(&textureDescriptor);
+  wgpu::TextureViewDescriptor viewDescriptor{
+      .label = slabLabel.c_str(),
+      .format = key.format,
+      .dimension = wgpu::TextureViewDimension::e2DArray,
+      .mipLevelCount = key.mips,
+      .arrayLayerCount = layers,
+  };
+  wgpu::TextureComponentSwizzleDescriptor swizzle;
+  if (setup_swizzle(swizzle, key.gxFormat)) {
+    viewDescriptor.nextInChain = &swizzle;
+  }
+  slab->view = slab->texture.CreateView(&viewDescriptor);
+  for (uint32_t layer = layers; layer-- > 1;) slab->freeLayers.push_back(layer);
+  slabs.push_back(slab);
+  ++g_arrayStats.slabs;
+  ++g_arrayStats.layersUsed;
+  return {slab, 0u};
+}
+
+void release_array_layer(const std::shared_ptr<ArraySlab>& slab, uint32_t layer) {
+  std::lock_guard lock{g_arrayMutex};
+  slab->freeLayers.push_back(layer);
+  --g_arrayStats.layersUsed;
+}
+
+// Compatibility mode (GLES): declare the texture's binding view dimension so
+// Dawn creates a GL_TEXTURE_2D_ARRAY object even for a single layer, matching
+// the array samplers in GX shaders without wasting a second layer.
+const wgpu::TextureBindingViewDimension* array_binding_chain() {
+  static const wgpu::TextureBindingViewDimension chain{wgpu::TextureBindingViewDimension::Init{
+      .textureBindingViewDimension = wgpu::TextureViewDimension::e2DArray,
+  }};
+  return webgpu::g_hasCoreFeatures ? nullptr : &chain;
+}
+
 wgpu::Extent3D physical_size(wgpu::Extent3D size, TextureFormatInfo info) {
   const uint32_t width = ((size.width + info.blockWidth - 1) / info.blockWidth) * info.blockWidth;
   const uint32_t height = ((size.height + info.blockHeight - 1) / info.blockHeight) * info.blockHeight;
@@ -98,10 +218,11 @@ u16 wgpu_aniso(GXAnisotropy aniso) {
 } // namespace
 
 TextureHandle new_static_texture_2d(uint32_t width, uint32_t height, uint32_t mips, u32 format, ArrayRef<uint8_t> data,
-                                    bool tlut, const char* label) noexcept {
+                                    bool tlut, const char* label, uint64_t groupKey) noexcept {
   ZoneScoped;
 
-  auto handle = new_dynamic_texture_2d(width, height, mips, format, label);
+  // TLUTs are read by the palette conversion shaders as plain 2D textures.
+  auto handle = new_dynamic_texture_2d(width, height, mips, format, label, tlut ? UINT64_MAX : groupKey);
   auto& ref = *handle;
 
   ConvertedTexture converted;
@@ -137,6 +258,7 @@ TextureHandle new_static_texture_2d(uint32_t width, uint32_t height, uint32_t mi
     const wgpu::TexelCopyTextureInfo dstView{
         .texture = ref.texture,
         .mipLevel = mip,
+        .origin = {0, 0, ref.layer},
     };
     if constexpr (UseTextureBuffer) {
       queue_texture_upload_data(data.data() + offset, bytesPerRow, heightBlocks, std::move(dstView), physicalSize);
@@ -156,7 +278,7 @@ TextureHandle new_static_texture_2d(uint32_t width, uint32_t height, uint32_t mi
 }
 
 TextureHandle new_dynamic_texture_2d(uint32_t width, uint32_t height, uint32_t mips, u32 gxFormat,
-                                     const char* label) noexcept {
+                                     const char* label, uint64_t groupKey) noexcept {
   ZoneScopedS(3);
   const auto wgpuFormat = to_wgpu(gxFormat);
   const wgpu::Extent3D size{
@@ -164,6 +286,15 @@ TextureHandle new_dynamic_texture_2d(uint32_t width, uint32_t height, uint32_t m
       .height = height,
       .depthOrArrayLayers = 1,
   };
+  if (texture_arrays_enabled() && groupKey != UINT64_MAX) {
+    const ArrayKey key{width, height, mips, wgpuFormat, gxFormat, groupKey};
+    auto [slab, layer] = allocate_array_layer(key, label);
+    auto handle = std::make_shared<TextureRef>(slab->texture, slab->view, wgpu::TextureView{}, size, wgpuFormat, mips,
+                                               gxFormat);
+    handle->layer = layer;
+    handle->onRelease = [slab, layer] { release_array_layer(slab, layer); };
+    return handle;
+  }
   const wgpu::TextureDescriptor textureDescriptor{
       .label = label,
       .usage = wgpu::TextureUsage::TextureBinding | wgpu::TextureUsage::CopyDst,
@@ -190,18 +321,18 @@ TextureHandle new_dynamic_texture_2d(uint32_t width, uint32_t height, uint32_t m
                                       mips, gxFormat);
 }
 
-TextureHandle new_render_texture(uint32_t width, uint32_t height, u32 gxFormat, const char* label) noexcept {
-  ZoneScoped;
-
-  const auto wgpuFormat = webgpu::g_graphicsConfig.surfaceConfiguration.format;
+static TextureHandle new_sampled_target(uint32_t width, uint32_t height, wgpu::TextureFormat wgpuFormat, u32 gxFormat,
+                                        wgpu::TextureUsage usage, const char* label) noexcept {
+  const bool arrays = texture_arrays_enabled();
   const wgpu::Extent3D size{
       .width = width,
       .height = height,
       .depthOrArrayLayers = 1,
   };
   const wgpu::TextureDescriptor textureDescriptor{
+      .nextInChain = arrays ? array_binding_chain() : nullptr,
       .label = label,
-      .usage = wgpu::TextureUsage::TextureBinding | wgpu::TextureUsage::CopyDst | wgpu::TextureUsage::RenderAttachment,
+      .usage = usage,
       .dimension = wgpu::TextureDimension::e2D,
       .size = size,
       .format = wgpuFormat,
@@ -210,50 +341,43 @@ TextureHandle new_render_texture(uint32_t width, uint32_t height, u32 gxFormat,
   };
   auto texture = g_device.CreateTexture(&textureDescriptor);
 
-  // Create texture view for color attachments
+  // Color attachment view: layer 0 as a 2D view. Sample view: the whole array
+  // when GX shaders sample arrays, else the same 2D view.
   const auto viewLabel = fmt::format("{} view", label);
-  wgpu::TextureViewDescriptor textureViewDescriptor{
+  wgpu::TextureViewDescriptor attachmentDescriptor{
       .label = viewLabel.c_str(),
       .format = wgpuFormat,
       .dimension = wgpu::TextureViewDimension::e2D,
+      .arrayLayerCount = 1,
   };
-  auto attachmentTextureView = texture.CreateView(&textureViewDescriptor);
+  auto attachmentTextureView = texture.CreateView(&attachmentDescriptor);
   wgpu::TextureView sampleTextureView = attachmentTextureView;
+  if (arrays) {
+    const auto sampleLabel = fmt::format("{} array view", label);
+    wgpu::TextureViewDescriptor sampleDescriptor{
+        .label = sampleLabel.c_str(),
+        .format = wgpuFormat,
+        .dimension = wgpu::TextureViewDimension::e2DArray,
+        .arrayLayerCount = 1,
+    };
+    sampleTextureView = texture.CreateView(&sampleDescriptor);
+  }
   return std::make_shared<TextureRef>(std::move(texture), std::move(sampleTextureView),
                                       std::move(attachmentTextureView), size, wgpuFormat, 1, gxFormat);
 }
 
-TextureHandle new_conv_texture(uint32_t width, uint32_t height, u32 gxFormat, const char* label) noexcept {
+TextureHandle new_render_texture(uint32_t width, uint32_t height, u32 gxFormat, const char* label) noexcept {
   ZoneScoped;
+  return new_sampled_target(width, height, webgpu::g_graphicsConfig.surfaceConfiguration.format, gxFormat,
+                            wgpu::TextureUsage::TextureBinding | wgpu::TextureUsage::CopyDst |
+                                wgpu::TextureUsage::RenderAttachment,
+                            label);
+}
 
-  const auto wgpuFormat = to_wgpu(gxFormat);
-  const wgpu::Extent3D size{
-      .width = width,
-      .height = height,
-      .depthOrArrayLayers = 1,
-  };
-  const wgpu::TextureDescriptor textureDescriptor{
-      .label = label,
-      .usage = wgpu::TextureUsage::TextureBinding | wgpu::TextureUsage::RenderAttachment,
-      .dimension = wgpu::TextureDimension::e2D,
-      .size = size,
-      .format = wgpuFormat,
-      .mipLevelCount = 1,
-      .sampleCount = 1,
-  };
-  auto texture = g_device.CreateTexture(&textureDescriptor);
-
-  // Create texture view for color attachments
-  const auto viewLabel = fmt::format("{} view", label);
-  wgpu::TextureViewDescriptor textureViewDescriptor{
-      .label = viewLabel.c_str(),
-      .format = wgpuFormat,
-      .dimension = wgpu::TextureViewDimension::e2D,
-  };
-  auto attachmentTextureView = texture.CreateView(&textureViewDescriptor);
-  wgpu::TextureView sampleTextureView = attachmentTextureView;
-  return std::make_shared<TextureRef>(std::move(texture), std::move(sampleTextureView),
-                                      std::move(attachmentTextureView), size, wgpuFormat, 1, gxFormat);
+TextureHandle new_conv_texture(uint32_t width, uint32_t height, u32 gxFormat, const char* label) noexcept {
+  ZoneScoped;
+  return new_sampled_target(width, height, to_wgpu(gxFormat), gxFormat,
+                            wgpu::TextureUsage::TextureBinding | wgpu::TextureUsage::RenderAttachment, label);
 }
 
 void write_texture(TextureRef& ref, ArrayRef<uint8_t> data) noexcept {
@@ -286,6 +410,7 @@ void write_texture(TextureRef& ref, ArrayRef<uint8_t> data) noexcept {
     const wgpu::TexelCopyTextureInfo dstView{
         .texture = ref.texture,
         .mipLevel = mip,
+        .origin = {0, 0, ref.layer},
     };
     if constexpr (UseTextureBuffer) {
       queue_texture_upload_data(data.data() + offset, bytesPerRow, heightBlocks, std::move(dstView), physicalSize);
diff --git a/lib/gfx/texture.hpp b/lib/gfx/texture.hpp
index 0a5ca83..57071e4 100644
--- a/lib/gfx/texture.hpp
+++ b/lib/gfx/texture.hpp
@@ -1,4 +1,6 @@
 #pragma once
+
+#include <functional>
 #include <dolphin/gx.h>
 
 #include <utility>
@@ -43,6 +45,13 @@ struct TextureRef {
   u32 gxFormat;
   bool hasArbitraryMips = false;
   bool isReplacement = false;
+  // Texture arrays (Flip): this texture lives in `layer` of a shared 2D array
+  // texture; `sampleTextureView` is the array view shared by all its layers.
+  uint32_t layer = 0;
+  std::function<void()> onRelease;
+  ~TextureRef() {
+    if (onRelease) onRelease();
+  }
 
   TextureRef(wgpu::Texture texture, wgpu::TextureView sampleTextureView, wgpu::TextureView attachmentTextureView,
              wgpu::Extent3D size, wgpu::TextureFormat format, uint32_t mipCount, u32 gxFormat)
@@ -55,10 +64,15 @@ struct TextureRef {
   , gxFormat(gxFormat) {}
 };
 
+// MELEE_FLIP_TEXTURE_ARRAYS (Flip, default on): GX-sampled textures are layers
+// of shared 2D array textures grouped by size/format/sampler (`groupKey`), so
+// draws that differ only by texture share a bind group and can merge.
+bool texture_arrays_enabled() noexcept;
 TextureHandle new_static_texture_2d(uint32_t width, uint32_t height, uint32_t mips, u32 gxFormat,
-                                    ArrayRef<uint8_t> data, bool tlut, const char* label) noexcept;
+                                    ArrayRef<uint8_t> data, bool tlut, const char* label,
+                                    uint64_t groupKey = 0) noexcept;
 TextureHandle new_dynamic_texture_2d(uint32_t width, uint32_t height, uint32_t mips, u32 gxFormat,
-                                     const char* label) noexcept;
+                                     const char* label, uint64_t groupKey = 0) noexcept;
 TextureHandle new_render_texture(uint32_t width, uint32_t height, u32 gxFormat, const char* label) noexcept;
 TextureHandle new_conv_texture(uint32_t width, uint32_t height, u32 gxFormat, const char* label) noexcept;
 void write_texture(TextureRef& ref, ArrayRef<uint8_t> data) noexcept;
diff --git a/lib/gx/command_processor.cpp b/lib/gx/command_processor.cpp
index b0dce17..5cc659f 100644
--- a/lib/gx/command_processor.cpp
+++ b/lib/gx/command_processor.cpp
@@ -1,20 +1,32 @@
+#ifdef MELEE_MIYOO_FLIP
+#include "flip_vertex.hpp"
+#include "flip_resident.hpp"
+#include "../gfx/flip_profile.hpp"
+#endif
 #include "command_processor.hpp"
 
 #include "../gfx/depth_peek.hpp"
 #include "../gfx/recording.hpp"
+#include "../gfx/frame.hpp"
+#include "../gfx/hash.hpp"
+#include "../gfx/resource_cache.hpp"
 #include "../internal.hpp"
 #include "dolphin/gd/GDGeometry.h"
 #include "dolphin/gx/GXAurora.h"
+#include "fifo.hpp"
 #include "gx.hpp"
 #include "pipeline.hpp"
 #include "regs.hpp"
 #include "shader_info.hpp"
 #include "texture.hpp"
 
+#include <unordered_map>
 #include <tracy/Tracy.hpp>
 
 #include <algorithm>
 #include <cmath>
+#include <cstdio>
+#include <cstdlib>
 #include <cstdint>
 #include <span>
 #include <vector>
@@ -23,6 +35,95 @@ namespace aurora::gx::fifo {
 namespace {
 constexpr Module Log{"aurora::gx::fifo"};
 
+#ifdef MELEE_MIYOO_FLIP
+struct FlipBatchStats {
+  uint64_t attempts = 0;
+  uint64_t merged = 0;
+  // Resident-draw merge failures, first mismatch only.
+  uint64_t rNoPrev = 0, rKind = 0, rPipeline = 0, rTexture = 0, rRecord = 0, rDstAlpha = 0, rFog = 0, rVtxStart = 0, rIndexGap = 0;
+  uint64_t noPreviousDraw = 0;
+  uint64_t pipeline = 0;
+  uint64_t pipelineShader = 0;
+  uint64_t shaderAttrs = 0;
+  uint64_t shaderTev = 0;
+  uint64_t shaderTevOnly = 0;
+  uint64_t shaderChannels = 0;
+  uint64_t shaderTcgs = 0;
+  uint64_t shaderAlpha = 0;
+  uint64_t shaderIndirect = 0;
+  uint64_t shaderFog = 0;
+  uint64_t shaderLine = 0;
+  uint64_t pipelineBlend = 0;
+  uint64_t pipelineDepth = 0;
+  uint64_t pipelineCull = 0;
+  uint64_t pipelineWrites = 0;
+  uint64_t pipelineOther = 0;
+  uint64_t texture = 0;
+  uint64_t dstAlpha = 0;
+  uint64_t fog = 0;
+  uint64_t uniformWindow = 0;
+  uint64_t vertexGap = 0;
+  uint64_t vertexLimit = 0;
+  uint64_t indexGap = 0;
+};
+FlipBatchStats sFlipBatchStats;
+PipelineConfig sFlipPreviousPipeline{};
+bool sFlipHasPreviousPipeline=false;
+
+ShaderConfig flip_canonical_shader(ShaderConfig shader) {
+  shader.vtxStride=0;
+  for(auto& attr:shader.attrs) {
+    if(attr.attrType==GX_NONE) continue;
+    attr.attrType=GX_DIRECT; attr.compType=GX_F32;
+    attr.offset=attr.stride=attr.frac=0; attr.le=attr.nbt3=false;
+  }
+  return shader;
+}
+
+void classify_flip_pipeline(const PipelineConfig& current) {
+  if(!sFlipHasPreviousPipeline) { ++sFlipBatchStats.pipelineOther; return; }
+  const auto a=flip_canonical_shader(sFlipPreviousPipeline.shaderConfig);
+  const auto b=flip_canonical_shader(current.shaderConfig);
+  if(a!=b) {
+    ++sFlipBatchStats.pipelineShader;
+    unsigned shaderMask=0;
+    if(std::memcmp(a.attrs.data(),b.attrs.data(),sizeof(a.attrs))) { ++sFlipBatchStats.shaderAttrs; shaderMask|=1; }
+    if(std::memcmp(a.tevSwapTable.data(),b.tevSwapTable.data(),sizeof(a.tevSwapTable)) ||
+       std::memcmp(a.tevStages.data(),b.tevStages.data(),sizeof(a.tevStages)) || a.tevStageCount!=b.tevStageCount) {
+      ++sFlipBatchStats.shaderTev; shaderMask|=2;
+    }
+    if(std::memcmp(a.colorChannels.data(),b.colorChannels.data(),sizeof(a.colorChannels))) {
+      ++sFlipBatchStats.shaderChannels; shaderMask|=4;
+    }
+    if(std::memcmp(a.tcgs.data(),b.tcgs.data(),sizeof(a.tcgs))) { ++sFlipBatchStats.shaderTcgs; shaderMask|=8; }
+    if(a.alphaCompare!=b.alphaCompare) { ++sFlipBatchStats.shaderAlpha; shaderMask|=16; }
+    if(std::memcmp(a.indStages.data(),b.indStages.data(),sizeof(a.indStages)) || a.numIndStages!=b.numIndStages) {
+      ++sFlipBatchStats.shaderIndirect; shaderMask|=32;
+    }
+    if(a.fogType!=b.fogType || a.fogRangeEnabled!=b.fogRangeEnabled) {
+      ++sFlipBatchStats.shaderFog; shaderMask|=64;
+    }
+    if(a.lineMode!=b.lineMode) { ++sFlipBatchStats.shaderLine; shaderMask|=128; }
+    if(shaderMask==2) ++sFlipBatchStats.shaderTevOnly;
+  }
+  else if(sFlipPreviousPipeline.blendMode!=current.blendMode ||
+          sFlipPreviousPipeline.blendFacSrc!=current.blendFacSrc ||
+          sFlipPreviousPipeline.blendFacDst!=current.blendFacDst ||
+          sFlipPreviousPipeline.blendOp!=current.blendOp ||
+          sFlipPreviousPipeline.dstAlpha!=current.dstAlpha) ++sFlipBatchStats.pipelineBlend;
+  else if(sFlipPreviousPipeline.depthFunc!=current.depthFunc ||
+          sFlipPreviousPipeline.depthCompare!=current.depthCompare ||
+          sFlipPreviousPipeline.depthUpdate!=current.depthUpdate) ++sFlipBatchStats.pipelineDepth;
+  else if(sFlipPreviousPipeline.cullMode!=current.cullMode ||
+          sFlipPreviousPipeline.polygonOffsetBits!=current.polygonOffsetBits ||
+          sFlipPreviousPipeline.polygonOffsetScaleBits!=current.polygonOffsetScaleBits ||
+          sFlipPreviousPipeline.polygonOffsetClampBits!=current.polygonOffsetClampBits) ++sFlipBatchStats.pipelineCull;
+  else if(sFlipPreviousPipeline.alphaUpdate!=current.alphaUpdate ||
+          sFlipPreviousPipeline.colorUpdate!=current.colorUpdate) ++sFlipBatchStats.pipelineWrites;
+  else ++sFlipBatchStats.pipelineOther;
+}
+#endif
+
 u16 prepare_idx_buffer(ByteBuffer& buf, GXPrimitive prim, u16 vtxStart, u16 vtxCount) noexcept {
   u16 numIndices = 0;
   if (prim == GX_QUADS) {
@@ -130,6 +231,7 @@ struct DrawCache {
   PipelineConfig config{};
   ShaderInfo shaderInfo{};
   gfx::PipelineRef pipelineRef{};
+  gfx::PipelineRef constantRef{};
   GXBindGroups bindGroups{};
   uint64_t bindGeneration = 0;
   GXVtxFmt fmt = GX_MAX_VTXFMT;
@@ -375,33 +477,130 @@ static u32 calc_vtx_size(GXVtxFmt fmt) noexcept {
   return vtxSize;
 }
 
-static void push_gx_draw(GXPrimitive prim, GXVtxFmt fmt, u16 vtxCount, gfx::Range vertRange, gfx::Range idxRange,
-                         u32 numIndices) noexcept {
-  auto& state = g_gxState;
-  auto& cache = sDrawCache;
+#ifdef MELEE_MIYOO_FLIP
+struct PipelineMemo {
+  PipelineConfig config;
+  ShaderInfo shaderInfo;
+  gfx::PipelineRef pipelineRef;
+  gfx::PipelineRef constantRef;
+};
+// Constant-record pipeline variant for draws that never merge
+// (MELEE_FLIP_CONSTANT_RECORDS=1, experiment). Off by default: on the g29 Mali
+// driver every per-draw uniform change (UBO range rebind or glUniform) costs
+// ~15 us, so the render worker lost ~5 ms per frame for ~1 ms of GPU; the
+// vertex-embedded record index stays the default.
+static bool flip_constant_records_enabled() {
+  static const bool enabled = [] { const char* v = std::getenv("MELEE_FLIP_CONSTANT_RECORDS"); return v && std::strcmp(v, "0"); }();
+  return enabled;
+}
+static gfx::PipelineRef request_constant_variant(const PipelineConfig& config, gfx::PipelineRef pipelineRef) {
+  if (!flip_constant_records_enabled() || !(config.shaderConfig.flipFlags & FlipUniformTable) || !(config.shaderConfig.flipFlags & FlipBatchedDraws)) return {};
+  PipelineConfig constant = config;
+  constant.shaderConfig.flipFlags |= FlipConstantRecord;
+  const auto ref = gfx::pipeline_ref_async(constant);
+  register_flip_constant_variant(pipelineRef, ref);
+  return ref;
+}
+static absl::flat_hash_map<uint64_t, PipelineMemo> sPipelineMemo;
 
-  DrawImmediateData immediates{.vtxStart = vertRange.offset, .currentPnMtx = state.currentPnMtx};
-  for (int i = GX_VA_POS; i <= GX_VA_TEX7; ++i) {
-    if (state.vtxDesc[i] != GX_INDEX8 && state.vtxDesc[i] != GX_INDEX16) {
-      continue;
+// Hash of everything populate_pipeline_config() and pipeline_ref() derive their
+// result from, taken from the raw register state (much cheaper than rebuilding).
+static uint64_t flip_pipeline_state_hash(GXPrimitive prim, GXVtxFmt fmt) noexcept {
+  const auto& st = g_gxState;
+  Hasher h;
+  const auto put = [&](const auto& v) { h.update(&v, sizeof(v)); };
+  put(prim); put(fmt);
+  for (unsigned i = 0; i < MaxVtxAttr; ++i) {
+    const u8 type = static_cast<u8>(st.vtxDesc[i]);
+    put(type);
+    if (st.vtxDesc[i] != GX_NONE) {
+      put(st.vtxFmts[fmt].attrs[i]);
+      if (st.vtxDesc[i] == GX_INDEX8 || st.vtxDesc[i] == GX_INDEX16) { put(st.arrays[i].stride); put(st.arrays[i].le); }
     }
-    auto& array = state.arrays[i];
-    if (array.cachedRange.size == 0) {
-      array.cachedRange = gfx::push_storage(static_cast<const uint8_t*>(array.data), array.size);
-    }
-    immediates.arrayStart[i - GX_VA_POS] = array.cachedRange.offset;
   }
+  put(st.fog.type); put(st.fog.rangeEnabled);
+  put(st.tevSwapTable);
+  put(st.numTevStages);
+  for (u8 i = 0; i < st.numTevStages; ++i) put(st.tevStages[i]);
+  put(st.numIndStages);
+  for (u8 i = 0; i < st.numIndStages; ++i) put(st.indStages[i]);
+  put(st.colorChannelConfig);
+  put(st.numTexGens);
+  for (u8 i = 0; i < st.numTexGens; ++i) put(st.tcgs[i]);
+  put(st.alphaCompare);
+  put(st.cullMode); put(st.depthFunc); put(st.blendMode); put(st.blendFacSrc); put(st.blendFacDst); put(st.blendOp);
+  put(st.dstAlpha);
+  put(st.frontOffset); put(st.frontScale); put(st.backOffset); put(st.backScale); put(st.clamp);
+  put(st.depthCompare); put(st.depthUpdate); put(st.alphaUpdate); put(st.colorUpdate);
+  const uint32_t samples = gfx::get_sample_count();
+  put(samples);
+  return h.digest();
+}
+#endif
+
+#ifdef MELEE_MIYOO_FLIP
+// Pipeline variant for point draws recorded into the half-res sprite pass:
+// same shader, premultiplied accumulation blend (FlipSpriteAccum).
+static gfx::PipelineRef flip_sprite_variant(const DrawCache& cache) {
+  static std::unordered_map<gfx::PipelineRef, gfx::PipelineRef> memo;
+  if (const auto it = memo.find(cache.pipelineRef); it != memo.end()) return it->second;
+  auto variant = cache.config;
+  variant.shaderConfig.flipFlags |= FlipSpriteAccum;
+  const auto ref = gfx::pipeline_ref(variant);
+  memo.emplace(cache.pipelineRef, ref);
+  return ref;
+}
+#endif
 
+// Resolve pipeline, textures, uniforms and fog range for the current GX state.
+// Shared by streamed draws and resident display-list draws.
+static void prepare_draw_state(GXPrimitive prim, GXVtxFmt fmt, DrawImmediateData& immediates) noexcept {
+  auto& state = g_gxState;
+  auto& cache = sDrawCache;
   const u8 lineMode = line_mode_for_prim(prim);
   const bool pipelineValid = cache.hasPipeline && (state.dirty & DirtyPipeline) == 0 && cache.fmt == fmt &&
                              cache.lineMode == lineMode && cache.config.msaaSamples == gfx::get_sample_count();
   if (!pipelineValid) {
+#ifdef MELEE_MIYOO_FLIP
+    gfx::detail::flip_profile::Scope profile("pipeline_build");
+#endif
     const bool hadPipeline = cache.hasPipeline;
     const auto prevSampledTextures = cache.shaderInfo.sampledTextures;
     const auto prevSampledIndTextures = cache.shaderInfo.sampledIndTextures;
+#ifdef MELEE_MIYOO_FLIP
+    // Most "dirty pipeline" events re-send an identical material. Hash the raw
+    // pipeline-relevant GX state and reuse the resolved config/shader info/ref.
+    static const bool memoEnabled = [] { const char* v = std::getenv("MELEE_FLIP_PIPELINE_MEMO"); return !v || std::strcmp(v, "0"); }();
+    bool resolved = false;
+    if (memoEnabled) {
+      const uint64_t key = flip_pipeline_state_hash(prim, fmt);
+      auto& memo = sPipelineMemo;
+      if (auto it = memo.find(key); it != memo.end()) {
+        cache.config = it->second.config;
+        cache.shaderInfo = it->second.shaderInfo;
+        cache.pipelineRef = it->second.pipelineRef;
+        cache.constantRef = it->second.constantRef;
+        resolved = true;
+      } else {
+        populate_pipeline_config(cache.config, prim, fmt);
+        cache.shaderInfo = build_shader_info(cache.config.shaderConfig);
+        cache.pipelineRef = gfx::pipeline_ref(cache.config);
+        cache.constantRef = request_constant_variant(cache.config, cache.pipelineRef);
+        if (memo.size() >= 1024) memo.clear();
+        memo.emplace(key, PipelineMemo{cache.config, cache.shaderInfo, cache.pipelineRef, cache.constantRef});
+        resolved = true;
+      }
+    }
+    if (!resolved)
+#endif
+    {
     populate_pipeline_config(cache.config, prim, fmt);
     cache.shaderInfo = build_shader_info(cache.config.shaderConfig);
     cache.pipelineRef = gfx::pipeline_ref(cache.config);
+#ifdef MELEE_MIYOO_FLIP
+    cache.constantRef = request_constant_variant(cache.config, cache.pipelineRef);
+#endif
+    }
     cache.fmt = fmt;
     cache.lineMode = lineMode;
     cache.hasPipeline = true;
@@ -415,20 +614,28 @@ static void push_gx_draw(GXPrimitive prim, GXVtxFmt fmt, u16 vtxCount, gfx::Rang
   const bool bindGroupsValid =
       (state.dirty & DirtyTextures) == 0 && cache.bindGeneration == texture::current_bind_generation();
   if (!bindGroupsValid) {
+#ifdef MELEE_MIYOO_FLIP
+    gfx::detail::flip_profile::Scope profile("texture_resolve_bind");
+#endif
     const auto prevBindGroup = cache.bindGroups.textureBindGroup;
+    const auto prevRefChanges = texture::texture_ref_changes();
     resolve_sampled_textures(cache.shaderInfo);
     cache.bindGroups = build_bind_groups(cache.shaderInfo);
     cache.bindGeneration = texture::current_bind_generation();
     state.dirty &= ~DirtyTextures;
-    // For texture_size_bias uniform
-    if (cache.bindGroups.textureBindGroup != prevBindGroup) {
+    // For texture_size_bias uniform (size, LOD bias and, with texture arrays,
+    // the layer: textures in one array share the bind group).
+    if (cache.bindGroups.textureBindGroup != prevBindGroup || texture::texture_ref_changes() != prevRefChanges) {
       state.dirty |= DirtyUniform;
     }
   }
 
   const bool uniformValid = (state.dirty & DirtyUniform) == 0 && cache.uniformRange.size != 0;
   if (!uniformValid) {
-    cache.uniformRange = build_uniform(cache.shaderInfo);
+#ifdef MELEE_MIYOO_FLIP
+    gfx::detail::flip_profile::Scope profile("uniform_build");
+#endif
+    cache.uniformRange = build_uniform(cache.shaderInfo, cache.config.shaderConfig);
     state.dirty &= ~DirtyUniform;
   }
   if (cache.config.shaderConfig.fogRangeEnabled) {
@@ -442,6 +649,130 @@ static void push_gx_draw(GXPrimitive prim, GXVtxFmt fmt, u16 vtxCount, gfx::Rang
   immediates.fogRangeBase = cache.fogRange.offset / sizeof(u32);
 
   state.dirty &= ~DirtyImmediates;
+}
+
+static void push_gx_draw(GXPrimitive prim, GXVtxFmt fmt, u16 vtxCount, gfx::Range vertRange, gfx::Range idxRange,
+                         u32 numIndices, [[maybe_unused]] std::span<const u8> flipRaw = {},
+                         [[maybe_unused]] std::span<const u8> flipIndices = {}) noexcept {
+  auto& state = g_gxState;
+  auto& cache = sDrawCache;
+
+#ifdef MELEE_MIYOO_FLIP
+  gfx::detail::flip_profile::Scope drawProfile("draw_prepare");
+#endif
+
+  DrawImmediateData immediates{.vtxStart = vertRange.offset, .currentPnMtx = state.currentPnMtx};
+  for (int i = GX_VA_POS; i <= GX_VA_TEX7; ++i) {
+#ifdef MELEE_MIYOO_FLIP
+    if(flip_vertex_enabled()) break; // CPU decoder already resolved these arrays.
+#endif
+    if (state.vtxDesc[i] != GX_INDEX8 && state.vtxDesc[i] != GX_INDEX16) {
+      continue;
+    }
+    auto& array = state.arrays[i];
+    if (array.cachedRange.size == 0) {
+      array.cachedRange = gfx::push_storage(static_cast<const uint8_t*>(array.data), array.size);
+    }
+    immediates.arrayStart[i - GX_VA_POS] = array.cachedRange.offset;
+  }
+
+  prepare_draw_state(prim, fmt, immediates);
+  const u8 lineMode = line_mode_for_prim(prim);
+#ifdef MELEE_MIYOO_FLIP
+  if(flip_batch_enabled() && !flipRaw.empty()) {
+    // Diagnostic (MELEE_FLIP_SKIP_POINTS=1): drop GX_POINTS draws to attribute
+    // their GPU cost (Fountain of Dreams sprites). Renders incorrectly.
+    static const bool skipPoints=[] {const char* v=std::getenv("MELEE_FLIP_SKIP_POINTS");return v&&v[0]=='1';}();
+    if(skipPoints&&lineMode==3) return;
+    const unsigned record=(cache.uniformRange.offset % FlipUniformWindow)/FlipUniformStride;
+    // Instanced points: vtxCount stays the point count; each point is one
+    // instance of a shared 6-index quad, and merges add instances.
+    const bool instancedPoints=lineMode==3&&(cache.config.shaderConfig.flipFlags&FlipInstancedPoints);
+    // Half-res sprite pass: eligible point draws in a sprite-heavy frame render
+    // into the half-res target with the accumulation pipeline variant.
+    bool spriteAccum=false;
+    if(instancedPoints) spriteAccum=gfx::flip_sprite_point_draw(cache.config,vtxCount);
+    else gfx::flip_sprite_settle();
+    const gfx::PipelineRef drawPipeline=spriteAccum?flip_sprite_variant(cache):cache.pipelineRef;
+    if(lineMode&&!instancedPoints) {
+      const uint32_t instances=lineMode==3 ? vtxCount : lineMode==1 ? vtxCount/2 : vtxCount-1;
+      AURORA_ASSERT(instances<=16383,"Too many expanded GX lines/points");
+      vtxCount=instances*4;
+    }
+    auto* previous=gfx::get_last_draw_command<DrawData>();
+    ++sFlipBatchStats.attempts;
+    bool merge=false;
+    unsigned textureBank=0;
+    if(!previous) ++sFlipBatchStats.noPreviousDraw;
+    else if(previous->pipeline!=drawPipeline) { ++sFlipBatchStats.pipeline; classify_flip_pipeline(cache.config); }
+    else if(previous->bindGroups.textureBindGroup==cache.bindGroups.textureBindGroup) merge=true;
+    else if(previous->bindGroups.textureBindGroup && cache.bindGroups.textureBindGroup) {
+      if(flip_texture_pairs_enabled()) {
+        const int existingBank=gfx::texture_bind_group_bank(previous->bindGroups.textureBindGroup,
+                                                            cache.bindGroups.textureBindGroup);
+        if(existingBank>=0) {
+          merge=true; textureBank=static_cast<unsigned>(existingBank);
+        } else {
+          int appendedBank=-1;
+          const uint32_t bankEntries=static_cast<uint32_t>(cache.shaderInfo.sampledTextures.count())*2;
+          auto combined=gfx::append_texture_bind_group_ref(previous->bindGroups.textureBindGroup,
+                                                            cache.bindGroups.textureBindGroup,
+                                                            bankEntries,appendedBank);
+          if(combined) {
+            previous->bindGroups.textureBindGroup=combined;
+            merge=true; textureBank=static_cast<unsigned>(appendedBank);
+          } else ++sFlipBatchStats.texture;
+        }
+      } else ++sFlipBatchStats.texture;
+    } else ++sFlipBatchStats.texture;
+    if(merge && previous->dstAlpha!=state.dstAlpha) { merge=false; ++sFlipBatchStats.dstAlpha; }
+    else if(merge && previous->immediateData.fogRangeBase!=immediates.fogRangeBase) { merge=false; ++sFlipBatchStats.fog; }
+    else if(merge && previous->uniformRange.offset/FlipUniformWindow!=cache.uniformRange.offset/FlipUniformWindow) { merge=false; ++sFlipBatchStats.uniformWindow; }
+    else if(merge && previous->vtxCount+vtxCount>65535) { merge=false; ++sFlipBatchStats.vertexLimit; }
+    else if(merge && !instancedPoints && !gfx::flip_indices_follow(previous->idxRange)) { merge=false; ++sFlipBatchStats.indexGap; }
+    if(merge) ++sFlipBatchStats.merged;
+    vertRange=flip_push_vertices(prim,fmt,flipRaw.data(),flipRaw.size(),4,record,&cache.config.shaderConfig,textureBank);
+    if(merge && previous->vertRange.offset+previous->vertRange.size!=vertRange.offset) {
+      merge=false; ++sFlipBatchStats.vertexGap;
+    }
+    const uint16_t base=merge ? previous->vtxCount : 0;
+    static ByteBuffer indices;
+    indices.clear();
+    if(instancedPoints) {
+      if(!merge) indices.append(std::array<uint16_t,6>{0,1,3,3,2,0});
+    } else if(!flipIndices.empty()) {
+      for(size_t i=0;i<flipIndices.size();i+=2) {
+        uint16_t index;std::memcpy(&index,flipIndices.data()+i,2);
+        AURORA_ASSERT(index<vtxCount,"GX index outside decoded vertices");
+        indices.append<uint16_t>(base+index);
+      }
+    } else if(lineMode) {
+      for(uint32_t i=0;i<vtxCount;i+=4) {
+        const uint16_t b=base+i;
+        indices.append(std::array<uint16_t,6>{b,uint16_t(b+1),uint16_t(b+3),uint16_t(b+3),uint16_t(b+2),b});
+      }
+    } else prepare_idx_buffer(indices,prim,base,vtxCount);
+    numIndices=indices.size()/sizeof(uint16_t);
+    idxRange=gfx::push_indices(indices.data(),indices.size(),merge ? 0 : 4);
+    cache.lastDrawFmt=fmt;
+    if(merge) {
+      previous->vertRange.size+=vertRange.size;
+      if(instancedPoints) previous->instanceCount+=vtxCount;
+      else { previous->idxRange.size+=idxRange.size; previous->indexCount+=numIndices; }
+      previous->vtxCount+=vtxCount;
+      previous->resident&=~ResidentConstantRecord; // records now differ within the draw
+      gfx::detail::increment_merged_draw_count();
+    } else {
+      gfx::push_draw_command(DrawData{.pipeline=drawPipeline,.vertRange=vertRange,.idxRange=idxRange,
+        .uniformRange=cache.uniformRange,.immediateData=immediates,.vtxCount=vtxCount,.indexCount=numIndices,
+        .instanceCount=instancedPoints?vtxCount:1u,.bindGroups=cache.bindGroups,.dstAlpha=state.dstAlpha,
+        .resident=cache.constantRef?ResidentConstantRecord:0u});
+    }
+    sFlipPreviousPipeline=cache.config;
+    sFlipHasPreviousPipeline=true;
+    return;
+  }
+#endif
 
   uint32_t instanceCount = 1;
   if (prim == GX_LINES) {
@@ -451,6 +782,21 @@ static void push_gx_draw(GXPrimitive prim, GXVtxFmt fmt, u16 vtxCount, gfx::Rang
   } else if (prim == GX_POINTS) {
     instanceCount = vtxCount;
   }
+#ifdef MELEE_MIYOO_FLIP
+  if(flip_vertex_enabled() && lineMode) {
+    // Expanded quads retain the GX endpoint selection and screen-space width.
+    // Use 16-bit local indices; split huge inputs rather than silently wrap.
+    AURORA_ASSERT(instanceCount <= 16383, "Too many expanded GX lines/points");
+    static ByteBuffer denseIndices;
+    denseIndices.clear();
+    for(uint32_t i=0;i<instanceCount;++i) {
+      const uint16_t b=i*4;
+      denseIndices.append(std::array<uint16_t,6>{b,uint16_t(b+1),uint16_t(b+3),uint16_t(b+3),uint16_t(b+2),b});
+    }
+    idxRange=gfx::push_indices(denseIndices.data(),denseIndices.size(),4);
+    numIndices=instanceCount*6; vtxCount=instanceCount*4; instanceCount=1;
+  }
+#endif
   cache.lastDrawFmt = fmt;
   gfx::push_draw_command(DrawData{
       .pipeline = cache.pipelineRef,
@@ -494,6 +840,13 @@ static void draw_prim(GXPrimitive prim, GXVtxFmt fmt, u16 vtxCount, ByteReader&
   if (totalVtxBytes > reader.remaining())
     UNLIKELY { handle_draw_overrun(totalVtxBytes, reader); }
 
+#ifdef MELEE_MIYOO_FLIP
+  if(flip_batch_enabled()) {
+    const auto raw=reader.take(totalVtxBytes);
+    push_gx_draw(prim,fmt,vtxCount,{},{},0,raw);
+    return;
+  }
+#endif
   const bool cleanState = g_gxState.dirty == 0 && fmt == sDrawCache.lastDrawFmt && sDrawCache.lineMode == 0 &&
                           prim != GX_LINES && prim != GX_LINESTRIP && prim != GX_POINTS;
   auto* lastDraw = cleanState ? gfx::get_last_draw_command<DrawData>() : nullptr;
@@ -501,7 +854,13 @@ static void draw_prim(GXPrimitive prim, GXVtxFmt fmt, u16 vtxCount, ByteReader&
 
   // Push raw vertex data to buffer. Merged draws must remain contiguous with the previous range.
   const auto vertexData = reader.take(totalVtxBytes);
-  gfx::Range vertRange = gfx::push_verts(vertexData.data(), vertexData.size(), canMerge ? 0 : 4);
+  gfx::Range vertRange =
+#ifdef MELEE_MIYOO_FLIP
+      flip_push_vertices(prim,fmt,vertexData.data(),vertexData.size(),canMerge ? 0 : 4)
+#else
+      gfx::push_verts(vertexData.data(), vertexData.size(), canMerge ? 0 : 4)
+#endif
+;
 
   // Try to merge with previous draw call
   if (canMerge) {
@@ -547,6 +906,265 @@ static void handle_draw(u8 cmd, ByteReader& reader) noexcept {
   draw_prim(prim, fmt, reader.read<u16>(), reader);
 }
 
+
+#ifdef MELEE_MIYOO_FLIP
+namespace {
+// Decode every primitive of a display list into the resident arena for its
+// vertex layout. Returns false when the list must be processed inline instead.
+bool build_resident_entry(const u8* dl, u32 size, resident::Entry& entry) {
+  auto& state = g_gxState;
+  // Determine the vertex format used by the list and validate its contents.
+  u32 pos = 0; int fmtSeen = -1; size_t totalVertices = 0;
+  while (pos < size) {
+    const u8 op = dl[pos];
+    if (op == GX_NOP) { ++pos; continue; }
+    const u8 prim = op & CP_OPCODE_MASK;
+    auto& rstats = resident::state().stats;
+    if (prim != GX_QUADS && prim != GX_TRIANGLES && prim != GX_TRIANGLESTRIP && prim != GX_TRIANGLEFAN) { ++rstats.refusePrim; return false; }
+    if (pos + 3 > size) { ++rstats.refuseMalformed; return false; }
+    const u16 count = u16(dl[pos + 1]) << 8 | dl[pos + 2];
+    const int fmt = op & CP_VAT_MASK;
+    if (fmtSeen >= 0 && fmt != fmtSeen) { ++rstats.refuseFormat; return false; }
+    fmtSeen = fmt;
+    const u32 vtxSize = calc_vtx_size(static_cast<GXVtxFmt>(fmt));
+    if (!vtxSize) { ++rstats.refuseMalformed; return false; }
+    const u64 bytes = u64(count) * vtxSize;
+    if (pos + 3 + bytes > size) { ++rstats.refuseMalformed; return false; }
+    totalVertices += count;
+    pos += 3 + static_cast<u32>(bytes);
+  }
+  if (fmtSeen < 0 || totalVertices == 0 || totalVertices > 0x00FFFFFF) { ++resident::state().stats.refuseEmpty; return false; }
+  PipelineConfig pipeline{};
+  populate_pipeline_config(pipeline, GX_TRIANGLES, static_cast<GXVtxFmt>(fmtSeen));
+  const auto& c = pipeline.shaderConfig;
+  const auto& loader = flip_loader(c);
+  if (!loader.ok || c.lineMode) { ++resident::state().stats.refuseLoader; return false; }
+  if (!loader.hasMatrices) { ++resident::state().stats.refuseMatrices; return false; }
+  const uint32_t stride = loader.layout.stride;
+  uint32_t arenaIndex = 0;
+  auto& arena = resident::arena_for_stride(stride, arenaIndex);
+  const size_t bytes = totalVertices * size_t(stride);
+  const size_t offset = resident::reserve(arena, bytes);
+  entry.arena = arenaIndex;
+  entry.firstVertex = static_cast<uint32_t>(offset / stride);
+  entry.vertexCount = static_cast<uint32_t>(totalVertices);
+  entry.indices.clear();
+  entry.indices.reserve(totalVertices * 3);
+  for (unsigned ai = GX_VA_POS; ai <= GX_VA_TEX7; ++ai)
+    entry.arrays[ai] = (c.attrs[ai].attrType == GX_INDEX8 || c.attrs[ai].attrType == GX_INDEX16) ? state.arrays[ai].data : nullptr;
+  u8* out = arena.mirror.data() + offset;
+  uint32_t base = entry.firstVertex; // indices are stored absolute
+  pos = 0;
+  while (pos < size) {
+    const u8 op = dl[pos];
+    if (op == GX_NOP) { ++pos; continue; }
+    const u8 prim = op & CP_OPCODE_MASK;
+    const u16 count = u16(dl[pos + 1]) << 8 | dl[pos + 2];
+    const u32 vtxSize = state.lastVtxSize;
+    flip_decode_with(loader, dl + pos + 3, count, out + size_t(base - entry.firstVertex) * stride, state.arrays, state.currentPnMtx, 0);
+    auto& idx = entry.indices; // absolute arena indices (entry.firstVertex already added)
+    if (prim == GX_TRIANGLES) {
+      for (u32 v = 0; v < count; ++v) idx.push_back(base + v);
+    } else if (prim == GX_QUADS) {
+      for (u32 v = 0; v + 3 < count; v += 4) {
+        idx.insert(idx.end(), {base + v, base + v + 1, base + v + 2, base + v + 2, base + v + 3, base + v});
+      }
+    } else if (prim == GX_TRIANGLEFAN) {
+      for (u32 v = 0; v < count; ++v) {
+        if (v < 3) idx.push_back(base + v);
+        else idx.insert(idx.end(), {base, base + v - 1, base + v});
+      }
+    } else { // strip
+      for (u32 v = 0; v < count; ++v) {
+        if (v < 3) idx.push_back(base + v);
+        else if ((v & 1) == 0) idx.insert(idx.end(), {base + v - 2, base + v - 1, base + v});
+        else idx.insert(idx.end(), {base + v - 1, base + v - 2, base + v});
+      }
+    }
+    base += count;
+    pos += 3 + count * vtxSize;
+  }
+  resident::publish(arena, offset, bytes);
+  resident::state().stats.bytes += bytes;
+  return true;
+}
+
+// Per-frame record stream: one u32 per resident arena vertex, allocated in the
+// frame's vertex stream on first use. 0xFFFFFFFF marks "not assigned this frame".
+constexpr uint32_t RecordUnassigned = 0xFFFFFFFFu;
+bool ensure_record_region(gfx::detail::FramePacket& frame) {
+  if (frame.flipRecordOffset != UINT32_MAX) return true;
+  static const bool enabled = [] { const char* v = std::getenv("MELEE_FLIP_RESIDENT_RECORDS"); return !v || std::strcmp(v, "0"); }();
+  if (!enabled) return false;
+  auto& rs = resident::state();
+  uint32_t total = 0;
+  for (uint32_t i = 0; i < rs.arenas.size() && i < 8; ++i) {
+    auto& a = *rs.arenas[i];
+    frame.flipRecordArenaBase[i] = total;
+    frame.flipRecordArenaCount[i] = a.stride ? static_cast<uint32_t>(a.used / a.stride) : 0;
+    total += frame.flipRecordArenaCount[i];
+  }
+  if (!total) return false;
+  u8* destination = nullptr;
+  const auto range = gfx::map_flip_verts(size_t(total) * 4, 4, destination);
+  if (!destination) return false;
+  std::memset(destination, 0xFF, range.size);
+  frame.flipRecordShadow.assign(total, RecordUnassigned);
+  frame.flipRecordOffset = range.offset;
+  return true;
+}
+
+// Emit one draw for a resident entry. Draws whose vertices can carry the record
+// through the per-frame record stream merge with the previous resident draw
+// whenever pipeline, textures and uniform window match, exactly like streamed
+// geometry; otherwise the record travels in the immediates and merges only with
+// draws sharing the same record.
+void push_resident_draw(const resident::Entry& entry, GXVtxFmt fmt) {
+  auto& state = g_gxState;
+  auto& cache = sDrawCache;
+  gfx::flip_sprite_settle();
+  gfx::detail::flip_profile::Scope drawProfile("draw_prepare");
+  DrawImmediateData immediates{.vtxStart = 0, .currentPnMtx = state.currentPnMtx};
+  prepare_draw_state(GX_TRIANGLES, fmt, immediates);
+  const uint32_t record = (cache.uniformRange.offset % FlipUniformWindow) / FlipUniformStride;
+  auto& frame = gfx::detail::recording_frame_packet();
+  bool perVertex = false;
+  if (entry.arena < 8 && ensure_record_region(frame)) {
+    const uint32_t base = frame.flipRecordArenaBase[entry.arena];
+    const uint32_t capacity = frame.flipRecordArenaCount[entry.arena];
+    if (entry.firstVertex + entry.vertexCount <= capacity) {
+      auto* records = reinterpret_cast<uint32_t*>(frame.verts.data() + frame.flipRecordOffset) + base + entry.firstVertex;
+      auto* shadow = frame.flipRecordShadow.data() + base + entry.firstVertex;
+      if (shadow[0] == RecordUnassigned) {
+        for (uint32_t i = 0; i < entry.vertexCount; ++i) { records[i] = record; shadow[i] = record; }
+        perVertex = true;
+      } else if (shadow[0] == record) {
+        perVertex = true; // same instance drawn again with the same record
+      } // else: conflicting record this frame; fall back to immediates
+    }
+  }
+  const uint32_t resident = (entry.arena + 1) | (perVertex ? ResidentPerVertexRecord : 0u);
+  if (perVertex) immediates.vtxStart = frame.flipRecordOffset + frame.flipRecordArenaBase[entry.arena] * 4; // slot-1 byte offset
+  auto* previous = gfx::get_last_draw_command<DrawData>();
+  ++sFlipBatchStats.attempts;
+  bool merge = false;
+  {
+    auto& st = sFlipBatchStats;
+    if (!previous) ++st.rNoPrev;
+    else if ((previous->resident & ~ResidentConstantRecord) != resident) ++st.rKind;
+    else if (previous->pipeline != cache.pipelineRef) ++st.rPipeline;
+    else if (previous->bindGroups.textureBindGroup != cache.bindGroups.textureBindGroup) ++st.rTexture;
+    else if (!(perVertex ? previous->uniformRange.offset / FlipUniformWindow == cache.uniformRange.offset / FlipUniformWindow
+                         : previous->uniformRange.offset == cache.uniformRange.offset)) ++st.rRecord;
+    else if (previous->dstAlpha != state.dstAlpha) ++st.rDstAlpha;
+    else if (previous->immediateData.fogRangeBase != immediates.fogRangeBase) ++st.rFog;
+    else if (previous->immediateData.vtxStart != immediates.vtxStart) ++st.rVtxStart;
+    else if (!gfx::flip_indices_follow(previous->idxRange)) ++st.rIndexGap;
+    else merge = true;
+  }
+  // Entries hold absolute 32-bit arena indices: a straight copy per call.
+  const auto& absolute = entry.indices;
+  const auto idxRange = gfx::push_indices(reinterpret_cast<const u8*>(absolute.data()), absolute.size() * 4, merge ? 0 : 4);
+  if (merge && previous->idxRange.offset + previous->idxRange.size != idxRange.offset) merge = false;
+  const auto& arena = *resident::state().arenas[entry.arena];
+  // vertRange for resident draws spans the referenced arena vertices (min..max),
+  // which glDrawRangeElements passes to the driver; Dawn binds the whole arena.
+  const uint32_t stride = arena.stride;
+  const gfx::Range vertRange{entry.firstVertex * stride, entry.vertexCount * stride};
+  if (merge) {
+    ++sFlipBatchStats.merged;
+    previous->resident &= ~ResidentConstantRecord; // records may now differ within the draw
+    previous->idxRange.size += idxRange.size;
+    previous->indexCount += static_cast<uint32_t>(absolute.size());
+    previous->vtxCount += entry.vertexCount;
+    const uint32_t begin = std::min(previous->vertRange.offset, vertRange.offset);
+    const uint32_t end = std::max(previous->vertRange.offset + previous->vertRange.size, vertRange.offset + vertRange.size);
+    previous->vertRange = {begin, end - begin};
+    gfx::detail::increment_merged_draw_count();
+  } else {
+    gfx::push_draw_command(DrawData{.pipeline = cache.pipelineRef, .vertRange = vertRange, .idxRange = idxRange,
+      .uniformRange = cache.uniformRange, .immediateData = immediates, .vtxCount = entry.vertexCount,
+      .indexCount = static_cast<uint32_t>(absolute.size()), .instanceCount = 1, .bindGroups = cache.bindGroups,
+      .dstAlpha = state.dstAlpha, .resident = resident | (cache.constantRef ? ResidentConstantRecord : 0u)});
+  }
+  cache.lastDrawFmt = fmt;
+  sFlipPreviousPipeline = cache.config;
+  sFlipHasPreviousPipeline = true;
+}
+
+// Format key derived from the raw GX state that decodes a display list: vertex
+// descriptors and attribute formats for the list's vertex format, the source
+// arrays (identity, stride, endianness) of indexed attributes, and the matrix
+// slot baked into rigid geometry. Cheaper than populating a full pipeline config.
+uint64_t resident_format_hash(GXVtxFmt fmt) {
+  const auto& state = g_gxState;
+  struct Key {
+    std::array<u8, MaxVtxAttr> desc;
+    std::array<VtxAttrFmt, MaxVtxAttr> attrs;
+    std::array<const void*, MaxVtxAttr> arrays;
+    std::array<u8, MaxVtxAttr> strides;
+    std::array<u8, MaxVtxAttr> little;
+    u32 pnMtx; u8 fmt;
+  } key{};
+  for (unsigned ai = 0; ai < MaxVtxAttr; ++ai) key.desc[ai] = static_cast<u8>(state.vtxDesc[ai]);
+  key.attrs = state.vtxFmts[fmt].attrs;
+  for (unsigned ai = GX_VA_POS; ai <= GX_VA_TEX7; ++ai) {
+    const bool indexed = state.vtxDesc[ai] == GX_INDEX8 || state.vtxDesc[ai] == GX_INDEX16;
+    key.arrays[ai] = indexed ? state.arrays[ai].data : nullptr;
+    key.strides[ai] = indexed ? state.arrays[ai].stride : 0;
+    key.little[ai] = indexed ? state.arrays[ai].le : 0;
+  }
+  key.pnMtx = state.currentPnMtx;
+  key.fmt = static_cast<u8>(fmt);
+  return XXH3_64bits(&key, sizeof(key));
+}
+
+// Cheap per-call content check: the list's ends plus its length. Full hashing of
+// every call cost ~2 ms/frame; archive memory is invalidated explicitly when
+// released, so this guards against in-place rewrites and reuse cheaply.
+uint64_t resident_quick_hash(const u8* dl, u32 size) {
+  const u32 head = std::min<u32>(size, 64), tail = std::min<u32>(size - head, 64);
+  const uint64_t a = XXH3_64bits(dl, head);
+  const uint64_t b = tail ? XXH3_64bits(dl + size - tail, tail) : 0;
+  return a ^ (b * 0x9E3779B97F4A7C15ull) ^ (uint64_t(size) << 40);
+}
+
+void handle_call_display_list(const u8* dl, u32 size) {
+  auto& rs = resident::state();
+  ++rs.stats.calls;
+  if (!resident::enabled() || !size) {
+    ++rs.stats.fallbacks;
+    process(dl, size);
+    return;
+  }
+  const u8 firstOp = dl[0] == GX_NOP ? 0 : dl[0];
+  const auto fmt = static_cast<GXVtxFmt>(firstOp & CP_VAT_MASK);
+  const uint64_t formatHash = resident_format_hash(fmt);
+  const uint64_t quick = resident_quick_hash(dl, size);
+  const uint64_t hash = XXH3_64bits_withSeed(&formatHash, sizeof(formatHash), reinterpret_cast<uintptr_t>(dl) ^ (uint64_t(size) << 40));
+  auto it = rs.entries.find(hash);
+  if (it != rs.entries.end() && it->second.dlHash == quick && it->second.dl == dl && it->second.dlBytes == size) {
+    ++rs.stats.hits;
+  } else {
+    if (it != rs.entries.end()) resident::erase_entry(hash);
+    if (resident::total_used() > rs.budget) resident::reset_all();
+    resident::Entry entry{};
+    entry.dl = dl; entry.dlBytes = size; entry.dlHash = quick;
+    if (!build_resident_entry(dl, size, entry)) {
+      ++rs.stats.fallbacks;
+      process(dl, size);
+      return;
+    }
+    ++rs.stats.misses;
+    it = rs.entries.emplace(hash, std::move(entry)).first;
+    resident::index_entry(hash, it->second);
+  }
+  it->second.lastUsed = gfx::current_frame();
+  push_resident_draw(it->second, fmt);
+}
+} // namespace
+#endif
+
 void handle_aurora(ByteReader& reader) noexcept {
   ZoneScoped;
   const u16 subCmd = reader.read<u16>();
@@ -729,7 +1347,11 @@ void handle_aurora(ByteReader& reader) noexcept {
     const size_t idxBytes = static_cast<size_t>(indexCount) * sizeof(u16);
     // Index data is always host-endian; push it to the GPU buffer as-is
     const auto indexData = reader.take(idxBytes);
-    const gfx::Range idxRange = gfx::push_indices(indexData.data(), indexData.size(), 4);
+    gfx::Range idxRange{};
+#ifdef MELEE_MIYOO_FLIP
+    if(!flip_batch_enabled())
+#endif
+    idxRange = gfx::push_indices(indexData.data(), indexData.size(), 4);
     u32 vtxSize;
     if (g_gxState.lastVtxFmt == fmt) {
       vtxSize = g_gxState.lastVtxSize;
@@ -738,10 +1360,40 @@ void handle_aurora(ByteReader& reader) noexcept {
     }
     const u32 totalVtxBytes = vtxCount * vtxSize;
     const auto vertexData = reader.take(totalVtxBytes);
-    const gfx::Range vertRange = gfx::push_verts(vertexData.data(), vertexData.size(), 4);
+#ifdef MELEE_MIYOO_FLIP
+    if(flip_batch_enabled()) {
+      if(indexCount) push_gx_draw(prim,fmt,vtxCount,{},{},indexCount,vertexData,indexData);
+      return;
+    }
+#endif
+    const gfx::Range vertRange =
+#ifdef MELEE_MIYOO_FLIP
+        flip_push_vertices(prim,fmt,vertexData.data(),vertexData.size(),4)
+#else
+        gfx::push_verts(vertexData.data(), vertexData.size(), 4)
+#endif
+;
     if (indexCount != 0) {
       push_gx_draw(prim, fmt, vtxCount, vertRange, idxRange, indexCount);
     }
+#ifdef MELEE_MIYOO_FLIP
+  } else if (subCmd == GX_AURORA_CALL_DL) {
+    const auto* dl = reinterpret_cast<const u8*>(reader.read<u64>());
+    const u32 size = reader.read<u32>();
+    handle_call_display_list(dl, size);
+  } else if (subCmd == GX_AURORA_INVALIDATE_RESIDENT) {
+    const auto* base = reinterpret_cast<const void*>(reader.read<u64>());
+    const u32 size = reader.read<u32>();
+    resident::invalidate(base, size);
+  } else if (subCmd == GX_AURORA_FRAME_BEGIN) {
+    gfx::detail::begin_frame_worker(reader.read<u32>());
+  } else if (subCmd == GX_AURORA_FRAME_END) {
+    fifo::end_frame_worker();
+    texture::end_frame();
+    gfx::finish();
+    gfx::detail::end_frame_worker();
+    fifo::dispatch_after_frame();
+#endif
   } else if (subCmd == GX_AURORA_DEBUG_GROUP_PUSH) {
     auto label = reader.read_string();
     gfx::push_debug_group(std::move(label));
@@ -758,6 +1410,55 @@ void handle_aurora(ByteReader& reader) noexcept {
 }
 
 void clear_draw_cache() noexcept {
+#ifdef MELEE_MIYOO_FLIP
+  static uint64_t frame=0;
+  if(++frame%300==0 && std::getenv("MELEE_FLIP_PROFILE")) {
+    std::fprintf(stderr,
+      "[flip-batch] frames=300 attempts=%llu merged=%llu no-prev=%llu pipeline=%llu shader=%llu attrs=%llu tev=%llu tev-only=%llu channels=%llu tcgs=%llu alpha=%llu indirect=%llu shader-fog=%llu line=%llu blend=%llu depth=%llu cull=%llu writes=%llu other=%llu texture=%llu dst-alpha=%llu fog=%llu window=%llu vertex-gap=%llu vertex-limit=%llu index-gap=%llu\n",
+      static_cast<unsigned long long>(sFlipBatchStats.attempts),
+      static_cast<unsigned long long>(sFlipBatchStats.merged),
+      static_cast<unsigned long long>(sFlipBatchStats.noPreviousDraw),
+      static_cast<unsigned long long>(sFlipBatchStats.pipeline),
+      static_cast<unsigned long long>(sFlipBatchStats.pipelineShader),
+      static_cast<unsigned long long>(sFlipBatchStats.shaderAttrs),
+      static_cast<unsigned long long>(sFlipBatchStats.shaderTev),
+      static_cast<unsigned long long>(sFlipBatchStats.shaderTevOnly),
+      static_cast<unsigned long long>(sFlipBatchStats.shaderChannels),
+      static_cast<unsigned long long>(sFlipBatchStats.shaderTcgs),
+      static_cast<unsigned long long>(sFlipBatchStats.shaderAlpha),
+      static_cast<unsigned long long>(sFlipBatchStats.shaderIndirect),
+      static_cast<unsigned long long>(sFlipBatchStats.shaderFog),
+      static_cast<unsigned long long>(sFlipBatchStats.shaderLine),
+      static_cast<unsigned long long>(sFlipBatchStats.pipelineBlend),
+      static_cast<unsigned long long>(sFlipBatchStats.pipelineDepth),
+      static_cast<unsigned long long>(sFlipBatchStats.pipelineCull),
+      static_cast<unsigned long long>(sFlipBatchStats.pipelineWrites),
+      static_cast<unsigned long long>(sFlipBatchStats.pipelineOther),
+      static_cast<unsigned long long>(sFlipBatchStats.texture),
+      static_cast<unsigned long long>(sFlipBatchStats.dstAlpha),
+      static_cast<unsigned long long>(sFlipBatchStats.fog),
+      static_cast<unsigned long long>(sFlipBatchStats.uniformWindow),
+      static_cast<unsigned long long>(sFlipBatchStats.vertexGap),
+      static_cast<unsigned long long>(sFlipBatchStats.vertexLimit),
+      static_cast<unsigned long long>(sFlipBatchStats.indexGap));
+    std::fprintf(stderr,"[flip-batch-resident] no-prev=%llu kind=%llu pipeline=%llu texture=%llu record=%llu dst-alpha=%llu fog=%llu vtx-start=%llu index-gap=%llu\n",
+      static_cast<unsigned long long>(sFlipBatchStats.rNoPrev), static_cast<unsigned long long>(sFlipBatchStats.rKind),
+      static_cast<unsigned long long>(sFlipBatchStats.rPipeline), static_cast<unsigned long long>(sFlipBatchStats.rTexture),
+      static_cast<unsigned long long>(sFlipBatchStats.rRecord), static_cast<unsigned long long>(sFlipBatchStats.rDstAlpha),
+      static_cast<unsigned long long>(sFlipBatchStats.rFog), static_cast<unsigned long long>(sFlipBatchStats.rVtxStart),
+      static_cast<unsigned long long>(sFlipBatchStats.rIndexGap));
+    sFlipBatchStats={};
+    auto& rs=resident::state().stats;
+    std::fprintf(stderr,"[flip-resident-refused] prim=%llu format=%llu malformed=%llu loader=%llu matrices=%llu empty=%llu\n",
+      (unsigned long long)rs.refusePrim,(unsigned long long)rs.refuseFormat,(unsigned long long)rs.refuseMalformed,
+      (unsigned long long)rs.refuseLoader,(unsigned long long)rs.refuseMatrices,(unsigned long long)rs.refuseEmpty);
+    std::fprintf(stderr,"[flip-resident] calls=%llu hits=%llu misses=%llu fallbacks=%llu invalidations=%llu resets=%llu entries=%zu bytes=%zu\n",
+      (unsigned long long)rs.calls,(unsigned long long)rs.hits,(unsigned long long)rs.misses,(unsigned long long)rs.fallbacks,
+      (unsigned long long)rs.invalidations,(unsigned long long)rs.resets,resident::state().entries.size(),resident::total_used());
+    rs.calls=rs.hits=rs.misses=rs.fallbacks=0;
+  }
+  sFlipHasPreviousPipeline=false;
+#endif
   sDrawCache.bindGeneration = 0;
   sDrawCache.uniformRange = {};
   sDrawCache.fogRange = {};
diff --git a/lib/gx/fifo.cpp b/lib/gx/fifo.cpp
index bcaba60..a03e04a 100644
--- a/lib/gx/fifo.cpp
+++ b/lib/gx/fifo.cpp
@@ -1,13 +1,19 @@
 #include "fifo.hpp"
+#ifdef MELEE_MIYOO_FLIP
+#include "../gfx/flip_profile.hpp"
+#endif
 
 #include "../thread.hpp"
 #include "command_processor.hpp"
 
 #include <algorithm>
 #include <atomic>
+#include <chrono>
+#include <cstdlib>
 #include <cstring>
 #include <limits>
 #include <mutex>
+#include <vector>
 
 #include <tracy/Tracy.hpp>
 
@@ -36,6 +42,17 @@ std::mutex sBufferMutex;
 std::atomic<uint32_t> sWorkerWake{0};
 thread::Thread sWorkerThread;
 std::atomic<DrawDoneCallback> sDrawDoneCallback{nullptr};
+// Cheap critical-path accounting (MELEE_FLIP_PROFILE): time the producer
+// spends blocked in drain() and time the worker spends translating.
+std::atomic<uint64_t> sDrainWaitNs{0};
+std::atomic<uint64_t> sProcessNs{0};
+bool profile_accounting() noexcept {
+  static const bool enabled = [] { const char* v = std::getenv("MELEE_FLIP_PROFILE"); return v && std::strcmp(v, "0"); }();
+  return enabled;
+}
+uint64_t now_ns() noexcept {
+  return std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now().time_since_epoch()).count();
+}
 
 void dispatch_draw_done() noexcept {
   if (const auto callback = sDrawDoneCallback.load(std::memory_order_acquire); callback != nullptr) {
@@ -49,6 +66,16 @@ void wake_worker() noexcept {
 }
 
 void process_to(uint64_t target, std::memory_order order) noexcept {
+#ifdef MELEE_MIYOO_FLIP
+  namespace profile=gfx::detail::flip_profile;
+  if(profile::enabled()) {
+    const auto frame=profile::fifoFrame.load(std::memory_order_acquire);
+    if(profile::state.frame!=frame||std::strcmp(profile::state.lane,"fifo")) {
+      profile::end();profile::begin(frame,"fifo");
+    }
+  }
+  profile::Scope processProfile("fifo_process");
+#endif
   uint64_t processed = sProcessed.load(std::memory_order_relaxed);
   while (processed < target) {
     ProcessResult result{};
@@ -80,7 +107,13 @@ void worker_main(std::stop_token token) noexcept {
     const uint64_t processed = sProcessed.load(std::memory_order_relaxed);
     const uint64_t published = sPublished.load(std::memory_order_acquire);
     if (published != processed) {
-      process_to(published, std::memory_order_release);
+      if (profile_accounting()) {
+        const uint64_t start = now_ns();
+        process_to(published, std::memory_order_release);
+        sProcessNs.fetch_add(now_ns() - start, std::memory_order_relaxed);
+      } else {
+        process_to(published, std::memory_order_release);
+      }
       continue;
     }
 
@@ -251,10 +284,12 @@ void drain() {
 
     uint64_t processed = sProcessed.load(std::memory_order_acquire);
     if (processed < target) {
+      const uint64_t start = profile_accounting() ? now_ns() : 0;
       do {
         sProcessed.wait(processed, std::memory_order_acquire);
         processed = sProcessed.load(std::memory_order_acquire);
       } while (processed < target);
+      if (start) sDrainWaitNs.fetch_add(now_ns() - start, std::memory_order_relaxed);
     }
     break;
   }
@@ -268,6 +303,66 @@ void drain() {
   sPendingDraws = 0;
 }
 
+uint64_t drain_wait_ns() noexcept { return sDrainWaitNs.load(std::memory_order_relaxed); }
+
+bool async_frames() noexcept {
+  static const bool enabled = [] { const char* v = std::getenv("MELEE_FLIP_ASYNC_FIFO"); return v && !std::strcmp(v, "1"); }();
+  return enabled && kProcessingMode == ProcessingMode::Thread;
+}
+namespace { std::atomic<uint64_t> sDrawDoneTarget{0}; }
+void mark_draw_done() noexcept { sDrawDoneTarget.store(sStreamBase + detail::sBufferSize, std::memory_order_release); }
+void wait_draw_done() noexcept {
+  const uint64_t target = sDrawDoneTarget.load(std::memory_order_acquire);
+  if (target == 0) return;
+  // The token was published by GXSetDrawDone; make sure nothing precedes it unpublished.
+  if (sPublished.load(std::memory_order_relaxed) < target) { sPublished.store(target, std::memory_order_release); wake_worker(); }
+  uint64_t processed = sProcessed.load(std::memory_order_acquire);
+  if (processed < target) {
+    const uint64_t start = profile_accounting() ? now_ns() : 0;
+    do {
+      sProcessed.wait(processed, std::memory_order_acquire);
+      processed = sProcessed.load(std::memory_order_acquire);
+    } while (processed < target);
+    if (start) sDrainWaitNs.fetch_add(now_ns() - start, std::memory_order_relaxed);
+  }
+}
+void end_frame_producer() noexcept { sFrameActive = false; }
+namespace { std::mutex sAfterFrameMutex; std::vector<std::function<void()>> sAfterFrame; }
+void run_after_frame(std::function<void()> fn) {
+  std::lock_guard lock{sAfterFrameMutex};
+  sAfterFrame.push_back(std::move(fn));
+}
+void dispatch_after_frame() {
+  std::vector<std::function<void()>> pending;
+  {
+    std::lock_guard lock{sAfterFrameMutex};
+    pending.swap(sAfterFrame);
+  }
+  for (auto& fn : pending) fn();
+}
+void end_frame_worker() noexcept { clear_draw_cache(); }
+void recycle() noexcept {
+  // Producer, between frames (never inside GXBegin/GXEnd, whose patch offsets
+  // are buffer-relative). Rewind when everything was consumed; otherwise drop the
+  // consumed prefix so a lagging worker cannot make the buffer grow unbounded.
+  std::lock_guard lock{sBufferMutex};
+  const uint64_t processed = sProcessed.load(std::memory_order_acquire);
+  const uint64_t end = sStreamBase + detail::sBufferSize;
+  if (processed == end) {
+    sStreamBase = processed;
+    detail::sBufferSize = 0;
+    sPendingDraws = 0;
+    return;
+  }
+  const uint64_t consumed = processed - sStreamBase;
+  if (consumed >= 65536 && consumed * 2 >= detail::sBufferSize) {
+    std::memmove(detail::sBufferData, detail::sBufferData + consumed, detail::sBufferSize - consumed);
+    sStreamBase += consumed;
+    detail::sBufferSize -= static_cast<uint32_t>(consumed);
+  }
+}
+uint64_t process_ns() noexcept { return sProcessNs.load(std::memory_order_relaxed); }
+
 const uint8_t* get_buffer_data() { return detail::sBufferData; }
 uint32_t get_buffer_size() { return detail::sBufferSize; }
 
diff --git a/lib/gx/fifo.hpp b/lib/gx/fifo.hpp
index 0d6a5d8..e6cff98 100644
--- a/lib/gx/fifo.hpp
+++ b/lib/gx/fifo.hpp
@@ -3,6 +3,7 @@
 #include "../internal.hpp"
 
 #include <cstring>
+#include <functional>
 
 namespace aurora::gx::fifo {
 
@@ -32,6 +33,25 @@ void shutdown();
 void begin_frame() noexcept;
 void end_frame() noexcept;
 
+// Accumulated nanoseconds (MELEE_FLIP_PROFILE only): producer time blocked in
+// drain(), and worker time spent translating the command stream.
+uint64_t drain_wait_ns() noexcept;
+uint64_t process_ns() noexcept;
+
+// Asynchronous frames (MELEE_FLIP_ASYNC_FIFO=1): the producer never joins the
+// worker at frame end. Frame boundaries travel through the stream instead, and
+// GXWaitDrawDone waits only for the most recent draw-done token, as on hardware.
+bool async_frames() noexcept;
+void mark_draw_done() noexcept;       // producer: after writing the draw-done token
+void wait_draw_done() noexcept;       // producer: block until that token was processed
+void end_frame_producer() noexcept;   // producer: frame closed (no more publishes)
+void end_frame_worker() noexcept;     // worker: per-frame processor state reset
+void recycle() noexcept;              // producer: rewind the buffer when fully consumed
+// Producer: run `fn` on the worker right after the next frame end is processed
+// (used for readbacks that must observe a complete frame in async mode).
+void run_after_frame(std::function<void()> fn);
+void dispatch_after_frame();          // worker
+
 // Out-of-line slow path: grows internal buffer then appends data
 void write_data_grow(const void* data, uint32_t length);
 
diff --git a/lib/gx/flip_gles_uber.cpp b/lib/gx/flip_gles_uber.cpp
new file mode 100644
index 0000000..c7a19bf
--- /dev/null
+++ b/lib/gx/flip_gles_uber.cpp
@@ -0,0 +1,294 @@
+#include "flip_gles_uber.hpp"
+
+#ifdef MELEE_MIYOO_FLIP
+#include "flip_uber.hpp"
+#include <map>
+#include <filesystem>
+#include <vector>
+#include <unistd.h>
+#include <xxhash.h>
+#include <algorithm>
+#include <array>
+#include <cstdio>
+#include <string>
+
+namespace aurora::gx {
+namespace {
+std::array<GLuint,17> sPrograms{};
+std::array<bool,17> sAttempted{};
+// Exact packed GX state keys avoid accidentally sharing a program between
+// materials with different TEV, lighting, alpha-test, or texgen behavior.
+std::map<std::array<u32,96>,GLuint> sSpecializedPrograms;
+
+std::filesystem::path binary_path(const std::string& vertex,const std::string& fragment) {
+  const char* base=std::getenv("MELEE_FLIP_CACHE_HOME");
+  if(!base||!*base) return {};
+  std::string key="flip-native-gles-v1\n"+vertex+"\n"+fragment;
+  for(auto name:{GL_VENDOR,GL_RENDERER,GL_VERSION,GL_SHADING_LANGUAGE_VERSION}) {
+    const auto* value=glGetString(name);
+    if(value) key+=reinterpret_cast<const char*>(value);
+    key+='\n';
+  }
+  char hash[32];std::snprintf(hash,sizeof(hash),"%016llx.bin",
+      static_cast<unsigned long long>(XXH3_64bits(key.data(),key.size())));
+  return std::filesystem::path(base)/"native-gles"/hash;
+}
+
+GLuint load_binary(const std::filesystem::path& path) {
+  if(path.empty()) return 0;
+  FILE* file=std::fopen(path.c_str(),"rb");
+  if(!file) return 0;
+  uint32_t header[3]{};
+  if(std::fread(header,sizeof(header),1,file)!=1||header[0]!=0x46474c31||!header[2]||header[2]>16*1024*1024) {
+    std::fclose(file);return 0;
+  }
+  std::vector<unsigned char> bytes(header[2]);
+  const bool complete=std::fread(bytes.data(),1,bytes.size(),file)==bytes.size()&&std::fgetc(file)==EOF;
+  std::fclose(file);
+  if(!complete) return 0;
+  const auto p=glCreateProgram();glProgramBinary(p,header[1],bytes.data(),bytes.size());
+  GLint linked=GL_FALSE;glGetProgramiv(p,GL_LINK_STATUS,&linked);
+  const auto error=glGetError();
+  if(linked!=GL_TRUE||error!=GL_NO_ERROR) {glDeleteProgram(p);return 0;}
+  return p;
+}
+
+void save_binary(GLuint program,const std::filesystem::path& path) {
+  if(path.empty()) return;
+  GLint size=0;glGetProgramiv(program,GL_PROGRAM_BINARY_LENGTH,&size);
+  if(size<=0||size>16*1024*1024) return;
+  std::vector<unsigned char> bytes(size);
+  GLenum format=0;GLsizei written=0;glGetProgramBinary(program,size,&written,&format,bytes.data());
+  if(written<=0) return;
+  std::error_code error;std::filesystem::create_directories(path.parent_path(),error);
+  if(error) return;
+  auto temporary=path;temporary+=".tmp-"+std::to_string(getpid());
+  FILE* file=std::fopen(temporary.c_str(),"wb");
+  if(!file) return;
+  const uint32_t header[3]={0x46474c31,format,static_cast<uint32_t>(written)};
+  bool ok=std::fwrite(header,sizeof(header),1,file)==1;
+  ok=ok&&std::fwrite(bytes.data(),1,written,file)==static_cast<size_t>(written);
+  ok=std::fclose(file)==0&&ok;
+  if(ok) std::filesystem::rename(temporary,path,error);
+  if(!ok||error) std::filesystem::remove(temporary,error);
+}
+
+GLuint compile(GLenum type,const char* source,const char* label) {
+  const GLuint shader=glCreateShader(type);
+  glShaderSource(shader,1,&source,nullptr);
+  glCompileShader(shader);
+  GLint ok=GL_FALSE;glGetShaderiv(shader,GL_COMPILE_STATUS,&ok);
+  if(ok==GL_TRUE) return shader;
+  char log[4096]{};GLsizei size=0;glGetShaderInfoLog(shader,sizeof(log),&size,log);
+  std::fprintf(stderr,"[flip-gles-uber] %s compile failed: %.*s\n",label,int(size),log);
+  glDeleteShader(shader);return 0;
+}
+
+constexpr const char* VertexSource=R"GLSL(#version 310 es
+precision highp float;
+precision highp int;
+
+struct Light { vec4 pos; vec4 dir; vec4 color; vec4 cos_att; vec4 dist_att; };
+struct Fog { vec4 color; vec4 params; vec4 range_k[3]; };
+struct GXUniform {
+  vec2 render_size; vec2 logical_size; mat4 proj;
+  mat3x4 pos_mtx[20]; mat3x4 nrm_mtx[10]; vec4 tevreg[4];
+  Light lights[8]; uvec4 light_state; vec4 cc_amb[4]; vec4 cc_mat[4]; vec4 kcolor[4];
+  mat3x4 post_mtx[3]; Fog fog; vec4 texcoord_scale[8]; vec4 tex_size_bias[8];
+  uvec4 state[24]; uvec4 padding[50];
+};
+layout(std140,binding=0) uniform GXBlock { GXUniform table[16]; };
+
+layout(location=0) in uvec3 a_matrices;
+layout(location=1) in vec3 a_pos;
+layout(location=2) in vec3 a_normal;
+layout(location=3) in vec4 a_color0;
+layout(location=4) in vec4 a_color1;
+layout(location=5) in vec2 a_tex0; layout(location=6) in vec2 a_tex1;
+layout(location=7) in vec2 a_tex2; layout(location=8) in vec2 a_tex3;
+layout(location=9) in vec2 a_tex4; layout(location=10) in vec2 a_tex5;
+layout(location=11) in vec2 a_tex6; layout(location=12) in vec2 a_tex7;
+layout(location=13) in vec3 a_binormal; layout(location=14) in vec3 a_tangent;
+
+layout(location=0) flat out uint v_record;
+layout(location=11) flat out uint v_bank;
+layout(location=1) out vec4 v_color0;
+layout(location=2) out vec4 v_color1;
+layout(location=3) out vec3 v_tex0; layout(location=4) out vec3 v_tex1;
+layout(location=5) out vec3 v_tex2; layout(location=6) out vec3 v_tex3;
+layout(location=7) out vec3 v_tex4; layout(location=8) out vec3 v_tex5;
+layout(location=9) out vec3 v_tex6; layout(location=10) out vec3 v_tex7;
+
+uint state_word(uint record,uint index) { return table[record].state[index>>2u][index&3u]; }
+
+vec4 light_channel(uint record,uint index,vec4 vertex,vec3 mv_pos,vec3 mv_nrm) {
+  uint cfg=state_word(record,4u+index);
+  vec4 material=(cfg&1u)!=0u?vertex:table[record].cc_mat[index];
+  if((cfg&64u)==0u) return material;
+  vec4 lighting=(cfg&2u)!=0u?vertex:table[record].cc_amb[index];
+  uint mask=table[record].light_state[index];
+  for(uint i=0u;i<8u;++i) {
+    if((mask&(1u<<i))==0u) continue;
+    Light light=table[record].lights[i];
+    vec3 ldir=light.pos.xyz-mv_pos;float dist2=dot(ldir,ldir);float dist=sqrt(max(dist2,1e-20));ldir/=dist;
+    float attn;uint af=(cfg>>4u)&3u;uint df=(cfg>>2u)&3u;
+    if(af==2u) attn=1.0;
+    else if(af==1u) {float cosine=max(0.0,dot(ldir,light.dir.xyz));float ca=dot(light.cos_att.xyz,vec3(1,cosine,cosine*cosine));float da=dot(light.dist_att.xyz,vec3(1,dist,dist2));attn=max(0.0,ca/da);}
+    else {float x=dot(mv_nrm,ldir)>=0.0?max(0.0,dot(mv_nrm,light.dir.xyz)):0.0;float ca=dot(light.cos_att.xyz,vec3(1,x,x*x));float da=max(0.0,dot(normalize(light.dist_att.xyz),vec3(1,x,x*x)));attn=max(0.0,ca/da);}
+    float diffuse=1.0;if(df==1u)diffuse=dot(ldir,mv_nrm);else if(df==2u)diffuse=max(0.0,dot(ldir,mv_nrm));
+    lighting+=attn*diffuse*light.color;
+  }
+  return material*clamp(lighting,0.0,1.0);
+}
+
+void main() {
+  uint record=(a_matrices.z>>8u)&15u;v_record=record;v_bank=(a_matrices.z>>12u)&1u;
+  uint pn=a_matrices.x&255u;
+  vec3 mv=vec4(a_pos,1.0)*table[record].pos_mtx[pn];
+  vec4 clip=vec4(mv,1.0)*table[record].proj;
+  gl_Position=vec4(clip.x,-clip.y,2.0*clip.z-clip.w,clip.w);
+  vec3 normal=(state_word(record,1u)&(1u<<10u))!=0u?a_normal:vec3(1,0,0);
+  vec3 nt=vec4(normal,0.0)*table[record].nrm_mtx[pn];
+  vec3 n=dot(nt,nt)>1e-10?normalize(nt):nt;
+  vec4 c0=light_channel(record,0u,a_color0,mv,n),a0=light_channel(record,2u,a_color0,mv,n);
+  vec4 c1=light_channel(record,1u,a_color1,mv,n),a1=light_channel(record,3u,a_color1,mv,n);
+  v_color0=vec4(c0.rgb,a0.a);v_color1=vec4(c1.rgb,a1.a);
+  vec2 inputs[8]=vec2[8](a_tex0,a_tex1,a_tex2,a_tex3,a_tex4,a_tex5,a_tex6,a_tex7);
+  uint texmtx[8]=uint[8]((a_matrices.x>>8u)&255u,(a_matrices.x>>16u)&255u,(a_matrices.x>>24u)&255u,
+    a_matrices.y&255u,(a_matrices.y>>8u)&255u,(a_matrices.y>>16u)&255u,(a_matrices.y>>24u)&255u,a_matrices.z&255u);
+  vec3 coords[8];
+  for(uint i=0u;i<8u;++i) {
+    uint cfg=state_word(record,8u+i),ty=cfg&255u,src=(cfg>>8u)&255u;vec4 base;
+    if(src==0u)base=vec4(a_pos,1);else if(src==1u)base=vec4(normal,1);else if(src==2u)base=vec4(a_binormal,1);
+    else if(src==3u)base=vec4(a_tangent,1);else if(src>=4u&&src<=11u)base=vec4(inputs[src-4u],1,1);
+    else if(src==19u)base=a_color0;else if(src==20u)base=a_color1;else base=vec4(0);
+    vec3 tmp;
+    if(ty<=1u) {uint configured=(cfg>>16u)&255u;bool has=(state_word(record,1u)&(1u<<(i+1u)))!=0u;uint mi=has?texmtx[i]/3u:configured/3u;tmp=(configured!=60u||has)?base*table[record].pos_mtx[mi]:base.xyz;if(ty==1u)tmp.z=1.0;}
+    else tmp=vec3(base.xy,1);
+    if((state_word(record,16u+i)&1u)!=0u)tmp=normalize(tmp);
+    uint post=(cfg>>24u)&255u;if(post!=125u)tmp=vec4(tmp,1)*table[record].post_mtx[(post-64u)/3u];coords[i]=tmp;
+  }
+  v_tex0=coords[0];v_tex1=coords[1];v_tex2=coords[2];v_tex3=coords[3];
+  v_tex4=coords[4];v_tex5=coords[5];v_tex6=coords[6];v_tex7=coords[7];
+}
+)GLSL";
+
+constexpr const char* FragmentSource=R"GLSL(#version 310 es
+precision highp float;
+precision highp int;
+struct Light { vec4 pos; vec4 dir; vec4 color; vec4 cos_att; vec4 dist_att; };
+struct Fog { vec4 color; vec4 params; vec4 range_k[3]; };
+struct GXUniform { vec2 render_size;vec2 logical_size;mat4 proj;mat3x4 pos_mtx[20];mat3x4 nrm_mtx[10];vec4 tevreg[4];Light lights[8];uvec4 light_state;vec4 cc_amb[4];vec4 cc_mat[4];vec4 kcolor[4];mat3x4 post_mtx[3];Fog fog;vec4 texcoord_scale[8];vec4 tex_size_bias[8];uvec4 state[24];uvec4 padding[50]; };
+layout(std140,binding=0) uniform GXBlock { GXUniform table[16]; };
+uniform sampler2D tex0;uniform sampler2D tex1;uniform sampler2D tex2;uniform sampler2D tex3;
+uniform sampler2D tex4;uniform sampler2D tex5;uniform sampler2D tex6;uniform sampler2D tex7;
+uniform sampler2D tex8;uniform sampler2D tex9;uniform sampler2D tex10;uniform sampler2D tex11;
+uniform sampler2D tex12;uniform sampler2D tex13;uniform sampler2D tex14;uniform sampler2D tex15;
+layout(location=0) flat in uint v_record;layout(location=1) in vec4 v_color0;layout(location=2) in vec4 v_color1;
+layout(location=11) flat in uint v_bank;
+layout(location=3) in vec3 v_tex0;layout(location=4) in vec3 v_tex1;layout(location=5) in vec3 v_tex2;layout(location=6) in vec3 v_tex3;
+layout(location=7) in vec3 v_tex4;layout(location=8) in vec3 v_tex5;layout(location=9) in vec3 v_tex6;layout(location=10) in vec3 v_tex7;
+layout(location=0) out vec4 out_color;
+ivec4 regs[4];
+uint word(uint i){return table[v_record].state[i>>2u][i&3u];}
+float comp(vec4 v,uint i){return v[i&3u];}
+ivec4 swizzle(ivec4 v,uint which){uint s=(word(3u)>>(which*8u))&255u;return ivec4(v[s&3u],v[(s>>2u)&3u],v[(s>>4u)&3u],v[(s>>6u)&3u]);}
+ivec4 sample_tex(uint id,uint bank,vec2 uv,float bias){vec4 v;id+=bank*8u;if(id==0u)v=texture(tex0,uv,bias);else if(id==1u)v=texture(tex1,uv,bias);else if(id==2u)v=texture(tex2,uv,bias);else if(id==3u)v=texture(tex3,uv,bias);else if(id==4u)v=texture(tex4,uv,bias);else if(id==5u)v=texture(tex5,uv,bias);else if(id==6u)v=texture(tex6,uv,bias);else if(id==7u)v=texture(tex7,uv,bias);else if(id==8u)v=texture(tex8,uv,bias);else if(id==9u)v=texture(tex9,uv,bias);else if(id==10u)v=texture(tex10,uv,bias);else if(id==11u)v=texture(tex11,uv,bias);else if(id==12u)v=texture(tex12,uv,bias);else if(id==13u)v=texture(tex13,uv,bias);else if(id==14u)v=texture(tex14,uv,bias);else if(id==15u)v=texture(tex15,uv,bias);else v=vec4(1);return ivec4(round(v*255.0));}
+ivec3 konst_color(uint sel){if(sel<8u)return ivec3(int(8u-sel)*32);if(sel>=12u&&sel<16u)return ivec3(round(table[v_record].kcolor[sel-12u].rgb*255.0));uint x=sel-16u;return ivec3(int(round(comp(table[v_record].kcolor[x&3u],x/4u)*255.0)));}
+int konst_alpha(uint sel){if(sel<8u)return int(8u-sel)*32;uint x=sel-16u;return int(round(comp(table[v_record].kcolor[x&3u],x/4u)*255.0));}
+ivec3 color_arg(uint a,ivec4 texel,ivec4 rast,ivec3 k){if(a==0u)return regs[0].rgb&255;if(a==1u)return ivec3(regs[0].a&255);if(a==2u)return regs[1].rgb&255;if(a==3u)return ivec3(regs[1].a&255);if(a==4u)return regs[2].rgb&255;if(a==5u)return ivec3(regs[2].a&255);if(a==6u)return regs[3].rgb&255;if(a==7u)return ivec3(regs[3].a&255);if(a==8u)return texel.rgb;if(a==9u)return ivec3(texel.a);if(a==10u)return rast.rgb;if(a==11u)return ivec3(rast.a);if(a==12u)return ivec3(255);if(a==13u)return ivec3(128);if(a==14u)return k;return ivec3(0);}
+int alpha_arg(uint a,ivec4 texel,ivec4 rast,int k){if(a<4u)return regs[a].a&255;if(a==4u)return texel.a;if(a==5u)return rast.a;if(a==6u)return k;return 0;}
+ivec3 tev_lerp3(ivec3 A,ivec3 B,ivec3 C,ivec3 D,uint bias,bool sub,uint scale){C+=C>>7;if(bias==1u)D+=128;else if(bias==2u)D-=128;ivec3 v=(A<<8)+(B-A)*C;if(scale!=3u){v<<=int(scale);D<<=int(scale);v+=sub?127:128;}v>>=8;v=sub?D-v:D+v;if(scale==3u)v>>=1;return v;}
+int tev_lerp1(int A,int B,int C,int D,uint bias,bool sub,uint scale){C+=C>>7;if(bias==1u)D+=128;else if(bias==2u)D-=128;int v=(A<<8)+(B-A)*C;if(scale!=3u){v<<=int(scale);D<<=int(scale);v+=sub?127:128;}v>>=8;v=sub?D-v:D+v;if(scale==3u)v>>=1;return v;}
+bool tev_compare(uint op,ivec3 A,ivec3 B){if(op==0u)return A.r>B.r;if(op==1u)return A.r==B.r;if(op==2u)return (A.r|(A.g<<8))>(B.r|(B.g<<8));if(op==3u)return A.r==B.r&&A.g==B.g;if(op==4u)return (A.r|(A.g<<8)|(A.b<<16))>(B.r|(B.g<<8)|(B.b<<16));if(op==5u)return all(equal(A,B));return false;}
+bool alpha_compare(uint value,uint fn,uint ref){if(fn==0u)return false;if(fn==1u)return value<ref;if(fn==2u)return value==ref;if(fn==3u)return value<=ref;if(fn==4u)return value>ref;if(fn==5u)return value!=ref;if(fn==6u)return value>=ref;return true;}
+void main(){
+  for(int r=0;r<4;++r)regs[r]=ivec4(round(table[v_record].tevreg[r]*255.0));
+  vec3 tc[8]=vec3[8](v_tex0,v_tex1,v_tex2,v_tex3,v_tex4,v_tex5,v_tex6,v_tex7);ivec4 rastv[2]=ivec4[2](ivec4(round(v_color0*255.0)),ivec4(round(v_color1*255.0)));
+  uint count=word(0u)&255u,lastc=0u,lasta=0u;
+  for(uint i=0u;i<uint(FLIP_TEV_STAGES);++i){uint w0=word(24u+i*3u),w1=word(25u+i*3u),w2=word(26u+i*3u);uint ti=(w2>>14u)&15u,ci=(w2>>10u)&15u;ivec4 texel=ivec4(255);if(ti<8u&&ci<8u){vec3 q=tc[ci];vec2 uv=((word(8u+ci)&255u)==0u)?q.xy/q.z:q.xy;texel=sample_tex(ti,v_bank,uv,table[v_record].tex_size_bias[ti].z);}texel=swizzle(texel,(w2>>28u)&3u);uint ch=(w2>>18u)&255u;ivec4 rast=ch<6u?rastv[ch&1u]:ivec4(0);rast=swizzle(rast,(w2>>26u)&3u);ivec3 kc=konst_color(w2&31u);int ka=konst_alpha((w2>>5u)&31u);
+    uint ca0=w0&15u,ca1=(w0>>4u)&15u,ca2=(w0>>8u)&15u,ca3=(w0>>12u)&15u;ivec3 A=color_arg(ca0,texel,rast,kc),B=color_arg(ca1,texel,rast,kc),C=color_arg(ca2,texel,rast,kc),D=color_arg(ca3,texel,rast,kc);uint cop=w1&2047u,op=cop&15u,dst=(cop>>8u)&3u;ivec3 rgb;if(op<2u)rgb=tev_lerp3(A,B,C,D,(cop>>4u)&3u,op==1u,(cop>>6u)&3u);else{uint cmp=op-8u;if(cmp<6u)rgb=D+(tev_compare(cmp,A,B)?C:ivec3(0));else rgb=D+mix(ivec3(0),C,bvec3(cmp==7u?equal(A,B):greaterThan(A,B)));}regs[dst].rgb=(cop&1024u)!=0u?clamp(rgb,0,255):clamp(rgb,-1024,1023);lastc=dst;
+    uint aa0=(w0>>16u)&7u,aa1=(w0>>19u)&7u,aa2=(w0>>22u)&7u,aa3=(w0>>25u)&7u;int XA=alpha_arg(aa0,texel,rast,ka),XB=alpha_arg(aa1,texel,rast,ka),XC=alpha_arg(aa2,texel,rast,ka),XD=alpha_arg(aa3,texel,rast,ka);uint aop=(w1>>16u)&2047u,aopc=aop&15u,adst=(aop>>8u)&3u;int av;if(aopc<2u)av=tev_lerp1(XA,XB,XC,XD,(aop>>4u)&3u,aopc==1u,(aop>>6u)&3u);else{uint cmp=aopc-8u;bool yes=cmp==6u?XA>XB:cmp==7u?XA==XB:tev_compare(cmp,ivec3(A),ivec3(B));av=XD+(yes?XC:0);}regs[adst].a=(aop&1024u)!=0u?clamp(av,0,255):clamp(av,-1024,1023);lasta=adst;
+  }
+  ivec4 result=ivec4(regs[lastc].rgb,regs[lasta].a)&255;uint ac=word(2u);if((ac&(1u<<24u))!=0u){uint a=uint(result.a);bool p0=alpha_compare(a,ac&7u,(ac>>3u)&255u),p1=alpha_compare(a,(ac>>13u)&7u,(ac>>16u)&255u);uint op=(ac>>11u)&3u;bool pass=op==0u?p0&&p1:op==1u?p0||p1:op==2u?p0!=p1:p0==p1;if(!pass)discard;}
+  vec4 color=vec4(result)/255.0;uint ft=(word(0u)>>8u)&255u;if(ft!=0u){Fog f=table[v_record].fog;float depth=1.0-gl_FragCoord.z;float base=(ft&8u)!=0u?f.params.x*depth:f.params.x/(f.params.y-depth);float ff=clamp(base-f.params.z,0.0,1.0),z;if(ft==2u||ft==10u)z=ff;else if(ft==4u||ft==12u)z=1.0-exp2(-8.0*ff);else if(ft==5u||ft==13u)z=1.0-exp2(-8.0*ff*ff);else if(ft==6u||ft==14u)z=exp2(-8.0*(1.0-ff));else{ff=1.0-ff;z=exp2(-8.0*ff*ff);}color.rgb=mix(color.rgb,f.color.rgb,clamp(z,0.0,1.0));}out_color=color;
+}
+)GLSL";
+}
+
+GLuint flip_gles_uber_program(const ShaderConfig& config) noexcept {
+  const auto tevStages=std::clamp(config.tevStageCount,1u,16u);
+  const bool specialized=flip_native_specialized_enabled();
+  const auto state=flip_uber_state(config);
+  if(specialized) {
+    const auto found=sSpecializedPrograms.find(state);
+    if(found!=sSpecializedPrograms.end()) return found->second;
+    sSpecializedPrograms.emplace(state,0);
+  } else {
+    if(sPrograms[tevStages]) return sPrograms[tevStages];
+    if(sAttempted[tevStages]) return 0;
+    sAttempted[tevStages]=true;
+  }
+  std::string vertex=VertexSource;
+  std::string fragment=FragmentSource;
+  const auto versionEnd=fragment.find('\n');
+  fragment.insert(versionEnd+1,"#define FLIP_TEV_STAGES "+std::to_string(tevStages)+"\n");
+  if(specialized) {
+    std::string constants="uint material_word(uint index) { switch(index) {\n";
+    for(size_t i=0;i<state.size();++i)
+      constants+="case "+std::to_string(i)+"u: return "+std::to_string(state[i])+"u;\n";
+    constants+="default: return 0u; } }\n";
+    const auto replace=[](std::string& source,const std::string& from,const std::string& to) {
+      size_t pos=0;
+      while((pos=source.find(from,pos))!=std::string::npos) {
+        source.replace(pos,from.size(),to);pos+=to.size();
+      }
+    };
+    replace(vertex,"uint state_word(uint record,uint index) { return table[record].state[index>>2u][index&3u]; }",
+            constants+"uint state_word(uint record,uint index) { return material_word(index); }");
+    replace(fragment,"uint word(uint i){return table[v_record].state[i>>2u][i&3u];}",
+            constants+"uint word(uint i){return material_word(i);}");
+    // Explicitly unroll the TEV interpreter so constant propagation eliminates
+    // unused textures, varyings, register branches, and arithmetic paths.
+    const std::string loop="for(uint i=0u;i<uint(FLIP_TEV_STAGES);++i){";
+    const auto begin=fragment.find(loop);
+    size_t end=begin+loop.size(),depth=1;
+    for(;end<fragment.size()&&depth;++end) {
+      if(fragment[end]=='{') ++depth;
+      else if(fragment[end]=='}') --depth;
+    }
+    const auto body=fragment.substr(begin+loop.size(),end-begin-loop.size()-1);
+    std::string stages;
+    for(u32 i=0;i<tevStages;++i)
+      stages+="{ const uint i="+std::to_string(i)+"u; "+body+"}\n";
+    fragment.replace(begin,end-begin,stages);
+    if(!(config.flipFlags&FlipTexturePairs))
+      replace(fragment,"sample_tex(ti,v_bank,uv,", "sample_tex(ti,0u,uv,");
+  }
+  const auto path=binary_path(vertex,fragment);
+  GLuint p=load_binary(path);
+  const bool cached=p!=0;
+  if(!p) {
+  const GLuint vs=compile(GL_VERTEX_SHADER,vertex.c_str(),"vertex"),fs=compile(GL_FRAGMENT_SHADER,fragment.c_str(),"fragment");
+  if(!vs||!fs){if(vs)glDeleteShader(vs);if(fs)glDeleteShader(fs);return 0;}
+  p=glCreateProgram();glProgramParameteri(p,GL_PROGRAM_BINARY_RETRIEVABLE_HINT,GL_TRUE);
+  glAttachShader(p,vs);glAttachShader(p,fs);glLinkProgram(p);glDeleteShader(vs);glDeleteShader(fs);
+  GLint ok=GL_FALSE;glGetProgramiv(p,GL_LINK_STATUS,&ok);if(ok!=GL_TRUE){char log[4096]{};GLsizei size=0;glGetProgramInfoLog(p,sizeof(log),&size,log);std::fprintf(stderr,"[flip-gles-uber] link failed: %.*s\n",int(size),log);glDeleteProgram(p);return 0;}
+  save_binary(p,path);
+  }
+  glUseProgram(p);for(int i=0;i<16;++i){char name[8];std::snprintf(name,sizeof(name),"tex%d",i);const GLint loc=glGetUniformLocation(p,name);if(loc>=0)glUniform1i(loc,i);}
+  const GLuint block=glGetUniformBlockIndex(p,"GXBlock");GLint blockBytes=0;
+  if(block!=GL_INVALID_INDEX)glGetActiveUniformBlockiv(p,block,GL_UNIFORM_BLOCK_DATA_SIZE,&blockBytes);
+  if(specialized) sSpecializedPrograms[state]=p;
+  else sPrograms[tevStages]=p;
+  std::fprintf(stderr,"[flip-gles-uber] native program=%u stages=%u block=%d cached=%u ready\n",p,tevStages,blockBytes,cached);return p;
+}
+
+void shutdown_flip_gles_uber() noexcept {
+  for(auto& [state,program]:sSpecializedPrograms) if(program) glDeleteProgram(program);
+  sSpecializedPrograms.clear();
+  for(auto& program:sPrograms)if(program){glDeleteProgram(program);program=0;}
+  sAttempted.fill(false);
+}
+}
+#endif
diff --git a/lib/gx/flip_gles_uber.hpp b/lib/gx/flip_gles_uber.hpp
new file mode 100644
index 0000000..5c67018
--- /dev/null
+++ b/lib/gx/flip_gles_uber.hpp
@@ -0,0 +1,11 @@
+#pragma once
+
+#ifdef MELEE_MIYOO_FLIP
+#include <GLES3/gl31.h>
+
+namespace aurora::gx {
+struct ShaderConfig;
+GLuint flip_gles_uber_program(const ShaderConfig& config) noexcept;
+void shutdown_flip_gles_uber() noexcept;
+}
+#endif
diff --git a/lib/gx/flip_resident.hpp b/lib/gx/flip_resident.hpp
new file mode 100644
index 0000000..a40259e
--- /dev/null
+++ b/lib/gx/flip_resident.hpp
@@ -0,0 +1,218 @@
+#pragma once
+// Resident display-list geometry (Flip). HSD models draw static GX display
+// lists that index static vertex arrays inside archive memory. Instead of
+// re-decoding and re-uploading them every frame, each (display list, vertex
+// format, matrix slot) is decoded once into a GPU-resident arena and later
+// calls only append 32-bit indices. Arrays are released through explicit
+// invalidation when their archive region is unregistered; display-list bytes
+// are hashed on every call so stale or reused memory is never trusted blindly.
+#include "flip_vertex.hpp"
+#include "../gfx/frame.hpp"
+#include "../gfx/render_worker.hpp"
+#include "../webgpu/gpu.hpp"
+
+#include <absl/container/flat_hash_map.h>
+#include <map>
+#include <memory>
+#include <mutex>
+#include <utility>
+#include <vector>
+
+namespace aurora::gx::resident {
+
+inline bool enabled() {
+  static const bool value = [] {
+    const char* v = std::getenv("MELEE_FLIP_RESIDENT_DL");
+    return !v || std::strcmp(v, "0");
+  }();
+  return value && flip_batch_enabled();
+}
+
+struct Arena {
+  uint32_t stride = 0;
+  std::vector<uint8_t> mirror;              // CPU copy; FIFO worker appends
+  size_t used = 0;                          // bytes decoded so far
+  std::mutex mutex;                         // guards mirror growth, pending, gpu fields
+  std::vector<std::pair<size_t, size_t>> pending; // byte ranges awaiting upload
+  wgpu::Buffer buffer;                      // render worker owned
+  size_t bufferSize = 0;
+  uint32_t generation = 0;                  // bumped when buffer is recreated
+};
+
+struct Entry {
+  uint32_t arena = 0;
+  uint32_t firstVertex = 0;
+  uint32_t vertexCount = 0;
+  std::vector<uint32_t> indices;            // absolute arena vertex indices
+  uint64_t dlHash = 0;
+  const void* dl = nullptr;
+  uint32_t dlBytes = 0;
+  std::array<const void*, MaxVtxAttr> arrays{};
+  uint64_t lastUsed = 0;
+};
+
+struct Stats { uint64_t calls = 0, hits = 0, misses = 0, fallbacks = 0, invalidations = 0, resets = 0, bytes = 0;
+  // Why build_resident_entry refused: primitive type, mixed formats, malformed, loader, no matrices, empty.
+  uint64_t refusePrim = 0, refuseFormat = 0, refuseMalformed = 0, refuseLoader = 0, refuseMatrices = 0, refuseEmpty = 0; };
+
+struct State {
+  std::vector<std::unique_ptr<Arena>> arenas;
+  absl::flat_hash_map<uint64_t, Entry> entries;
+  // Every pointer an entry depends on (list, arrays) -> entry key, so releasing
+  // a memory region is a range query instead of a scan of all entries. Moving
+  // scenes release regions hundreds of times per frame.
+  std::multimap<uintptr_t, uint64_t> pointerIndex;
+  // Optional render-thread uploader for partial ranges (GPU-side copy ordered in
+  // the GL stream). Dawn's WriteBuffer becomes glBufferSubData into a buffer the
+  // GPU is still reading, which stalls the CPU for a frame on this driver.
+  void (*directUpload)(const wgpu::Buffer& buffer, const uint8_t* data, const std::pair<size_t, size_t>* ranges, size_t count) = nullptr;
+  Stats stats;
+  size_t budget = [] {
+    const char* v = std::getenv("MELEE_FLIP_RESIDENT_MB");
+    const size_t mb = v ? std::max(8, std::atoi(v)) : 64;
+    return mb * 1024 * 1024;
+  }();
+};
+inline State& state() { static State s; return s; }
+
+inline Arena& arena_for_stride(uint32_t stride, uint32_t& index) {
+  auto& s = state();
+  for (uint32_t i = 0; i < s.arenas.size(); ++i)
+    if (s.arenas[i]->stride == stride) { index = i; return *s.arenas[i]; }
+  auto arena = std::make_unique<Arena>();
+  arena->stride = stride;
+  arena->mirror.reserve(4 * 1024 * 1024);
+  index = static_cast<uint32_t>(s.arenas.size());
+  s.arenas.push_back(std::move(arena));
+  return *s.arenas.back();
+}
+
+inline size_t total_used() {
+  size_t total = 0;
+  for (auto& a : state().arenas) total += a->used;
+  return total;
+}
+
+inline void index_entry(uint64_t key, const Entry& entry) {
+  auto& s = state();
+  s.pointerIndex.emplace(reinterpret_cast<uintptr_t>(entry.dl), key);
+  for (const void* p : entry.arrays) if (p) s.pointerIndex.emplace(reinterpret_cast<uintptr_t>(p), key);
+}
+inline void unindex_entry(uint64_t key, const Entry& entry) {
+  auto& s = state();
+  const auto drop = [&](const void* p) {
+    if (!p) return;
+    auto range = s.pointerIndex.equal_range(reinterpret_cast<uintptr_t>(p));
+    for (auto it = range.first; it != range.second; ++it) if (it->second == key) { s.pointerIndex.erase(it); break; }
+  };
+  drop(entry.dl);
+  for (const void* p : entry.arrays) drop(p);
+}
+inline void erase_entry(uint64_t key) {
+  auto& s = state();
+  if (auto it = s.entries.find(key); it != s.entries.end()) { unindex_entry(key, it->second); s.entries.erase(it); }
+}
+
+// Drop every entry and rewind the arenas. GPU buffers stay allocated.
+inline void reset_all() {
+  auto& s = state();
+  s.entries.clear();
+  s.pointerIndex.clear();
+  for (auto& a : s.arenas) {
+    std::lock_guard lock{a->mutex};
+    a->used = 0;
+    a->pending.clear();
+  }
+  ++s.stats.resets;
+}
+
+// Reserve `bytes` in the arena and return the mirror offset. Growth happens
+// under the lock because the render worker reads pending ranges from `mirror`.
+inline size_t reserve(Arena& a, size_t bytes) {
+  std::lock_guard lock{a.mutex};
+  const size_t offset = a.used;
+  if (a.mirror.size() < offset + bytes) a.mirror.resize(std::max(a.mirror.size() * 2, offset + bytes));
+  a.used = offset + bytes;
+  return offset;
+}
+inline void publish(Arena& a, size_t offset, size_t bytes) {
+  std::lock_guard lock{a.mutex};
+  if (!a.pending.empty() && a.pending.back().first + a.pending.back().second == offset) a.pending.back().second += bytes;
+  else a.pending.emplace_back(offset, bytes);
+}
+
+// Render worker: create/grow buffers and upload pending ranges. Called before
+// each render pass is encoded so every arena a pass references is current.
+inline void render_flush() {
+  auto& s = state();
+  for (uint32_t i = 0; i < s.arenas.size(); ++i) {
+    auto& a = *s.arenas[i];
+    std::lock_guard lock{a.mutex};
+    if (a.pending.empty() && a.bufferSize >= a.used) continue;
+    const size_t needed = a.used;
+    if (a.bufferSize < needed) {
+      size_t size = std::max<size_t>(a.bufferSize ? a.bufferSize * 2 : 4 * 1024 * 1024, needed);
+      size = (size + 255) & ~size_t(255);
+      const wgpu::BufferDescriptor desc{.label = "Resident vertex arena",
+                                        .usage = wgpu::BufferUsage::Vertex | wgpu::BufferUsage::CopyDst,
+                                        .size = size};
+      a.buffer = webgpu::g_device.CreateBuffer(&desc);
+      a.bufferSize = size;
+      ++a.generation;
+      // New storage: upload everything decoded so far.
+      const size_t bytes = (needed + 3) & ~size_t(3);
+      if (bytes) webgpu::g_queue.WriteBuffer(a.buffer, 0, a.mirror.data(), bytes);
+      a.pending.clear();
+      continue;
+    }
+    std::vector<std::pair<size_t, size_t>> aligned;
+    for (const auto& [offset, bytes] : a.pending) {
+      const size_t begin = offset & ~size_t(3);
+      const size_t end = std::min(a.mirror.size(), (offset + bytes + 3) & ~size_t(3));
+      if (end > begin) aligned.emplace_back(begin, end - begin);
+    }
+    if (s.directUpload) s.directUpload(a.buffer, a.mirror.data(), aligned.data(), aligned.size());
+    else for (const auto& [begin, bytes] : aligned) webgpu::g_queue.WriteBuffer(a.buffer, begin, a.mirror.data() + begin, bytes);
+    a.pending.clear();
+  }
+}
+
+inline bool has_pending() {
+  for (auto& a : state().arenas) {
+    std::lock_guard lock{a->mutex};
+    if (!a->pending.empty() || a->bufferSize < a->used) return true;
+  }
+  return false;
+}
+
+// Render worker: buffer for a DrawData::resident value (index + 1).
+inline const wgpu::Buffer& buffer(uint32_t resident, uint32_t* generation = nullptr) {
+  auto& a = *state().arenas[resident - 1];
+  if (generation) *generation = a.generation;
+  return a.buffer;
+}
+inline uint32_t stride(uint32_t resident) { return state().arenas[resident - 1]->stride; }
+inline size_t gpu_bytes(uint32_t resident) {
+  auto& a = *state().arenas[resident - 1];
+  std::lock_guard lock{a.mutex};
+  return a.bufferSize;
+}
+
+// FIFO worker: forget entries referencing released memory (range query).
+inline void invalidate(const void* base, size_t size) {
+  auto& s = state();
+  const auto lo = reinterpret_cast<uintptr_t>(base), hi = lo + size;
+  std::vector<uint64_t> keys;
+  for (auto it = s.pointerIndex.lower_bound(lo); it != s.pointerIndex.end() && it->first < hi; ++it) keys.push_back(it->second);
+  for (uint64_t key : keys) {
+    if (s.entries.contains(key)) { erase_entry(key); ++s.stats.invalidations; }
+  }
+}
+
+inline void shutdown() {
+  auto& s = state();
+  s.entries.clear();
+  s.arenas.clear();
+}
+
+} // namespace aurora::gx::resident
diff --git a/lib/gx/flip_uber.cpp b/lib/gx/flip_uber.cpp
new file mode 100644
index 0000000..97a6312
--- /dev/null
+++ b/lib/gx/flip_uber.cpp
@@ -0,0 +1,342 @@
+#include "flip_uber.hpp"
+
+#ifdef MELEE_MIYOO_FLIP
+#include "texture.hpp"
+
+#include <algorithm>
+#include <array>
+#include <bit>
+#include <cmath>
+#include <cstring>
+
+#include <fmt/format.h>
+
+namespace aurora::gx {
+namespace {
+constexpr Module Log{"aurora::gx::flip_uber"};
+constexpr u32 UberMaxTevStages = 16;
+constexpr u32 UberStateWords = 96;
+
+template <typename T>
+constexpr u32 enum_u32(T value) noexcept {
+  return static_cast<u32>(value);
+}
+
+Vec4<float> uber_texture_size_bias(const gfx::TextureBind& tex) {
+  const float width = static_cast<float>(tex.texObj.width());
+  const float height = static_cast<float>(tex.texObj.height());
+  float viewportBias = 0.f;
+  if (tex.ref && tex.ref->hasArbitraryMips) {
+    const float viewportScale =
+        std::min(g_gxState.renderViewport.width / std::max(g_gxState.logicalViewport.width, 1.f),
+                 g_gxState.renderViewport.height / std::max(g_gxState.logicalViewport.height, 1.f));
+    const float replacementScale = static_cast<float>(tex.ref->size.width) / std::max(width, 1.f);
+    viewportBias = std::log2(viewportScale / std::max(replacementScale, 0.001f));
+  }
+  return {width, height, tex.texObj.lod_bias() + viewportBias, tex.ref ? static_cast<float>(tex.ref->layer) : 0.f};
+}
+
+u32 pack_tev_op(const TevOp& op) noexcept {
+  return enum_u32(op.op) | (enum_u32(op.bias) << 4) | (enum_u32(op.scale) << 6) |
+         (enum_u32(op.outReg) << 8) | (u32(op.clamp) << 10);
+}
+
+u32 pack_channel(const ColorChannelConfig& channel) noexcept {
+  return enum_u32(channel.matSrc) | (enum_u32(channel.ambSrc) << 1) |
+         (enum_u32(channel.diffFn) << 2) | (enum_u32(channel.attnFn) << 4) |
+         (u32(channel.lightingEnabled) << 6);
+}
+} // namespace
+
+bool flip_uber_compatible(const ShaderConfig& config) noexcept {
+  if (config.lineMode || config.fogRangeEnabled || config.numIndStages || config.tevStageCount == 0 ||
+      config.tevStageCount > UberMaxTevStages)
+    return false;
+  for (const auto& tcg : config.tcgs) {
+    if (tcg.type >= GX_TG_BUMP0 && tcg.type <= GX_TG_BUMP7)
+      return false;
+    if (tcg.src > GX_TG_TEX7 && tcg.src != GX_TG_COLOR0 && tcg.src != GX_TG_COLOR1 && tcg.src != GX_MAX_TEXGENSRC)
+      return false;
+    if (tcg.postMtx != GX_PTIDENTITY &&
+        (tcg.postMtx < GX_PTTEXMTX0 || (tcg.postMtx - GX_PTTEXMTX0) / 3 >= 3))
+      return false;
+  }
+  return true;
+}
+
+void flip_uber_canonicalize(ShaderConfig& config) noexcept {
+  // A native specialized program must retain the GX material state in its key.
+  if (flip_native_specialized_enabled()) return;
+  const u16 flags = config.flipFlags;
+  const u32 tevStageCount = config.tevStageCount;
+  config = {};
+  config.flipFlags = flags;
+  // TEV count is a cheap, high-value native GLES specialization. Keeping it
+  // in the otherwise canonical key also prevents a merged draw from mixing
+  // records that require different constant loop bounds.
+  config.tevStageCount = tevStageCount;
+}
+
+void fill_flip_uber_uniform(ByteBuffer& buf, const ShaderConfig& config) noexcept {
+  buf.reserve_extra(MaxUniformSize);
+  buf.append<f32>(g_gxState.renderViewport.width);
+  buf.append<f32>(g_gxState.renderViewport.height);
+  buf.append<f32>(g_gxState.logicalViewport.width);
+  buf.append<f32>(g_gxState.logicalViewport.height);
+
+  auto proj = g_gxState.proj;
+  if constexpr (UseReversedZ)
+    proj.m2 = proj.m2 * Vec4{-1.f, -1.f, -1.f, -1.f};
+  else
+    proj.m2 = proj.m2 + proj.m3;
+  buf.append(proj);
+  for (const auto& matrix : g_gxState.pnMtx) buf.append(matrix.pos);
+  for (const auto& matrix : g_gxState.texMtxs) buf.append(matrix);
+  for (const auto& matrix : g_gxState.pnMtx) buf.append(matrix.nrm);
+  buf.append(g_gxState.colorRegs);
+  buf.append(g_gxState.lights);
+  for (const auto& channel : g_gxState.colorChannelState)
+    buf.append<u32>(channel.lightMask.to_ulong());
+  for (const auto& channel : g_gxState.colorChannelState) buf.append(channel.ambColor);
+  for (const auto& channel : g_gxState.colorChannelState) buf.append(channel.matColor);
+  buf.append(g_gxState.kcolors);
+  for (u32 i = 0; i < 3; ++i) buf.append(g_gxState.ptTexMtxs[i]);
+
+  const auto& fogState = g_gxState.fog;
+  const float logicalWidth = std::max(g_gxState.logicalViewport.width, 1.f);
+  const float renderWidth = std::max(g_gxState.renderViewport.width, 1.f);
+  Fog fog{
+      .color = fogState.color,
+      .a = fogState.a,
+      .b = fogState.b,
+      .c = fogState.c,
+      .rangeCenter = ((static_cast<float>(fogState.rangeCenter) - g_gxState.logicalViewport.left) / logicalWidth) * 2.f -
+                     1.f + (g_gxState.renderViewport.left / renderWidth) * 2.f,
+  };
+  for (u32 i = 0; i < fogState.rangeK.size(); ++i) {
+    const u32 source = (i & ~1u) | (1u - (i & 1u));
+    fog.rangeK[i / 4][i % 4] = static_cast<float>(fogState.rangeK[source]) / 64.f;
+  }
+  fog.rangeK[2][2] = fog.rangeK[2][1];
+  fog.rangeK[2][3] = fog.rangeK[2][1];
+  buf.append(fog);
+
+  for (const auto& scale : g_gxState.texCoordScales)
+    buf.append(Vec4{static_cast<f32>(scale.scaleS) + 1.f, static_cast<f32>(scale.scaleT) + 1.f, 0.f, 0.f});
+  for (u32 i = 0; i < MaxTextures; ++i) {
+    const auto& texture = get_texture(static_cast<GXTexMapID>(i));
+    buf.append(texture ? uber_texture_size_bias(texture) : Vec4{1.f, 1.f, 0.f, 0.f});
+  }
+
+  buf.append(flip_uber_state(config));
+  if (buf.size() < MaxUniformSize) buf.append_zeroes(MaxUniformSize - buf.size());
+  AURORA_ASSERT(buf.size() == MaxUniformSize, "Flip ubershader uniform size mismatch: {}", buf.size());
+}
+
+std::array<u32,96> flip_uber_state(const ShaderConfig& config) noexcept {
+  std::array<u32, UberStateWords> state{};
+  state[0] = config.tevStageCount | (u32(config.fogType) << 8) | (u32(config.fogRangeEnabled) << 16);
+  for (u32 i = 0; i <= GX_VA_TEX7; ++i)
+    state[1] |= u32(config.attrs[i].attrType != GX_NONE) << i;
+  state[2] = enum_u32(config.alphaCompare.comp0) | ((config.alphaCompare.ref0 & 255u) << 3) |
+             (enum_u32(config.alphaCompare.op) << 11) | (enum_u32(config.alphaCompare.comp1) << 13) |
+             ((config.alphaCompare.ref1 & 255u) << 16) | (u32(bool(config.alphaCompare)) << 24);
+  for (u32 i = 0; i < MaxTevSwap; ++i) {
+    const auto& swap = config.tevSwapTable[i];
+    const u32 packed = enum_u32(swap.red) | (enum_u32(swap.green) << 2) | (enum_u32(swap.blue) << 4) |
+                       (enum_u32(swap.alpha) << 6);
+    state[3] |= packed << (i * 8);
+  }
+  for (u32 i = 0; i < MaxColorChannels; ++i) state[4 + i] = pack_channel(config.colorChannels[i]);
+  for (u32 i = 0; i < MaxTexCoord; ++i) {
+    const auto& tcg = config.tcgs[i];
+    state[8 + i] = enum_u32(tcg.type) | (enum_u32(tcg.src) << 8) | (enum_u32(tcg.mtx) << 16) |
+                     (enum_u32(tcg.postMtx) << 24);
+    state[16 + i] = u32(tcg.normalize) | (u32(tcg.embossSrc) << 1);
+  }
+  for (u32 i = 0; i < config.tevStageCount; ++i) {
+    const auto& stage = config.tevStages[i];
+    const u32 base = 24 + i * 3;
+    state[base] = enum_u32(stage.colorPass.a) | (enum_u32(stage.colorPass.b) << 4) |
+                  (enum_u32(stage.colorPass.c) << 8) | (enum_u32(stage.colorPass.d) << 12) |
+                  (enum_u32(stage.alphaPass.a) << 16) | (enum_u32(stage.alphaPass.b) << 19) |
+                  (enum_u32(stage.alphaPass.c) << 22) | (enum_u32(stage.alphaPass.d) << 25);
+    state[base + 1] = pack_tev_op(stage.colorOp) | (pack_tev_op(stage.alphaOp) << 16);
+    state[base + 2] = enum_u32(stage.kcSel) | (enum_u32(stage.kaSel) << 5) |
+                      ((enum_u32(stage.texCoordId) & 15u) << 10) | ((enum_u32(stage.texMapId) & 15u) << 14) |
+                      ((enum_u32(stage.channelId) & 255u) << 18) | (enum_u32(stage.tevSwapRas) << 26) |
+                      (enum_u32(stage.tevSwapTex) << 28);
+  }
+  return state;
+}
+
+std::string build_flip_uber_shader_source(bool texturePairs) noexcept {
+  std::string bindings;
+  const bool arrays = gfx::texture_arrays_enabled();
+  const char* texType = arrays ? "texture_2d_array<f32>" : "texture_2d<f32>";
+  const char* layerArg = arrays ? " layer," : "";
+  for (u32 i = 0; i < MaxTextures; ++i) {
+    bindings += fmt::format("@group(2) @binding({0}) var tex{1}: {3};\n"
+                            "@group(2) @binding({2}) var samp{1}: sampler;\n", i * 2, i, i * 2 + 1, texType);
+    if (texturePairs)
+      bindings += fmt::format("@group(2) @binding({0}) var tex{1}b: {3};\n"
+                              "@group(2) @binding({2}) var samp{1}b: sampler;\n", 16 + i * 2, i, 17 + i * 2, texType);
+  }
+  std::string sampleCases;
+  for (u32 i = 0; i < MaxTextures; ++i) {
+    sampleCases += fmt::format("case {0}u: {{ if (bank == 0u) {{ return textureSampleGrad(tex{0}, samp{0}, uv,{1} dx, dy); }}", i, layerArg);
+    if (texturePairs)
+      sampleCases += fmt::format(" return textureSampleGrad(tex{0}b, samp{0}b, uv,{1} dx, dy);", i, layerArg);
+    else
+      sampleCases += fmt::format(" return textureSampleGrad(tex{0}, samp{0}, uv,{1} dx, dy);", i, layerArg);
+    sampleCases += " }\n";
+  }
+
+  return fmt::format(R"WGSL(
+diagnostic(off, derivative_uniformity);
+struct Light {{ pos: vec4f, dir: vec4f, color: vec4f, cos_att: vec4f, dist_att: vec4f }};
+struct Fog {{ color: vec4f, a: f32, b: f32, c: f32, range_center: f32, range_k: array<vec4f, 3> }};
+struct Uniform {{
+  render_viewport_size: vec2f, logical_viewport_size: vec2f,
+  proj: mat4x4f, postex_mtx: array<mat3x4f, 20>, nrm_mtx: array<mat3x4f, 10>,
+  tevreg: array<vec4f, 4>, lights: array<Light, 8>, light_state: vec4u,
+  cc_amb: array<vec4f, 4>, cc_mat: array<vec4f, 4>, kcolor: array<vec4f, 4>,
+  postmtx: array<mat3x4f, 3>, fog: Fog,
+  texcoord_scale: array<vec4f, 8>, tex_size_bias: array<vec4f, 8>, state: array<vec4u, 24>,
+}};
+struct FlipUniformRecord {{ @size(4096) value: Uniform }};
+@group(1) @binding(0) var<uniform> table: array<FlipUniformRecord, 16>;
+{0}
+
+var<private> record_index: u32;
+var<private> texture_bank: u32;
+fn word(index: u32) -> u32 {{ return table[record_index].value.state[index / 4u][index & 3u]; }}
+fn component(v: vec4f, i: u32) -> f32 {{ return v[i & 3u]; }}
+fn swap_color(v: vec4f, which: u32) -> vec4f {{
+  let s = (word(3u) >> (which * 8u)) & 255u;
+  return vec4f(component(v,s), component(v,s>>2u), component(v,s>>4u), component(v,s>>6u));
+}}
+fn overflow1(v: f32) -> f32 {{ let b=v*255.0; return (b-floor(b/256.0)*256.0)/255.0; }}
+fn overflow3(v: vec3f) -> vec3f {{ let b=v*255.0; return (b-floor(b/256.0)*256.0)/255.0; }}
+fn reg_color_normalized(arg:u32, cm:u32, am:u32)->bool {{
+  if(arg<8u) {{ let r=arg/2u; return select((am&(1u<<r))!=0u,(cm&(1u<<r))!=0u,(arg&1u)==0u); }}
+  return true;
+}}
+fn reg_alpha_normalized(arg:u32, am:u32)->bool {{ return arg>=4u || (am&(1u<<arg))!=0u; }}
+fn konst_color(sel:u32)->vec3f {{
+  if(sel<8u) {{ return vec3f(f32(8u-sel)/8.0); }}
+  if(sel>=12u && sel<16u) {{ return table[record_index].value.kcolor[sel-12u].rgb; }}
+  let x=sel-16u; return vec3f(component(table[record_index].value.kcolor[x&3u],x/4u));
+}}
+fn konst_alpha(sel:u32)->f32 {{
+  if(sel<8u) {{ return f32(8u-sel)/8.0; }}
+  let x=sel-16u; return component(table[record_index].value.kcolor[x&3u],x/4u);
+}}
+fn color_arg(arg:u32, regs:array<vec4f,4>, sampled:vec4f, rast:vec4f, konst:vec3f)->vec3f {{
+  switch arg {{
+    case 0u: {{return regs[0].rgb;}} case 1u: {{return vec3f(regs[0].a);}}
+    case 2u: {{return regs[1].rgb;}} case 3u: {{return vec3f(regs[1].a);}}
+    case 4u: {{return regs[2].rgb;}} case 5u: {{return vec3f(regs[2].a);}}
+    case 6u: {{return regs[3].rgb;}} case 7u: {{return vec3f(regs[3].a);}}
+    case 8u: {{return sampled.rgb;}} case 9u: {{return vec3f(sampled.a);}}
+    case 10u: {{return rast.rgb;}} case 11u: {{return vec3f(rast.a);}}
+    case 12u: {{return vec3f(1.0);}} case 13u: {{return vec3f(0.5);}}
+    case 14u: {{return konst;}} default: {{return vec3f(0.0);}}
+  }}
+}}
+fn alpha_arg(arg:u32, regs:array<vec4f,4>, sampled:vec4f, rast:vec4f, konst:f32)->f32 {{
+  switch arg {{ case 0u: {{return regs[0].a;}} case 1u: {{return regs[1].a;}}
+    case 2u: {{return regs[2].a;}} case 3u: {{return regs[3].a;}}
+    case 4u: {{return sampled.a;}} case 5u: {{return rast.a;}}
+    case 6u: {{return konst;}} default: {{return 0.0;}} }}
+}}
+fn bias_scale1(v:f32,b:u32,s:u32)->f32 {{
+  var r=v; if(b==1u){{r+=0.5;}} else if(b==2u){{r-=0.5;}}
+  if(s==1u){{r*=2.0;}} else if(s==2u){{r*=4.0;}} else if(s==3u){{r*=0.5;}} return r;
+}}
+fn bias_scale3(v:vec3f,b:u32,s:u32)->vec3f {{
+  var r=v; if(b==1u){{r+=vec3f(0.5);}} else if(b==2u){{r-=vec3f(0.5);}}
+  if(s==1u){{r*=2.0;}} else if(s==2u){{r*=4.0;}} else if(s==3u){{r*=0.5;}} return r;
+}}
+fn tev_color(op:u32,bias:u32,scale:u32,clamp_out:bool,a:vec3f,b:vec3f,c:vec3f,d:vec3f)->vec3f {{
+  var r:vec3f;
+  if(op<2u) {{ r=bias_scale3(select(mix(a,b,c)+d,-mix(a,b,c)+d,op==1u),bias,scale); }}
+  else if(op==8u || op==9u) {{r=select(vec3f(0),c,select(round(a.r*255.0)>round(b.r*255.0),round(a.r*255.0)==round(b.r*255.0),op==9u))+d;}}
+  else if(op==10u || op==11u) {{let av=round(dot(a.rg*255.0,vec2f(1,256)));let bv=round(dot(b.rg*255.0,vec2f(1,256)));r=select(vec3f(0),c,select(av>bv,av==bv,op==11u))+d;}}
+  else if(op==12u || op==13u) {{let av=round(dot(a*255.0,vec3f(1,256,65536)));let bv=round(dot(b*255.0,vec3f(1,256,65536)));r=select(vec3f(0),c,select(av>bv,av==bv,op==13u))+d;}}
+  else {{r=select(vec3f(0),c,select(round(a*255.0)>round(b*255.0),round(a*255.0)==round(b*255.0),op==15u))+d;}}
+  return clamp(r,select(vec3f(-4),vec3f(0),clamp_out),select(vec3f(4),vec3f(1),clamp_out));
+}}
+fn tev_alpha(op:u32,bias:u32,scale:u32,clamp_out:bool,a:f32,b:f32,c:f32,d:f32)->f32 {{
+  var r:f32; if(op<2u){{r=bias_scale1(select(mix(a,b,c)+d,-mix(a,b,c)+d,op==1u),bias,scale);}}
+  else {{r=select(0.0,c,select(round(a*255.0)>round(b*255.0),round(a*255.0)==round(b*255.0),(op&1u)!=0u))+d;}}
+  return clamp(r,select(-4.0,0.0,clamp_out),select(4.0,1.0,clamp_out));
+}}
+fn compare_alpha(value:u32,comp:u32,threshold:u32)->bool {{
+  switch comp {{case 0u:{{return false;}}case 1u:{{return value<threshold;}}case 2u:{{return value==threshold;}}
+    case 3u:{{return value<=threshold;}}case 4u:{{return value>threshold;}}case 5u:{{return value!=threshold;}}
+    case 6u:{{return value>=threshold;}}default:{{return true;}}}}
+}}
+fn sample_texture(id:u32,bank:u32,uv:vec2f,layer:i32,bias:f32)->vec4f {{
+  let scale=exp2(bias); let dx=dpdx(uv)*scale; let dy=dpdy(uv)*scale;
+  switch id {{ {1} default: {{return vec4f(1.0);}} }}
+}}
+fn light_channel(index:u32,vtx:vec4f,mv_pos:vec3f,mv_nrm:vec3f)->vec4f {{
+  let cfg=word(4u+index); let mat=select(table[record_index].value.cc_mat[index],vtx,(cfg&1u)!=0u);
+  if((cfg&64u)==0u){{return mat;}}
+  let amb=select(table[record_index].value.cc_amb[index],vtx,(cfg&2u)!=0u); var lighting=amb;
+  let mask=table[record_index].value.light_state[index];
+  for(var i=0u;i<8u;i++){{ if((mask&(1u<<i))==0u){{continue;}} let light=table[record_index].value.lights[i];
+    var ldir=light.pos.xyz-mv_pos;let dist2=dot(ldir,ldir);let dist=sqrt(dist2);ldir/=dist;var attn:f32;
+    let af=(cfg>>4u)&3u;let df=(cfg>>2u)&3u;
+    if(af==2u){{attn=1.0;}} else if(af==1u){{let cosine=max(0.0,dot(ldir,light.dir.xyz));let ca=dot(light.cos_att.xyz,vec3f(1,cosine,cosine*cosine));let da=dot(light.dist_att.xyz,vec3f(1,dist,dist2));attn=max(0.0,ca/da);}}
+    else {{attn=select(0.0,max(0.0,dot(mv_nrm,light.dir.xyz)),dot(mv_nrm,ldir)>=0.0);let ca=dot(light.cos_att.xyz,vec3f(1,attn,attn*attn));let da=max(0.0,dot(normalize(light.dist_att.xyz),vec3f(1,attn,attn*attn)));attn=max(0.0,ca/da);}}
+    var diff=1.0;if(df==1u){{diff=dot(ldir,mv_nrm);}}else if(df==2u){{diff=max(0.0,dot(ldir,mv_nrm));}}lighting+=attn*diff*light.color;
+  }} return mat*clamp(lighting,vec4f(0),vec4f(1));
+}}
+
+struct VertexOutput {{ @builtin(position) pos:vec4f, @location(0) @interpolate(flat) rec:u32,
+ @location(1) @interpolate(flat) bank:u32, @location(2) clr0:vec4f, @location(3) clr1:vec4f,
+ @location(4) tc0:vec3f,@location(5) tc1:vec3f,@location(6) tc2:vec3f,@location(7) tc3:vec3f,
+ @location(8) tc4:vec3f,@location(9) tc5:vec3f,@location(10) tc6:vec3f,@location(11) tc7:vec3f }};
+@vertex fn vs_main(@location(0) matrices:vec3u,@location(1) pos:vec3f,@location(2) normal_in:vec3f,
+ @location(3) color0:vec4f,@location(4) color1:vec4f,@location(5) tex0:vec2f,@location(6) tex1:vec2f,
+ @location(7) tex2:vec2f,@location(8) tex3:vec2f,@location(9) tex4:vec2f,@location(10) tex5:vec2f,
+ @location(11) tex6:vec2f,@location(12) tex7:vec2f,@location(13) binormal:vec3f,@location(14) tangent:vec3f)->VertexOutput{{
+  var out:VertexOutput;record_index=(matrices.z>>8u)&15u;texture_bank=(matrices.z>>12u)&1u;out.rec=record_index;out.bank=texture_bank;
+  let pn=matrices.x&255u;let mv=vec4f(pos,1)*table[record_index].value.postex_mtx[pn];out.pos=vec4f(mv,1)*table[record_index].value.proj;
+  let normal=select(vec3f(1,0,0),normal_in,(word(1u)&(1u<<10u))!=0u);let nt=vec4f(normal,0)*table[record_index].value.nrm_mtx[pn];let n=select(nt,normalize(nt),dot(nt,nt)>1e-10);
+  let lit0=light_channel(0u,color0,mv,n);let lita0=light_channel(2u,color0,mv,n);out.clr0=vec4f(lit0.rgb,lita0.a);
+  let lit1=light_channel(1u,color1,mv,n);let lita1=light_channel(3u,color1,mv,n);out.clr1=vec4f(lit1.rgb,lita1.a);
+  let inputs=array<vec2f,8>(tex0,tex1,tex2,tex3,tex4,tex5,tex6,tex7);
+  let texmtx=array<u32,8>((matrices.x>>8u)&255u,(matrices.x>>16u)&255u,(matrices.x>>24u)&255u,matrices.y&255u,(matrices.y>>8u)&255u,(matrices.y>>16u)&255u,(matrices.y>>24u)&255u,matrices.z&255u);
+  var coords:array<vec3f,8>;
+  for(var i=0u;i<8u;i++){{let cfg=word(8u+i);let ty=cfg&255u;let src=(cfg>>8u)&255u;var base:vec4f;
+    if(src==0u){{base=vec4f(pos,1);}}else if(src==1u){{base=vec4f(normal,1);}}else if(src==2u){{base=vec4f(binormal,1);}}else if(src==3u){{base=vec4f(tangent,1);}}
+    else if(src>=4u&&src<=11u){{base=vec4f(inputs[src-4u],1,1);}}else if(src==19u){{base=color0;}}else if(src==20u){{base=color1;}}else{{base=vec4f(0);}}
+    var tmp:vec3f;if(ty<=1u){{let configured=(cfg>>16u)&255u;let has=(word(1u)&(1u<<(i+1u)))!=0u;let mi=select(configured/3u,texmtx[i]/3u,has);tmp=select(base.xyz,base*table[record_index].value.postex_mtx[mi],configured!=60u||has);if(ty==1u){{tmp.z=1.0;}}}}else{{tmp=vec3f(base.xy,1);}}
+    if((word(16u+i)&1u)!=0u){{tmp=normalize(tmp);}}let post=(cfg>>24u)&255u;if(post!=125u){{tmp=vec4f(tmp,1)*table[record_index].value.postmtx[(post-64u)/3u];}}coords[i]=tmp;
+  }} out.tc0=coords[0];out.tc1=coords[1];out.tc2=coords[2];out.tc3=coords[3];out.tc4=coords[4];out.tc5=coords[5];out.tc6=coords[6];out.tc7=coords[7];return out;
+}}
+@fragment fn fs_main(in:VertexOutput)->@location(0) vec4f{{record_index=in.rec;texture_bank=in.bank;var regs=table[record_index].value.tevreg;var cm=0u;var am=0u;var lastc=0u;var lasta=0u;
+ let coords=array<vec3f,8>(in.tc0,in.tc1,in.tc2,in.tc3,in.tc4,in.tc5,in.tc6,in.tc7);let rast=array<vec4f,2>(in.clr0,in.clr1);let count=word(0u)&255u;
+ for(var i=0u;i<6u;i++){{if(i>=count){{break;}}let w0=word(24u+i*3u);let w1=word(25u+i*3u);let w2=word(26u+i*3u);
+  let tcid=(w2>>10u)&15u;let texid=(w2>>14u)&15u;var sample=vec4f(1);if(texid<8u&&tcid<8u){{let tc=coords[tcid];let ty=word(8u+tcid)&255u;let uv=select(tc.xy,tc.xy/tc.z,ty==0u);sample=sample_texture(texid,texture_bank,uv,i32(table[record_index].value.tex_size_bias[texid].w),table[record_index].value.tex_size_bias[texid].z);}}sample=swap_color(sample,(w2>>28u)&3u);
+  let chan=(w2>>18u)&255u;var rv=vec4f(0);if(chan<6u){{rv=rast[chan&1u];}}rv=swap_color(rv,(w2>>26u)&3u);
+  let kc=konst_color(w2&31u);let ka=konst_alpha((w2>>5u)&31u);let ca=array<u32,4>(w0&15u,(w0>>4u)&15u,(w0>>8u)&15u,(w0>>12u)&15u);let aa=array<u32,4>((w0>>16u)&7u,(w0>>19u)&7u,(w0>>22u)&7u,(w0>>25u)&7u);
+  var a=color_arg(ca[0],regs,sample,rv,kc);var b=color_arg(ca[1],regs,sample,rv,kc);var c=color_arg(ca[2],regs,sample,rv,kc);let d=color_arg(ca[3],regs,sample,rv,kc);
+  if(!reg_color_normalized(ca[0],cm,am)){{a=overflow3(a);}}if(!reg_color_normalized(ca[1],cm,am)){{b=overflow3(b);}}if(!reg_color_normalized(ca[2],cm,am)){{c=overflow3(c);}}
+  let cop=w1&2047u;let co=(cop>>8u)&3u;let rgb=tev_color(cop&15u,(cop>>4u)&3u,(cop>>6u)&3u,(cop&1024u)!=0u,a,b,c,d);regs[co]=vec4f(rgb,regs[co].a);cm=(cm&~(1u<<co))|select(0u,1u<<co,(cop&1024u)!=0u);lastc=co;
+  var xa=alpha_arg(aa[0],regs,sample,rv,ka);var xb=alpha_arg(aa[1],regs,sample,rv,ka);var xc=alpha_arg(aa[2],regs,sample,rv,ka);let xd=alpha_arg(aa[3],regs,sample,rv,ka);
+  if(!reg_alpha_normalized(aa[0],am)){{xa=overflow1(xa);}}if(!reg_alpha_normalized(aa[1],am)){{xb=overflow1(xb);}}if(!reg_alpha_normalized(aa[2],am)){{xc=overflow1(xc);}}
+  let aop=(w1>>16u)&2047u;let ao=(aop>>8u)&3u;let av=tev_alpha(aop&15u,(aop>>4u)&3u,(aop>>6u)&3u,(aop&1024u)!=0u,xa,xb,xc,xd);regs[ao]=vec4f(regs[ao].rgb,av);am=(am&~(1u<<ao))|select(0u,1u<<ao,(aop&1024u)!=0u);lasta=ao;
+ }} var prev=vec4f(regs[lastc].rgb,regs[lasta].a);if((cm&(1u<<lastc))==0u){{prev=vec4f(overflow3(prev.rgb),prev.a);}}if((am&(1u<<lasta))==0u){{prev.a=overflow1(prev.a);}}
+ let ac=word(2u);if((ac&(1u<<24u))!=0u){{let value=u32(round(clamp(prev.a,0,1)*255.0));let p0=compare_alpha(value,ac&7u,(ac>>3u)&255u);let p1=compare_alpha(value,(ac>>13u)&7u,(ac>>16u)&255u);let op=(ac>>11u)&3u;let passed=select(select(p0!=p1,p0==p1,op==3u),select(p0&&p1,p0||p1,op==1u),op<2u);if(!passed){{discard;}}}}
+ let fog_type=(word(0u)>>8u)&255u;if(fog_type!=0u){{let f=table[record_index].value.fog;let depth=1.0-in.pos.z;var base:f32;if((fog_type&8u)!=0u){{base=f.a*depth;}}else{{base=f.a/(f.b-depth);}}var ff=clamp(base-f.c,0,1);var z:f32;if(fog_type==2u||fog_type==10u){{z=ff;}}else if(fog_type==4u||fog_type==12u){{z=1-exp2(-8*ff);}}else if(fog_type==5u||fog_type==13u){{z=1-exp2(-8*ff*ff);}}else if(fog_type==6u||fog_type==14u){{z=exp2(-8*(1-ff));}}else{{ff=1-ff;z=exp2(-8*ff*ff);}}prev=vec4f(mix(prev.rgb,f.color.rgb,clamp(z,0,1)),prev.a);}}
+ return prev;
+}}
+)WGSL", bindings, sampleCases);
+}
+
+} // namespace aurora::gx
+#endif
diff --git a/lib/gx/flip_uber.hpp b/lib/gx/flip_uber.hpp
new file mode 100644
index 0000000..5dc3710
--- /dev/null
+++ b/lib/gx/flip_uber.hpp
@@ -0,0 +1,38 @@
+#pragma once
+
+#include "gx.hpp"
+
+#ifdef MELEE_MIYOO_FLIP
+#include <cstdlib>
+#include <cstring>
+#include <string>
+
+namespace aurora::gx {
+
+inline bool flip_native_specialized_enabled() {
+  static const bool enabled=[] {const char* v=std::getenv("MELEE_FLIP_NATIVE_SPECIALIZED");return v&&std::strcmp(v,"0");}();
+  return enabled;
+}
+
+inline bool flip_uber_enabled() {
+  static const bool enabled = [] {
+    const char* value = std::getenv("MELEE_FLIP_UBERSHADER");
+    const char* native = std::getenv("MELEE_FLIP_NATIVE_UBER");
+    return (value && std::strcmp(value, "0")) || (native && std::strcmp(native, "0")) || flip_native_specialized_enabled();
+  }();
+  return enabled;
+}
+
+inline bool flip_native_uber_enabled() {
+  static const bool enabled=[] {const char* value=std::getenv("MELEE_FLIP_NATIVE_UBER");return value&&std::strcmp(value,"0");}();
+  return enabled || flip_native_specialized_enabled();
+}
+
+bool flip_uber_compatible(const ShaderConfig& config) noexcept;
+std::array<u32,96> flip_uber_state(const ShaderConfig& config) noexcept;
+void flip_uber_canonicalize(ShaderConfig& config) noexcept;
+std::string build_flip_uber_shader_source(bool texturePairs) noexcept;
+void fill_flip_uber_uniform(ByteBuffer& buffer, const ShaderConfig& config) noexcept;
+
+} // namespace aurora::gx
+#endif
diff --git a/lib/gx/flip_vertex.hpp b/lib/gx/flip_vertex.hpp
new file mode 100644
index 0000000..b4049dc
--- /dev/null
+++ b/lib/gx/flip_vertex.hpp
@@ -0,0 +1,1011 @@
+#pragma once
+#include <fmt/format.h>
+#include <string>
+// Flip conventional-input backend. Decode GX's independent indices once on the
+// FIFO worker, before immutable frame data is uploaded. No vertex texture loads.
+#include "pipeline.hpp"
+#include "../gfx/recording.hpp"
+#include "../gfx/flip_profile.hpp"
+#include <bit>
+#include <cmath>
+#include <cstdlib>
+#include <cstring>
+#include <vector>
+#include <deque>
+#include <memory>
+#include <cassert>
+#include <type_traits>
+#include <xxhash.h>
+#if defined(__aarch64__)
+#include <arm_neon.h>
+#endif
+
+namespace aurora::gx {
+inline bool flip_vertex_enabled() {
+  static const bool enabled = [] {
+    auto* s = std::getenv("MELEE_FLIP_VERTEX_INPUT");
+    return s && std::strcmp(s, "0");
+  }();
+  return enabled;
+}
+inline bool flip_uniform_enabled() {
+  static const bool enabled = [] {
+    auto* s = std::getenv("MELEE_FLIP_UNIFORM_TABLE");
+    return s && std::strcmp(s, "0");
+  }();
+  return enabled;
+}
+inline bool flip_batch_enabled() {
+  static const bool enabled = [] {
+    auto* s = std::getenv("MELEE_FLIP_BATCH_DRAWS");
+    return s && std::strcmp(s, "0");
+  }();
+  return enabled && flip_vertex_enabled() && flip_uniform_enabled();
+}
+inline bool flip_cpu_upload_enabled() {
+  static const bool enabled = [] { const char* s=std::getenv("MELEE_FLIP_CPU_UPLOAD"); return !s || std::strcmp(s,"0"); }();
+  return enabled && flip_vertex_enabled();
+}
+inline bool flip_uniform_windows_enabled() {
+  static const bool enabled = [] { const char* s=std::getenv("MELEE_FLIP_UNIFORM_WINDOWS"); return !s || std::strcmp(s,"0"); }();
+  return enabled && flip_uniform_enabled() && flip_cpu_upload_enabled();
+}
+inline bool flip_texture_pairs_enabled() {
+  static const bool enabled = [] { const char* s=std::getenv("MELEE_FLIP_TEXTURE_PAIRS"); return s&&std::strcmp(s,"0"); }();
+  return enabled && flip_batch_enabled();
+}
+inline constexpr uint32_t FlipUniformStride = 4096;
+inline constexpr uint32_t FlipUniformWindow = 65536;
+static_assert(MaxUniformSize <= FlipUniformStride);
+struct FlipVertex {
+  uint32_t matrices[3]{};
+  float pos[3]{}, normal[3]{}, color[2][4]{}, tex[8][2]{};
+  float binormal[3]{}, tangent[3]{}, lineEnd[4]{};
+};
+static_assert(sizeof(FlipVertex) == 172);
+struct FlipVertexLayout {
+  std::array<wgpu::VertexAttribute, 16> attributes{};
+  std::array<size_t, 16> sources{};
+  unsigned count = 0, stride = 0;
+};
+inline bool flip_vertex_location(const ShaderConfig& c, unsigned i) {
+  if (c.flipFlags & FlipUberShader)
+    return i < 15;
+  if (i == 0) {
+    if (c.lineMode || (c.flipFlags & FlipBatchedDraws))
+      return true;
+    for (unsigned a = GX_VA_PNMTXIDX; a <= GX_VA_TEX7MTXIDX; ++a)
+      if (c.attrs[a].attrType != GX_NONE)
+        return true;
+    return false;
+  }
+  if (i == 1)
+    return true;
+  if (i == 2)
+    return c.attrs[GX_VA_NRM].attrType != GX_NONE;
+  if (i < 5)
+    return c.attrs[GX_VA_CLR0 + i - 3].attrType != GX_NONE;
+  if (i < 13)
+    return c.attrs[GX_VA_TEX0 + i - 5].attrType != GX_NONE;
+  if (i < 15)
+    return c.attrs[GX_VA_NRM].attrType != GX_NONE && c.attrs[GX_VA_NRM].cnt == 9;
+  return c.lineMode == 1 || c.lineMode == 2;
+}
+inline unsigned flip_attribute_bytes(unsigned i) {
+  return i == 3 || i == 4 || i == 15 ? 16 : i >= 5 && i <= 12 ? 8 : 12;
+}
+inline auto flip_vertex_attributes(const ShaderConfig& c) {
+  FlipVertexLayout out{};
+  const auto set = [&](unsigned i, size_t off, wgpu::VertexFormat f) {
+    if (!flip_vertex_location(c, i))
+      return;
+    auto& a = out.attributes[out.count];
+    a.shaderLocation = i;
+    a.offset = out.stride;
+    a.format = f;
+    out.sources[out.count++] = off;
+    out.stride += flip_attribute_bytes(i);
+  };
+  set(0, offsetof(FlipVertex, matrices), wgpu::VertexFormat::Uint32x3);
+  set(1, offsetof(FlipVertex, pos), wgpu::VertexFormat::Float32x3);
+  set(2, offsetof(FlipVertex, normal), wgpu::VertexFormat::Float32x3);
+  for (unsigned i = 0; i < 2; ++i)
+    set(3 + i, offsetof(FlipVertex, color) + 16 * i, wgpu::VertexFormat::Float32x4);
+  for (unsigned i = 0; i < 8; ++i)
+    set(5 + i, offsetof(FlipVertex, tex) + 8 * i, wgpu::VertexFormat::Float32x2);
+  set(13, offsetof(FlipVertex, binormal), wgpu::VertexFormat::Float32x3);
+  set(14, offsetof(FlipVertex, tangent), wgpu::VertexFormat::Float32x3);
+  set(15, offsetof(FlipVertex, lineEnd), wgpu::VertexFormat::Float32x4);
+  return out;
+}
+inline uint32_t flip_integer(const u8* p, unsigned bytes, bool le) {
+  uint32_t v = 0;
+  for (unsigned i = 0; i < bytes; ++i)
+    v |= uint32_t(p[i]) << ((le ? i : bytes - i - 1) * 8);
+  return v;
+}
+inline FlipVertex flip_decode_vertex(const ShaderConfig& c, const u8* raw,
+                                     const std::array<AttrArray, MaxVtxAttr>& arrays, u32 pnMtx) {
+  FlipVertex out{};
+  out.matrices[0] = pnMtx;
+  for (unsigned ai = GX_VA_PNMTXIDX; ai <= GX_VA_TEX7; ++ai) {
+    const auto& m = c.attrs[ai];
+    if (m.attrType == GX_NONE)
+      continue;
+    const auto address = [&](unsigned slice, unsigned bytes) -> const u8* {
+      const unsigned componentBytes =
+          (m.compType == GX_F32 ? 4 : (m.compType == GX_U16 || m.compType == GX_S16 ? 2 : 1));
+      const unsigned within = slice * 3 * componentBytes;
+      if (m.attrType == GX_DIRECT)
+        return raw + m.offset + within;
+      const unsigned indexBytes = m.attrType == GX_INDEX8 ? 1 : 2;
+      const auto index = flip_integer(raw + m.offset + (m.nbt3 ? slice * indexBytes : 0), indexBytes, false);
+      const auto& array = arrays[ai];
+      const uint64_t off = uint64_t(index) * m.stride + within;
+      // Match robust GPU reads, but never read outside the CPU source array.
+      static const u8 zero[16]{};
+      if (!array.data || off + bytes > array.size)
+        return zero;
+      return static_cast<const u8*>(array.data) + off;
+    };
+    if (ai <= GX_VA_TEX7MTXIDX) {
+      unsigned v = *address(0, 1);
+      if (ai == GX_VA_PNMTXIDX)
+        v /= 3;
+      out.matrices[ai / 4] &= ~(255u << ((ai % 4) * 8));
+      out.matrices[ai / 4] |= v << ((ai % 4) * 8);
+      continue;
+    }
+    const bool le = m.attrType != GX_DIRECT && m.le;
+    if (ai == GX_VA_CLR0 || ai == GX_VA_CLR1) {
+      float* dst = out.color[ai - GX_VA_CLR0];
+      unsigned bytes = (m.compType == GX_RGB565 || m.compType == GX_RGBA4)
+                           ? 2
+                           : (m.compType == GX_RGB8 || m.compType == GX_RGBA6 ? 3 : 4);
+      const u8* p = address(0, bytes);
+      const uint32_t v = flip_integer(p, bytes, le);
+      if (m.compType == GX_RGB565) {
+        dst[0] = float((v >> 11) & 31) / 31;
+        dst[1] = float((v >> 5) & 63) / 63;
+        dst[2] = float(v & 31) / 31;
+        dst[3] = 1;
+      } else if (m.compType == GX_RGBA4 || m.compType == GX_RGBA6) {
+        unsigned bits = m.compType == GX_RGBA4 ? 4 : 6, mask = (1u << bits) - 1;
+        for (unsigned j = 0; j < 4; ++j)
+          dst[j] = float((v >> ((3 - j) * bits)) & mask) / mask;
+      } else {
+        for (unsigned j = 0; j < 4; ++j)
+          dst[j] = (j == 3 && m.compType != GX_RGBA8) ? 1.f : float(p[j]) / 255.f;
+      }
+      continue;
+    }
+    const unsigned bytes = m.compType == GX_F32 ? 4 : (m.compType == GX_U16 || m.compType == GX_S16 ? 2 : 1);
+    const unsigned slices = ai == GX_VA_NRM && m.cnt == 9 ? 3 : 1;
+    for (unsigned slice = 0; slice < slices; ++slice) {
+      float* dst = ai == GX_VA_POS   ? out.pos
+                   : ai == GX_VA_NRM ? (slice == 0   ? out.normal
+                                        : slice == 1 ? out.binormal
+                                                     : out.tangent)
+                                     : out.tex[ai - GX_VA_TEX0];
+      unsigned count = ai == GX_VA_NRM ? 3 : m.cnt;
+      const u8* p = address(slice, count * bytes);
+      for (unsigned j = 0; j < count; ++j) {
+        uint32_t v = flip_integer(p + j * bytes, bytes, le);
+        if (m.compType == GX_F32)
+          dst[j] = std::bit_cast<float>(v);
+        else {
+          int32_t n = m.compType == GX_S8 ? int8_t(v) : m.compType == GX_S16 ? int16_t(v) : int32_t(v);
+          dst[j] = float(n) / float(uint32_t(1) << m.frac);
+        }
+      }
+    }
+  }
+  return out;
+}
+// Attribute-major decoding hoists format selection and scalar conversion out
+// of the per-vertex loop. The reference decoder remains for NBT and diagnostics.
+inline bool flip_decode_bulk(const ShaderConfig& c, const u8* raw, size_t count,
+                             std::vector<FlipVertex>& out,
+                             const std::array<AttrArray, MaxVtxAttr>& arrays, u32 pnMtx) {
+  for (unsigned ai = GX_VA_POS; ai <= GX_VA_TEX7; ++ai) {
+    const auto& m = c.attrs[ai];
+    if (m.attrType == GX_NONE) continue;
+    if (ai == GX_VA_NRM && m.cnt == 9) return false;
+  }
+  out.resize(count);
+  std::memset(out.data(), 0, count * sizeof(FlipVertex));
+  for (auto& v : out) v.matrices[0] = pnMtx;
+  for (unsigned ai = GX_VA_PNMTXIDX; ai <= GX_VA_TEX7; ++ai) {
+    const auto& m = c.attrs[ai];
+    if (m.attrType == GX_NONE) continue;
+    const auto& array = arrays[ai];
+    const auto address = [&](size_t i, unsigned bytes) -> const u8* {
+      const auto* src = raw + i * c.vtxStride + m.offset;
+      if (m.attrType == GX_DIRECT) return src;
+      const unsigned index = m.attrType == GX_INDEX8 ? *src : unsigned(src[0]) * 256 + src[1];
+      const uint64_t off = uint64_t(index) * m.stride;
+      static constexpr u8 zero[16]{};
+      return array.data && off + bytes <= array.size ? static_cast<const u8*>(array.data) + off : zero;
+    };
+    if (ai <= GX_VA_TEX7MTXIDX) {
+      for (size_t i = 0; i < count; ++i) {
+        const unsigned value = *address(i, 1) / (ai == GX_VA_PNMTXIDX ? 3 : 1);
+        auto& word = out[i].matrices[ai / 4];
+        word = (word & ~(255u << ((ai % 4) * 8))) | (value << ((ai % 4) * 8));
+      }
+      continue;
+    }
+    if (ai == GX_VA_CLR0 || ai == GX_VA_CLR1) {
+      const unsigned bytes=(m.compType==GX_RGB565 || m.compType==GX_RGBA4) ? 2
+        : (m.compType==GX_RGB8 || m.compType==GX_RGBA6) ? 3 : 4;
+      const bool le=m.attrType!=GX_DIRECT && m.le;
+      for (size_t i=0;i<count;++i) {
+        auto* dst=out[i].color[ai-GX_VA_CLR0];
+        const auto* src=address(i,bytes);
+        const uint32_t v=flip_integer(src,bytes,le);
+        if (m.compType==GX_RGB565) {
+          dst[0]=float((v>>11)&31)/31; dst[1]=float((v>>5)&63)/63;
+          dst[2]=float(v&31)/31; dst[3]=1;
+        } else if (m.compType==GX_RGBA4 || m.compType==GX_RGBA6) {
+          const unsigned bits=m.compType==GX_RGBA4 ? 4 : 6,mask=(1u<<bits)-1;
+          for(unsigned j=0;j<4;++j) dst[j]=float((v>>((3-j)*bits))&mask)/mask;
+        } else {
+          for(unsigned j=0;j<3;++j) dst[j]=float(src[j])/255.f;
+          dst[3]=m.compType==GX_RGBA8 ? float(src[3])/255.f : 1.f;
+        }
+      }
+      continue;
+    }
+    const unsigned components = ai == GX_VA_NRM ? 3 : m.cnt;
+    const unsigned offset = ai == GX_VA_POS ? offsetof(FlipVertex, pos)
+      : ai == GX_VA_NRM ? offsetof(FlipVertex, normal)
+      : offsetof(FlipVertex, tex) + (ai - GX_VA_TEX0) * sizeof(out[0].tex[0]);
+    const float scale = float(uint32_t(1) << m.frac);
+    const auto convert = [&]<typename T, bool Little>() {
+      for (size_t i = 0; i < count; ++i) {
+        const auto* src = address(i, components * sizeof(T));
+        auto* dst = reinterpret_cast<float*>(reinterpret_cast<u8*>(&out[i]) + offset);
+        for (unsigned j = 0; j < components; ++j) {
+          T value;
+          if constexpr (sizeof(T) == 4) {
+            uint32_t bits; std::memcpy(&bits, src + j * 4, 4);
+            if constexpr (!Little) bits = __builtin_bswap32(bits);
+            std::memcpy(&value, &bits, 4);
+          } else if constexpr (sizeof(T) == 2) {
+            uint16_t bits; std::memcpy(&bits, src + j * 2, 2);
+            if constexpr (!Little) bits = __builtin_bswap16(bits);
+            std::memcpy(&value, &bits, 2);
+          } else std::memcpy(&value, src + j, 1);
+          if constexpr (std::is_same_v<T, float>) dst[j] = value;
+          else dst[j] = float(value) / scale;
+        }
+      }
+    };
+    const bool le = m.attrType != GX_DIRECT && m.le;
+#define FLIP_CONVERT(TYPE) do { if (le) convert.template operator()<TYPE, true>(); else convert.template operator()<TYPE, false>(); } while (0)
+    switch (m.compType) {
+      case GX_F32: FLIP_CONVERT(float); break;
+      case GX_U16: FLIP_CONVERT(uint16_t); break;
+      case GX_S16: FLIP_CONVERT(int16_t); break;
+      case GX_S8: FLIP_CONVERT(int8_t); break;
+      default: FLIP_CONVERT(uint8_t); break;
+    }
+#undef FLIP_CONVERT
+  }
+  return true;
+}
+
+// Generic attribute-major decoder. Retained as the fallback and test oracle for
+// the specialized loaders below; it re-derives every format decision per draw.
+inline void flip_decode_packed_generic(const ShaderConfig& c, const FlipVertexLayout& layout,
+                               const u8* raw, size_t count, std::span<u8> out,
+                               const std::array<AttrArray, MaxVtxAttr>& arrays, u32 pnMtx) {
+  gfx::detail::flip_profile::Scope profile("vertex_decode",0,count);
+  assert(out.size()==count*layout.stride);
+  std::memset(out.data(), 0, out.size());
+  const auto destination_offset = [&](unsigned location) -> size_t {
+    for (unsigned i = 0; i < layout.count; ++i)
+      if (layout.attributes[i].shaderLocation == location) return layout.attributes[i].offset;
+    return SIZE_MAX;
+  };
+  const size_t matrixOffset=destination_offset(0);
+  if(matrixOffset!=SIZE_MAX)
+    for(size_t i=0;i<count;++i) std::memcpy(out.data()+i*layout.stride+matrixOffset,&pnMtx,sizeof(pnMtx));
+
+  for(unsigned ai=GX_VA_PNMTXIDX;ai<=GX_VA_TEX7;++ai) {
+    const auto& m=c.attrs[ai];
+    if(m.attrType==GX_NONE) continue;
+    // Stable, decodable format key: attribute/type/components/address/endian/NBT.
+    const uint64_t format=ai|(uint64_t(m.compType)<<8)|(uint64_t(m.cnt)<<16)|
+      (uint64_t(m.attrType)<<24)|(uint64_t(m.le)<<32)|(uint64_t(m.nbt3)<<33);
+    gfx::detail::flip_profile::Scope attributeProfile("vertex_attribute",format,count);
+    const unsigned componentBytes=m.compType==GX_F32 ? 4 :
+      (m.compType==GX_U16 || m.compType==GX_S16) ? 2 : 1;
+    const auto address=[&](size_t vertex,unsigned slice,unsigned bytes)->const u8* {
+      const unsigned within=slice*3*componentBytes;
+      const auto* src=raw+vertex*c.vtxStride+m.offset;
+      if(m.attrType==GX_DIRECT) return src+within;
+      const unsigned indexBytes=m.attrType==GX_INDEX8 ? 1 : 2;
+      src+=m.nbt3 ? slice*indexBytes : 0;
+      const unsigned index=indexBytes==1 ? src[0] : unsigned(src[0])*256+src[1];
+      const auto& array=arrays[ai];
+      const uint64_t offset=uint64_t(index)*m.stride+within;
+      static constexpr u8 zero[48]{};
+      return array.data && offset+bytes<=array.size ? static_cast<const u8*>(array.data)+offset : zero;
+    };
+    if(ai<=GX_VA_TEX7MTXIDX) {
+      if(matrixOffset==SIZE_MAX) continue;
+      const unsigned shift=(ai%4)*8;
+      for(size_t i=0;i<count;++i) {
+        unsigned value=*address(i,0,1)/(ai==GX_VA_PNMTXIDX ? 3 : 1);
+        auto* dst=out.data()+i*layout.stride+matrixOffset+(ai/4)*4;
+        uint32_t word;std::memcpy(&word,dst,4);
+        word=(word&~(255u<<shift))|(value<<shift);std::memcpy(dst,&word,4);
+      }
+      continue;
+    }
+    const unsigned location=ai==GX_VA_POS ? 1 : ai==GX_VA_NRM ? 2 :
+      ai==GX_VA_CLR0 ? 3 : ai==GX_VA_CLR1 ? 4 : 5+ai-GX_VA_TEX0;
+    const size_t baseOffset=destination_offset(location);
+    if(baseOffset==SIZE_MAX) continue;
+    if(ai==GX_VA_CLR0 || ai==GX_VA_CLR1) {
+      const unsigned bytes=(m.compType==GX_RGB565 || m.compType==GX_RGBA4) ? 2 :
+        (m.compType==GX_RGB8 || m.compType==GX_RGBA6) ? 3 : 4;
+      const bool le=m.attrType!=GX_DIRECT && m.le;
+      for(size_t i=0;i<count;++i) {
+        auto* dst=reinterpret_cast<float*>(out.data()+i*layout.stride+baseOffset);
+        const auto* src=address(i,0,bytes);
+        const uint32_t value=flip_integer(src,bytes,le);
+        if(m.compType==GX_RGB565) {
+          dst[0]=float((value>>11)&31)/31;dst[1]=float((value>>5)&63)/63;
+          dst[2]=float(value&31)/31;dst[3]=1;
+        } else if(m.compType==GX_RGBA4 || m.compType==GX_RGBA6) {
+          const unsigned bits=m.compType==GX_RGBA4 ? 4 : 6,mask=(1u<<bits)-1;
+          for(unsigned j=0;j<4;++j) dst[j]=float((value>>((3-j)*bits))&mask)/mask;
+        } else {
+          for(unsigned j=0;j<3;++j) dst[j]=float(src[j])/255.f;
+          dst[3]=m.compType==GX_RGBA8 ? float(src[3])/255.f : 1.f;
+        }
+      }
+      continue;
+    }
+    const unsigned components=ai==GX_VA_NRM ? 3 : m.cnt;
+    const unsigned slices=ai==GX_VA_NRM && m.cnt==9 ? 3 : 1;
+    const float inverseScale=1.f/float(uint32_t(1)<<m.frac);
+    const auto convert=[&]<typename T,bool Little,unsigned Components,unsigned Slices>() {
+      // Select record dimensions once per attribute, so the inner loops have
+      // fixed bounds. In particular, ordinary XYZ is not an NBT loop and XY
+      // does not carry a runtime component loop through every indexed load.
+      for(size_t i=0;i<count;++i) for(unsigned slice=0;slice<Slices;++slice) {
+        const size_t sliceOffset=slice==0 ? baseOffset : destination_offset(12+slice);
+        if(sliceOffset==SIZE_MAX) continue;
+        auto* dst=reinterpret_cast<float*>(out.data()+i*layout.stride+sliceOffset);
+        const auto* src=address(i,slice,Components*sizeof(T));
+#if defined(__aarch64__)
+        // The common GX positions/texcoords use two or three 16-bit fixed
+        // point components. Convert them together, without reading beyond
+        // the six-byte XYZ record (arrays can end immediately after it).
+        if constexpr(sizeof(T)==2) {
+          static const bool neon=[] {const char* v=std::getenv("MELEE_FLIP_NEON_DECODE");return !v||std::strcmp(v,"0");}();
+          if(neon&&(Components==2||Components==3)) {
+            uint32_t xy;std::memcpy(&xy,src,4);
+            uint16x4_t h=vreinterpret_u16_u32(vdup_n_u32(xy));
+            if constexpr(Components==3) {
+              uint16_t z;std::memcpy(&z,src+4,2);h=vset_lane_u16(z,h,2);
+            }
+            if constexpr(!Little) h=vreinterpret_u16_u8(vrev16_u8(vreinterpret_u8_u16(h)));
+            float32x4_t values;
+            if constexpr(std::is_signed_v<T>) values=vcvtq_f32_s32(vmovl_s16(vreinterpret_s16_u16(h)));
+            else values=vcvtq_f32_u32(vmovl_u16(h));
+            values=vmulq_n_f32(values,inverseScale);
+            vst1_f32(dst,vget_low_f32(values));
+            if constexpr(Components==3) vst1q_lane_f32(dst+2,values,2);
+            continue;
+          }
+        }
+#endif
+        for(unsigned j=0;j<Components;++j) {
+          T value;
+          if constexpr(sizeof(T)==4) {
+            uint32_t bits;std::memcpy(&bits,src+j*4,4);
+            if constexpr(!Little) bits=__builtin_bswap32(bits);
+            std::memcpy(&value,&bits,4);
+          } else if constexpr(sizeof(T)==2) {
+            uint16_t bits;std::memcpy(&bits,src+j*2,2);
+            if constexpr(!Little) bits=__builtin_bswap16(bits);
+            std::memcpy(&value,&bits,2);
+          } else std::memcpy(&value,src+j,1);
+          if constexpr(std::is_same_v<T,float>) dst[j]=value;
+          else dst[j]=float(value)*inverseScale;
+        }
+      }
+    };
+    const auto dimensions=[&]<typename T,bool Little>() {
+      if(slices==3) convert.template operator()<T,Little,3,3>();
+      else if(components==3) convert.template operator()<T,Little,3,1>();
+      else if(components==2) convert.template operator()<T,Little,2,1>();
+      else convert.template operator()<T,Little,1,1>();
+    };
+    const bool le=m.attrType!=GX_DIRECT && m.le;
+#define FLIP_PACKED_CONVERT(TYPE) do { if(le) dimensions.template operator()<TYPE,true>(); else dimensions.template operator()<TYPE,false>(); } while(0)
+    switch(m.compType) {
+      case GX_F32: FLIP_PACKED_CONVERT(float);break;
+      case GX_U16: FLIP_PACKED_CONVERT(uint16_t);break;
+      case GX_S16: FLIP_PACKED_CONVERT(int16_t);break;
+      case GX_S8: FLIP_PACKED_CONVERT(int8_t);break;
+      default: FLIP_PACKED_CONVERT(uint8_t);break;
+    }
+#undef FLIP_PACKED_CONVERT
+  }
+}
+
+
+// ---------------------------------------------------------------------------
+// Specialized vertex loaders (Dolphin VertexLoaderManager-style). A loader is
+// built once per distinct GX attribute configuration and caches the decoded
+// layout plus one monomorphic conversion routine per attribute. Every byte of
+// the output record is written exactly once, so no memset is needed, and the
+// batching record word is written in place instead of patched afterwards.
+// ---------------------------------------------------------------------------
+struct FlipStep {
+  enum Kind : uint8_t { Numeric, Color, MatrixIndex };
+  using Fn = void (*)(const FlipStep&, const u8*, size_t, size_t, u8*, size_t, const u8*, size_t);
+  Fn fn = nullptr;
+  Kind kind = Numeric;
+  uint8_t ai = 0;          // GX attribute (array selector)
+  uint8_t indexBytes = 0;  // 0 = direct, 1 = INDEX8, 2 = INDEX16
+  uint8_t total = 0;       // components in the destination location
+  uint8_t srcOffset = 0;   // within the raw GX vertex
+  uint16_t dstOffset = 0;  // within the decoded vertex
+  uint32_t stride = 0;     // indexed array stride
+  uint32_t within = 0;     // byte offset inside the array record (NBT slices)
+  float scale = 1.f;       // 1 / 2^frac
+};
+struct FlipLoader {
+  FlipVertexLayout layout{};
+  std::vector<FlipStep> steps;
+  bool ok = false;                 // false: use flip_decode_packed_generic
+  bool hasMatrices = false;
+  uint16_t matrixOffset = 0;
+  uint16_t posOffset = 0xFFFF;     // location 1 (lines/points expansion)
+  uint16_t lineOffset = 0xFFFF;    // location 15: line end + end matrix (lineMode layouts)
+  uint8_t vtxStride = 0;
+};
+
+template <int IndexBytes, typename T, bool Little, int Comps>
+static void flip_step_numeric(const FlipStep& s, const u8* __restrict raw, size_t count, size_t vtxStride,
+                              u8* __restrict out, size_t outStride, const u8* base, size_t baseSize) {
+  static constexpr u8 zero[16]{};
+  constexpr size_t bytes = Comps * sizeof(T);
+  const size_t within = s.within, stride = s.stride;
+  const unsigned total = s.total;
+  const float scale = s.scale;
+  const u8* src = raw + s.srcOffset;
+  u8* dst = out + s.dstOffset;
+  for (size_t i = 0; i < count; ++i, src += vtxStride, dst += outStride) {
+    const u8* p;
+    if constexpr (IndexBytes == 0) {
+      p = src + within;
+    } else {
+      const size_t index = IndexBytes == 1 ? size_t(src[0]) : (size_t(src[0]) << 8) | src[1];
+      const size_t off = index * stride + within;
+      p = off + bytes <= baseSize ? base + off : zero;
+    }
+    float* d = reinterpret_cast<float*>(dst);
+#if defined(__aarch64__)
+    if constexpr (sizeof(T) == 2 && (Comps == 2 || Comps == 3)) {
+      uint32_t xy; std::memcpy(&xy, p, 4);
+      uint16x4_t h = vreinterpret_u16_u32(vdup_n_u32(xy));
+      if constexpr (Comps == 3) { uint16_t z; std::memcpy(&z, p + 4, 2); h = vset_lane_u16(z, h, 2); }
+      if constexpr (!Little) h = vreinterpret_u16_u8(vrev16_u8(vreinterpret_u8_u16(h)));
+      float32x4_t v;
+      if constexpr (std::is_signed_v<T>) v = vcvtq_f32_s32(vmovl_s16(vreinterpret_s16_u16(h)));
+      else v = vcvtq_f32_u32(vmovl_u16(h));
+      v = vmulq_n_f32(v, scale);
+      vst1_f32(d, vget_low_f32(v));
+      if constexpr (Comps == 3) vst1q_lane_f32(d + 2, v, 2);
+      else if (total > 2) d[2] = 0.f;
+      continue;
+    }
+#endif
+    for (int j = 0; j < Comps; ++j) {
+      if constexpr (sizeof(T) == 4) {
+        uint32_t bits; std::memcpy(&bits, p + j * 4, 4);
+        if constexpr (!Little) bits = __builtin_bswap32(bits);
+        d[j] = std::bit_cast<float>(bits);
+      } else if constexpr (sizeof(T) == 2) {
+        uint16_t bits; std::memcpy(&bits, p + j * 2, 2);
+        if constexpr (!Little) bits = __builtin_bswap16(bits);
+        d[j] = float(std::bit_cast<T>(bits)) * scale;
+      } else {
+        d[j] = float(std::bit_cast<T>(p[j])) * scale;
+      }
+    }
+    for (unsigned j = Comps; j < total; ++j) d[j] = 0.f;
+  }
+}
+
+template <int IndexBytes, int Fmt, bool Little>
+static void flip_step_color(const FlipStep& s, const u8* __restrict raw, size_t count, size_t vtxStride,
+                            u8* __restrict out, size_t outStride, const u8* base, size_t baseSize) {
+  static constexpr u8 zero[16]{};
+  constexpr size_t bytes = (Fmt == GX_RGB565 || Fmt == GX_RGBA4) ? 2 : (Fmt == GX_RGB8 || Fmt == GX_RGBA6) ? 3 : 4;
+  const size_t stride = s.stride;
+  const u8* src = raw + s.srcOffset;
+  u8* dst = out + s.dstOffset;
+  for (size_t i = 0; i < count; ++i, src += vtxStride, dst += outStride) {
+    const u8* p;
+    if constexpr (IndexBytes == 0) {
+      p = src;
+    } else {
+      const size_t index = IndexBytes == 1 ? size_t(src[0]) : (size_t(src[0]) << 8) | src[1];
+      const size_t off = index * stride;
+      p = off + bytes <= baseSize ? base + off : zero;
+    }
+    float* d = reinterpret_cast<float*>(dst);
+    if constexpr (Fmt == GX_RGB565 || Fmt == GX_RGBA4 || Fmt == GX_RGBA6) {
+      uint32_t v = 0;
+      for (unsigned k = 0; k < bytes; ++k) v |= uint32_t(p[k]) << ((Little ? k : bytes - k - 1) * 8);
+      if constexpr (Fmt == GX_RGB565) {
+        d[0] = float((v >> 11) & 31) / 31; d[1] = float((v >> 5) & 63) / 63; d[2] = float(v & 31) / 31; d[3] = 1;
+      } else {
+        constexpr unsigned bits = Fmt == GX_RGBA4 ? 4 : 6, mask = (1u << bits) - 1;
+        for (unsigned j = 0; j < 4; ++j) d[j] = float((v >> ((3 - j) * bits)) & mask) / mask;
+      }
+    } else {
+      d[0] = float(p[0]) / 255.f; d[1] = float(p[1]) / 255.f; d[2] = float(p[2]) / 255.f;
+      d[3] = Fmt == GX_RGBA8 ? float(p[3]) / 255.f : 1.f;
+    }
+  }
+}
+
+// Locations present in the layout without a source attribute (position is
+// always declared) read as zero, matching the reference decoder's memset.
+static void flip_step_zero(const FlipStep& s, const u8*, size_t count, size_t,
+                           u8* __restrict out, size_t outStride, const u8*, size_t) {
+  u8* dst = out + s.dstOffset;
+  const size_t bytes = s.within;
+  for (size_t i = 0; i < count; ++i, dst += outStride) std::memset(dst, 0, bytes);
+}
+
+static void flip_step_matrix_index(const FlipStep& s, const u8* __restrict raw, size_t count, size_t vtxStride,
+                                   u8* __restrict out, size_t outStride, const u8*, size_t) {
+  const u8* src = raw + s.srcOffset;
+  u8* dst = out + s.dstOffset;
+  if (s.ai == GX_VA_PNMTXIDX) {
+    for (size_t i = 0; i < count; ++i, src += vtxStride, dst += outStride) *dst = u8(*src / 3);
+  } else {
+    for (size_t i = 0; i < count; ++i, src += vtxStride, dst += outStride) *dst = *src;
+  }
+}
+
+template <int IndexBytes, typename T, bool Little>
+static FlipStep::Fn flip_numeric_fn_comps(unsigned comps) {
+  switch (comps) {
+  case 1: return &flip_step_numeric<IndexBytes, T, Little, 1>;
+  case 2: return &flip_step_numeric<IndexBytes, T, Little, 2>;
+  default: return &flip_step_numeric<IndexBytes, T, Little, 3>;
+  }
+}
+template <int IndexBytes, typename T>
+static FlipStep::Fn flip_numeric_fn_le(bool le, unsigned comps) {
+  return le ? flip_numeric_fn_comps<IndexBytes, T, true>(comps) : flip_numeric_fn_comps<IndexBytes, T, false>(comps);
+}
+template <int IndexBytes>
+static FlipStep::Fn flip_numeric_fn_type(unsigned compType, bool le, unsigned comps) {
+  switch (compType) {
+  case GX_F32: return flip_numeric_fn_le<IndexBytes, float>(le, comps);
+  case GX_U16: return flip_numeric_fn_le<IndexBytes, uint16_t>(le, comps);
+  case GX_S16: return flip_numeric_fn_le<IndexBytes, int16_t>(le, comps);
+  case GX_S8: return flip_numeric_fn_le<IndexBytes, int8_t>(le, comps);
+  default: return flip_numeric_fn_le<IndexBytes, uint8_t>(le, comps);
+  }
+}
+inline FlipStep::Fn flip_numeric_fn(unsigned indexBytes, unsigned compType, bool le, unsigned comps) {
+  switch (indexBytes) {
+  case 0: return flip_numeric_fn_type<0>(compType, le, comps);
+  case 1: return flip_numeric_fn_type<1>(compType, le, comps);
+  default: return flip_numeric_fn_type<2>(compType, le, comps);
+  }
+}
+template <int IndexBytes, bool Little>
+static FlipStep::Fn flip_color_fn_fmt(unsigned fmt) {
+  switch (fmt) {
+  case GX_RGB565: return &flip_step_color<IndexBytes, GX_RGB565, Little>;
+  case GX_RGB8: return &flip_step_color<IndexBytes, GX_RGB8, Little>;
+  case GX_RGBX8: return &flip_step_color<IndexBytes, GX_RGBX8, Little>;
+  case GX_RGBA4: return &flip_step_color<IndexBytes, GX_RGBA4, Little>;
+  case GX_RGBA6: return &flip_step_color<IndexBytes, GX_RGBA6, Little>;
+  default: return &flip_step_color<IndexBytes, GX_RGBA8, Little>;
+  }
+}
+template <int IndexBytes>
+static FlipStep::Fn flip_color_fn_le(unsigned fmt, bool le) {
+  return le ? flip_color_fn_fmt<IndexBytes, true>(fmt) : flip_color_fn_fmt<IndexBytes, false>(fmt);
+}
+inline FlipStep::Fn flip_color_fn(unsigned indexBytes, unsigned fmt, bool le) {
+  switch (indexBytes) {
+  case 0: return flip_color_fn_le<0>(fmt, le);
+  case 1: return flip_color_fn_le<1>(fmt, le);
+  default: return flip_color_fn_le<2>(fmt, le);
+  }
+}
+
+inline void flip_loader_report(const ShaderConfig& c, const char* why, unsigned ai) {
+  if (!std::getenv("MELEE_FLIP_PROFILE")) return;
+  std::string attrs;
+  for (unsigned i = GX_VA_PNMTXIDX; i <= GX_VA_TEX7; ++i) {
+    const auto& m = c.attrs[i];
+    if (m.attrType == GX_NONE) continue;
+    attrs += fmt::format(" a{}:t{}c{}k{}f{}", i, m.attrType, m.cnt, m.compType, m.frac);
+  }
+  std::fprintf(stderr, "[flip-loader-generic] %s attr=%u lineMode=%u stride=%u%s\n", why, ai, c.lineMode, c.vtxStride, attrs.c_str());
+}
+inline FlipLoader flip_build_loader(const ShaderConfig& c) {
+  FlipLoader L;
+  L.layout = flip_vertex_attributes(c);
+  L.vtxStride = c.vtxStride;
+  const auto destination_offset = [&](unsigned location) -> size_t {
+    for (unsigned i = 0; i < L.layout.count; ++i)
+      if (L.layout.attributes[i].shaderLocation == location) return L.layout.attributes[i].offset;
+    return SIZE_MAX;
+  };
+  const size_t matrixOffset = destination_offset(0);
+  L.hasMatrices = matrixOffset != SIZE_MAX;
+  L.matrixOffset = static_cast<uint16_t>(L.hasMatrices ? matrixOffset : 0);
+  const size_t posOffset = destination_offset(1), lineOffset = destination_offset(15);
+  L.posOffset = posOffset == SIZE_MAX ? 0xFFFF : static_cast<uint16_t>(posOffset);
+  L.lineOffset = lineOffset == SIZE_MAX ? 0xFFFF : static_cast<uint16_t>(lineOffset);
+  // Line/point layouts decode like any other; flip_push_vertices expands the
+  // decoded records into quads. The direct reference decoder still handles the
+  // (unused by GX) indexed matrix index forms.
+  for (unsigned ai = GX_VA_PNMTXIDX; ai <= GX_VA_TEX7; ++ai) {
+    const auto& m = c.attrs[ai];
+    if (m.attrType == GX_NONE) continue;
+    const unsigned indexBytes = m.attrType == GX_DIRECT ? 0 : m.attrType == GX_INDEX8 ? 1 : 2;
+    const bool le = m.attrType != GX_DIRECT && m.le;
+    if (ai <= GX_VA_TEX7MTXIDX) {
+      if (indexBytes != 0) { flip_loader_report(c, "indexed-matrix-index", ai); return L; }
+      if (!L.hasMatrices) continue;
+      FlipStep s{};
+      s.fn = &flip_step_matrix_index; s.kind = FlipStep::MatrixIndex; s.ai = ai;
+      s.srcOffset = m.offset;
+      s.dstOffset = static_cast<uint16_t>(matrixOffset + (ai / 4) * 4 + (ai % 4));
+      L.steps.push_back(s);
+      continue;
+    }
+    const unsigned location = ai == GX_VA_POS ? 1 : ai == GX_VA_NRM ? 2 : ai == GX_VA_CLR0 ? 3 : ai == GX_VA_CLR1 ? 4 : 5 + ai - GX_VA_TEX0;
+    const size_t baseOffset = destination_offset(location);
+    if (baseOffset == SIZE_MAX) continue;
+    if (ai == GX_VA_CLR0 || ai == GX_VA_CLR1) {
+      FlipStep s{};
+      s.fn = flip_color_fn(indexBytes, m.compType, le); s.kind = FlipStep::Color; s.ai = ai;
+      s.indexBytes = indexBytes; s.srcOffset = m.offset; s.dstOffset = static_cast<uint16_t>(baseOffset);
+      s.stride = m.stride;
+      L.steps.push_back(s);
+      continue;
+    }
+    const unsigned componentBytes = m.compType == GX_F32 ? 4 : (m.compType == GX_U16 || m.compType == GX_S16) ? 2 : 1;
+    const unsigned comps = ai == GX_VA_NRM ? 3 : m.cnt;
+    if (comps == 0 || comps > 3) { flip_loader_report(c, "component-count", ai); return L; }
+    const unsigned slices = ai == GX_VA_NRM && m.cnt == 9 ? 3 : 1;
+    for (unsigned slice = 0; slice < slices; ++slice) {
+      const size_t sliceOffset = slice == 0 ? baseOffset : destination_offset(12 + slice);
+      if (sliceOffset == SIZE_MAX) continue;
+      FlipStep s{};
+      s.fn = flip_numeric_fn(indexBytes, m.compType, le, comps); s.kind = FlipStep::Numeric; s.ai = ai;
+      s.indexBytes = indexBytes;
+      s.total = ai == GX_VA_TEX0 || (ai > GX_VA_TEX0 && ai <= GX_VA_TEX7) ? 2 : 3;
+      s.srcOffset = static_cast<uint8_t>(m.offset + (m.nbt3 ? slice * indexBytes : 0));
+      s.dstOffset = static_cast<uint16_t>(sliceOffset);
+      s.stride = m.stride;
+      s.within = slice * 3 * componentBytes;
+      s.scale = 1.f / float(uint32_t(1) << m.frac);
+      L.steps.push_back(s);
+    }
+  }
+  for (unsigned i = 0; i < L.layout.count; ++i) {
+    const auto& attr = L.layout.attributes[i];
+    if (attr.shaderLocation == 0) continue;
+    bool written = false;
+    for (const auto& step : L.steps) written |= step.kind != FlipStep::MatrixIndex && step.dstOffset == attr.offset;
+    if (written) continue;
+    FlipStep s{};
+    s.fn = &flip_step_zero; s.kind = FlipStep::Numeric; s.dstOffset = static_cast<uint16_t>(attr.offset);
+    s.within = flip_attribute_bytes(attr.shaderLocation);
+    L.steps.push_back(s);
+  }
+  L.ok = true;
+  return L;
+}
+
+inline const FlipLoader& flip_loader(const ShaderConfig& c) {
+  struct Key { std::array<AttrConfig, MaxVtxAttr> attrs; u8 vtxStride, lineMode; u16 flipFlags; };
+  static_assert(std::has_unique_object_representations_v<AttrConfig>);
+  Key key{c.attrs, c.vtxStride, c.lineMode, c.flipFlags};
+  const uint64_t hash = XXH3_64bits(&key, sizeof(key));
+  static uint64_t lastHash = 0;
+  static const FlipLoader* last = nullptr;
+  if (last && hash == lastHash) return *last;
+  static absl::flat_hash_map<uint64_t, std::unique_ptr<FlipLoader>> loaders;
+  auto it = loaders.find(hash);
+  if (it == loaders.end()) it = loaders.emplace(hash, std::make_unique<FlipLoader>(flip_build_loader(c))).first;
+  lastHash = hash; last = it->second.get();
+  return *last;
+}
+
+// Decode with a prepared loader. word2 carries the batching record/texture bank
+// bits of matrices[2]; every byte of out[0, count * stride) is written.
+inline void flip_decode_with(const FlipLoader& L, const u8* raw, size_t count, u8* out,
+                             const std::array<AttrArray, MaxVtxAttr>& arrays, u32 pnMtx, uint32_t word2) {
+  gfx::detail::flip_profile::Scope profile("vertex_decode",0,count);
+  const size_t stride = L.layout.stride;
+  if (L.hasMatrices) {
+    u8* dst = out + L.matrixOffset;
+    const uint32_t words[3] = {pnMtx, 0u, word2};
+    for (size_t i = 0; i < count; ++i, dst += stride) std::memcpy(dst, words, 12);
+  }
+  for (const auto& s : L.steps) {
+    const u8* base = nullptr; size_t size = 0;
+    if (s.indexBytes) {
+      const auto& array = arrays[s.ai];
+      if (array.data) { base = static_cast<const u8*>(array.data); size = array.size; }
+    }
+    s.fn(s, raw, count, L.vtxStride, out, stride, base, size);
+  }
+}
+
+inline void flip_decode_packed_into(const ShaderConfig& c, const FlipVertexLayout& layout,
+                               const u8* raw, size_t count, std::span<u8> out,
+                               const std::array<AttrArray, MaxVtxAttr>& arrays, u32 pnMtx) {
+  static const bool specialized = [] { const char* v = std::getenv("MELEE_FLIP_LOADERS"); return !v || std::strcmp(v, "0"); }();
+  const auto& L = flip_loader(c);
+  if (!specialized || !L.ok || c.lineMode || L.layout.stride != layout.stride || L.layout.count != layout.count)
+    return flip_decode_packed_generic(c, layout, raw, count, out, arrays, pnMtx);
+  assert(out.size() == count * layout.stride);
+  flip_decode_with(L, raw, count, out.data(), arrays, pnMtx, 0);
+}
+
+inline void flip_decode_packed(const ShaderConfig& c,const FlipVertexLayout& layout,
+                               const u8* raw,size_t count,std::vector<u8>& out,
+                               const std::array<AttrArray,MaxVtxAttr>& arrays,u32 pnMtx) {
+  out.resize(count*layout.stride);
+  flip_decode_packed_into(c,layout,raw,count,out,arrays,pnMtx);
+}
+
+struct FlipVertexCache {
+  using Key = std::array<uint64_t, 2>;
+  absl::flat_hash_map<Key, std::vector<u8>> entries;
+  std::deque<Key> order;
+  size_t bytes = 0;
+  uint64_t hits = 0, misses = 0;
+  void store(const Key& key, const std::vector<u8>& data) {
+    constexpr size_t budget=16*1024*1024;
+    if(data.size()>budget) return;
+    while(!order.empty() && (entries.size()>=2048 || bytes+data.size()>budget)) {
+      auto old=entries.find(order.front());
+      if(old!=entries.end()){bytes-=old->second.size();entries.erase(old);}
+      order.pop_front();
+    }
+    if(entries.emplace(key,data).second){bytes+=data.size();order.push_back(key);}
+  }
+};
+inline FlipVertexCache& flip_vertex_cache() { static FlipVertexCache cache; return cache; }
+inline FlipVertexCache::Key flip_vertex_key(const ShaderConfig& c,const u8* raw,size_t bytes,
+  const std::array<AttrArray,MaxVtxAttr>& arrays,u32 pnMtx) {
+  std::array<uint64_t,96> signature{};
+  unsigned fields=0;
+  const auto hash=[&](const void* p,size_t n) {
+    const auto h=XXH3_128bits(p,n); signature[fields++]=h.low64; signature[fields++]=h.high64;
+  };
+  hash(c.attrs.data(),sizeof(c.attrs)); hash(raw,bytes);
+  signature[fields++]=c.lineMode;signature[fields++]=c.vtxStride;
+  // Universal and compact shaders can have identical GX input formats but
+  // different decoded layouts. Never share packed bytes across those layouts.
+  signature[fields++]=c.flipFlags;
+  signature[fields++]=c.attrs[GX_VA_PNMTXIDX].attrType==GX_NONE?pnMtx:0;
+  const size_t count=bytes/c.vtxStride;
+  for(unsigned ai=GX_VA_POS;ai<=GX_VA_TEX7;++ai) {
+    const auto& m=c.attrs[ai];
+    if(m.attrType!=GX_INDEX8 && m.attrType!=GX_INDEX16) continue;
+    const auto& array=arrays[ai];
+    const unsigned indexBytes=m.attrType==GX_INDEX8?1:2;
+    unsigned minimum=65535,maximum=0;
+    for(size_t i=0;i<count;++i) for(unsigned slice=0;slice<(m.nbt3?3u:1u);++slice) {
+      const auto* p=raw+i*c.vtxStride+m.offset+slice*indexBytes;
+      const unsigned index=indexBytes==1 ? p[0] : (unsigned(p[0])*256+p[1]);
+      minimum=std::min(minimum,index);maximum=std::max(maximum,index);
+    }
+    unsigned componentBytes=m.compType==GX_F32?4:(m.compType==GX_U16||m.compType==GX_S16)?2:1;
+    unsigned recordBytes=m.cnt*componentBytes;
+    if(ai==GX_VA_CLR0 || ai==GX_VA_CLR1)
+      recordBytes=(m.compType==GX_RGB565||m.compType==GX_RGBA4)?2:(m.compType==GX_RGB8||m.compType==GX_RGBA6)?3:4;
+    const uint64_t begin=uint64_t(minimum)*m.stride;
+    const uint64_t end=std::min<uint64_t>(uint64_t(maximum)*m.stride+recordBytes,array.size);
+    signature[fields++]=array.data?array.size:0;
+    const unsigned slices=m.nbt3?3:1;
+    const unsigned referencedBytes=m.nbt3?3*componentBytes:recordBytes;
+    if(array.data && count && end>begin) {
+      if(end-begin<=count*slices*referencedBytes*2) {
+        hash(static_cast<const u8*>(array.data)+begin,end-begin);
+      } else {
+        // Sparse display lists can reference opposite ends of a large model
+        // array. Gather only the records decoded by this draw before hashing.
+        // This remains content based, including mutable and aliased arrays.
+        static std::vector<u8> gathered;
+        gathered.resize(count*slices*referencedBytes);
+        for(size_t i=0;i<count;++i) for(unsigned slice=0;slice<slices;++slice) {
+          const auto* p=raw+i*c.vtxStride+m.offset+slice*indexBytes;
+          const unsigned index=indexBytes==1?p[0]:(unsigned(p[0])*256+p[1]);
+          const uint64_t offset=uint64_t(index)*m.stride+slice*3*componentBytes;
+          auto* dst=gathered.data()+(i*slices+slice)*referencedBytes;
+          if(offset+referencedBytes<=array.size)
+            std::memcpy(dst,static_cast<const u8*>(array.data)+offset,referencedBytes);
+          else std::memset(dst,0,referencedBytes);
+        }
+        hash(gathered.data(),gathered.size());
+      }
+    } else hash(nullptr,0);
+  }
+  const auto key=XXH3_128bits(signature.data(),fields*sizeof(uint64_t));
+  return {key.low64,key.high64};
+}
+
+inline gfx::Range flip_push_vertices(GXPrimitive prim, GXVtxFmt fmt, const u8* raw, size_t bytes, size_t alignment,
+                                     unsigned record = 0, const ShaderConfig* prepared = nullptr,
+                                     unsigned textureBank = 0) {
+  gfx::detail::flip_profile::Scope profile("vertex_prepare",0,bytes);
+  constexpr Module Log{"aurora::gx::flip"};
+  if (!flip_vertex_enabled())
+    return gfx::push_verts(raw, bytes, alignment);
+  PipelineConfig pipeline{};
+  if (!prepared) populate_pipeline_config(pipeline, prim, fmt);
+  const auto& c = prepared ? *prepared : pipeline.shaderConfig;
+  AURORA_ASSERT(c.vtxStride && bytes % c.vtxStride == 0, "Invalid GX vertex record size");
+  const size_t count = bytes / c.vtxStride;
+  // Hashing all referenced arrays costs more than decoding this game's small
+  // display lists. Keep the cache available for diagnostics, but opt into it.
+  static const bool useCache = [] { const char* s=std::getenv("MELEE_FLIP_VERTEX_CACHE");return s&&std::strcmp(s,"0"); }();
+  auto& cache=flip_vertex_cache();
+  static uint64_t calls=0;
+  if(++calls%60000==0 && std::getenv("MELEE_FLIP_PROFILE"))
+    std::fprintf(stderr,"[flip-vertex-cache] hits=%llu misses=%llu entries=%zu bytes=%zu\n",
+      (unsigned long long)cache.hits,(unsigned long long)cache.misses,cache.entries.size(),cache.bytes);
+  // For very small primitives, decoding is cheaper than a content-key lookup.
+  const bool cacheDraw=useCache&&count>=16;
+  const auto key=cacheDraw ? flip_vertex_key(c,raw,bytes,g_gxState.arrays,g_gxState.currentPnMtx) : FlipVertexCache::Key{};
+  const auto& loader = flip_loader(c);
+  const auto& layout = loader.layout;
+#ifdef MELEE_MIYOO_FLIP
+  static const bool specialized=[] {const char* v=std::getenv("MELEE_FLIP_LOADERS");return !v||std::strcmp(v,"0");}();
+  // Instanced points keep one record per point; only lines and non-instanced points expand to quads.
+  const bool expand=c.lineMode&&!(c.lineMode==3&&(c.flipFlags&FlipInstancedPoints));
+  if(specialized&&loader.ok&&!cacheDraw&&!expand) {
+    // Specialized loader: decode once, directly into immutable frame storage.
+    u8* destination=nullptr;
+    const auto range=gfx::map_flip_verts(count*layout.stride,alignment,destination);
+    if(!destination) return {};
+    const uint32_t word2=(record||textureBank)&&loader.hasMatrices ? (record<<8)|(textureBank<<12) : 0u;
+    flip_decode_with(loader,raw,count,destination,g_gxState.arrays,g_gxState.currentPnMtx,word2);
+    return range;
+  }
+  // Points (lineMode 3) carry no flip_line attribute; lines (1, 2) need it.
+  if(specialized&&loader.ok&&!cacheDraw&&expand&&loader.hasMatrices&&loader.posOffset!=0xFFFF&&(c.lineMode==3||loader.lineOffset!=0xFFFF)) {
+    // Lines/points: decode the GX vertices once with the specialized loader, then
+    // expand each segment/point into four quad corners by copying whole records.
+    // Same output as the reference expansion below (corner in matrices.z bits
+    // 24+, start position and matrix in every corner, end position and matrix in
+    // flip_line), at memcpy speed: Fountain of Dreams draws ~25k points a frame.
+    const size_t stride=layout.stride;
+    const size_t instances=c.lineMode==3?count:c.lineMode==1?count/2:count?count-1:0;
+    AURORA_ASSERT(instances<=16383,"Too many expanded GX lines/points");
+    static std::vector<u8> scratch; scratch.resize(count*stride);
+    const uint32_t word2=(record||textureBank)?(record<<8)|(textureBank<<12):0u;
+    flip_decode_with(loader,raw,count,scratch.data(),g_gxState.arrays,g_gxState.currentPnMtx,word2);
+    u8* destination=nullptr;
+    const auto range=gfx::map_flip_verts(instances*4*stride,alignment,destination);
+    if(!destination) return {};
+    const size_t mo=loader.matrixOffset, po=loader.posOffset, lo=loader.lineOffset;
+    // The destination is persistently mapped (write-combined) memory: build each
+    // record locally and stream it out once; never read the destination back.
+    alignas(16) u8 recA[sizeof(FlipVertex)], recB[sizeof(FlipVertex)];
+    u8* out=destination;
+    for(size_t i=0;i<instances;++i) {
+      const u8* a=scratch.data()+(c.lineMode==1?i*2:i)*stride;
+      const u8* b=scratch.data()+(c.lineMode==3?i:c.lineMode==1?i*2+1:i+1)*stride;
+      std::memcpy(recA,a,stride);
+      uint32_t aM0,bM0; std::memcpy(&aM0,a+mo,4); std::memcpy(&bM0,b+mo,4);
+      if(lo!=0xFFFF) { float lineEnd[4]; std::memcpy(lineEnd,b+po,12); lineEnd[3]=float(bM0&255u); std::memcpy(recA+lo,lineEnd,16); }
+      if(c.lineMode==3) std::memcpy(recB,recA,stride);
+      else {
+        std::memcpy(recB,b,stride);
+        std::memcpy(recB+po,a+po,12);
+        const uint32_t m0=(bM0&~255u)|(aM0&255u); std::memcpy(recB+mo,&m0,4);
+        if(lo!=0xFFFF) std::memcpy(recB+lo,recA+lo,16);
+      }
+      uint32_t m2A,m2B; std::memcpy(&m2A,recA+mo+8,4); std::memcpy(&m2B,recB+mo+8,4);
+      for(unsigned corner=0;corner<4;++corner,out+=stride) {
+        u8* rec=corner>=2?recB:recA;
+        const uint32_t m2=(corner>=2?m2B:m2A)|(corner<<24);
+        std::memcpy(rec+mo+8,&m2,4);
+        std::memcpy(out,rec,stride);
+      }
+    }
+    return range;
+  }
+  static const bool direct=[] {const char* s=std::getenv("MELEE_FLIP_DECODE_DIRECT");return s&&!std::strcmp(s,"1");}();
+  if(direct&&!cacheDraw&&!expand) {
+    u8* destination=nullptr;
+    const auto range=gfx::map_flip_verts(count*layout.stride,alignment,destination);
+    if(!destination) return {};
+    flip_decode_packed_into(c,layout,raw,count,{destination,range.size},g_gxState.arrays,g_gxState.currentPnMtx);
+    if((record||textureBank)&&flip_vertex_location(c,0)) {
+      for(size_t i=0;i<range.size;i+=layout.stride) {
+        uint32_t word;std::memcpy(&word,destination+i+8,4);
+        word=(word&~0xff00u)|(record<<8)|(textureBank<<12);std::memcpy(destination+i+8,&word,4);
+      }
+    }
+    return range;
+  }
+#endif
+  static std::vector<u8> packed;
+  const auto upload=[&]() {
+    gfx::detail::flip_profile::Scope uploadProfile("vertex_stage_copy",0,packed.size());
+    if((record || textureBank) && flip_vertex_location(c,0)) {
+      for(size_t i=0;i<packed.size();i+=layout.stride) {
+        uint32_t word;std::memcpy(&word,packed.data()+i+8,4);
+        word=(word&~0xff00u)|(record<<8)|(textureBank<<12);std::memcpy(packed.data()+i+8,&word,4);
+      }
+    }
+    return gfx::push_verts(packed.data(),packed.size(),alignment);
+  };
+  if(cacheDraw) {
+    if(auto it=cache.entries.find(key);it!=cache.entries.end()) {
+      ++cache.hits;packed=it->second;return upload();
+    }
+    ++cache.misses;
+  }
+  if(!expand) {
+    flip_decode_packed(c,layout,raw,count,packed,g_gxState.arrays,g_gxState.currentPnMtx);
+    if(cacheDraw) cache.store(key,packed);
+    return upload();
+  }
+  static std::vector<FlipVertex> decoded, expanded;
+  static const bool bulk = [] { const auto* s=std::getenv("MELEE_FLIP_BULK_DECODE"); return !s || std::strcmp(s,"0"); }();
+  if (!bulk || !flip_decode_bulk(c, raw, count, decoded, g_gxState.arrays, g_gxState.currentPnMtx)) {
+    decoded.resize(count);
+    for (size_t i = 0; i < count; ++i)
+      decoded[i] = flip_decode_vertex(c, raw + i * c.vtxStride, g_gxState.arrays, g_gxState.currentPnMtx);
+  }
+  if (expand) {
+    const size_t instances = c.lineMode == 3 ? count : c.lineMode == 1 ? count / 2 : count ? count - 1 : 0;
+    AURORA_ASSERT(instances <= 16383, "Too many expanded GX lines/points");
+    expanded.resize(instances * 4);
+    for (size_t i = 0; i < instances; ++i) {
+      const auto& a = decoded[c.lineMode == 1 ? i * 2 : i];
+      const auto& b = decoded[c.lineMode == 3 ? i : c.lineMode == 1 ? i * 2 + 1 : i + 1];
+      for (unsigned corner = 0; corner < 4; ++corner) {
+        auto& v = expanded[i * 4 + corner];
+        v = corner >= 2 ? b : a;
+        v.matrices[2] |= corner << 24;
+        std::memcpy(v.pos, a.pos, sizeof(v.pos));
+        v.matrices[0] = (v.matrices[0] & ~255u) | (a.matrices[0] & 255);
+        std::memcpy(v.lineEnd, b.pos, sizeof(b.pos));
+        v.lineEnd[3] = float(b.matrices[0] & 255);
+      }
+    }
+  }
+  auto& vertices = expand ? expanded : decoded;
+  packed.resize(vertices.size() * layout.stride);
+  for (size_t i = 0; i < vertices.size(); ++i) {
+    const auto* source = reinterpret_cast<const u8*>(&vertices[i]);
+    for (unsigned a = 0; a < layout.count; ++a) {
+      const auto& attr = layout.attributes[a];
+      std::memcpy(packed.data() + i * layout.stride + attr.offset, source + layout.sources[a],
+                  flip_attribute_bytes(attr.shaderLocation));
+    }
+  }
+  if(cacheDraw) cache.store(key,packed);
+  return upload();
+}
+} // namespace aurora::gx
diff --git a/lib/gx/gx.cpp b/lib/gx/gx.cpp
index b12b327..d30400d 100644
--- a/lib/gx/gx.cpp
+++ b/lib/gx/gx.cpp
@@ -1,3 +1,7 @@
+#ifdef MELEE_MIYOO_FLIP
+#include "flip_vertex.hpp"
+#include "flip_uber.hpp"
+#endif
 #include "gx.hpp"
 
 #include "pipeline.hpp"
@@ -42,6 +46,16 @@ wgpu::BindGroupLayout sTextureBindGroupLayout;
 wgpu::BindGroupLayout sSamplerBindGroupLayout;
 wgpu::PipelineLayout sPipelineLayout;
 
+size_t texture_entry_count() {
+#ifdef MELEE_MIYOO_FLIP
+  // The second texture bank belongs only to the optional texture-pair
+  // experiment. Dummy entries still cost resource tracking on every group.
+  return MaxTextures * 2 * (flip_texture_pairs_enabled() ? 2 : 1);
+#else
+  return MaxTextures * 2;
+#endif
+}
+
 std::atomic<int> sPendingViewportPolicy{-1};
 
 template <typename T>
@@ -313,6 +327,14 @@ void set_render_scissor(const gfx::ClipRect& scissor) noexcept {
 
 const gfx::TextureBind& get_texture(GXTexMapID id) noexcept { return g_gxState.textures[static_cast<size_t>(id)]; }
 
+bool flip_instanced_points_enabled() {
+  static const bool enabled = [] {
+    const char* v = std::getenv("MELEE_FLIP_INSTANCED_POINTS");
+    return v == nullptr || std::strcmp(v, "0") != 0;
+  }();
+  return enabled;
+}
+
 wgpu::RenderPipeline build_pipeline(const PipelineConfig& config, ArrayRef<wgpu::VertexBufferLayout> vtxBuffers,
                                     wgpu::ShaderModule shader, const char* label) noexcept {
   ZoneScoped;
@@ -327,12 +349,34 @@ wgpu::RenderPipeline build_pipeline(const PipelineConfig& config, ArrayRef<wgpu:
       .depthBiasSlopeScale = depthBiasSlopeScale,
       .depthBiasClamp = depthBiasClamp,
   };
-  const auto blendState =
+  auto blendState =
       to_blend_state(config.blendMode, config.blendFacSrc, config.blendFacDst, config.blendOp, config.dstAlpha);
+#ifdef MELEE_MIYOO_FLIP
+  // Half-res sprite accumulation: rgb blends as the game asked (src*a + dst*f),
+  // alpha accumulates coverage (1 - product of (1 - a)) for dst factor
+  // INVSRCALPHA and stays untouched for dst factor ONE.
+  const bool spriteAccum = (config.shaderConfig.flipFlags & FlipSpriteAccum) != 0;
+  if (spriteAccum) {
+    if (config.blendMode == GX_BM_NONE) {
+      // Opaque (alpha-tested) sprites: store premultiplied color and coverage.
+      blendState.color = {.operation = wgpu::BlendOperation::Add, .srcFactor = wgpu::BlendFactor::SrcAlpha, .dstFactor = wgpu::BlendFactor::Zero};
+      blendState.alpha = {.operation = wgpu::BlendOperation::Add, .srcFactor = wgpu::BlendFactor::One, .dstFactor = wgpu::BlendFactor::Zero};
+    } else {
+      const bool attenuates = config.blendFacDst == GX_BL_INVSRCALPHA;
+      blendState.alpha = {
+          .operation = wgpu::BlendOperation::Add,
+          .srcFactor = attenuates ? wgpu::BlendFactor::One : wgpu::BlendFactor::Zero,
+          .dstFactor = attenuates ? wgpu::BlendFactor::OneMinusSrcAlpha : wgpu::BlendFactor::One,
+      };
+    }
+  }
+#else
+  constexpr bool spriteAccum = false;
+#endif
   const std::array colorTargets{wgpu::ColorTargetState{
       .format = g_graphicsConfig.surfaceConfiguration.format,
       .blend = &blendState,
-      .writeMask = to_write_mask(config.colorUpdate, config.alphaUpdate),
+      .writeMask = spriteAccum ? wgpu::ColorWriteMask::All : to_write_mask(config.colorUpdate, config.alphaUpdate),
   }};
   const wgpu::FragmentState fragmentState{
       .module = shader,
@@ -366,6 +410,12 @@ void populate_pipeline_config(PipelineConfig& config, GXPrimitive primitive, GXV
 
   const auto& vtxFmt = g_gxState.vtxFmts[fmt];
   config.shaderConfig = {};
+#ifdef MELEE_MIYOO_FLIP
+  config.shaderConfig.flipFlags = (flip_vertex_enabled() ? FlipVertexInput | FlipCompactVertices : 0u) |
+                                  (flip_uniform_enabled() ? FlipUniformTable : 0u) |
+                                  (flip_batch_enabled() ? FlipBatchedDraws : 0u) |
+                                  (flip_texture_pairs_enabled() ? FlipTexturePairs : 0u);
+#endif
   config.shaderConfig.fogType = g_gxState.fog.type;
   config.shaderConfig.fogRangeEnabled = g_gxState.fog.rangeEnabled;
   u8 vtxOffset = 0;
@@ -416,6 +466,12 @@ void populate_pipeline_config(PipelineConfig& config, GXPrimitive primitive, GXV
     config.shaderConfig.lineMode = 2;
   } else if (primitive == GX_POINTS) {
     config.shaderConfig.lineMode = 3;
+#ifdef MELEE_MIYOO_FLIP
+    constexpr u32 needed = FlipVertexInput | FlipBatchedDraws;
+    if ((config.shaderConfig.flipFlags & needed) == needed && flip_instanced_points_enabled()) {
+      config.shaderConfig.flipFlags |= FlipInstancedPoints;
+    }
+#endif
   } else {
     config.shaderConfig.lineMode = 0;
   }
@@ -445,6 +501,10 @@ void populate_pipeline_config(PipelineConfig& config, GXPrimitive primitive, GXV
   if (g_gxState.alphaCompare) {
     config.shaderConfig.alphaCompare = g_gxState.alphaCompare;
   }
+#ifdef MELEE_MIYOO_FLIP
+  if (flip_uber_enabled() && flip_uber_compatible(config.shaderConfig))
+    config.shaderConfig.flipFlags |= FlipUberShader;
+#endif
   const auto cullMode = config.shaderConfig.lineMode == 0 ? g_gxState.cullMode : GX_CULL_NONE;
   const auto [polygonOffset, polygonOffsetScale] = polygon_offset_for_cull_mode(cullMode);
   config = {
@@ -468,6 +528,9 @@ void populate_pipeline_config(PipelineConfig& config, GXPrimitive primitive, GXV
 }
 
 GXBindGroups build_bind_groups(const ShaderInfo& info) noexcept {
+#ifdef MELEE_MIYOO_FLIP
+  gfx::detail::flip_profile::Scope profile("texture_bindgroup_build");
+#endif
   ZoneScoped;
 
   if (!info.sampledTextures.any() && !info.sampledIndTextures.any()) {
@@ -476,14 +539,33 @@ GXBindGroups build_bind_groups(const ShaderInfo& info) noexcept {
   }
 
   // Using C WGPU types instead of C++ wrappers to avoid destructor overhead
-  std::array<WGPUBindGroupEntry, MaxTextures * 2> textureEntries{};
+  constexpr size_t textureBankCount =
+#ifdef MELEE_MIYOO_FLIP
+      2;
+#else
+      1;
+#endif
+  std::array<WGPUBindGroupEntry, MaxTextures * 2 * textureBankCount> textureEntries{};
+  for (u32 i = 0; i < texture_entry_count() / 2; ++i) {
+    textureEntries[i * 2].binding = i * 2;
+    textureEntries[i * 2].textureView = sEmptyTextureView.Get();
+    textureEntries[i * 2 + 1].binding = i * 2 + 1;
+    textureEntries[i * 2 + 1].sampler = sEmptySampler.Get();
+  }
+  u32 compactSlot = 0;
   for (u32 i = 0; i < MaxTextures; ++i) {
     const auto& tex = g_gxState.textures[i];
-    WGPUBindGroupEntry& textureEntry = textureEntries[i * 2];
-    WGPUBindGroupEntry& samplerEntry = textureEntries[i * 2 + 1];
-    textureEntry.binding = i * 2;
-    samplerEntry.binding = i * 2 + 1;
-    if (tex && (info.sampledTextures[i] || info.sampledIndTextures[i])) {
+    const bool sampled = info.sampledTextures[i] || info.sampledIndTextures[i];
+#ifdef MELEE_MIYOO_FLIP
+    const u32 slot = flip_texture_pairs_enabled() && sampled ? compactSlot++ : i;
+#else
+    const u32 slot = i;
+#endif
+    WGPUBindGroupEntry& textureEntry = textureEntries[slot * 2];
+    WGPUBindGroupEntry& samplerEntry = textureEntries[slot * 2 + 1];
+    textureEntry.binding = slot * 2;
+    samplerEntry.binding = slot * 2 + 1;
+    if (tex && sampled) {
       textureEntry.textureView = tex.ref->sampleTextureView.Get();
       samplerEntry.sampler = gfx::sampler_ref(tex.get_descriptor()).Get();
     } else {
@@ -494,25 +576,33 @@ GXBindGroups build_bind_groups(const ShaderInfo& info) noexcept {
   const WGPUBindGroupDescriptor textureBindGroupDescriptor{
       .label = {"GX Texture Bind Group", WGPU_STRLEN},
       .layout = sTextureBindGroupLayout.Get(),
-      .entryCount = textureEntries.size(),
+      .entryCount = texture_entry_count(),
       .entries = textureEntries.data(),
   };
+  const auto textureBindGroup = gfx::bind_group_ref(textureBindGroupDescriptor);
   return {
-      .textureBindGroup = gfx::bind_group_ref(textureBindGroupDescriptor),
+      .textureBindGroup = textureBindGroup,
   };
 }
 
 void initialize() noexcept {
   {
-    std::array<wgpu::BindGroupLayoutEntry, MaxTextures * 2> textureEntries;
-    for (u32 i = 0; i < MaxTextures; ++i) {
+    constexpr size_t textureBankCount =
+#ifdef MELEE_MIYOO_FLIP
+        2;
+#else
+        1;
+#endif
+    std::array<wgpu::BindGroupLayoutEntry, MaxTextures * 2 * textureBankCount> textureEntries;
+    for (u32 i = 0; i < texture_entry_count() / 2; ++i) {
       textureEntries[i * 2] = {
           .binding = i * 2,
           .visibility = wgpu::ShaderStage::Fragment,
           .texture =
               {
                   .sampleType = wgpu::TextureSampleType::Float,
-                  .viewDimension = wgpu::TextureViewDimension::e2D,
+                  .viewDimension = gfx::texture_arrays_enabled() ? wgpu::TextureViewDimension::e2DArray
+                                                                 : wgpu::TextureViewDimension::e2D,
               },
       };
       textureEntries[i * 2 + 1] = {
@@ -523,7 +613,7 @@ void initialize() noexcept {
     }
     const wgpu::BindGroupLayoutDescriptor descriptor{
         .label = "GX Texture Bind Group Layout",
-        .entryCount = textureEntries.size(),
+        .entryCount = texture_entry_count(),
         .entries = textureEntries.data(),
     };
     sTextureBindGroupLayout = g_device.CreateBindGroupLayout(&descriptor);
@@ -533,18 +623,33 @@ void initialize() noexcept {
     sEmptySampler = gfx::sampler_ref(descriptor);
   }
   {
-    constexpr wgpu::TextureDescriptor descriptor{
+    const bool arrays = gfx::texture_arrays_enabled();
+    const wgpu::TextureBindingViewDimension arrayChain{wgpu::TextureBindingViewDimension::Init{
+        .textureBindingViewDimension = wgpu::TextureViewDimension::e2DArray,
+    }};
+    const wgpu::TextureDescriptor descriptor{
+        .nextInChain = arrays && !webgpu::g_hasCoreFeatures ? &arrayChain : nullptr,
         .label = "Empty texture",
         .usage = wgpu::TextureUsage::TextureBinding,
-        .size = {1, 1},
+        .size = {1, 1, 1},
         .format = wgpu::TextureFormat::RGBA8Unorm,
     };
     sEmptyTexture = g_device.CreateTexture(&descriptor);
-    sEmptyTextureView = sEmptyTexture.CreateView();
+    const wgpu::TextureViewDescriptor viewDescriptor{
+        .label = "Empty texture view",
+        .dimension = arrays ? wgpu::TextureViewDimension::e2DArray : wgpu::TextureViewDimension::e2D,
+    };
+    sEmptyTextureView = sEmptyTexture.CreateView(&viewDescriptor);
   }
   {
-    std::array<wgpu::BindGroupEntry, MaxTextures * 2> entries;
-    for (u32 i = 0; i < MaxTextures; ++i) {
+    constexpr size_t textureBankCount =
+#ifdef MELEE_MIYOO_FLIP
+        2;
+#else
+        1;
+#endif
+    std::array<wgpu::BindGroupEntry, MaxTextures * 2 * textureBankCount> entries;
+    for (u32 i = 0; i < texture_entry_count() / 2; ++i) {
       entries[i * 2] = {
           .binding = i * 2,
           .textureView = sEmptyTextureView,
@@ -557,7 +662,7 @@ void initialize() noexcept {
     const wgpu::BindGroupDescriptor desc{
         .label = "GX Empty Texture Bind Group",
         .layout = sTextureBindGroupLayout,
-        .entryCount = entries.size(),
+        .entryCount = texture_entry_count(),
         .entries = entries.data(),
     };
     g_emptyTextureBindGroup = g_device.CreateBindGroup(&desc);
diff --git a/lib/gx/gx.hpp b/lib/gx/gx.hpp
index a97b987..33b2a98 100644
--- a/lib/gx/gx.hpp
+++ b/lib/gx/gx.hpp
@@ -66,7 +66,7 @@ constexpr u32 MaxIndTexMtxs = 3;
 constexpr u32 MaxVtxFmt = GX_MAX_VTXFMT;
 constexpr u32 MaxPnMtx = (GX_PNMTX9 / 3) + 1;
 constexpr u32 MaxIndexAttr = 12; // VA_POS -> VA_TEX7
-constexpr u32 MaxUniformSize = 3840;
+constexpr u32 MaxUniformSize = 4096;
 constexpr u32 XfRegCount = 0x58; // 0x1000-0x1057
 
 enum DirtyFlag : u8 {
@@ -486,6 +486,20 @@ struct AttrConfig {
   bool le = true;
   bool nbt3 = false; // GX_NRM_NBT3
 };
+enum FlipShaderFlags : u16 {
+  FlipVertexInput = 1,
+  FlipUniformTable = 2,
+  FlipCompactVertices = 4,
+  FlipBatchedDraws = 8,
+  FlipTexturePairs = 16,
+  FlipUberShader = 32,
+  FlipConstantRecord = 64, // uniform record bound per draw; no dynamic UBO indexing
+  FlipInstancedPoints = 128, // GX_POINTS: one record per point, quad corner from the vertex index
+  FlipSpriteAccum = 256, // half-res sprite pass: premultiplied accumulation blend, alpha written
+};
+// MELEE_FLIP_INSTANCED_POINTS (default on): GX_POINTS draws step their vertex
+// attributes per instance instead of expanding every point into four records.
+bool flip_instanced_points_enabled();
 struct ShaderConfig {
   u8 fogType = GX_FOG_NONE;
   u8 vtxStride = 0;
@@ -493,6 +507,8 @@ struct ShaderConfig {
   u8 fogRangeEnabled : 1 = false;
   u8 pad1 : 5 = 0;
   u8 pad2 = 0;
+  u16 flipFlags = 0; // FlipShaderFlags; zero on other backends.
+  u16 pad3 = 0;
   std::array<AttrConfig, MaxVtxAttr> attrs;
   std::array<TevSwap, MaxTevSwap> tevSwapTable;
   std::array<TevStage, MaxTevStages> tevStages;
diff --git a/lib/gx/pipeline.cpp b/lib/gx/pipeline.cpp
index 0c3631c..76a09fb 100644
--- a/lib/gx/pipeline.cpp
+++ b/lib/gx/pipeline.cpp
@@ -1,3 +1,7 @@
+#ifdef MELEE_MIYOO_FLIP
+#include "flip_vertex.hpp"
+#include "flip_resident.hpp"
+#endif
 #include "pipeline.hpp"
 
 #include "../gfx/encoding.hpp"
@@ -9,15 +13,62 @@
 #include "shader_info.hpp"
 
 #include <tracy/Tracy.hpp>
+#ifdef MELEE_MIYOO_FLIP
+#include <absl/container/flat_hash_map.h>
+#include <mutex>
+#endif
 
 namespace aurora::gx {
 
+#ifdef MELEE_MIYOO_FLIP
+namespace {
+std::mutex sFlipPipelineConfigMutex;
+absl::flat_hash_map<gfx::PipelineRef,PipelineConfig> sFlipPipelineConfigs;
+absl::flat_hash_map<gfx::PipelineRef,gfx::PipelineRef> sFlipConstantVariants;
+}
+void register_flip_constant_variant(gfx::PipelineRef pipeline,gfx::PipelineRef constant) {
+  std::lock_guard lock{sFlipPipelineConfigMutex};
+  sFlipConstantVariants[pipeline]=constant;
+}
+gfx::PipelineRef flip_constant_variant(gfx::PipelineRef pipeline) {
+  std::lock_guard lock{sFlipPipelineConfigMutex};
+  const auto it=sFlipConstantVariants.find(pipeline);
+  return it==sFlipConstantVariants.end()?gfx::PipelineRef{}:it->second;
+}
+
+bool find_flip_pipeline_config(gfx::PipelineRef ref,PipelineConfig& config) {
+  std::lock_guard lock{sFlipPipelineConfigMutex};
+  const auto it=sFlipPipelineConfigs.find(ref);
+  if(it==sFlipPipelineConfigs.end()) return false;
+  config=it->second;
+  return true;
+}
+#endif
+
 wgpu::RenderPipeline create_pipeline(const PipelineConfig& config) {
   ZoneScoped;
   const auto shader = build_shader(config.shaderConfig);
   const auto label =
       fmt::format("GX Pipeline {:x} shader {:x}", xxh3_hash(config, static_cast<HashType>(gfx::ShaderType::GX)),
                   xxh3_hash(config.shaderConfig));
+#ifdef MELEE_MIYOO_FLIP
+  {
+    std::lock_guard lock{sFlipPipelineConfigMutex};
+    sFlipPipelineConfigs[xxh3_hash(config,static_cast<HashType>(gfx::ShaderType::GX))]=config;
+  }
+#endif
+  #ifdef MELEE_MIYOO_FLIP
+  if(config.shaderConfig.flipFlags & FlipVertexInput) {
+    const auto attrs=flip_vertex_attributes(config.shaderConfig);
+    const wgpu::VertexAttribute recordAttribute{.format=wgpu::VertexFormat::Uint32,.offset=0,.shaderLocation=15};
+    const bool instancedPoints=(config.shaderConfig.flipFlags & FlipInstancedPoints)!=0;
+    const std::array buffers{wgpu::VertexBufferLayout{.stepMode=instancedPoints?wgpu::VertexStepMode::Instance:wgpu::VertexStepMode::Vertex,
+      .arrayStride=attrs.stride,.attributeCount=attrs.count,.attributes=attrs.attributes.data()},
+      wgpu::VertexBufferLayout{.stepMode=wgpu::VertexStepMode::Vertex,.arrayStride=4,.attributeCount=1,.attributes=&recordAttribute}};
+    const bool recordSlot=(config.shaderConfig.flipFlags & FlipBatchedDraws) && config.shaderConfig.lineMode==0;
+    return build_pipeline(config, recordSlot ? ArrayRef<wgpu::VertexBufferLayout>{buffers.data(),2} : ArrayRef<wgpu::VertexBufferLayout>{buffers.data(),1}, shader, label.c_str());
+  }
+#endif
   return build_pipeline(config, {}, shader, label.c_str());
 }
 
@@ -27,13 +78,45 @@ void render(const DrawData& data, const wgpu::RenderPassEncoder& pass) {
   }
 
   const auto& resources = gfx::detail::resources();
-  pass.SetImmediates(0, &data.immediateData, sizeof(data.immediateData));
-  const std::array offsets{data.uniformRange.offset};
-  pass.SetBindGroup(1, resources.uniformBindGroup, offsets.size(), offsets.data());
+  #ifdef MELEE_MIYOO_FLIP
+  if(resident_arena(data)) pass.SetVertexBuffer(0,resident::buffer(resident_arena(data)),0,resident::gpu_bytes(resident_arena(data)));
+  else if(flip_vertex_enabled()) pass.SetVertexBuffer(0,resources.vertexBuffer,data.vertRange.offset,data.vertRange.size);
+  if(flip_batch_enabled()) {
+    // Slot 1: per-vertex uniform records (resident, merged) or the zero stream.
+    if(resident_per_vertex(data)) pass.SetVertexBuffer(1,resources.vertexBuffer,data.immediateData.vtxStart);
+    else pass.SetVertexBuffer(1,resources.flipZeroRecords);
+  }
+#endif
+  auto immediate=data.immediateData;
+  auto uniformOffset=data.uniformRange.offset;
+  auto uniformGroup=resources.uniformBindGroup;
+#ifdef MELEE_MIYOO_FLIP
+  if(flip_uniform_enabled()) {
+    const uint32_t record=(uniformOffset % FlipUniformWindow)/FlipUniformStride;
+    // Batched shaders add imm._pad to the per-vertex record bits: streamed draws
+    // carry the record in their vertices (pad 0); resident draws carry 0 there.
+    const bool batched=flip_batch_enabled();
+    immediate._pad=(!batched||(resident_arena(data)&&!resident_per_vertex(data))) ? record : 0;
+    uniformOffset=uniformOffset/FlipUniformWindow*FlipUniformWindow;
+    uniformGroup=resources.flipUniformBindGroup;
+    if (flip_uniform_windows_enabled()) {
+      uniformGroup=gfx::detail::flip_uniform_window(uniformOffset/FlipUniformWindow);
+      uniformOffset=0;
+    }
+  }
+#endif
+  pass.SetImmediates(0, &immediate, sizeof(immediate));
+  const std::array offsets{uniformOffset};
+  pass.SetBindGroup(1, uniformGroup, offsets.size(), offsets.data());
   if (data.bindGroups.textureBindGroup) {
     pass.SetBindGroup(2, gfx::find_bind_group(data.bindGroups.textureBindGroup));
   }
+#ifdef MELEE_MIYOO_FLIP
+  pass.SetIndexBuffer(resources.indexBuffer, resident_arena(data) ? wgpu::IndexFormat::Uint32 : wgpu::IndexFormat::Uint16,
+                      data.idxRange.offset, data.idxRange.size);
+#else
   pass.SetIndexBuffer(resources.indexBuffer, wgpu::IndexFormat::Uint16, data.idxRange.offset, data.idxRange.size);
+#endif
   if (data.dstAlpha != UINT32_MAX) {
     const wgpu::Color color{0.f, 0.f, 0.f, data.dstAlpha / 255.f};
     pass.SetBlendConstant(&color);
diff --git a/lib/gx/pipeline.hpp b/lib/gx/pipeline.hpp
index 1abcbd5..ae2636f 100644
--- a/lib/gx/pipeline.hpp
+++ b/lib/gx/pipeline.hpp
@@ -15,7 +15,24 @@ struct DrawData {
   uint32_t instanceCount;
   GXBindGroups bindGroups;
   uint32_t dstAlpha;
+  // Flip: 0 = frame vertex stream with 16-bit indices; otherwise resident arena
+  // index + 1 with absolute 32-bit indices. Bit 31 set: the draw's uniform record
+  // comes from the per-frame record stream (vertex slot 1), so draws with
+  // different records were merged; clear: the record travels in the immediates.
+  uint32_t resident = 0;
 };
+static_assert(sizeof(DrawData) <= 128);
+inline constexpr uint32_t ResidentPerVertexRecord = 0x80000000u;
+// Bit 30: the draw never merged, so the direct path may use the material's
+// constant-record pipeline variant with its uniform record bound directly.
+inline constexpr uint32_t ResidentConstantRecord = 0x40000000u;
+inline constexpr uint32_t ResidentFlagMask = ResidentPerVertexRecord | ResidentConstantRecord;
+inline uint32_t resident_arena(const DrawData& d) { return d.resident & ~ResidentFlagMask; }
+inline bool resident_per_vertex(const DrawData& d) { return (d.resident & ResidentPerVertexRecord) != 0; }
+inline bool resident_constant_record(const DrawData& d) { return (d.resident & ResidentConstantRecord) != 0; }
+// Constant-record variant of a material pipeline (0 when none was requested).
+void register_flip_constant_variant(gfx::PipelineRef pipeline, gfx::PipelineRef constant);
+gfx::PipelineRef flip_constant_variant(gfx::PipelineRef pipeline);
 
 constexpr uint32_t GXPipelineConfigVersion = 13;
 struct PipelineConfig {
@@ -37,6 +54,9 @@ static_assert(std::has_unique_object_representations_v<PipelineConfig>);
 
 wgpu::RenderPipeline create_pipeline([[maybe_unused]] const PipelineConfig& config);
 void render(const DrawData& data, const wgpu::RenderPassEncoder& pass);
+#ifdef MELEE_MIYOO_FLIP
+bool find_flip_pipeline_config(gfx::PipelineRef ref, PipelineConfig& config);
+#endif
 
 void queue_surface(const u8* dlStart, uint32_t dlSize, bool bigEndian) noexcept;
 } // namespace aurora::gx
diff --git a/lib/gx/regs.cpp b/lib/gx/regs.cpp
index 0c3925b..1e31965 100644
--- a/lib/gx/regs.cpp
+++ b/lib/gx/regs.cpp
@@ -1,4 +1,8 @@
 #include "regs.hpp"
+#include <cstdlib>
+#include <cstdio>
+#include <array>
+#include <set>
 
 #include "gx_fmt.hpp"
 #include "texture.hpp"
@@ -274,6 +278,18 @@ void bp_efb_copy(u8, u32 value) noexcept {
   if (reg_get(value, 1, 14) != 0) {
     Log.warn("STUB: display copy is not implemented");
   } else {
+#ifdef MELEE_MIYOO_FLIP
+    static const bool profile = std::getenv("MELEE_FLIP_PROFILE") != nullptr;
+    if (profile) {
+      static std::set<std::array<int32_t, 8>> seen;
+      const auto& r = g_gxState.texCopySrc;
+      const std::array<int32_t, 8> key{r.x, r.y, r.width, r.height, int32_t(g_gxState.texCopyDstWidth), int32_t(g_gxState.texCopyDstHeight),
+                                       int32_t(g_gxState.texCopyFmt), clear};
+      if (seen.insert(key).second)
+        std::fprintf(stderr, "[flip-efb-copy] src=%d,%d %dx%d dst=%ux%u fmt=%u clear=%d\n", r.x, r.y, r.width, r.height,
+                     g_gxState.texCopyDstWidth, g_gxState.texCopyDstHeight, unsigned(g_gxState.texCopyFmt), clear);
+    }
+#endif
     copy_tex(g_gxState.texCopyDest, clear);
   }
 }
diff --git a/lib/gx/shader.cpp b/lib/gx/shader.cpp
index 92f358b..b7052fa 100644
--- a/lib/gx/shader.cpp
+++ b/lib/gx/shader.cpp
@@ -1,11 +1,18 @@
+#ifdef MELEE_MIYOO_FLIP
+#include "flip_vertex.hpp"
+#include "flip_uber.hpp"
+#endif
 #include "../gfx/hash.hpp"
 #include "../gfx/types.hpp"
 
 #include "../internal.hpp"
 #include "../webgpu/gpu.hpp"
 #include "gx.hpp"
+#include "../gfx/texture.hpp"
 #include "gx_fmt.hpp"
 #include "shader_info.hpp"
+#include <cctype>
+#include <cstdlib>
 
 #include <dolphin/gx/GXEnum.h>
 
@@ -688,6 +695,18 @@ auto attr_address(const AttrConfig& mapping, GXAttr attr, std::string_view vidx,
 }
 
 auto attr_load(const ShaderConfig& config, GXAttr attr, std::string_view vidx) -> std::string {
+#ifdef MELEE_MIYOO_FLIP
+  if(config.flipFlags & FlipVertexInput) {
+    if(attr==GX_VA_POS) return vidx=="vidx_b" ? "flip_line.xyz" : "flip_pos";
+    if(attr==GX_VA_NRM) return "flip_normal";
+    if(attr<=GX_VA_TEX7MTXIDX) {
+      if(attr==GX_VA_PNMTXIDX && vidx=="vidx_b") return "u32(flip_line.w)";
+      return fmt::format("((flip_matrices.{} >> {}u) & 255u)","xyz"[attr/4],(attr%4)*8);
+    }
+    if(attr==GX_VA_CLR0 || attr==GX_VA_CLR1) return fmt::format("flip_color{}",attr-GX_VA_CLR0);
+    if(attr>=GX_VA_TEX0 && attr<=GX_VA_TEX7) return fmt::format("flip_tex{}",attr-GX_VA_TEX0);
+  }
+#endif
   const auto& mapping = config.attrs[attr];
   if (mapping.attrType == GX_NONE) {
     return vtx_attr(config, attr);
@@ -749,6 +768,9 @@ enum class NbtSlice : u8 {
 };
 
 auto attr_load_nbt_slice(const ShaderConfig& config, NbtSlice slice, std::string_view vidx) -> std::string {
+#ifdef MELEE_MIYOO_FLIP
+  if(config.flipFlags & FlipVertexInput) return slice==NbtSlice::B ? "flip_binormal" : "flip_tangent";
+#endif
   const auto& mapping = config.attrs[GX_VA_NRM];
   if (mapping.attrType == GX_NONE || mapping.cnt != 9) {
     Log.fatal("attr_load_nbt_slice: GX_TG_BINRM/TANGENT requires GX_NRM_NBT or GX_NRM_NBT3");
@@ -872,6 +894,10 @@ absl::flat_hash_set<gfx::ShaderRef> s_seenShaders;
 
 std::string build_shader_source(const ShaderConfig& config) noexcept {
   ZoneScoped;
+#ifdef MELEE_MIYOO_FLIP
+  if (config.flipFlags & FlipUberShader)
+    return build_flip_uber_shader_source(config.flipFlags & FlipTexturePairs);
+#endif
   const auto hash = xxh3_hash(config);
   const auto info = build_shader_info(config);
   if (EnableDebugPrints && !s_seenShaders.contains(hash)) {
@@ -950,7 +976,35 @@ std::string build_shader_source(const ShaderConfig& config) noexcept {
   std::string vtxXfrAttrsPre;
   std::string vtxXfrAttrs;
   size_t vtxOutIdx = 0;
-
+#ifdef MELEE_MIYOO_FLIP
+  if(config.flipFlags & FlipVertexInput) {
+    constexpr std::array<std::string_view,16> fields{
+      "flip_matrices: vec3u", "flip_pos: vec3f", "flip_normal: vec3f",
+      "flip_color0: vec4f", "flip_color1: vec4f", "flip_tex0: vec2f", "flip_tex1: vec2f",
+      "flip_tex2: vec2f", "flip_tex3: vec2f", "flip_tex4: vec2f", "flip_tex5: vec2f",
+      "flip_tex6: vec2f", "flip_tex7: vec2f", "flip_binormal: vec3f", "flip_tangent: vec3f", "flip_line: vec4f"};
+    for(unsigned i=0;i<fields.size();++i) if(flip_vertex_location(config,i))
+      vtxInAttrs += fmt::format(",\n @location({}) {}",i,fields[i]);
+    // Resident geometry: per-vertex uniform record from vertex buffer slot 1.
+    if((config.flipFlags & FlipBatchedDraws) && config.lineMode == 0)
+      vtxInAttrs += ",\n @location(15) flip_record_in: u32";
+  }
+#endif
+
+#ifdef MELEE_MIYOO_FLIP
+  if(config.flipFlags & FlipBatchedDraws) {
+    vtxOutAttrs += fmt::format("\n    @location({}) @interpolate(flat, either) flip_record: u32,",vtxOutIdx++);
+  }
+  if(config.flipFlags & FlipTexturePairs) {
+    vtxOutAttrs += fmt::format("\n    @location({}) @interpolate(flat, either) flip_texture_bank_out: u32,",vtxOutIdx++);
+  }
+#endif
+  // Texture arrays: GX textures are layers of array textures; the layer rides
+  // in tex{i}_size_bias.w of the uniform record.
+  const bool texArrays = gfx::texture_arrays_enabled();
+  const auto layerArg = [&](unsigned t) {
+    return texArrays ? fmt::format(" i32(ubuf.tex{}_size_bias.w),", t) : std::string{};
+  };
   // Load points for line/point expansion
   std::string_view vidxAttr = "vidx"sv;
   if (config.lineMode != 0) {
@@ -990,6 +1044,10 @@ std::string build_shader_source(const ShaderConfig& config) noexcept {
     }
     vidxAttr = "in_vidx"sv;
   } else if (config.attrs[GX_VA_PNMTXIDX].attrType == GX_NONE) {
+#ifdef MELEE_MIYOO_FLIP
+    if(config.flipFlags & FlipBatchedDraws) vtxXfrAttrsPre += "\n    let in_pnmtxidx = flip_matrices.x & 255u;";
+    else
+#endif
     vtxXfrAttrsPre += "\n    let in_pnmtxidx = imm.current_pnmtx;";
   }
 
@@ -1335,9 +1393,9 @@ std::string build_shader_source(const ShaderConfig& config) noexcept {
                     ind_scale(indStage.scaleS), ind_scale(indStage.scaleT), texMapId);
     fragmentFnPre += fmt::format(
         "\n    // Indirect stage {0}"
-        "\n    var t_IndTexCoord{0} = 255.0 * textureSampleBias(tex{1}, tex{1}_samp, {2}, "
+        "\n    var t_IndTexCoord{0} = 255.0 * textureSampleBias(tex{1}, tex{1}_samp, {2},{3} "
         "ubuf.tex{1}_size_bias.z).abg;",
-        i, texMapId, scaleExpr);
+        i, texMapId, scaleExpr, layerArg(texMapId));
   }
   if (info.usedIndStages.any()) {
     fragmentFnPre += "\n    var t_TexCoord = vec2f(0.0);";
@@ -1515,8 +1573,8 @@ std::string build_shader_source(const ShaderConfig& config) noexcept {
       uvIn = fmt::format("tex{0}_uv", underlying(stage.texCoordId));
     }
     fragmentFnPre +=
-        fmt::format("\n    var sampled{0} = textureSampleBias(tex{1}, tex{1}_samp, {2}, ubuf.tex{1}_size_bias.z);", i,
-                    underlying(stage.texMapId), uvIn);
+        fmt::format("\n    var sampled{0} = textureSampleBias(tex{1}, tex{1}_samp, {2},{3} ubuf.tex{1}_size_bias.z);", i,
+                    underlying(stage.texMapId), uvIn, layerArg(underlying(stage.texMapId)));
   }
   if (info.usesPTTexMtx.any()) {
     uniBufAttrs += fmt::format("\n    postmtx: array<mat3x4f, {}>,", MaxPTTexMtx);
@@ -1576,17 +1634,42 @@ std::string build_shader_source(const ShaderConfig& config) noexcept {
   if (info.usedIndTexMtxs.any()) {
     uniBufAttrs += fmt::format("\n    ind_mtx: array<mat2x4f, {}>,", MaxIndTexMtxs);
   }
+  const unsigned flipTextureCount=static_cast<unsigned>(info.sampledTextures.count());
+  unsigned flipTextureRank=0;
   for (int i = 0; i < info.sampledTextures.size(); ++i) {
     if (!info.sampledTextures.test(i)) {
       continue;
     }
     uniBufAttrs += fmt::format("\n    tex{}_size_bias: vec4f,", i);
+#ifdef MELEE_MIYOO_FLIP
+    if(config.flipFlags & FlipTexturePairs) {
+      const unsigned bankCount=15/flipTextureCount;
+      for(unsigned bank=0;bank<bankCount;++bank) {
+        const unsigned slot=bank*flipTextureCount+flipTextureRank;
+        texBindings += fmt::format(
+            "\n@group(2) @binding({1})\n"
+            "var tex{0}_bank{3}: {4};\n"
+            "@group(2) @binding({2})\n"
+            "var tex{0}_bank{3}_samp: sampler;",
+            i,slot*2,slot*2+1,bank,texArrays?"texture_2d_array<f32>":"texture_2d<f32>");
+      }
+      texBindings += fmt::format("\nfn flip_sample{}(uv: vec2f,{} bias: f32) -> vec4f {{\n  switch flip_texture_bank {{",i,texArrays?" layer: i32,":"");
+      for(unsigned bank=0;bank<bankCount;++bank)
+        texBindings += fmt::format(
+            "\n    case {1}u: {{ return textureSampleBias(tex{0}_bank{1}, tex{0}_bank{1}_samp, uv,{2} bias); }}",
+            i,bank,texArrays?" layer,":"");
+      texBindings += fmt::format(
+          "\n    default: {{ return textureSampleBias(tex{0}_bank0, tex{0}_bank0_samp, uv,{1} bias); }}\n  }}\n}}",i,texArrays?" layer,":"");
+      ++flipTextureRank;
+      continue;
+    }
+#endif
     texBindings += fmt::format(
         "\n@group(2) @binding({1})\n"
-        "var tex{0}: texture_2d<f32>;\n"
+        "var tex{0}: {3};\n"
         "@group(2) @binding({2})\n"
         "var tex{0}_samp: sampler;",
-        i, i * 2, i * 2 + 1);
+        i, i * 2, i * 2 + 1, texArrays ? "texture_2d_array<f32>" : "texture_2d<f32>");
   }
   if (!prevColorNormalized && !prevAlphaNormalized) {
     fragmentFn += "\n    prev = tev_overflow_vec4f(prev);";
@@ -1628,7 +1711,7 @@ std::string build_shader_source(const ShaderConfig& config) noexcept {
     fragmentFn += "\n    prev = vec4f(in.nrm, prev.a);";
   }
 
-  const auto shaderSource = fmt::format(R"""(
+  auto shaderSource = fmt::format(R"""(
 fn bswap32(v: u32, le: bool) -> u32 {{
   if (le) {{
     return v;
@@ -1998,6 +2081,99 @@ fn fs_main(in: VertexOutput) -> @location(0) vec4f {{{6}{5}
 )""",
                                         uniBufAttrs, texBindings, vtxOutAttrs, vtxInAttrs, vtxXfrAttrs, fragmentFn,
                                         fragmentFnPre, vtxXfrAttrsPre, uniformPre);
+#ifdef MELEE_MIYOO_FLIP
+  const auto replace = [&](std::string_view from, std::string_view to) {
+    size_t pos = 0;
+    while ((pos = shaderSource.find(from, pos)) != std::string::npos) {
+      shaderSource.replace(pos, from.size(), to);
+      pos += to.size();
+    }
+  };
+  if(config.flipFlags & FlipVertexInput) {
+    if(config.flipFlags & FlipInstancedPoints) {
+      // Instanced points: attributes step per instance (one record per point);
+      // the quad corner is the vertex index of the shared 6-index quad.
+      replace(",\n    @builtin(instance_index) iidx: u32", "");
+      replace("var out: VertexOutput;", "var out: VertexOutput; let iidx = 0u;");
+    } else {
+      replace("@builtin(vertex_index) vidx: u32,", "");
+      replace(",\n    @builtin(instance_index) iidx: u32", "");
+      replace("var out: VertexOutput;", config.lineMode ? "var out: VertexOutput; let vidx = flip_matrices.z >> 24u; let iidx = 0u;" : "var out: VertexOutput;");
+    }
+  }
+  if(config.flipFlags & FlipUniformTable) {
+    // The wrapper fixes the stride without duplicating Uniform's WGSL layout.
+    replace("@group(1) @binding(0)\nvar<uniform> ubuf: Uniform;",
+      "struct FlipUniformRecord { @size(4096) value: Uniform };\n@group(1) @binding(0)\nvar<uniform> flip_uniform_table: array<FlipUniformRecord, 16>;");
+    // Constant-record variant (unmerged draws): the record index comes from the
+    // immediates in both stages, so every table access is dynamically uniform
+    // instead of indexed by a per-fragment varying.
+    const bool constantRecord = (config.flipFlags & FlipConstantRecord) != 0;
+    // Experiment (MELEE_FLIP_FS_VARYING_CONSTANTS=1): the vertex stage reads the
+    // TEV constants (konst colors, register initial values) from the
+    // dynamically indexed record and hands them to the fragment stage as flat
+    // varyings, so the fragment shader has no dynamically indexed UBO loads for
+    // them. Skipped when the inter-stage location budget (15) would overflow.
+    static const bool varyingConstants = [] { const char* v = std::getenv("MELEE_FLIP_FS_VARYING_CONSTANTS"); return v && v[0] == '1'; }();
+    if (varyingConstants && (config.flipFlags & FlipBatchedDraws) && !constantRecord) {
+      std::vector<std::string> names;
+      for (const char* base : {"kcolor0", "kcolor1", "kcolor2", "kcolor3", "tevprev", "tevreg0", "tevreg1", "tevreg2"})
+        if (uniBufAttrs.find(fmt::format("\n    {}: vec4f,", base)) != std::string::npos) names.emplace_back(base);
+      const size_t fsPos = shaderSource.find("fn fs_main(");
+      if (!names.empty() && vtxOutIdx + names.size() <= 15 && fsPos != std::string::npos) {
+        std::string outs, assigns;
+        for (const auto& name : names) {
+          outs += fmt::format("\n    @location({}) @interpolate(flat, either) flip_c_{}: vec4f,", vtxOutIdx++, name);
+          assigns += fmt::format("\n    out.flip_c_{0} = ubuf.{0};", name);
+        }
+        replace("struct VertexOutput {\n    @builtin(position) pos: vec4f,", "struct VertexOutput {\n    @builtin(position) pos: vec4f," + outs);
+        const size_t ret = shaderSource.find("    return out;\n}");
+        if (ret != std::string::npos) shaderSource.insert(ret, assigns + "\n");
+        std::string fs = shaderSource.substr(shaderSource.find("fn fs_main("));
+        for (const auto& name : names) {
+          const std::string from = "ubuf." + name, to = "in.flip_c_" + name;
+          for (size_t pos = fs.find(from); pos != std::string::npos; pos = fs.find(from, pos + to.size())) {
+            // Whole-token match: the next character must not extend the name.
+            const char next = pos + from.size() < fs.size() ? fs[pos + from.size()] : ' ';
+            if (std::isalnum(static_cast<unsigned char>(next)) || next == '_') { pos += from.size(); continue; }
+            fs.replace(pos, from.size(), to);
+          }
+        }
+        shaderSource.replace(shaderSource.find("fn fs_main("), std::string::npos, fs);
+      }
+    }
+    replace("ubuf.", (config.flipFlags & FlipBatchedDraws) ? "flip_uniform_table[flip_record_index].value." : "flip_uniform_table[imm._pad].value.");
+    if(config.flipFlags & FlipBatchedDraws) {
+      replace("struct VertexOutput {", "var<private> flip_record_index: u32;\nstruct VertexOutput {");
+      // Streamed vertices carry their record in flip_matrices.z; resident geometry
+      // carries 0 there and receives the record through the (cheap) immediates.
+      // MELEE_FLIP_GPU_TEST=1 is a timing-only diagnostic: a constant record index
+      // renders incorrectly but shows what dynamic UBO indexing costs the GPU.
+      static const bool gpuTest = [] { const char* v = std::getenv("MELEE_FLIP_GPU_TEST"); return v && v[0] == '1'; }();
+      if (gpuTest) replace("var out: VertexOutput;", "var out: VertexOutput; flip_record_index = 0u; out.flip_record = 0u;");
+      else if (constantRecord) replace("var out: VertexOutput;", "var out: VertexOutput; flip_record_index = imm._pad & 15u; out.flip_record = flip_record_index;");
+      else if (config.lineMode == 0) replace("var out: VertexOutput;", "var out: VertexOutput; flip_record_index = ((flip_matrices.z >> 8u) & 15u) + (imm._pad & 15u) + (flip_record_in & 15u); out.flip_record = flip_record_index;");
+      else replace("var out: VertexOutput;", "var out: VertexOutput; flip_record_index = ((flip_matrices.z >> 8u) & 15u) + (imm._pad & 15u); out.flip_record = flip_record_index;");
+      replace("fn fs_main(in: VertexOutput) -> @location(0) vec4f {", constantRecord ? "fn fs_main(in: VertexOutput) -> @location(0) vec4f { flip_record_index = imm._pad & 15u;" : "fn fs_main(in: VertexOutput) -> @location(0) vec4f { flip_record_index = in.flip_record;");
+    }
+    if(config.flipFlags & FlipTexturePairs) {
+      shaderSource.insert(0,"diagnostic(off, derivative_uniformity);\n");
+      replace("struct VertexOutput {", "var<private> flip_texture_bank: u32;\nstruct VertexOutput {");
+      replace("var out: VertexOutput;", "var out: VertexOutput; flip_texture_bank = (flip_matrices.z >> 12u) & 15u; out.flip_texture_bank_out = flip_texture_bank;");
+      replace("fn fs_main(in: VertexOutput) -> @location(0) vec4f {", "fn fs_main(in: VertexOutput) -> @location(0) vec4f { flip_texture_bank = in.flip_texture_bank_out;");
+      for(unsigned i=0;i<MaxTextures;++i)
+        replace(fmt::format("textureSampleBias(tex{}, tex{}_samp,",i,i),fmt::format("flip_sample{}(",i));
+    }
+  }
+  replace("ptr<storage, array<u32>>", "texture_2d<u32>");
+  replace("var<storage, read> vbuf: array<u32>", "var vbuf: texture_2d<u32>");
+  replace("var<storage, read> abuf: array<u32>", "var abuf: texture_2d<u32>");
+  replace("&vbuf", "vbuf");
+  replace("&abuf", "abuf");
+  replace("arrayLength(p)", "textureDimensions(p).x * textureDimensions(p).y");
+  replace("p[word_idx]", "textureLoad(p, vec2i(i32(word_idx % 1024u), i32(word_idx / 1024u)), 0).x");
+  replace("abuf[imm.fog_range_base + u32(in.pos.x)]", "load_word(abuf, imm.fog_range_base + u32(in.pos.x))");
+#endif
   if (EnableDebugPrints) {
     Log.info("Generated shader (hash {:x}): {}", hash, shaderSource);
   }
diff --git a/lib/gx/shader_info.cpp b/lib/gx/shader_info.cpp
index c74bd6d..2a453ef 100644
--- a/lib/gx/shader_info.cpp
+++ b/lib/gx/shader_info.cpp
@@ -1,3 +1,7 @@
+#ifdef MELEE_MIYOO_FLIP
+#include "flip_vertex.hpp"
+#include "flip_uber.hpp"
+#endif
 #include "shader_info.hpp"
 
 #include "../gfx/recording.hpp"
@@ -23,7 +27,8 @@ Vec4<float> texture_size_bias(const gfx::TextureBind& tex) {
     const float replacementScale = static_cast<float>(tex.ref->size.width) / std::max(width, 1.f);
     vpBias = std::log2(viewportScale / std::max(replacementScale, 0.001f));
   }
-  return {width, height, tex.texObj.lod_bias() + vpBias, 0.0f};
+  // .w: array layer of the texture (texture arrays), 0 otherwise.
+  return {width, height, tex.texObj.lod_bias() + vpBias, tex.ref ? static_cast<float>(tex.ref->layer) : 0.0f};
 }
 
 void color_arg_reg_info(GXTevColorArg arg, const TevStage& stage, ShaderInfo& info) {
@@ -218,6 +223,16 @@ u32 line_texcoord_mask() noexcept {
 ShaderInfo build_shader_info(const ShaderConfig& config) noexcept {
   ZoneScoped;
 
+#ifdef MELEE_MIYOO_FLIP
+  if (config.flipFlags & FlipUberShader) {
+    auto specialized = config;
+    specialized.flipFlags &= ~FlipUberShader;
+    ShaderInfo info = build_shader_info(specialized);
+    info.uniformSize = MaxUniformSize;
+    return info;
+  }
+#endif
+
   ShaderInfo info{
       // render/logical viewport size, proj
       .uniformSize = 8 + 8 + 64,
@@ -492,11 +507,19 @@ static void fill_uniform(ByteBuffer& buf, const ShaderInfo& info) noexcept {
   }
 }
 
-gfx::Range build_uniform(const ShaderInfo& info) noexcept {
+gfx::Range build_uniform(const ShaderInfo& info, const ShaderConfig& config) noexcept {
   ZoneScoped;
   static ByteBuffer buf;
   buf.clear();
+#ifdef MELEE_MIYOO_FLIP
+  if (config.flipFlags & FlipUberShader)
+    fill_flip_uber_uniform(buf, config);
+  else
+#endif
   fill_uniform(buf, info);
+#ifdef MELEE_MIYOO_FLIP
+  if(flip_uniform_enabled()) return gfx::push_flip_uniform(buf.data(),buf.size());
+#endif
   return gfx::push_uniform(buf.data(), buf.size());
 }
 } // namespace aurora::gx
diff --git a/lib/gx/shader_info.hpp b/lib/gx/shader_info.hpp
index 24d431b..51a5c84 100644
--- a/lib/gx/shader_info.hpp
+++ b/lib/gx/shader_info.hpp
@@ -4,6 +4,6 @@
 
 namespace aurora::gx {
 ShaderInfo build_shader_info(const ShaderConfig& config) noexcept;
-gfx::Range build_uniform(const ShaderInfo& info) noexcept;
+gfx::Range build_uniform(const ShaderInfo& info, const ShaderConfig& config) noexcept;
 u8 color_channel(GXChannelID id) noexcept;
 }; // namespace aurora::gx
diff --git a/lib/gx/texture.cpp b/lib/gx/texture.cpp
index a56709a..604da55 100644
--- a/lib/gx/texture.cpp
+++ b/lib/gx/texture.cpp
@@ -10,6 +10,9 @@
 #include <absl/container/flat_hash_set.h>
 #include <tracy/Tracy.hpp>
 #include <xxhash.h>
+#ifdef MELEE_MIYOO_FLIP
+#include "../gfx/flip_profile.hpp"
+#endif
 
 #include <algorithm>
 #include <atomic>
@@ -51,8 +54,71 @@ struct CachedTextureEntry {
   u32 tlutDataVersion = 0;
   uint64_t replacementId = 0;
   uint64_t lastUsedFrame = 0;
+  uint64_t sampleHash = 0; // Flip: sampled content fingerprint verified on reuse
+  uint64_t lastVerifiedFrame = 0;
 };
 
+#ifdef MELEE_MIYOO_FLIP
+// Content verification costs ~15 % of the FIFO worker on Fountain when every
+// bind re-hashes. MELEE_FLIP_TEXTURE_VERIFY_INTERVAL (default 4) re-hashes a
+// texture object at most once per N frames; texDataVersion changes still
+// invalidate immediately.
+bool verify_due(CachedTextureEntry& entry, uint64_t frame) {
+  static const uint64_t interval = [] {
+    const char* v = std::getenv("MELEE_FLIP_TEXTURE_VERIFY_INTERVAL");
+    const long n = v ? std::atol(v) : 4;
+    return n < 1 ? 1ull : static_cast<uint64_t>(n);
+  }();
+  if (interval <= 1) return true;
+  if (frame - entry.lastVerifiedFrame < interval) return false;
+  entry.lastVerifiedFrame = frame;
+  return true;
+}
+// Sampled content fingerprint (Dolphin "fast" texture-cache style): small
+// sources are hashed completely, larger ones through 32 spread samples plus
+// the tail. Object identities are derived from image descriptions, so this is
+// what catches reused memory, in-place edits and identity collisions.
+enum class VerifyMode { Sampled, Full, None };
+VerifyMode verify_mode() {
+  static const VerifyMode mode = [] {
+    const char* v = std::getenv("MELEE_FLIP_TEXTURE_VERIFY");
+    if (v && !std::strcmp(v, "full")) return VerifyMode::Full;
+    if (v && !std::strcmp(v, "none")) return VerifyMode::None;
+    return VerifyMode::Sampled;
+  }();
+  return mode;
+}
+uint64_t sample_hash(const void* data, size_t bytes, const void* tlut = nullptr, size_t tlutBytes = 0) {
+  if (verify_mode() == VerifyMode::None) return 0;
+  constexpr size_t Samples = 32, Sample = 64;
+  uint8_t gathered[Samples * Sample + 4096];
+  size_t used = 0;
+  const auto gather = [&](const uint8_t* p, size_t n) {
+    if (!p || !n) return;
+    if (n <= Samples * Sample || verify_mode() == VerifyMode::Full) {
+      if (verify_mode() == VerifyMode::Full) { return; }
+      std::memcpy(gathered + used, p, n); used += n; return;
+    }
+    const size_t stride = (n - Sample) / (Samples - 1);
+    for (size_t i = 0; i < Samples - 1; ++i) { std::memcpy(gathered + used, p + i * stride, Sample); used += Sample; }
+    std::memcpy(gathered + used, p + n - Sample, Sample); used += Sample;
+  };
+  if (verify_mode() == VerifyMode::Full) {
+    uint64_t h = XXH3_64bits(data, bytes);
+    if (tlut && tlutBytes) h ^= XXH3_64bits(tlut, tlutBytes) * 0x9E3779B97F4A7C15ull;
+    return h | 1;
+  }
+  gather(static_cast<const uint8_t*>(data), bytes);
+  if (tlut && tlutBytes) gather(static_cast<const uint8_t*>(tlut), std::min<size_t>(tlutBytes, 4096));
+  return XXH3_64bits(gathered, used) | 1;
+}
+uint64_t sample_hash_texture(const GXTexObj_& obj, const GXTlutObj_* tlut) {
+  const size_t bytes = texture::texture_source_size(obj.format(), obj.width(), obj.height(), obj.mip_count());
+  const size_t tlutBytes = tlut ? texture::tlut_source_size(tlut->numEntries) : 0;
+  return sample_hash(obj.data, bytes, tlut ? tlut->data : nullptr, tlutBytes);
+}
+#endif
+
 struct CachedTlutTextureEntry {
   gfx::TextureHandle handle;
   u32 tlutDataVersion = 0;
@@ -126,6 +192,7 @@ uint64_t s_contentCacheBytes = 0;
 uint64_t s_contentCacheBudgetBytes = texture::ContentCacheBudgetBytes;
 uint64_t s_frameCount = 0;
 uint64_t s_bindGeneration = 1;
+uint64_t s_textureRefChanges = 0;
 std::atomic<uint64_t> s_pendingInvalidations = 0;
 std::atomic<uint64_t> s_pendingCacheClears = 0;
 TextureStats s_stats;
@@ -211,6 +278,14 @@ void store_cached_texture(const GXTexObj_& obj, gfx::TextureHandle handle, u32 t
   entry.tlutDataVersion = tlutDataVersion;
   entry.replacementId = replacementId;
   entry.lastUsedFrame = s_frameCount;
+#ifdef MELEE_MIYOO_FLIP
+  entry.lastVerifiedFrame = s_frameCount;
+  if (obj.has_data()) {
+    const GXTlutObj_* tlut = nullptr;
+    if (tlutObjId != 0 && static_cast<size_t>(obj.tlut) < g_gxState.loadedTluts.size()) tlut = &g_gxState.loadedTluts[static_cast<size_t>(obj.tlut)];
+    entry.sampleHash = sample_hash_texture(obj, tlut);
+  }
+#endif
 
   if (tlutObjId != 0) {
     auto& cache = s_tlutObjectCaches[tlutObjId];
@@ -281,6 +356,9 @@ struct TextureKeys {
 TextureKeys hash_texture_source(const GXTexObj_& obj, const GXTlutObj_* tlut, bool buildSourceKey) {
   ZoneScoped;
   const size_t textureBytes = texture::texture_source_size(obj.format(), obj.width(), obj.height(), obj.mip_count());
+#ifdef MELEE_MIYOO_FLIP
+  gfx::detail::flip_profile::Scope profile("texture_hash",obj.format(),textureBytes);
+#endif
   CHECK(obj.has_data() && textureBytes != 0, "invalid texture source for content hash");
 
   TextureKeys keys;
@@ -598,7 +676,11 @@ gfx::TextureHandle resolve_static_texture(const GXTexObj_& obj) {
   if (obj.texObjId != 0) {
     if (const auto it = s_textureObjectCaches.find(obj.texObjId); it != s_textureObjectCaches.end()) {
       auto& entry = it->second;
-      if (entry.handle && entry.texDataVersion == obj.texDataVersion && entry.tlutObjId == 0) {
+      if (entry.handle && entry.texDataVersion == obj.texDataVersion && entry.tlutObjId == 0
+#ifdef MELEE_MIYOO_FLIP
+          && (!entry.sampleHash || !verify_due(entry, s_frameCount) || entry.sampleHash == sample_hash_texture(obj, nullptr))
+#endif
+      ) {
         entry.lastUsedFrame = s_frameCount;
         ++s_stats.objectHits;
         return entry.handle;
@@ -630,7 +712,8 @@ gfx::TextureHandle resolve_static_texture(const GXTexObj_& obj) {
 #endif
       const size_t sourceBytes = texture_source_size(obj.format(), obj.width(), obj.height(), obj.mip_count());
       handle = gfx::new_static_texture_2d(obj.width(), obj.height(), obj.mip_count(), obj.format(),
-                                          {static_cast<const uint8_t*>(obj.data), sourceBytes}, false, nameStr);
+                                          {static_cast<const uint8_t*>(obj.data), sourceBytes}, false, nameStr,
+                                          obj.mode0 | (static_cast<uint64_t>(obj.mode1) << 32));
       ++s_stats.misses;
       s_stats.uploadBytes += texture_handle_size(handle);
       cache_content_texture(std::move(keys->contentKey), handle);
@@ -649,7 +732,11 @@ gfx::TextureHandle resolve_static_palette_texture(const GXTexObj_& obj, const GX
     if (const auto it = s_textureObjectCaches.find(obj.texObjId); it != s_textureObjectCaches.end()) {
       auto& entry = it->second;
       if (entry.handle && entry.texDataVersion == obj.texDataVersion && entry.tlutObjId == tlut.tlutObjId &&
-          entry.tlutDataVersion == tlut.tlutDataVersion) {
+          entry.tlutDataVersion == tlut.tlutDataVersion
+#ifdef MELEE_MIYOO_FLIP
+          && (!entry.sampleHash || !verify_due(entry, s_frameCount) || entry.sampleHash == sample_hash_texture(obj, &tlut))
+#endif
+      ) {
         entry.lastUsedFrame = s_frameCount;
         if (auto tlutIt = s_tlutObjectCaches.find(tlut.tlutObjId); tlutIt != s_tlutObjectCaches.end()) {
           tlutIt->second.lastUsedFrame = s_frameCount;
@@ -685,7 +772,7 @@ gfx::TextureHandle resolve_static_palette_texture(const GXTexObj_& obj, const GX
       }
       handle = gfx::new_static_texture_2d(obj.width(), obj.height(), obj.mip_count(), GX_TF_RGBA8_PC,
                                           {converted.data.data(), converted.data.size()}, false,
-                                          "GX Static Palette Texture");
+                                          "GX Static Palette Texture", obj.mode0 | (static_cast<uint64_t>(obj.mode1) << 32));
       handle->hasArbitraryMips = converted.hasArbitraryMips;
       ++s_stats.misses;
       s_stats.uploadBytes += texture_handle_size(handle);
@@ -835,6 +922,9 @@ void evict_copy_texture(const void* dest) noexcept {
 }
 
 void resolve_sampled_textures(const ShaderInfo& info) noexcept {
+#ifdef MELEE_MIYOO_FLIP
+  gfx::detail::flip_profile::Scope profile("texture_resolve");
+#endif
   ZoneScoped;
   apply_pending_invalidations();
 
@@ -854,6 +944,11 @@ void resolve_sampled_textures(const ShaderInfo& info) noexcept {
     gfx::TextureHandle handle;
     const auto copyIt = g_gxState.copyTextures.find(obj.data);
     const GXState::CopyTextureRef* copyRef = copyIt != g_gxState.copyTextures.end() ? &copyIt->second : nullptr;
+#ifdef MELEE_MIYOO_FLIP
+    if (copyRef != nullptr) {
+      gfx::flip_fusion_texture_bound(copyRef->handle);
+    }
+#endif
     if (is_palette_format(obj.format())) {
       const auto tlutIdx = static_cast<size_t>(obj.tlut);
       if (tlutIdx < g_gxState.loadedTluts.size()) {
@@ -873,7 +968,16 @@ void resolve_sampled_textures(const ShaderInfo& info) noexcept {
     }
 
     obj.mFormat = resolved_format_for_handle(handle);
+    // Texture arrays: a different texture in the same array keeps the bind
+    // group but changes the record (layer, size, bias). Count it.
+    if (textureBind.ref.get() != handle.get() || textureBind.texObj.width() != obj.width() ||
+        textureBind.texObj.height() != obj.height() || textureBind.texObj.mode1 != obj.mode1) {
+      ++s_textureRefChanges;
+    }
     textureBind = gfx::TextureBind{obj, std::move(handle), s_bindGeneration};
   }
 }
+namespace texture {
+uint64_t texture_ref_changes() noexcept { return s_textureRefChanges; }
+} // namespace texture
 } // namespace aurora::gx
diff --git a/lib/gx/texture.hpp b/lib/gx/texture.hpp
index 29c9a48..667c288 100644
--- a/lib/gx/texture.hpp
+++ b/lib/gx/texture.hpp
@@ -25,7 +25,13 @@ const TextureStats& texture_stats() noexcept;
 
 namespace texture {
 constexpr uint64_t ContentCacheBudgetBytes = 128ull * 1024ull * 1024ull;
+#ifdef MELEE_MIYOO_FLIP
+// GX texture object IDs can be recreated every frame. Keep the content cache,
+// but reclaim idle object wrappers promptly on the 1 GiB handheld.
+constexpr uint64_t ObjectCacheIdleFrames = 60;
+#else
 constexpr uint64_t ObjectCacheIdleFrames = 600;
+#endif
 constexpr bool AsyncTextureReplacements = true;
 constexpr uint32_t ReplacementThumbnailDim = 64;
 constexpr uint64_t ReplacementPublishBudgetBytes = 12ull * 1024ull * 1024ull;
@@ -35,6 +41,7 @@ size_t tlut_source_size(u16 numEntries) noexcept;
 
 void invalidate_bindings() noexcept;
 uint64_t current_bind_generation() noexcept;
+uint64_t texture_ref_changes() noexcept; // bumps when a bound texture's handle/size/lod changes
 void invalidate_replacement(uint64_t replacementId) noexcept;
 void end_frame() noexcept;
 void shutdown() noexcept;
diff --git a/lib/imgui.cpp b/lib/imgui.cpp
index 494640b..b166a1d 100644
--- a/lib/imgui.cpp
+++ b/lib/imgui.cpp
@@ -220,6 +220,10 @@ DrawData freeze() noexcept {
   return DrawData{std::move(frozen)};
 }
 
+bool has_draws(const DrawData& drawData) noexcept {
+  return drawData.m_impl && drawData.m_impl->drawData.CmdListsCount > 0;
+}
+
 void render(const wgpu::RenderPassEncoder& pass, const DrawData& drawData) noexcept {
   ZoneScoped;
 
diff --git a/lib/imgui.hpp b/lib/imgui.hpp
index a11a7f8..e173f0d 100644
--- a/lib/imgui.hpp
+++ b/lib/imgui.hpp
@@ -25,6 +25,7 @@ private:
 
   friend DrawData freeze() noexcept;
   friend void render(const wgpu::RenderPassEncoder& pass, const DrawData& drawData) noexcept;
+  friend bool has_draws(const DrawData& drawData) noexcept;
 };
 
 void create_context() noexcept;
@@ -35,5 +36,6 @@ void process_event(const SDL_Event& event) noexcept;
 bool wants_capture_event(const SDL_Event& event) noexcept;
 void new_frame(const AuroraWindowSize& size) noexcept;
 DrawData freeze() noexcept;
+bool has_draws(const DrawData& drawData) noexcept;
 void render(const wgpu::RenderPassEncoder& pass, const DrawData& drawData) noexcept;
 } // namespace aurora::imgui
diff --git a/lib/webgpu/gpu.cpp b/lib/webgpu/gpu.cpp
index 592b8ab..9fb780f 100644
--- a/lib/webgpu/gpu.cpp
+++ b/lib/webgpu/gpu.cpp
@@ -1,4 +1,9 @@
 #include "gpu.hpp"
+#ifdef MELEE_MIYOO_FLIP
+#include <dawn/native/OpenGLBackend.h>
+extern "C" void* MeleeFlipEGLDisplay();
+extern "C" dawn::native::opengl::EGLFunctionPointerType MeleeFlipEGLProc(const char*);
+#endif
 
 #include <algorithm>
 #include <atomic>
@@ -247,6 +252,11 @@ wgpu::PresentMode select_present_mode(const wgpu::SurfaceCapabilities& capabilit
       return wgpu::PresentMode::FifoRelaxed;
     }
   } else {
+#ifdef MELEE_MIYOO_FLIP
+    if (supports(wgpu::PresentMode::Immediate)) {
+      return wgpu::PresentMode::Immediate;
+    }
+#endif
     // Dawn only disables CAMetalLayer displaySyncEnabled for Immediate on Metal
     if (g_backendType != wgpu::BackendType::Metal && supports(wgpu::PresentMode::Mailbox)) {
       return wgpu::PresentMode::Mailbox;
@@ -784,7 +794,15 @@ bool initialize(AuroraBackend auroraBackend, bool allowCpu) {
     return false;
   }
   {
+#ifdef MELEE_MIYOO_FLIP
+    dawn::native::opengl::RequestAdapterOptionsGetGLProc glOptions;
+    glOptions.getProc = MeleeFlipEGLProc;
+    glOptions.display = MeleeFlipEGLDisplay();
+#endif
     const wgpu::RequestAdapterOptions options{
+#ifdef MELEE_MIYOO_FLIP
+        .nextInChain = &glOptions,
+#endif
         .featureLevel = wgpu::FeatureLevel::Compatibility,
         .powerPreference = wgpu::PowerPreference::HighPerformance,
         .backendType = backend,
@@ -853,7 +871,11 @@ bool initialize(AuroraBackend auroraBackend, bool allowCpu) {
     wgpu::Limits supportedLimits{};
     g_adapter.GetLimits(&supportedLimits);
     wgpu::CompatibilityModeLimits compatibilityModeLimits{wgpu::CompatibilityModeLimits::Init{
+#ifdef MELEE_MIYOO_FLIP
+        .maxStorageBuffersInVertexStage = 0,
+#else
         .maxStorageBuffersInVertexStage = 2,
+#endif
         .maxStorageBuffersInFragmentStage = 2,
     }};
     const wgpu::Limits requiredLimits{
@@ -868,6 +890,9 @@ bool initialize(AuroraBackend auroraBackend, bool allowCpu) {
         .maxTextureArrayLayers = supportedLimits.maxTextureArrayLayers == 0 ? WGPU_LIMIT_U32_UNDEFINED
                                                                             : supportedLimits.maxTextureArrayLayers,
         .maxStorageBuffersPerShaderStage = 2,
+#ifdef MELEE_MIYOO_FLIP
+        .maxUniformBufferBindingSize = supportedLimits.maxUniformBufferBindingSize,
+#endif
         .minUniformBufferOffsetAlignment =
             supportedLimits.minUniformBufferOffsetAlignment < 64 ? 64 : supportedLimits.minUniformBufferOffsetAlignment,
         .minStorageBufferOffsetAlignment =
@@ -934,14 +959,14 @@ bool initialize(AuroraBackend auroraBackend, bool allowCpu) {
           store_to_cache(key.data(), key.size(), value.data(), value.size(), nullptr);
         });
 
-    constexpr std::array enableToggles{
+    std::vector<const char*> enableToggles{
 #if _WIN32
         "use_dxc",
 #ifndef NDEBUG
         "emit_hlsl_debug_symbols",
 #endif
 #endif
-#ifdef NDEBUG
+#if defined(NDEBUG) && !defined(MELEE_MIYOO_FLIP)
         "skip_validation",
         "disable_robustness",
 #endif
@@ -953,6 +978,12 @@ bool initialize(AuroraBackend auroraBackend, bool allowCpu) {
         "enable_immediate_error_handling",
         "gl_allow_context_on_multi_threads",
     };
+#ifdef MELEE_MIYOO_FLIP
+    if (const char* fast = std::getenv("MELEE_FLIP_FAST_VALIDATION"); fast && !std::strcmp(fast,"1"))
+      enableToggles.push_back("skip_validation");
+    if (const char* fast = std::getenv("MELEE_FLIP_NATIVE_BOUNDS"); fast && !std::strcmp(fast,"1"))
+      enableToggles.push_back("disable_robustness");
+#endif
     constexpr std::array disableToggles{
         "timestamp_quantization",
     };
@@ -1033,7 +1064,14 @@ bool initialize(AuroraBackend auroraBackend, bool allowCpu) {
       .surfaceConfiguration =
           wgpu::SurfaceConfiguration{
               .format = surfaceFormat,
+#ifdef MELEE_MIYOO_FLIP
+              // The scene renders straight into the presented texture, which
+              // EFB copies then sample and the capture path copies from.
+              .usage = wgpu::TextureUsage::RenderAttachment | wgpu::TextureUsage::TextureBinding |
+                       wgpu::TextureUsage::CopySrc | wgpu::TextureUsage::CopyDst,
+#else
               .usage = wgpu::TextureUsage::RenderAttachment,
+#endif
               .width = size.native_fb_width,
               .height = size.native_fb_height,
               .presentMode = presentMode,
